# 构建指纹链评估(2026-09-13)

> 范围:评估 D2「构建指纹链不闭环」的两种落地方案,**只评估、不改代码**。
> 依据:`docs/威胁模型与残余风险.md` D2(`docs/威胁模型与残余风险.md:23`)、
> `docs/残余任务与后续计划-2026-09-13.md` §三 3.2(`docs/残余任务与后续计划-2026-09-13.md:163`)、
> `docs/0.3.0-beta-后续修复-变更说明.md` §七「本批未做」。
> 方法:逐文件核对源码(所有 `文件:行号` 已用 grep 复核);对目标威胁「手工替换 exe」做漏检场景推演。

## 结论摘要

1. 现状的两张指纹**输入都是 `web/dist`,都不覆盖 exe 自身字节**(§一)。它们回答的问题是
   「两个 exe 是否由同一份前端构建而来」,而非「这个 exe 是否被人换过」。
2. 现有比对只发生在**两个 sidecar 之间**(测试版 vs 便携版),没有任何代码比对「二进制内嵌
   `build_id`」与「sidecar `dist_hash`」;且未来即便闭环,比对的一方(`build_id`)也由**被检 exe
   自己经 `/api/health` 报出**,无签名/信任根。故闭环对「手工替换 exe」的防护增益有限(§二)。
3. **推荐:维持现状 + 补文档**(§四)。理由:收益(仅能发现「配错包」这一窄场景,且可被受限
   攻击者伪造)低于成本(动发行链路、双端内嵌值全变、Android 受影响、需新增消费者与重建
   启动器、PS 侧算法复刻脆弱)。
4. 若主智能体判定「运行中的后端必须与 sidecar 可机检同源」这项发布 QA 收益必须落地,则
   **fallback 选方案②(sidecar 增 FNV 字段),不选方案①**(§三、§五)。
5. 评估中发现一条 brief 未列、但**对「防手工替换 exe」严格更优且更便宜**的路径:sidecar
   改存 `exe_sha256`(exe 字节的哈希),由持有 `Get-FileHash` 的 `start.ps1` 校验(§六)。
   它超出本轮二选一范围,仅登记供主智能体决策,本轮不实施。

---

## 一、现状链路(已核准)

### 1.1 两张指纹的构成

| 维度 | sidecar 侧 `dist_hash` | 二进制内嵌 `KEDAI_DIST_HASH` |
|---|---|---|
| 实现 | `tools/Write-BuildStamp.ps1::Get-KedaiDistHash`(L8–L30) | `server-rs/build.rs::fnv_hash_dir`(L38–L60) |
| 算法 | SHA256(`Get-FileHash`,聚合再取一次 SHA256) | FNV-1a 64-bit(零依赖手写) |
| 输入集合 | `web\dist` 递归全部文件(L12) | `../web/dist`(**同一目录**,L24–L25) |
| 排序 | 小写**完整绝对路径**(L13 `$_.FullName.ToLowerInvariant()`) | `Vec<PathBuf>` **相对路径**原生序(L41 `files.sort()`) |
| 单文件序列化 | `rel:UPPERHEX\n`,其中 `rel` 由 `Substring($dist.Length)` 得到,**带前导 `/`**(L20–L22) | `rel`(无前导 `/`)+`0x00`+**原始内容字节**+`0xFF`(L52–L57) |
| 输出形态 | 64 位小写 hex(L26) | 16 位小写 hex(L26 `{hash:016x}`) |
| 落点 | `<exe>.build.json` 的 `dist_hash` 字段(L46–L48) | 编译期 `cargo:rustc-env`(L26),经 `env!` 进二进制 |
| 实测 | `dist/kedai-server.exe.build.json` = `eb949437…efd4bc22`(64 hex) | `/api/health.build_id`(`server-rs/src/api/mod.rs:70`) |

> 已实测确认的两处细节(方案①必须复刻的坑):
> - `Get-ChildItem … ` 后 `$f.FullName.Substring($dist.Length)` 结果形如 `/assets/AboutSection-BwBiPNMC.js`
>   ——**前导斜杠**;而 Rust 侧 `strip_prefix` 得 `assets/AboutSection…`,**无前导斜杠**。
> - `Get-FileHash` 返回**大写** hex,PowerShell 写入 `rel:HASH\n`(LF,非 CRLF);Rust 侧是裸字节流,
>   两者序列化格式整体不同。
> - 两侧排序键也不同(小写绝对路径 vs 相对路径原生序),路径大小写与非 ASCII 时可能次序不一致。

### 1.2 写入点与消费点

**写入**:
- `build.ps1:282` → 测试版 `dist\kedai-server.exe` 的 sidecar
- `tools/build-portable.ps1:93` → 便携版 `dist\Kedai-portable\Kedai.exe` 的 sidecar
- `server-rs/build.rs:17–32` → 每次编译 `kedai-server` crate 时把 FNV 注入二进制
  (测试版 exe、便携版 Tauri 内嵌 lib、Android `libkedai_desktop_lib.so` 都走这条)

**消费**:

| 消费者 | 位置 | 比对对象 | 缺失/异常时行为 |
|---|---|---|---|
| `start.ps1` 漂移检测 | `start.ps1:82–90`、`:155` | **两个 sidecar** 的 `dist_hash` 互比 | 返回 `$false`,交由 mtime 兜底(L88) |
| launcher 漂移检测 | `launcher/src/main.rs:533–552`、`:658` | **两个 sidecar** 的 `dist_hash` 互比 | 视为「无法判定」不误报(L540–541) |
| `build.ps1` 收尾自检 | `build.ps1:404–411` | 同上 | 红字报警「两端指纹不一致或缺失」 |
| 设置中心展示 | `web/src/components/SettingsHub.vue:31` | 人眼看 `build_id` 前 8 位 | 显示 `--------` |

**没有任何代码把「二进制内嵌 `build_id`」与「sidecar `dist_hash`」做过比对**;
`/api/health.build_id` 目前只被前端当只读文本展示(`web/src/api/health.ts:11`、`SettingsHub.vue:31`)。
这就是 D2 所谓「链未闭环」的字面含义,也是「两套算法不可直接比对」的实际后果。

### 1.3 平台与约束补充

- **launcher 零依赖**:`launcher/Cargo.toml` 的 `[dependencies]` 为空,自述「零第三方依赖」,
  故 launcher 只能做字符串比较,不能在自身内计算哈希(除非新引入/内联哈希实现)。
- **src-tauri 壳无指纹逻辑**:`grep build_id|dist_hash|build.json src-tauri/src` 无命中。
- **Android 天然不在闭环内**:Android 只经 `libkedai_desktop_lib.so` 内嵌 `build_id`,APK 内没有
  也没有 sidecar 可比。无论选①还是②,**Android 都拿不到闭环校验**。

### 1.4 链路图

```
web/dist ──┬─► [PS] Get-KedaiDistHash (SHA256, 大写hex, 带前导/) ─► <exe>.build.json:dist_hash(64 hex)
           │                                                              │
           │                                   ┌──────────────────────────┴───┐
           │                                   ▼                              ▼
           │                   start.ps1:两个 sidecar 互比       launcher:两个 sidecar 互比
           │                                   │                              │
           │                                   └──── 不一致 → 触发 build.ps1 双端重建 ────┘
           │
           └─► [Rust build.rs] fnv_hash_dir (FNV-1a 64, 裸字节) ─► KEDAI_DIST_HASH ─► 编译进二进制
                                                                          │
                                                                          ▼
                                              /api/health.build_id(16 hex)──► 仅前端人眼展示
                                              ▲
                                              └── 无任何程序把它与 sidecar.dist_hash 比对(链断裂点)
```

关键观察:**两条支路的输入都是 `web/dist`;exe 自身字节不在任一哈希的输入集合内。**

---

## 二、实际防护强度(目标威胁:手工替换 exe)

前提:攻击/误操作者能写 `dist\` 目录(本地桌面应用,exe 与 sidecar 同目录、无签名、无 ACL 隔离)。

| 场景 | 现状能否发现 | 闭环(①或②)后能否发现 | 说明 |
|---|---|---|---|
| A. 只替换 `Kedai-portable\Kedai.exe`,不动 sidecar | **否** | **可能**(若加消费者) | 现有:两 sidecar 不变→不漂移;mtime 只比源码新,不比完整性→不拦。闭环后:若替换成**另一次构建**的 exe,其内嵌 `build_id` 与本地 sidecar 值不同→可发现;若攻击者让新 exe 的 `/api/health` 照抄 sidecar 值,则**仍漏** |
| B. 同时替换 exe 与 sidecar | 否 | **否** | 比对双方都在攻击者控制下;无签名/信任根,任何基于这两个文件的比对都无效 |
| C. 替换项目根 `Kedai.exe`(启动器本体) | 否 | **否** | 根 `Kedai.exe`(260KB,`find` 确认无 sidecar)就是校验逻辑的宿主;换掉它即绕过全部校验。「校验者必须比被校验者更受信」在此不成立 |
| D. 替换/篡改 `web/dist` 磁盘文件 | 不适用 | 不适用 | 前端已被 `include_dir!` **编译期嵌入二进制**,运行期磁盘上的 `web/dist` 不参与服务,改它无效果 |
| E. exe 复制损坏/半截文件 | **是**(粗粒度) | 是 | launcher 启动后 `try_wait` 检测秒退并弹窗报错(`launcher/src/main.rs:728–745`);无需指纹 |
| F. 配错包:把另一次构建的便携版盖到 `dist\Kedai-portable\` | **否** | **是** | 这是闭环唯一真实的净增益:能发现「包内容与 sidecar 不同源」 |

**要害结论**:
1. 哈希的对象是前端资产,不是 exe;所以「替换 exe 字节」这一威胁**根本不在指纹的覆盖面内**。
2. 闭环后新增的比对,其一端由**被检 exe 自己**产出(`/api/health`)。可信度不高于被检对象,
   可被伪造。这不是安全边界,只能算「发布一致性自检」。
3. 因此方案①、②的收益都只是把「两端一致性」升级为「单端 包↔sidecar 一致性」,
   **不能兑现 D2 措辞中「防手工替换 exe」的预期**。若要真正防替换,方向是签名/信任根,
   或至少校验 **exe 字节**(见 §六),而非前端哈希。

> **与本仓既有结论一致**:`docs/代码签名与SmartScreen说明.md` §3.4(L134–L146)已明确写下
> 「sidecar 未与 exe 做密码学绑定、可被一并替换,属一致性辅助而非信任根」「`dist_hash` 只覆盖
> `web/dist`,不是 exe 自身哈希」,并指出「真正防冒充需要 Authenticode 签名」。本评估独立复核后
> 与之一致,故推荐「维持现状 + 补文档」不会与该文档冲突。

---

## 三、两案对比

| 评估维度 | 方案① `build.rs` 改算 SHA256 | 方案② sidecar 增 FNV 字段 |
|---|---|---|
| 改动面 | `server-rs/build.rs` 重写哈希 + 更新其 2 个单测;`server-rs/Cargo.toml` 新增 `[build-dependencies] sha2`;`server-rs/Cargo.lock` 变化;文档/注释同步 | 仅 `tools/Write-BuildStamp.ps1` 新增 FNV 计算与 `fnv` 字段;`build.ps1`/`build-portable.ps1` 无需改(继续 dot-source 同一函数) |
| 需重建什么 | 桌面双端 exe + **Android `.so`**(build.rs 变更 → 内嵌值格式全变);launcher(若要加消费者) | 桌面双端(仅为刷新 sidecar);**Rust 侧零变更,Android 无需因本项重建**;launcher(若要加消费者) |
| 防护强度提升点 | 内嵌值与 sidecar **同算法同输入**,可字符串全等比对;算法统一、无「双算法并存」 | 保留原 SHA256 `dist_hash`(现有漂移检测零影响),另加同输入 FNV 供比对;算法仍双套 |
| 算法复刻风险 | **高**:需精确复刻 PS 的「小写绝对路径排序 + 前导 `/` + 大写 hex + `\n` 序列化」(§1.1 已列三处坑);任一处不同即永远不相等。变更说明原文即评「耦合脆」 | **中高**:需在 PS 内复刻 Rust 的「相对路径原生序 + `\0` + 裸内容 + `0xFF`」字节流;PS 侧 64 位乘法溢出(PowerShell 会把 Int64 溢出提升为 double 丢精度),须用 `BigInteger` 或 `Add-Type` 内联 C# 的 `unchecked` 实现;逐字节循环性能需靠 C# 兜住 |
| `sha2` build-dep 影响 | `sha2 0.10.9` 已在 `src-tauri/Cargo.lock:3625` 收录,**理论上可通过 `check-lock-sync`**(规则:server-rs 锁的 (name,version) 须被 src-tauri 锁覆盖);但其传递依赖 `digest/cfg-if/cpufeatures` 的解析版本须与 src-tauri 侧一致,否则触发锁同步门禁 → 需实跑 `npm run check:lock` 验证。新增 `cargo audit` 面(sha2 属主流 crate,风险低) | 无依赖变化,**不动任何 Cargo.lock**,锁同步/审计门禁零影响 |
| Tauri Android 构建 | 受影响:`build.rs` 变更进入 Android `.so`,内嵌 `build_id` 形态由 16→64 hex;无对端可比,纯属被动变更,需重出 APK 才对齐 | **不受影响**:`build.rs` 不动,Android `.so` 内嵌值保持 16 hex FNV |
| 双端产物指纹变更 | 是:两个 Windows exe + Android `.so` 的内嵌值全部变化,变更说明/文档中 16-hex 的示例需更新 | 否:内嵌值不变,仅在 sidecar 追加字段,旧产物仍可被现有逻辑读取 |
| launcher 零依赖约束 | 不破坏:闭环只需字符串比较(64 hex vs 64 hex) | 不破坏:字符串比较(16 hex vs 16 hex) |
| 「重建 launcher」是否真为必需 | **不是硬性必需**:现有 launcher 读 `sidecar.dist_hash` 的逻辑在两案下都不坏。只有**要新增闭环消费者**时才需要;消费者也可放进 `start.ps1`(有 `Get-FileHash`/HTTP 能力),此时 launcher 不必重建。brief 中「两者都要重建 launcher」应理解为「若要 launcher 承担闭环校验则需重建」 | 同左 |
| 其他风险 | `[build-dependencies]` 段为新增;构建脚本编译时间略增;`server-rs` 内嵌方版本解析变化可能连带锁同步 | PS 构建期一次全量 FNV(51 文件,纯字节循环);若用 PS 原生长循环会拖慢构建,必须以 C# 实现 |

**两案共同成本**(与选案无关):要真正产生收益,都必须新增一个「比对 `/api/health.build_id` 与
sidecar 对应字段」的消费者,并处理「不一致时拒绝启动 / 仅告警」的产品决策;否则只是改了值、
链路依旧无人消费。

---

## 四、推荐结论

### 首选:维持现状 + 补文档

理由(按权重):

1. **收益与 D2 的表述不符**:两案都只做到了「包与 sidecar 同源」的自检;被检 exe 自报值、
   无信任根,换 exe 者同样能换/伪造这两个文件(§二场景 B/C)。真要防替换需签名或校验 exe 字节,
   不在这两案的能力范围内。**收益被高估**。
2. **成本落在最敏感的发行链路上**:① 会同时改变 Windows 双端与 **Android `.so`** 的内嵌指纹,
   并引入新的 build-dependency 与锁同步/审计面;② 需在 PowerShell 里复刻 Rust 字节流并解决
   64 位溢出,构建期性能与正确性都需额外测试看护。而当前 `build.ps1` 收尾已在双端不一致时红字
   报警(`build.ps1:404–411`),**「配错包」这一唯一净增益场景已有半覆盖**。
3. **符合本仓纪律**:AGENTS.md 要求「影响面大的改动先确认」「同一件事第三次重估即停止」;
   D2 本身也未造成已观测故障(纯理论缺口),按「维持现状 + 登记为设计边界」处置更稳。

### Fallback(若必须落地机检同源):选方案②

若主智能体判定「发布 QA 要求运行中的后端可与 sidecar 机检同源」必须实现,则选 **②**,理由:

- **blast radius 最小**:不动 Rust、不动任何 `Cargo.lock`、不改变已发行 exe/Android `.so` 的内嵌值,
  现有 `dist_hash` 语义与所有既有消费者保持不变;把「易碎」收敛在单一文件 `Write-BuildStamp.ps1`。
- **明确不选①**:① 的唯一优势是算法统一,但要付出跨端内嵌值全变 + 新构建依赖 + PS 序列化复刻
  三重代价,收益却与②相同(都只是同源自检)。

---

## 五、最小落地步骤与验收断言

### 5.1 若采纳「维持现状 + 补文档」(推荐,本轮即可完成,零代码)

1. `docs/威胁模型与残余风险.md` D2 行的「处置」改为:**登记为设计边界**——指纹仅覆盖 `web/dist`、
   不覆盖 exe 字节,比对值可由被检 exe 自报;不计入「待修的闭环缺口」。(登记文本见文末「给主智能体」。)
2. 在 `MAINTENANCE.md` 「两版同步机制(构建指纹)」小节补一句范围声明:
   「本机制用于**两版同源一致性**检查,不构成对 exe 的完整性/防篡改保证;后者需代码签名。」
3. 在 `tools/Write-BuildStamp.ps1` 与 `server-rs/build.rs` 头部注释各补一行同样声明,
   防止后续会话把「覆盖 web/dist」误读为「覆盖 exe」。
4. (可选,建议)新增一个**负向元测试**:断言 `KEDAI_DIST_HASH` / sidecar `dist_hash` 的语义是
   「web/dist 的摘要」,不随 exe 二进制变化——用于把这条边界固化,防止将来被误用为完整性校验。

**验收断言(可测/可查)**:
- A1(可查):`grep -rn "不构成对 exe" MAINTENANCE.md docs/威胁模型与残余风险.md` 有命中,且
  `docs/威胁模型与残余风险.md` D2 行不再出现「防手工替换 exe 的能力有缺口」式待修表述。
- A2(一致):两条注释与文档对「输入=web/dist、不覆盖 exe」的表述相同,不含相互矛盾的说法。

### 5.2 若采纳方案②(sidecar 增 FNV 字段,fallback)

**最小步骤**:

1. `tools/Write-BuildStamp.ps1` 新增 `Get-KedaiDistFnv`:`Add-Type` 内联一小段 C# 实现
   FNV-1a 64(`unchecked` 乘法,`ulong`,offset `0xcbf29ce484222325`、prime `0x100000001b3`),
   与 Rust 完全同序:相对路径→`\`换`/`→`0x00`→裸内容→`0xFF`,按**相对路径序数序**遍历;
   输出 16 位小写 hex。
2. `Write-KedaiBuildStamp` 的 `[ordered]@{…}` 增加 `fnv = Get-KedaiDistFnv -Root $Root` 字段
   (保持扁平紧凑 JSON;launcher 的 `extract_json_string` 按 `"key":"` 子串提取,新增字段不影响
   `dist_hash` 读取)。
3. 新增消费者:在 `launcher/src/main.rs`(或 `start.ps1`)服务就绪后请求 `/api/health`,
   取 `build_id`,与对应 exe 的 sidecar `fnv` 比对;不一致→弹窗/报错并按产品决策拒绝启动或仅告警。
4. 更新 `MAINTENANCE.md` 同步机制小节与 D2 行,说明新增字段与语义。

**验收断言(可测)**:

- B1(正常同源):一次 `build.ps1` 双端构建后,启动任一 exe,`GET /api/health` 的 `build_id`
  必须**字符串全等**于该 exe 同名 `.build.json` 的 `fnv` 字段。
- B2(拒绝替换,核心断言):把 `dist\Kedai-portable\Kedai.exe` 替换为「**另一次构建**的 exe」
  (其内嵌 FNV ≠ 当前 `Kedai.exe.build.json.fnv`),双击 launcher 后,**启动器必须报告指纹不一致
  并拒绝启动**(非零退出或明确弹窗),而不是照旧启动。
- B3(不误伤缺失):删除或清空 sidecar、或字段缺失时,消费者按「无法判定」处理,**不得拒绝启动**
  (与现有 drift 语义 `launcher/src/main.rs:540–541`、`start.ps1:88` 一致)。
- B4(不误报重复):同一次构建的产物,连续多次启动不触发重建、不报不一致。
- B5(跨端一致):`dist\kedai-server.exe` 与 `dist\Kedai-portable\Kedai.exe` 的 `fnv` 相等;
  `build.ps1:404–411` 的既有 `dist_hash` 一致结论仍为绿。

> 说明:断言 B2 可检出「换了另一份 exe」,但**检不出**「攻击者同时伪造 `/api/health` 或替换
> sidecar」(§二场景 B),这属于方案②能力边界,交付时应连同文档一并声明。

### 5.3 若采纳方案①(`build.rs` 改算 SHA256)— 明确不推荐,步骤从略

若仍要实施:需复刻 §1.1 三处序列化细节、新增 `sha2` build-dep 并实跑 `npm run check:lock`,
验收断言同 B1–B5(把 `fnv` 换成 `dist_hash`),并额外断言 Android `.so` 的 `build_id` 为 64 hex。
**成本最高、收益与②持平,建议放弃。**

---

## 六、附:评估中发现的更优第三案(超出 brief 二选一范围,供决策)

针对「防手工替换 exe」本身,有一条**严格更有效且几乎零风险**的路径:

- 让 `Write-KedaiBuildStamp` 额外写入 `exe_sha256 = (Get-FileHash -Algorithm SHA256 $ExePath).Hash`
  (哈希对象是 **exe 字节**,不是前端)。
- 由已持有 `Get-FileHash` 的 `start.ps1` 在启动前重算 exe 哈希并与 sidecar 比对;不一致即拒绝启动。
- 优点:真正覆盖「exe 被替换」;无需 Rust/build-dep/锁变更;不触碰内嵌指纹与 Android;
  launcher 零依赖约束不需要动(校验放在 PS 侧)。
- 缺点:仍需信任 sidecar(攻击者同时换两者可绕过),且对「校验者自身被替换」无效;
  本质是把「前端同源自检」升级为「exe 字节完整性自检」,仍非签名级安全边界。
- **重要限定**:本仓既有做法是「用户从**可信渠道**取到公布哈希,再 `Get-FileHash` 逐字比对」
  (`docs/代码签名与SmartScreen说明.md` §3.2)。信任根来自**带外公布**的哈希,不是本机 sidecar。
  上述自动化只是把「配错包/本机被换」的自检工程化,**不能替代带外核验,更不替代 Authenticode**。
- 但相对①/②,它以更低成本命中 D2 的真实诉求（本机一致性）。**建议主智能体在「必须落地」时
  优先评估此案**;若诉求是「防外部冒充」,则应直接走 §3.2 的带外核验 + 第四节的签名路线。

---

## 七、未决问题(需主智能体确认)

1. **收益定性**:「防手工替换 exe」是否被列为正式安全需求?若仅是发布 QA 的「同源自检」,
   按 §四维持现状即可;若是安全需求,①② 都不满足,应转向 §六或代码签名。
2. **消费者归属**:闭环校验放 `launcher`(双击主入口,需重建)还是 `start.ps1`(零重建,
   但命令行用户才经过)?brief 假设「都要重建 launcher」,实际取决于此决策。
3. **Android 是否纳入**:①② 都无法覆盖 Android(无 sidecar)。是否接受「Android 不做指纹闭环」?
4. **不一致时的产品行为**:拒绝启动 / 仅告警 / 自动重建?影响验收断言 B2 的措辞。

---

### 给主智能体的登记文本建议(不改本仓库其它文档,仅提供文字)

**`docs/威胁模型与残余风险.md` D2 行替换建议**:

| # | 风险 | 现状与依据 | 处置 |
|---|---|---|---|
| D2 | 构建指纹链不闭环:sidecar(SHA256,覆盖 `web/dist`)与二进制内嵌(FNV-1a,覆盖 `web/dist`)不可直接比对;`/api/health` 的 `build_id` 无消费者 | `tools/Write-BuildStamp.ps1::Get-KedaiDistHash`(L8–30) vs `server-rs/build.rs::fnv_hash_dir`(L38–60);比对只发生在两个 sidecar 之间(`start.ps1:82–90`、`launcher/src/main.rs:533–552`);两份指纹**均不覆盖 exe 字节** | **2026-09-13 评估后登记为设计边界,不修**:该机制服务于「两版同源一致性」,比对值可由被检 exe 自报且无签名/信任根,不构成防篡改保证(与 [代码签名与SmartScreen说明.md](代码签名与SmartScreen说明.md) §3.4 定位一致)。评估与逐案对比见 [构建指纹链评估-2026-09-13.md](构建指纹链评估-2026-09-13.md)。**重新评估触发条件**:①引入代码签名;②出现真实「替换 exe」威胁场景;③需要发布 QA 同源机检(届时优先 §六 的 `exe_sha256` 路径,次选 sidecar 增 FNV) |

**`docs/残余任务与后续计划-2026-09-13.md` 3.2 项替换建议**:

> | 3.2 | 构建指纹链闭环(D2) | `tools/Write-BuildStamp.ps1`(SHA256) vs `server-rs/build.rs`(FNV-1a) | **已完成评估(2026-09-13,见 [构建指纹链评估-2026-09-13.md](构建指纹链评估-2026-09-13.md)):结论「维持现状 + 补文档」**。两案(① build.rs 改 SHA256;② sidecar 增 FNV)的净增益仅为「包↔sidecar 同源自检」,均不覆盖 exe 字节且比对值可由被检 exe 伪造,收益低于动发行链路的成本。若日后必须落地,优先评估文档 §六 的 `exe_sha256` 校验路径,次选方案②;不选方案① |
