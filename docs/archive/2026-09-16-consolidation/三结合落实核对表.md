# 三结合落实 · 核对表

> 计划见 [三结合彻底落实-实施计划.md](三结合彻底落实-实施计划.md)。
> **状态口径**:`待办` / `进行中` / `已验证`(附验证命令与证据) / `已放弃`(注明原因)。
> 每完成一项,更新本表对应行——这是「传帮带晋升跟踪」的落地载体。

**证据约定**:验证命令可复现;证据为 `文件:行号`,优先指向**落库的代码/配置**而非文档。

---

## 三条总验收断言

| # | 断言 | 验证命令 | 状态 |
|---|---|---|---|
| ① | 断环:`task_engine` 不再引用 `task_service` | `grep -rn "task_service" server-rs/src/services/task_engine/` → **零命中**;`check-arch` 规则 C = 0 | ✅ 已验证 |
| ② | L1 纯净:解析/模型/契约层不依赖服务层 | `check-arch` 规则 D = 0(原 3 处已清) | ✅ 已验证 |
| ③ | DoS 封堵:EJS 三类畸形输入返回 Err | `cargo test --lib ejs` → 46 通过(含 5 个新守卫测试) | ✅ 已验证 |

---

## 批次 0:基线固化 + 修正误导性文档

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| T0.1 | 提交 117 项工作区改动为基线 | `git log --oneline` → `70c8c79` | ✅ 已验证 |
| T0.2 | 移除误入库的 `软件 (D).lnk` 并忽略 `*.lnk`/`Kedai.exe` | `.gitignore:39-41`;`git ls-files \| grep -i lnk` 零命中;`21deec0` | ✅ 已验证 |
| T0.3 | 跑完整 check-all 建绿色基线 | `cargo fmt --check` OK、`cargo clippy` OK;`cargo test` 首轮因**并发 cargo 争用同一 target 目录**(os error 32)失败,已改串行重跑 | 见下方「基线测试记录」 |
| T0.4 | 修 android 状态位与陈旧测试数 | `docs/android-port-plan.md:3-5,310` | ✅ 已验证 |
| T0.5 | 修任务引擎陈旧描述(`run_task_background`) | `docs/任务引擎六模式.md:4-5,11,105,281`;grep 该文档零命中 | ✅ 已验证 |
| T0.6 | 归档过时契约漂移表 + 索引同步 | `docs/archive/contract-drift-2026-09-09.md`;`docs/README.md:27,46` | ✅ 已验证 |

### 基线测试记录

| 项 | 命令 | 结果 |
|---|---|---|
| fmt | `cargo fmt --check` | ✅ OK(1.1s) |
| clippy | `cargo clippy --all-targets -- -D warnings` | ✅ OK(29.4s,0 warning) |
| 后端测试 | `cargo test --workspace -j 2`(`CARGO_TARGET_DIR=D:\kedai-build`) | ✅ **887 passed / 1 failed**——唯一失败为既有 flaky: `settings_connector::mock_auto_switches_to_openai_on_save`(Windows 文件占用 `os error 32`),隔离重跑 **6/6 通过** |
| 前端测试 | `npm test -w web` | ✅ **79 文件 / 733 用例全通过** |
| typecheck | `npm run typecheck -w web` | ✅ 通过(无输出) |
| 类型 ratchet | `node tools/check-frontend-lint.mjs` | ✅ 五项均 = 基线 |
| 门禁自测 | 注入失败探针 → `build.ps1 -NoWeb -Dev` | ✅ **构建中止,EXITCODE=1**(探针已移除,文件已还原) |

> **环境说明**:`server-rs\target` 被执行拦截(`native-tls` 构建脚本 `拒绝访问 os error 5`),
> 构建/测试统一改用 `CARGO_TARGET_DIR=D:\kedai-build`。见实施计划 §〇之二。
> **并发纪律**:同一 target 目录不可并发跑两个 cargo(会 `os error 32` 争用 exe),必须串行。

---

## 批次 A:EJS 加固 + 自研引擎冻结(P0)

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| A.1 | 三处循环加迭代+时间预算 | `parsing/assistant/ejs/exec.rs` `charge_step()` 已插入 `For`/`ForOf`/`While` 三处循环体首行 | ✅ 已验证 |
| A.1b | 渲染入口初始化/恢复预算 | `parsing/assistant/ejs/mod.rs:87` 挂 `exec::RenderGuard`;`exec.rs` 的 `RenderGuard` 实现 Drop 恢复 | ✅ 已验证 |
| A.2 | 3 个解析深度守卫(含 `parse_unary`) | `parser.rs` `parse_assign`/`parse_unary`/`parse_primary` 三个守卫点(拆 `_inner`),`MAX_PARSE_DEPTH=64` | ✅ 已验证 |
| A.2b | 子解析器继承深度 | `parser.rs` 模板表达式与箭头探针改用 `Parser::with_depth(toks, self.depth)` | ✅ 已验证 |
| A.3 | 冻结登记 | `MAINTENANCE.md:20`(纪律条)+ `:30`(晋升表「L3·冻结」);`ARCHITECTURE-3H.md:58-61` | ✅ 已验证 |
| A.4 | 5 条验收测试 | `cargo test --lib ejs` → 46 passed(含 `while_true_is_aborted_by_budget`、`for_forever_is_aborted_by_budget`、`deep_unary_chain_is_rejected`、`deep_paren_nesting_is_rejected`、`normal_loop_still_works_and_budget_resets`) | ✅ 已验证 |

---

## 批次 B:任务引擎完整依赖倒置

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| B.1 | 终态值化:`TaskTerminal` 五变体 | `services/task_core/terminal.rs`(Complete/AwaitApproval/Followup/Failed/Noop) | ✅ 已验证 |
| B.1b | 删 `OnSuccess`/`self_finalized` | 已删除;引擎收尾收敛为 `TaskService::finalize_terminal` 单一出口 | ✅ 已验证 |
| B.1c | legacy/followup 改返回终态(不再写库) | `legacy.rs`/`followup.rs` 零写库调用(仅注释提及);plan 的 PlanExecutor 返回 AwaitApproval | ✅ 已验证 |
| B.2 | 抽 `task_core` 共享模块 | `task_core/{types,prompt_consts,delta,terminal,backend,mod}.rs`;宿主侧 re-export 保证调用方零改动 | ✅ 已验证 |
| B.3 | `TaskBackend` trait 依赖倒置 | `task_core/backend.rs`(20 方法,签名逐字照抄,**写库类保持 bool 不改成 Result**);`task_service/backend_impl.rs` 全一行薄委托 | ✅ 已验证 |
| B.3b | 删 `task_engine` 内全部 `task_service` import | **断言① 零命中**;规则 C 15→0 | ✅ 已验证 |
| B.4 | multi 委托 solo | `multi.rs` 收缩为 newtype 委托 `SoloExecutor` | ✅ 已验证 |
| B.4b | 抽 `step_runner::run_steps`,取消口径统一 | **已改为知情接受**(见下「未做项」) | 已放弃 |
| B.4c | `usage_as_output` 统一三份转换 | solo/custom 改调 helper(保留 finish_reason 诊断覆盖) | ✅ 已验证 |
| B.5 | StateMachine 收口 | `state_machine.rs:50` 迁移表补 Idle→Executing;solo.rs 5 处 `let _ =` 改 `tracing::warn!`;注释修正 | ✅ 已验证 |
| B.6 | L1 渗透清理 | `contracts/registry.rs` 改闭包注入;**`parsing/preset.rs` 的 `PromptFloor` 家族下沉 `models/types.rs`**;规则 D 3→0。`models/transport.rs` 拆分改为知情接受 | ✅ 已验证(部分) |
| B.7 | 每个执行器迁移即时回归 | tasks 50 / task_events 8 / lib 787 / contracts_e2e 4 全绿 | ✅ 已验证 |

---

## 批次 C:门禁接线

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| C.1 | build.ps1 硬门禁 + `-SkipChecks` | **实测注入失败测试 → `[FAIL] 门禁未通过,构建中止` + EXITCODE=1** | ✅ 已验证 |
| C.2 | pre-push hook + 安装脚本 | `tools/hooks/pre-push`(已装入 `.git/hooks/`)、`tools/install-hooks.ps1`、`npm run hooks:install` | ✅ 已验证 |
| C.3 | 测试数自动统计 | `tools/count-tests.mjs`;实测 **977(793+184)/ 81 文件 745 例**;`--check` 与 MAINTENANCE 一致 | ✅ 已验证 |
| C.4 | 前端类型逃逸 ratchet | `tools/check-frontend-lint.mjs`(五项基线全 = 基线;替代 ESLint 的理由见脚本头注释) | ✅ 已验证 |
| C.5 | check-arch 增 Rust 分层规则 C/D/E/G | 实测 **C=0、D=0、E=0、G=116≤117**;已切**硬门禁**(注入违规实测 exit=1) | ✅ 已验证 |

---

## 批次 D:数据一致性与错误一致性

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| D.1 | `data_raw` 并发写保护 | **改方案**(优于加 version 列,理由见下):新增 `services/character_data.rs::update_data_raw`,把 SELECT→改写→UPDATE 放进**同一写锁事务**;四处调用点改用(world_book/user_script/character.update/set_embedded_contract) | ✅ 已验证 |
| D.1b | 并发丢更新测试 | `tests/db_concurrency.rs::concurrent_data_raw_writes_do_not_lose_updates`(世界书保存 × 6 与角色更新 × 6 并发,断言两者同时保留) | ✅ 已验证 |
| D.2 | 生产 `.expect()` 清零 | 新增 `log_read_pool_failure` 兜底;7 个 service 全部清零。**规则 E 16→0** | ✅ 已验证 |
| D.2b | 「失败」与「空」可区分 | 本轮仅覆盖取连接失败路径;**memory/session 吞错返空未改**(需逐个定语义,登记为后续) | 部分完成 |
| D.3 | 错误形状统一(117 处裸 `{error}`) | **加 ratchet 规则 G 防新增**(基线 117,当前 116);存量逐个补 code 需逐处判定 HTTP 状态码,盲改会改 API 行为 → 登记跟踪 | 部分完成 |

---

## 批次 E:权限与隔离补口

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| E.1 | 脚本桥纳入授权裁决 | **复核后重新定性**:前端**已有**授权台账(`scriptAuthorization.ts`,按角色+脚本 hash,UI 可撤销),缺的是**后端执行路径**(`agents/engine/mod.rs:641`)与授权台账未打通。两项补齐方案与兼容约束已登记 known-limitations L12 | 待办(需定方案) |
| E.2 | 脚本硬超时 + 数量上限 | `run_scripts.rs`:5 秒墙钟硬超时(runtime 协作式中断拦不住阻塞在 native 闭包者)+ 单轮上限 32 | ✅ 已验证 |
| E.3 | MCP 行长上限 | `mcp/client.rs`:8MB 行长上限,超限判协议异常并结束读循环;**EOF 注销工具未做**(需改注册表生命周期,登记 L16) | 部分完成 |

---

## 批次 F:跨端协议单一事实源

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| F.1 | 导出共享 fixture | `docs/fixtures/mvu_patch_cases.json`(18 用例 + 书写规范/_comment 说明) | ✅ 已验证 |
| F.2 | 双端读同一 fixture 断言 | Rust `patch.rs::mvu_protocol_fixture_is_consistent`;TS `mvu/parser.contract.test.ts`(21 例) | ✅ 已验证 |
| F.3 | **对拍发现并修复真实跨端差异** | TS 允许 `< UpDateVariable >`(空白),Rust 不允许 → 给 Rust 的 UpdateVariable/JSONPatch 正则加 `<\s*`/`</\s*` | ✅ 已验证 |

---

## 批次 F:跨端协议单一事实源

## 批次 G:前端收敛与性能

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| G.1 | 聊天流渲染节流 | `ChatMessageItem.vue`:新增 `renderTextSource` computed + `paintText` 按帧快照(rAF/16ms 退化);**测试证明有效**(同帧内容连改不触发重算,帧到达一次性反映且不丢字)。注:首版 `void renderTick.value` 写法经测试证明**无效**(computed 仍因 content 变化立即重算),已改为快照方案 | ✅ 已验证 |
| G.2 | 契约标签收敛 + `satisfies` 穷尽校验 | 新增 `web/src/api/labels.ts`(`MODE_LABELS`/`MODE_OPTION_LABELS`/`MODE_ORDER`/`messageKindLabel`);`TaskBoard.vue` 与 `TaskModeSelect.vue` 均改为引用(消除两处手写) | ✅ 已验证 |
| G.3 | `scriptBlocksCache` 加上限 | 未做(见「未做项」) | 已放弃 |
| G.4 | `ChatMessageItem.vue` 行为测试 | 新增 `ChatMessageItem.test.ts`(**9 例**:角色分流/markdown/content_display 优先/流式光标/swipe 角标/状态栏注入与隐藏/节流语义/非流式不受节流) | ✅ 已验证 |
| G.5 | 弹窗开关单一注册表 | 未做(见「未做项」) | 已放弃 |

---

## 批次 H:文档治理与晋升跟踪

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| H.1 | `ARCHITECTURE-3H.md` §4 现状化 + **§7 偏差登记 + §8 遗留登记** | 已写入(7 项叙事与实现差距 + 3 项知情接受遗留) | ✅ 已验证 |
| H.2 | `MAINTENANCE.md` 测试数字改自动统计 + 门禁纪律 | 数字改为 977/745 且 `count-tests --check` 通过;新增「门禁纪律」小节 + EJS 冻结纪律 + flaky 登记 | ✅ 已验证 |
| H.3 | `任务引擎六模式.md` 补 `TaskTerminal`/`TaskBackend` 设计 | **实为已完成**:该文档 `:96-130` 已含 `task_core`/`TaskTerminal`/`TaskBackend` 完整设计 | ✅ 已验证（2026-09-14 勘误：此前误记「部分完成」，与文档实际内容矛盾） |
| H.4 | `known-limitations.md` 增补新发现 | 新增 **L12~L16**(脚本授权不对称/流式重渲染/核心组件无测试/契约文案/ MCP 无热重连)+ 末尾「已封堵项」索引 | ✅ 已验证 |
| H.5 | 本核对表全项收口 | 本文件(含「未做项与理由」章节) | ✅ 已验证 |
| H.6 | `docs/README.md` 索引同步 | 已同步(新增实施计划/核对表,归档漂移表) | ✅ 已验证 |

---

## 未做项与理由(知情接受,非遗忘)

| 项 | 为何不做 | 后续路径 |
|---|---|---|
| **B.4b 步进循环抽 `step_runner`** | 会同时改动 legacy/plan/custom 三个**已稳定运行**的模式,收益(取消口径一致)低于回归风险;三者取消兜底口径差异已如实登记 | 待实跑暴露不一致时再做;`ARCHITECTURE-3H.md §8` 有登记 |
| **B.6 `models/transport.rs` 拆分** | 仅文件整洁收益,却要大范围改 import;已由 check-arch 规则 D 的豁免项显式标注 | 需要时可机械搬迁,无行为风险 |
| **D.2b memory/session「吞错返空」** | 逐个函数判定「失败」与「空结果」的语义边界需业务知识(哪些调用方真能区分?),盲改会把「降级」变成「报错」 | 已登记;如需推进应逐个函数评审 |
| **D.3 存量 117 处裸 `{error}` 逐个补 code** | 每处需判定 HTTP 状态码,盲改**会改变 API 行为**(如把 400 变 500) | 已加 ratchet 规则 G 防新增;可分批按域推进 |
| **E.1 脚本授权打通** | 复核发现**前端已有**授权台账,缺的是后端路径与台账的对接——涉及跨端信任模型设计(后端不能盲信前端声明),非机械改动 | 两个方案已登记 known-limitations L12,需先定方案 |
| **E.3 MCP EOF 注销工具** | 需改 `ToolRegistry` 生命周期(注册表目前只增不减),属结构性改动 | 已登记 L16 |
| **G.3 `scriptBlocksCache` 上限** | 需先确认长生成期间的实际内存峰值是否构成问题(未测得压力证据),避免为未证实的风险引入 LRU 复杂度 | 已登记 L13/L14 附近;可加埋点测量后再定 |
| **G.5 弹窗开关单一注册表** | 纯整洁性重构,改动面涉及 App.vue 模板与返回键逻辑,收益低 | 已登记 L15 |

## 本轮的实际收益(量化)

| 维度 | 前 | 后 |
|---|---|---|
| EJS 死循环/深嵌套 DoS | 单张卡即可挂死 worker / 栈溢出 abort | 预算 + 三守卫点封堵,5 个测试锁定 |
| `task_engine` ↔ `task_service` 环依赖 | 双向 import(引擎侧 10 文件) | **零引用**,trait 缝 + 规则 C 硬门禁 |
| L1 被 L2 渗透 | 3 处 | 0 处(规则 D 硬门禁) |
| 生产 `.expect()`(panic 掉请求线程) | 16 处 | **0 处**(规则 E 硬门禁) |
| `characters.data_raw` 丢更新 | 4 处读改写无事务 | 同写锁事务 + 并发测试 |
| 测试/护栏自动触发 | 无(全靠人手跑) | build.ps1 硬门禁 + pre-push hook(实测能拦截) |
| 协议跨端一致性 | 仅注释声明「逐字对齐」 | 双端读同一 fixture;**并逮到 1 处真实差异**(空白标签) |
| 测试总数 | 后端 975 / 前端 733 | 后端 **977** / 前端 **745**(新增均为行为断言) |

> **数字说明(2026-09-14)**:上表为 2026-09-13 当时值。**测试数量的唯一真值源是
> `MAINTENANCE.md`**(受 `tools/count-tests.mjs --check` 守护),本表不再跟踪该数字
> ——历史上同一数字在 5 份文档并存(923/697、975/733、977/745、971/763、1017/768),
> 根因就是多处手抄。需要当前值时请跑 `npm run count:tests`。

---

## 规则 I/J 落地核对(2026-09-14 增补)

> 2026-09-14 为 `check-arch.mjs` 新增规则 I(代际归属完整性)与 J(跨代依赖方向),
> 其 SSOT 是 `tools/arch-layers.json`。以下为该项的独立核对(不在原计划批次内)。

| ID | 任务 | 验证命令/证据 | 状态 |
|---|---|---|---|
| IJ.1 | 失败分桶:前端违规不再写入后端桶 | `tools/check-arch.mjs` 的 `frontendFailures` 独立桶 | ✅ 已验证 |
| IJ.2 | 汇总两桶都打印后再统一退出 | 注入后端+前端各一条违规,实测**两段报告同时输出**且 exit=1 | ✅ 已验证 |
| IJ.3 | 26 个前端根文件补登记代际 | `arch-layers.json` 的 `frontendEntryFiles`(逐个含 `generation`/`responsibility`) | ✅ 已验证 |
| IJ.4 | 根文件两侧参与代际解析 | `feModuleGen` 同时查目录与根文件;实测前端跨目录边参与校验比例由 29% 恢复 | ✅ 已验证 |
| IJ.5 | SSOT 纳入版本控制 | `git ls-files tools/arch-layers.json` 非空 | ✅ 已验证 |
| IJ.6 | 消除 7 处悬空引用 | 补齐 `docs/架构分析-2026-09-14.md`;引用检查无输出 | ✅ 已验证 |
| IJ.7 | 前后端方向规则分离 | `dependencyRules.backendAllowed` / `frontendAllowed`;前端 `entry` 作被引用方豁免 | ✅ 已验证 |
| IJ.8 | 单值代际纪律 | `web/src/mvu/` 由「L1+L3」改为 L1 | ✅ 已验证 |
| IJ.9 | 新暴露的真实越代边登记 | `stores→characterScriptSandbox` 登记为 `debt` | ✅ 已验证 |
| IJ.10 | 规则 I/J 文档归属 | `MAINTENANCE.md §0` 新增「代际归属与跨代依赖门禁」条 | ✅ 已验证 |
| IJ.11 | 一次性分析脚本归位 | `.arch-scan.py`/`.fe-scan.py` 移入 `tools/dev/` 并加「非门禁」文件头 | ✅ 已验证 |

---

## 最终端到端验证记录(2026-09-13)

`tools/check-all.ps1` 全流程逐阶段结果:

| 阶段 | 结果 |
|---|---|
| `cargo fmt --check` | ✅ OK(首轮失败→已 `cargo fmt` 修正,见 commit `90b9ff4`) |
| `cargo clippy --all-targets -- -D warnings` | ✅ OK(首轮 3 处 warning→已修,见 commit `586483e`) |
| `cargo test --workspace` | ✅ **971 通过 / 0 失败**(19 个集成文件全跑到) |
| `check-contract.mjs` | ✅ 10 组映射一致 |
| `check-arch.mjs`(C/D/E/G 硬门禁) | ✅ 后端分层无违规 + 前端无新增违规 |

> **⚠️ 本表记录的是 2026-09-13 当时状态。2026-09-14 复核发现:`check-arch.mjs` 随后新增了
> 规则 I/J(代际归属 + 跨代依赖),而当时 `tools/arch-layers.json` **未纳入 Git**、26 个前端
> 根文件**未登记**,门禁实际处于 **FAIL(26 处)** 且失败被「早退」遮蔽(前端报告段永不输出)——
> 故 2026-09-13 之后的 `build.ps1` 门禁一度处于阻断态。该问题已于 2026-09-14 修复
> (失败分桶 + 两桶打印后统一退出 + 补登记 + SSOT 纳管),完整记录见
> [架构分析-2026-09-14.md](架构分析-2026-09-14.md) §4.1/§4.2 与 §6 批次 0。
| `check-frontend-lint.mjs`(ratchet) | ✅ 五项 = 基线 |
| `vue-tsc --noEmit` | ✅ 通过 |
| `vitest run` | ✅ **81 文件 / 763 用例全绿** |
| `vite build` | ✅ 通过(4.83s) |
| `count-tests --check` | ✅ 与 MAINTENANCE.md 一致 |

**已知环境噪声**(非代码缺陷,已在 MAINTENANCE.md 登记):
`settings_connector::mock_auto_switches_to_openai_on_save` 在**多测试二进制并发**时偶发失败
(Windows 文件占用 `os error 2/32`,共享临时目录被并发清理);隔离或串行运行 6/6 通过。
必要时用 `cargo test --workspace -- --test-threads=1` 或按 `--test` 分文件跑。
