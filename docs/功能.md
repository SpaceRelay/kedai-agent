# 功能 —— 已实现的能力面

> 本文件归属:**功能**类(判据:现在能用);字面接口规定见 `契约.md`,未实现项见 `计划.md`/`遗留.md`/`展望.md`。
>
> 收录标准只有一个:**代码里已跑通、有实测证据或已提交的能力**。未实现的计划、已知缺陷、未来方向
> 一律不写入本文件——已知缺陷与有意裁剪见 `遗留.md`(登记体例见
> [known-limitations](archive/2026-09-16-consolidation/known-limitations.md)),
> 未实施计划见 `计划.md`,长期方向见 `展望.md`。
>
> 引用写法:指向原 `docs/` 文档的引用一律写 `archive/2026-09-16-consolidation/<原文件名>`(相对 `docs/`);
> 指向 `MAINTENANCE.md` 的写 `../MAINTENANCE.md`。**测试数量、构建步骤等一切可计数事实以
> `../MAINTENANCE.md` 为准**,本文件只给取得命令,不手抄数值。
>
> 每章的「权威来源」给的是**能力现状**的取证落点(源码 `文件:函数名` 或文档小节);
> 接口字面(路径、字段、错误码取值)以 `契约.md` 系列为准,此处不重复。

---

## 聊天主链路

Kedai 的主链路是「一次发送 → 上下文装配 → LLM 流式生成 → 落库 → SSE 回推」,
`/api/chat/send` 是唯一入口,一次请求走完 Agent 引擎全流程。

**现状能力**

- **端点与分支**:`POST /api/chat/send` 解析请求体(空消息 400)→ 无 `session_id` 时
  `ensure_session` + `seed_first_message` 补开场白 → 引擎并发预检(`engine.is_active`,冲突 409)
  → 生成参数组装(请求体优先、回退运行期设置;`max_tool_rounds` 默认 32)
  → `pending_runs` 原子占位(先于任何消息写入)→ 三路分支:**重生成锚点**(校验末条 assistant)、
  **重发锚点**(校验末条 user 且正文一致)、**常规落 user 消息**(`extra` 带 ts 与 prompt_tokens)。
- **模式归一**:`agent` / `deep` / `custom`,缺省 `fast`;`custom` 分支取当前启用流程、
  `validate`、过滤 disabled 步骤后快照下发。
- **参数安全上限**:请求体参数有硬上限校验(65536 / 1048576 两档),越界拒绝而非截断。
- **字数注入联动**:简单模式「字数」项启用时自动上调 `max_tokens`(`output_budget_for_word_count`),
  防思考模型把正文预算烧在思考上。
- **占位生命周期**:`pending_runs` 的登记与清理由 `PendingRunGuard`(drop 守卫)承担,
  引擎 panic 也会清理——不再出现「一次 panic 后该会话永久 409」。
- **SSE 管线**:handler 建 `mpsc::channel::<SseEvent>(1024)`,`tokio::spawn(engine.run(..))`,
  SSE 响应以 `KeepAlive 30s` + `no-cache` 输出。事件类型:
  `token`(文本片段)、`step`(Agent 步骤)、`tool_call` / `tool_result`、
  `tool_authorization_required`、`vars`(变量树同步)、`interrupted`、`error`(含 code/retryable)、
  `finish`(含 usage)、`task`(任务模式事件,前端仅透传)。
- **中断与抢占**:同会话旧 run 在引擎入口先 `abort()`,再登记新 run;中断通过
  `watch::channel<bool>` 中止标志贯穿全链路;`finish_run` 按 run_id 清理登记(旧 run 不删新 run)。
- **收尾顺序纪律**:assistant 消息落库**必须先于** `Finish` 事件(消除前端 `loadHistory` 竞态);
  随后依次是角色卡后端脚本执行、其余作用域落盘、契约运行态提交、`Finish{usage, content}`、
  用量统计(`session_usage` + `global_usage`)。
- **swipes 版本**:重生成走 `upsert_regenerated_message`,版本数组落 `extra.swipes`,
  `POST /api/chat/messages/{id}/swipe` 切换版本;历史投影取当前版本进入上下文。
- **配套端点(已实现)**:`/api/chat/stop`、`/api/chat/history`、会话 CRUD、
  `sessions/{id}/truncate`(保留锚点删其后)、`sessions/{id}/regreet`(重生成开场白)、
  `/api/chat/clear`、`/api/export/chat` 与 `/api/import/chat`(SillyTavern 兼容)、
  `/api/chat/generate-raw`(角色卡资源页作者脚本的一次性非流式生成桥,不写会话)、
  `/api/chat/sessions/{id}/undo` + `/api/undo/{id}/restore`(快照回退)。

**权威来源**:`server-rs/src/api/chat.rs`(`send` / `generate_raw_budget` / `PendingRunGuard`)、
`server-rs/src/agents/engine/mod.rs`(主流程与收尾)、`server-rs/src/agents/engine/run_finish.rs`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 2 章
(33 步链路与引擎字段表)、[0.3.0-beta-后续修复-变更说明](archive/2026-09-16-consolidation/0.3.0-beta-后续修复-变更说明.md) §三、§六。

**关联契约**:`契约.md`(聊天与会话端点字面)、`契约-协议与配置.md`(SSE 事件格式与生成参数)。

---

## 上下文工程与压缩

上下文装配是「每轮重新算前缀、但前缀逐字节稳定」的设计,压缩在装配之前发生。

**现状能力**

- **装配顺序(函数级)**:`collect_context` 阻塞池读角色卡/历史/压缩摘要 → 历史投影
  (重生成取当前 swipes 版本)+ snip → 世界书条目收集/分离/分组/渲染 → mvu 状态块生成 →
  产出只读 `CollectedCtx`;`finalize_messages` 做
  `build_llm_messages_with_position` 六层拼装(系统提示 / 世界书常态位 / 注入 / 历史 /
  GENERATE 位 / @INJECT)→ 运行时主提示词前置 → **摘要槽 → 记忆槽**插入 →
  `trim_to_context` 按预算裁剪 → 发送统计落宏变量。
- **前缀分层**:消息组装固定为「system(静态)→ 摘要槽 → 记忆槽 → 尾部历史(只追加)」;
  同会话两次构建的公共前缀逐字节一致(有回归测试护航),使 provider 侧前缀缓存可命中。
  概率门控与随机命中不扰动该前缀(构建侧有不变式测试锁定:常驻条目进 system 前缀且不参与概率,
  触发条目进尾部注入区)。
- **缓存感知压缩**:每轮 LLM usage(含 DeepSeek `prompt_cache_hit_tokens` / OpenAI
  `cached_tokens`)落 `llm_requests` 表;`GET /api/diagnostics/cache` 报告命中率、费用估算与
  **四级水位**(soft / snip / compact / force);前端「优化」弹窗内 `CacheHealthPanel` 展示。
- **压缩本体**:`agents/engine/compaction.rs` 做**可逆投影 + LLM 摘要**——原文消息永不删除,
  摘要落 `session_compactions` 表,删摘要行即恢复完整历史。触发方式:
  manual(`POST /api/chat/compact`,清除走 `/api/chat/compact/clear`)或 auto(历史 token 超阈值
  时在 plan 之后、`collect_context` 之前执行);设置项 `compaction_mode` / `compaction_threshold`。
- **阶梯压缩(snip)**:LLM 摘要前先做零成本 snip——陈旧超长工具结果压成占位符、
  错误特征保留、尾部原文保留;`compaction_keep_recent`(默认 4)与 `compaction_snip_bytes`
  (默认 8192)可配置。
- **摘要槽增量化**:摘要改追加式增量(旧摘要字节冻结),压缩后缓存只 miss 尾部。
- **工具历史裁剪**:`run_tool_loop` 每轮 `execute_generation` 前执行 `trim_tool_history`——
  按「assistant(带 tool_calls)+ 后续连续 tool 消息」分轮,keep-recent + 预算双闸门在**轮**粒度操作:
  超出 `tool_history_keep_rounds`(默认 4,钳 1..=32)的最老轮工具结果原地替换为短摘要
  (幂等标记,已摘要轮凭前缀跳过、不占 keep 名额),该轮 assistant 的 `reasoning_content` 一并清空;
  估算总 token 超 `tool_history_budget_tokens`(默认 16384,0 = 禁用预算闸门)时继续摘要至达标;
  保底最近 1 轮始终完整;`tool_calls` 与 `tool_call_id` 全部保留以维持严格后端要求的配对。
- **token 计数口径**:`count_message_tokens` 计入 `tool_calls` 的 name/arguments
  (新增 `count_single_message_tokens` 统一口径),token 计数按 (编码, 文本哈希, 长度) 缓存;
  裁剪侧配套 `reclaim_round_arguments` 回收参数占位(占位保持**合法 JSON**,id/name 保留);
  `trim_tool_history` 返回 `ToolHistoryTrimOutcome`,未达标记走 warn 而非静默。
- **裁剪兜底**:压缩后仍超预算时 `trim_to_context` 从最早历史向前裁剪,系统层与世界书常态层不动;
  极端兜底分支按「1 token ≈ 2 字符」保守起步 + 真实 tokenizer 复测,仍超预算则按比例收缩一次。
- **跨会话记忆蒸馏**的注入侧在本章(记忆槽),蒸馏本体见「记忆」章。

**权威来源**:`server-rs/src/agents/engine/compaction.rs`(`maybe_compact` / `snip_tuples` / `should_snip`)、
`server-rs/src/agents/engine/messages/{build,trim,inject}.rs`(`trim_tool_history` /
`reclaim_round_arguments` / `insert_summary_slot` / `insert_memory_slot` / `protected_head_len`)、
`server-rs/src/services/cache_diagnostics.rs`、`server-rs/src/services/token_service.rs`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 3、4 章、
第 23 步链路,`../MAINTENANCE.md` §14「上下文压缩」「缓存感知压缩管线」,
[perf-baseline](archive/2026-09-16-consolidation/perf-baseline.md) §五(P0-1 两项与 P1-2)。

**关联契约**:`契约-协议与配置.md`(压缩与诊断端点、设置字段)、`契约-架构与数据.md`(摘要/记忆槽与表结构)。

---

## 世界书与提示词注入

世界书(World Info)、正则脚本与三层注入位(@INJECT / GENERATE / RENDER / 楼层)构成提示词注入面。

**现状能力**

- **来源两类**:独立上传世界书(SillyTavern 导出格式,顶层 `entries` 对象或数组,可绑定角色、空=全局)
  与角色卡内嵌 `character_book.entries`(兼容 V2 顶层与 V3 `data.character_book` 两种布局)。
  原始 JSON 无损保留在 `world_books.data_raw`。
- **匹配算法**:扫描池只取 `role=="user"` 的历史消息(assistant 文本不触发);逐条目
  disabled 跳过 → `@@` 装饰器(`@if`/`@unless` 用 EJS 白名单求值,失败容错为照常注入;
  `@var`/`@set` 解析存 `decorators`)→ `constant` 恒注入;
  **正则优先**(`use_regex` 且编译成功),否则 `keys` + `keys_secondary` 并列子串匹配
  (大小写按 `case_sensitive`);`use_probability` 时按纳秒 `%100 < probability` 门控。
- **扫描窗口 `scan_depth`**:关键词匹配只扫最近 N 条用户消息(0/负 = 全部历史);
  SillyTavern 语义正确落地——**`scan_depth` 是独立字段**(`extensions.scan_depth`),
  与「插入深度」`depth`(position=atDepth 用)语义分离,两条路径收敛到共享函数
  `scan_window_len`(优先 / 兜底 / 0 / 封顶四态);显式 `scanDepth` 覆盖 `depth` 兜底。
- **注入位置**:`WorldText{constant, triggered}` 两组——常态并入 system(显式 `role` 则作独立消息);
  激发默认追加**最新 user 消息尾部**(前缀缓存友好),`role=assistant` 作独立消息,
  `role=system` 钳制为 user。排序 `position → order → id` 稳定升序。
- **条目内容渲染**:每条命中 content 过一遍 EJS 渲染(`render_assistant_content_with`),
  渲染为空不注入。
- **上传自动规范化**:兼容酒馆变体——关键词字段名 `keys/key/keywords/keyword` 与逗号分隔字符串、
  `constant` 兼容字符串/数字并支持缺失时自动判定(无关键词无正则 → 常驻)、
  `role` 缺失即「自动」(常驻→system、激发→user);上传响应附转换统计 `conversion`。
- **机制自检**:世界书窗口「检查自动分配机制」按钮 / `GET /api/world-books/auto-assign-check`
  一键验证条目解析、属性自动分配、转换链路。
- **条目编辑与缓存**:条目编辑视图(含正则标记)、全量回写、新增空条目、角色卡内嵌条目读写;
  世界书条目按 character_id 缓存,5 条写路径全部失效;`save_data_raw` 随内容重算 `entry_count`。
- **@INJECT**:消息级精确插入(`pos` / `target` / `regex`),实现于 `messages/inject.rs`,
  协议见 [inject-protocol](archive/2026-09-16-consolidation/inject-protocol.md)。
- **GENERATE / RENDER 内容注入**:`[GENERATE:BEFORE/AFTER]`、`{idx}`、`REGEX` 三种内容注入,
  已在引擎挂载并有测试覆盖,协议见 [generate-render-protocol](archive/2026-09-16-consolidation/generate-render-protocol.md)。
- **EJS 兼容**:世界书条目中的 `<% %>` / `<%= %>` / `<%_ _%>` 按当前变量状态渲染;
  内建 `getvar/setvar/addvar`、`Math`、数组/字符串方法、if/for/箭头函数等 JS 子集;
  `<#escape-ejs>` 块用控制字符占位符转义;自研解释器带循环步数/墙钟预算与解析深度守卫。
- **正则脚本(`regex_scripts`)**:AI 输出中的占位符/变量标记替换为显示 HTML(如状态栏卡片),
  前端可开关;匹配与转义顺序为「先在原文匹配脚本 → 对 AI 输出整体 HTML 转义 →
  仅把命中片段还原为脚本自带内容(作者可信)」,`<script>` 源码提取后由沙箱 iframe 执行并受授权门禁。
- **提示词注入(三层)**:简单模式四项(字数 / 转述 / 对话 / 视角,按可调顺序合成一条拼入 system 末尾,
  支持酒馆宏);禁词库(自由编辑「禁词 → 替换词」映射,替换词留空 = 删除);
  复杂模式**楼层系统**(仿 SillyTavern Prompt Manager:角色/位置/深度/拖拽排序,
  可导入酒馆预设)。配置持久化 `<数据目录>/prompt_floors.json`。
- **LAST_SEND / LAST_RECEIVE 统计变量**:引擎收尾写入会话宏,模板可读;
  `POST /api/settings/prompt-preview` 可预览最终提示词(不发请求)。

**权威来源**:`server-rs/src/agents/engine/worldbook.rs`、`server-rs/src/parsing/world_book.rs`(`WorldEntry`)、
`server-rs/src/agents/engine/messages/{build,inject}.rs`、`server-rs/src/parsing/assistant/ejs/`、
`server-rs/src/services/world_book_service.rs`、`server-rs/src/services/prompt_inject_service.rs`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 3 章、
[角色卡世界书扫描窗口修复-变更说明](archive/2026-09-16-consolidation/角色卡世界书扫描窗口修复-变更说明.md)、
[0.3.0-beta-后续修复-变更说明](archive/2026-09-16-consolidation/0.3.0-beta-后续修复-变更说明.md) §九(概率门控不变式)、
`../MAINTENANCE.md` §0(晋升状态表)、§11(世界书注入用例范围)。

**关联契约**:`契约-协议与配置.md`(世界书/注入端点与条目字段)、`契约.md`(上传响应 `conversion` 形状)。

---

## 角色卡兼容

SillyTavern 生态原生兼容是产品的第一条硬承诺,落在解析层(L1)。

**现状能力**

- **角色卡**:支持 V2/V3 规范(`.png` tEXt 内嵌 或 独立 `.json`),**未知字段无损保留**
  (原卡存 `data_raw`);上传 20MB multipart;列表接口不返回 `data_raw`;
  `tools/inspect-card.mjs` 提供结构检查。
- **开场白**:`first_mes` 自动识别,「编辑提示词」弹窗可编辑,随卡保存在 `data_raw` 中,
  新建会话时作为角色第一句话;留空表示无开场白;`sessions/{id}/regreet` 可重新生成开场白。
- **角色卡内嵌世界书**:兼容 V2 顶层与 V3 `data.character_book`;条目编辑与回写有专用端点。
- **对话互操作**:消息数组与 SillyTavern 完全一致,导入/导出无障碍(SillyTavern 兼容 JSON)。
- **酒馆助手(SillyTavern-Assistant)兼容**:
  - **EJS 模板渲染**:见「世界书与提示词注入」章(同一渲染器)。
  - **变量系统**:会话级 `stat_data` 变量树(SQLite 持久化),`[InitVar]` 条目初始化,
    支持 YAML / JSON / `_.set()` 三种初始格式。
  - **输出协议**:模型回复中的 `<UpdateVariable><JSONPatch>` 块自动剥离并应用
    (`replace/delta/insert/remove/move`),同时兼容 MagVarUpdate 的 `_.set(...)` 格式;
    更新后变量树经 SSE `vars` 事件推送前端,消息快照随 `extra.mvu` 落库。
  - **状态注入**:`{{format_message_variable::path}}` 宏与 `<StatusPlaceHolderImpl/>` 占位符
    展开为当前变量状态(含 `{{getvar/setvar/addvar}}` 树路径);注入位置可在设置中切换
    (默认 `system`,可选「最新用户消息尾部」——变量更新不再使整个 system 前缀缓存失效)。
  - **内嵌插件识别**:「插件」窗口可查看当前角色卡内嵌插件(自动检测,含特性明细:
    初始变量 / EJS 模板 / 变量系统 / 状态注入 / 输出协议);kedai 内置实现无需安装。
- **消息 HTML 渲染**:角色卡 `extensions.regex_scripts` 驱动;顶栏「HTML」开关常驻显示
  (无论卡是否带脚本),状态**按角色卡记忆**、未设置回退全局默认,默认关闭,开启后仅对 AI 回复生效;
  脚本 HTML 清洗自动剥 markdown 代码围栏(` ```html `)、提取 `<head>` 内 `<style>` 前置并只取 `<body>` 内容。
- **后端脚本**:角色卡 / 全局 tavern_helper 脚本在 `rquickjs` 沙箱(`scripts/`)执行,
  经 `spawn_blocking` 串行;TavernHelper 兼容桥提供变量读写 6 函数、`eventOn/eventEmit`、
  `triggerSlash`(自定义 handler > SlashRegistry > 内置最小实现)与脚本元信息;
  扩展管理只读(`install*` 一律 throw)。前端沙箱有授权台账,后端执行路径的脚本授权门
  已在此前修复批次打通。
- **角色卡编辑**:编辑保存不再抹掉 `extensions`(该缺陷在扫描窗口修复批次一并解决)。

**权威来源**:`server-rs/src/parsing/character_card.rs`、`server-rs/src/api/characters.rs`、
`server-rs/src/agents/engine/run_scripts.rs`、`server-rs/src/scripts/bridge.rs`、
`web/src/rendering/render.ts` 的脚本抽取段;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 7 章、
[角色卡世界书扫描窗口修复-变更说明](archive/2026-09-16-consolidation/角色卡世界书扫描窗口修复-变更说明.md) §3.3。

**关联契约**:`契约-协议与配置.md`(角色卡上传/详情/更新字段)、`契约-架构与数据.md`(`data_raw` 无损语义)。

---

## Agent 引擎

Agent 引擎是角色扮演侧的编排核心(任务模式另起一章),与连接器、工具、契约系统共享同一批底座。

**现状能力**

- **状态机**:`idle → planning → executing ⇄ tool_call → reflecting → finished`,可 `interrupted` / `error`;
  中断通过 `watch::channel<bool>` 中止标志贯穿全链路;失败回退经 `retreat_to_generating_step`
  有界重试(不无限自我修复)。`agent_sessions` 表持久化 plan / state / steps。
- **四种模式**:
  - `fast`:单步直接生成;
  - `deep`:计划 → 执行 → 反思,质量更高(主生成不启用工具,反思轮已放开 `read`);
  - `agent`:工具自循环(`run_tool_loop`);
  - `custom`:自定义流程(`AgentFlowConfig` + 流程库)。
- **规划与反思**:`planner.rs` 产出 fast/deep 计划并识别算式;`reflector.rs` 质量反思三规则
  (空 / 截断 / 未答疑问);反思步剥离 `<UpdateVariable>` → 禁词 `censor_text` →
  `reflect_with_tools`(LLM 双轨)或机械 `reflect` → 通过则 `idx+1`,失败则回退重生成。
- **工具循环 `run_tool_loop`**:生成 → 响应含 `tool_calls` 则 `decide_with_policy` 裁决 →
  按风险分组执行(**Safe 级并发**,其余串行且含最长 300s 的用户授权等待,
  SSE `tool_authorization_required` 驱动前端授权卡)→ 回填 assistant(`tool_calls`)+ tool 结果 →
  再生成;上限 `max_tool_rounds`(默认 32)。循环带**重复调用熔断**
  (`utils/loop_guard.rs`:FNV 指纹 + 环形窗口 N=8/K=3,中止理由经 step 事件透出)与
  **工具历史裁剪**(见「上下文工程与压缩」章)。
- **截断自愈**:单轮生成被 `max_tokens` 截断到不可用(空正文且 `finish_reason=length`,
  或任一 tool_call 参数 JSON 非法 / 连接器流尾 flush 报错)时,该轮 `max_tokens` 翻倍原样重发一次
  (上限 8192),重发仍失败才透出原错误;普通文本截断(非空正文)维持「截断仍 `done`」语义;
  触发记录经 `ExecutorResult.self_heals` 透出。**推理感知自愈**:识别「推理耗尽预算」并一次给足预算
  (不再盲目翻倍);四路截断重试统一收敛为 `utils::retry` 纯函数。
- **三层 JS 能力分级**(对用户/卡片作者提供的可执行内容按信任度分层,**全部刻意回避 eval 与任意代码执行**):

  | 层 | 载体 | 实现 | 能力边界 | 资源限制 |
  |---|---|---|---|---|
  | 渲染期模板 | 角色卡/世界书 EJS | 自研解释器 `parsing/assistant/ejs/`(lexer→parser→ast→exec) | 60+ 封闭内建枚举(`value.rs::Builtin`),无 IO/网络/动态求值 | 调用深度 128,`evalTemplate` 嵌套 16(线程局部 `EVAL_TEMPLATE_DEPTH`) |
  | 脚本期 | 角色卡/全局 tavern_helper 脚本 | rquickjs 沙箱 `scripts/` | TavernHelper 兼容桥(见「角色卡兼容」章),桥外无能力 | 每次执行独立 Runtime;合作式超时 1s + 内存 64MB;经 `spawn_blocking` 串行 |
  | 工具期 | `<数据目录>/plugins/tools/*.json` | 白名单表达式求值器 | 必须以 `return/result =` 开头,唯一变量 `args`;白名单函数(JSON/String/Math)+ 成员方法;无循环、无函数定义、无环境 | 注册进 `ToolRegistry` 后受统一权限层与单工具超时约束 |

- **EJS 解释器容错纪律**:模板编译为单一 JS 程序(文本段→`__kedai_out__`,`<%= %>`→`String(...)`,
  `<% %>` 原样拼接,大括号跨标签配对);parser 语句级容错(错误收集后跳到下一语句边界恢复);
  Env 作用域链 `Vec<Rc<RefCell<HashMap>>>`(闭包捕获可写回外层);
  **逐语句容错 + 整体失败回退原文**;「无上下文容错」横切原则:读类函数缺上下文返回默认值,绝不 panic。
- **运行时主提示词**:`DATA_DIR/AGENTS_RUNTIME.md` 由后端文件服务、Engine 与设置页共用同一路径;
  内置默认回退(含角色扮演默认提示词 `from_config` + `load` 空值回填,修复空版本首装提示词为空)。
- **提示词共享原语**:`services/prompt_kit.rs` 承载世界书过滤 / 注入合成 / 占位符渲染 /
  `untrusted_boundary`,引擎与任务双侧调用,不在任一侧复制实现。
- **双模式提示词隔离**:由类型承载(`RoleplayPromptConfig` / `TaskPromptConfig`),
  settings.json 线格式不变;继承/隔离规则见
  [模式提示词边界](archive/2026-09-16-consolidation/模式提示词边界.md)。
- **可观测**:推理链经 SSE 流式推送前端;`/api/agent/plan` 可预览行动计划(不执行);
  `/api/agent/interrupt` 等价 stop。

**权威来源**:`server-rs/src/agents/{state_machine,planner,reflector}.rs`、
`server-rs/src/agents/engine/{mod.rs,executor.rs,run_loop.rs,run_finish.rs}`、
`server-rs/src/services/prompt_kit.rs`、`server-rs/src/tools/registry.rs`、
`server-rs/src/utils/loop_guard.rs`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 2、5、7 章,
`../MAINTENANCE.md` §1(状态机)、§0(EJS 冻结纪律与晋升状态),
[perf-baseline](archive/2026-09-16-consolidation/perf-baseline.md) §五(P0-2 熔断、P2-1 推理感知自愈)。

**关联契约**:`契约-协议与配置.md`(SSE 事件与 Agent 控制端点)、`契约-架构与数据.md`(状态机与 `agent_sessions`)。

---

## 六模式任务引擎

任务模式是与角色扮演**平级的第二顶层模式**(`appMode`)。本节为
[任务引擎六模式](archive/2026-09-16-consolidation/任务引擎六模式.md) 的全文移植(该文档原为
`services/task_engine/` 的设计依据,此处按能力现状收录;未实现项已改记为交叉引用)。

分层归属:契约在 L1(`models/`),引擎在 L2(`services/task_engine/`),六模式执行器
(含 legacy/followup)全部驻 `task_engine`,模式选择 UI 在 L3。

### 一、六模式语义

| 模式 | 语义 | 执行路径 |
|---|---|---|
| `legacy`(默认) | 三段式:规划 → 逐步执行 → 汇总。**行为逐字节不变** | `task_engine/legacy.rs`(`LegacyExecutor`,自 `task_service/executor.rs` 机械搬迁) |
| `solo` | 单主 agent 工具自循环:目标直接进 run_tool_loop,工具集按 `task_tool_policy` 编译(默认 `deny_dangerous`:危险级与元工具除外、bash 例外),步数上限 `max_tool_rounds` | `task_engine/solo.rs` |
| `multi` | solo + 子 agent 工具化:主 agent 可经 `agentgo` 派子 agent(走 run_tool_loop + 工具白名单,`agent_depth+1` 深度守卫生效) | `task_engine/multi.rs`(复用 solo 主循环 + `tools/agent_tools_agent.rs` 的工具化子任务路径) |
| `plan` | 只规划不执行:产出计划进 `planned` 待批准态,零副作用;`POST /api/tasks/{id}/approve`(可携修改后计划)批准后消费已批准计划逐步骤续跑(每步独立 agent 调用 + 汇总);放弃 = stop | `task_engine/plan.rs` |
| `team` | 规划器拆子目标(全局上限 15)并归并为 2~4 个主 agent,每主领 1~4 个子目标;首版仅自动拓扑(规划器动态决定);各主并行跑 solo 循环(主内按子目标逐个独立执行)并可派子 agent;末段审计 agent 做一致性/质量/覆盖度审查(缺漏打回指定主 agent 补做,最多 1 轮;补做后追加一次终审,只产出结论文本不再打回),再升华整合输出最终结果 | `task_engine/team.rs` |
| `custom` | 复用 `AgentFlowConfig` / `validate_flow` / 流程库(L2 既有资产),配轻量 step 执行器:只吃 steps+goal,不依赖角色卡/聊天历史;每步按 `step.tools` 白名单走 `execute_generation` 或 `run_tool_loop` | `task_engine/custom.rs` |

> `tools` 三态(易误配,2026-09-10 实跑修复 F8):`None` = 不使用工具,**`Some([])` = 全部工具**,
> `Some(list)` = 白名单。前端已在「全部工具」及「不生成正文 + 全部工具」组合下加警示文案。

#### 工具可用性矩阵(2026-09 授权改造后)

任务模式工具集合由 `task_tool_policy` 编译(见 [授权模式](archive/2026-09-16-consolidation/授权模式.md) 第四节),
且**恒不等待授权**(未放行工具立即拒绝为 `tool_policy_denied`)。默认策略 `deny_dangerous` 对 `bash`
按工具名开例外下发(任务模式需命令执行能力),但破坏性/提权命令仍由命令级硬门统一拒绝。
各模式实际可用工具:

| 模式 | 规划/编排阶段 | 执行阶段 |
|---|---|---|
| `legacy` | 只读侦察 4 个(`tool_sets::READONLY_SCOUT`,≤2 轮) | **无工具**(纯生成) |
| `solo` | 无规划 | 按 `task_tool_policy` 编译的集合(默认 `deny_dangerous`:危险级与元工具除外、bash 例外) |
| `multi` | 无规划 | 同 solo(主循环逐字节复用);子 agent 用 `tool_sets::SUBAGENT` 只读白名单 |
| `plan` | 只读侦察 4 个 | 批准后逐步执行,每步按 `task_tool_policy` |
| `team` | 规划/审计/汇总均无工具 | 各主 agent 按 `task_tool_policy` |
| `custom` | — | 按 `task_tool_policy` 编译后再与步骤 `PlanStep.tools` 取交(只能收窄) |

共享白名单常量集中在 `server-rs/src/tools/tool_sets.rs`(单一出处):
`META_TOOLS`(正文元工具 `get_state` / `apply_patch`,永不进正文列表)、`READONLY_SCOUT`、
`SUBAGENT`、`REFLECT`。

> 说明:`multi` 与 `solo` 的主循环参数相同,差异仅在「子 agent 链路」由 `agentgo` 提供——
> 而 `agentgo` 在 solo 的策略编译集中同样可用。两者的实质区别是模式语义标注与团队协作预期,
> 执行器层面等价。`legacy` 与其余模式的差异则远大于「有无规划阶段」:其执行步骤完全不启用工具。

### 二、L1 契约变更与兼容声明

变更(全部先写失败测试):

1. `tasks` 表新增 `task_mode TEXT NOT NULL DEFAULT 'legacy'` 列
   (`migration.rs` 的 `ensure_tasks_task_mode_column`,PRAGMA 探测缺失才 ALTER,幂等专测);
2. `TaskRunMode` 枚举:legacy/solo/multi/plan/team/custom,serde snake_case,
   `from_str_lossy` 容错回退 Legacy(DB 未知值不 panic);
3. `TaskRecord.task_mode` 字段(serde default,旧 JSON/旧客户端零变化;TS 同步);
4. `TaskStatus` 新增 `Planned` 变体(线格式 "planned";Rust 三枚举 + TS union + `taskStatus.ts` 三处同步);
5. `SseEvent::Task` 新增 kind:`agent_status`(主/子 agent 状态迁移)、
   `approval_required`(计划待批准)。

不动(兼容承诺):`TaskStatus` 既有变体、`TaskStep` 四字段、既有事件 kind
(created/status/plan/subtask/usage/llm_call/deleted)、REST 端点形状、`task_usage` / `task_llm_calls` 列。

### 三、引擎复用路径(关键设计决策)

任务模式复用聊天引擎的 `execute_generation` / `run_tool_loop`
(`agents/engine/executor.rs`),**不建影子 sessions 行**。

决策依据(2026-08-29 代码实测):

- `PRAGMA foreign_keys = ON`,`agent_sessions`/`llm_requests`/`messages` 等表全部
  FK 到 `sessions(id)`;影子行方案需贯穿多张表造影子数据,且 `sessions` 表会混入任务行
  (聊天列表污染面大);
- `run_tool_loop` 对 `agent_session` 的依赖仅为状态落库调用,可 `Option` 化;
- runs 互斥登记只在 `AgentEngine::run()` 内,直接调 `run_tool_loop` 天然绕开,
  任务级互斥由 `task_service/cancel.rs` 的 token 机制承担(既有)。

最小改动清单:

1. 可见性:`pub(super) mod executor` → `pub(crate)`;`execute_generation` /
   `run_tool_loop` / `ExecutorResult` / `AbortFlag::new` → `pub(crate)`;
2. `run_tool_loop` 的 `agent_session: &AgentSessionRecord` 参数改为
   `Option<&AgentSessionRecord>`(内部 `update`/`add_tool_call` 调用点改 `if let Some`),
   任务模式传 `None`,聊天路径传 `Some`(行为不变);
3. `llm_requests` 落库(`execute_generation` 内的 `save_llm_request` / `save_llm_cache_usage`)
   对 `task:` 前缀 session_id 跳过——任务侧调用追踪走 `task_llm_calls`,llm_requests 仅聊天链路,
   避免 FK 失败告警刷屏;
4. 构造顺序:`app_state` 先建 `AgentEngine`,再以 `Arc<AgentEngine>` 注入 `TaskService`
   (engine 不依赖 tasks,无循环);
5. 事件通道:task_engine 自建 mpsc,起 drain 任务把引擎事件映射为 `SseEvent::Task`
   (kind=agent_status 等)经 broadcast 转发,并落 `task_llm_calls`(phase=agent/subagent/audit);
6. 工具授权:任务模式无 UI 授权上下文,`run_tool_loop` 的 `step_whitelist` 恒传
   `Some(&[])`(solo/custom:空白名单=放行已按策略编译的集合)或显式白名单(multi 子 agent),
   避免危险工具 300s 授权等待;`TaskService` 的 bypass 设置语义不变。

### 四、task_engine 底座

```
services/task_core/            ← 依赖倒置后的「共享内核」(2026-09-13 批次 B.3)
  terminal.rs  TaskTerminal:执行器唯一产出(Complete/AwaitApproval/Followup/Failed/Noop)
  backend.rs   TaskBackend trait:引擎对宿主能力的唯一依赖面(读写库/事件/LLM 生成)
  types.rs     TaskGenOutput(执行器与宿主共用的生成产物类型)
  delta.rs     DeltaBatcher + DELTA_FLUSH_WINDOW(流式攒批,引擎与宿主共用)
  prompt_consts.rs  9 个提示词常量(planner/executor/summarizer/team/custom)

services/task_engine/
  mod.rs       TaskEngine(Arc<dyn TaskBackend> + Arc<AgentEngine>)、模式派发入口
  sink.rs      引擎事件桥:自建 mpsc 通道 + drain 任务,把引擎事件翻译为任务事件
               (SseEvent::Task kind=agent_status/delta)经 broadcast 转发
  executor.rs  ModeExecutor trait:run(ctx) -> (TaskTerminal, TokenUsage)
  context.rs   TaskRunContext(task_id/goal/settings 快照/character_id/cancel)
  solo.rs / multi.rs / plan.rs / team.rs / custom.rs / legacy.rs / followup.rs
```

**终态值化(批次 B.1)**:执行器**只返回 `TaskTerminal`、不写库**。此前 legacy/followup 经
`OnSuccess::SelfFinalized` 自行调 `complete_mode_run`/`finalize_mode_run` 写终态(旁路),
现改为返回终态值,由 `TaskService::finalize_terminal` 单点分派到既有 5 种写入形态
(Complete→写 result+消息、AwaitApproval→写 planned、Followup→result 与消息文本分离、
Failed→按取消判定写 ended/error、Noop→仅清理取消条目)。收益:收尾语义集中一处、
执行器不再拥有写库能力。

**依赖倒置(批次 B.3)**:`task_engine` 只依赖 `task_core::TaskBackend`,不再 `use task_service`
——**环依赖已断**(此前 task_engine ↔ task_service 双向 import)。宿主侧
`task_service/backend_impl.rs` 以一行薄委托实现 trait;签名逐字照抄既有方法
(**写库类保持 `bool` 返回,不得漂移为 `Result`**)。副产品:可写 mock `TaskBackend`
对执行器做纯单测(此前必须起完整 `AppState`)。

> 以上两项由 `tools/check-arch.mjs` 规则 C 硬门禁锁定(违反即构建失败)。

> legacy 已是 `task_engine/legacy.rs` 的 `LegacyExecutor`(批次 M3 上缝,自原三段式机械搬迁,
> 行为逐字节不变);followup 同样已上缝(`task_engine/followup.rs`)。「legacy 派发在
> task_service 侧」是搬迁前的陈旧描述,`run_task_background` 现已全库无此函数。
> 派发点统一在 `task_engine/mod.rs::run_mode`。

> `ModeSettings` 的「每子模式独立覆盖字段」属未落地项,见 `计划.md`(现状:子模式共享同一份任务模式设置)。

### 五、team 模式细化

- 拓扑:首版(批次 4.3b)只做自动拓扑(规划器决定);手动拓扑属未实现项,见 `计划.md`;
- 拆目标:规划器输出 ≤15 个子目标,归并映射到 N 个主 agent(自动 N∈[2,4],
  每主 1~4 个子目标;4×4=16 可超 15,全局封顶由解析侧从末位主倒序截断保证);
- 并行:各主 agent 各自跑 solo 循环(独立 `TaskRunContext` + 派生运行身份),
  主内按子目标逐个独立执行(每子目标一次 agent 调用,各写各自步骤的 result/status;
  同一主的多个子目标复用同一虚拟 session `task:{id}:main:{n}` 保持人设一致),
  可经 `agentgo` 派子 agent(深度守卫 `agent_depth+1`);
- 审计升华:审计 agent 收齐各主产出,做一致性/质量/覆盖度审查;
  发现缺漏 → 打回指定主 agent 补做(最多 1 轮);补做完成后追加一次终审
  (只产出结论文本,不再打回);通过后升华整合为最终结果
  (区别于 legacy 的机械拼接);最终 result 的「## 审计结论」段:
  无打回 = 首次审计结论,有打回 = 终审结论;
- 可观测:主/子/审计/终审全部经 EventSink 落 `task_llm_calls`
  (phase=agent/subagent/audit/final_audit)并发射 `agent_status` 事件,
  调用情况面板按模式渲染分工卡 + 审计结论。

> 未来能力预留方向见 `展望.md`(EventSink / ModeExecutor 作为 capability seam 的晋升门槛与
> 挂点说明不在此重复)。

### 六、批次 4.3b 落地要点(multi / custom / team + usage 口径)

共享骨架:`solo.rs` 抽出 `run_agent_loop`(`AgentLoopCall{task_id/session_id/goal/settings/phase/step_index/label}`),
内含完整 system 组装(`EXECUTOR_PROMPT` + 人设 + 世界书 + 注入,外部段落一律 `untrusted_boundary` 包裹)
+ `run_tool_loop`(工具集已按 `task_tool_policy` 编译,`step_whitelist=Some(&[])` 即放行该集合)
+ `record_llm_call` 统一出口;`solo`/`multi`/`team` 各主共用。

- **multi**(`task_engine/multi.rs`):主 agent = solo 同款 `run_agent_loop`;子 agent
  链路工具化改造(`tools/agent_tools_agent.rs` 的 `run_subtask`):engine 就绪时走
  `run_subtask_with_tools`——`run_tool_loop` + `SUBAGENT_TOOL_WHITELIST`
  (read/search/todo/sleep/calculator/memory_read,写类与编排类剔除,agentgo 双保险)
  + `agent_depth+1` 深度守卫(子 agent 不得再派子 agent);engine 缺省回退纯生成(旧行为)。
  `ToolDeps` 以 `OnceLock<Weak<AgentEngine>>/Weak<TaskService>` 后注入(`app_state` 构造完成后
  各 set 一次,Weak 防循环)。**FK 守卫**:`agent_subtasks` 表 FK 到 `sessions(id)`,
  `task:` 前缀虚拟 session 经 `AgentSubtaskService` 内存覆盖层(mem map,不落 DB,与
  `llm_requests` 跳过守卫同口径);`stop()` 末尾 `end_by_session_prefix("task:{id}")`
  连带结束全部孤儿子 agent。子 agent 事件经 `sink.rs`(label="子 agent")桥发 `agent_status`,
  LLM 调用落 `task_llm_calls`/`task_usage`(phase="subagent")。并发/截断沿用
  `settings.subagent_max_*` 参数。
- **custom**(`task_engine/custom.rs`):取 `AgentFlowService::get()` 当前启用流程
  (None/未启用 → error 文案提示「请先在设置中启用一个 Agent 流程」),`validate_flow` 校验;
  逐启用 step 顺序执行,plan 步骤名 = flow step 名、状态随执行推进;system =
  `EXECUTOR_PROMPT`(direct)/ `CUSTOM_REFLECT_PROMPT`(reflect)+ 宏渲染 agent prompt
  后经 `untrusted_boundary("flow_step")` 包裹 + `untrusted_boundary("task_goal", 目标)`;
  上一步产出串入下一步 user(任务目标全程贯穿);`step.tools` `None` → `generate_text`
  统一出口,`Some(list)` → `run_tool_loop`(白名单∩已注册,`tool_choice` 映射
  auto/none/required/function);每步 `record_usage`(phase="step", step_index=序号);
  result=末个 generates 步骤产出,含 error 步骤时 status=partial。
- **team**(`task_engine/team.rs`,首版仅自动拓扑):a)规划器(`TEAM_PLANNER_PROMPT`,
  低温度分级重试,`parse_team_topology` 剥围栏+归并:>4 主归并进第 4 位、每主 >4
  子目标截断、全局 >15 从末位主倒序截断(每主至少留 1)、1 主降级接受)拆 ≤15
  子目标归并 2~4 主,plan 步骤名 `【主Agent-N】子目标名`,phase="planner";
  b)各主 JoinSet 并行跑 solo 同款循环(独立 run_id、`task:{id}:main:{n}` session、
  AbortFlag、cancel 同源),主内按子目标逐个独立执行(每子目标一次 agent 调用,
  各写各自步骤的 result/status,**同主前序子目标产出注入后续子目标供衔接**,
  同一主复用同一虚拟 session),可派子 agent,主 phase="agent"
  step_index=子目标全局步骤下标,每主完成即推进 plan 步骤 + `record_usage`;
  c)审计 agent(`TEAM_AUDIT_PROMPT`)输出 JSON {通过/打回
  [{main,step?,instruction}]/结论},**`parse_audit` 解析失败按未通过兜底**
  (审计是质量闸门,读不出明确通过不判通过);有打回且未打过回 →
  指定定位补做(最多 1 轮,`step` 提供时只重跑该主该子目标并**替换其旧版本**,
  缺省整主),补做完成后追加一次终审(`TEAM_FINAL_AUDIT_PROMPT`,
  phase="final_audit",**结构化 JSON {通过,结论}**,空输出回退首次审计结论);
  d)升华整合(`SUMMARIZER_PROMPT`,phase="summary",**输入携带审计结论并要求
  只整合已选定版本、不得另起一版**),result = 整合文本 +
  `\n\n## 审计结论\n` + 审计文本(无打回 = 首次审计结论,有打回 = 终审结论);
  e)任一主失败,或审计/终审未通过:有产出 partial,全灭 error。
- **usage 口径**:solo/plan/multi/custom/team 收尾统一 `record_usage`(入参
  `executor.rs::usage_as_output`);phase 命名与 legacy 对齐:planner(规划)/
  agent(主执行)/subagent(子 agent)/step(custom 步骤)/audit/final_audit(team
  终审)/summary;plan 的 AwaitApproval 路径在 approve 续跑完成后由续跑执行器
  (`ApprovedPlanExecutor`,逐步 phase=agent step_index=步骤下标;汇总复用
  `legacy summarize_task_retry`,phase=summarize)补记;legacy 不动。
- **测试钩子**:mock 连接器新增 `[[reply_if:子串|内容]]`(可多条按序取首个命中,
  只查 system 消息,钩子语法自身出现不计命中),支撑 multi/custom/team 集成用例
  按提示词针定向回包。

### 七、2026-08-31 / 09-02 实测修复(截断自愈 / 规划器侦察 / 错误文本 / 孤儿恢复 / 工具历史回灌上限)

五项真实模型实测驱动的行为变化(均带集成测试,`tests/tasks.rs`):

1. **截断自愈(问题①)**:`run_tool_loop` 单轮生成被 `max_tokens` 截断到「不可用」
   (空正文且 `finish_reason=length`,或任一 tool_call 参数 JSON 非法/连接器流尾
   flush 报「arguments 不是合法 JSON」)时,该轮 `max_tokens` 翻倍(上限
   `TRUNCATION_HEAL_MAX_TOKENS_CAP = 8192`)原样重发一次;重发仍失败才透出原
   错误。普通文本截断(非空正文)不算,维持「截断仍 done」语义。触发记录经
   `ExecutorResult.self_heals` 透出,任务模式(solo/multi/team/plan 续跑/custom/
   子 agent)补落 `task_llm_calls` 标注行(status=error,response_summary 含
   「(截断自愈)…重发」),调用情况面板可见完整链路。legacy 路径的空输出分级
   重试(plan/step/summary 无工具纯生成)是既有同问题不同层机制,不受影响。
2. **规划器只读侦察(问题②)**:legacy 与 plan 共用的 `plan_task` 在产出计划 JSON
   前允许先调用只读白名单工具收集信息(`PLANNER_SCOUT_TOOLS` = read/search/
   memory_read/calculator,∩ 已注册工具;严禁写操作与编排类,agentgo/agentend/
   run_subtask 天然不在名单)。侦察轮上限 `PLANNER_SCOUT_MAX_ROUNDS = 2` 轮,
   随后最终轮不带工具强制产出计划 JSON(计划契约不变);模型幻觉白名单外工具
   名时回填拒绝错误 JSON 不执行;侦察轮失败(如协议异常)回退无工具直接规划,
   不沉任务。侦察轮与计划轮均经 `generate_text` 统一出口落 `task_llm_calls`
   (phase=planner);`PLANNER_PROMPT` 已加入「信息不足可先调用只读工具」指引。
   team 规划器(`plan_team`)维持无工具单次生成,未启用侦察。
3. **步骤 error 文本落库(问题③)**:步骤失败时失败原因与 status=error 同一次
   `set_plan` 落库,不得只置状态不留文本。`ApprovedPlanExecutor`(`plan.rs`)与 team
   主循环(`team.rs` 的 `run_mains_parallel`)两条路径同口径;错误文案带 `finish_reason`
   /截断定性(如「返回空内容(finish_reason=length)」「工具参数 JSON 截断
   (已达 token 上限,提高预算重发仍失败)」),前端步骤卡可读。
4. **孤儿任务恢复(问题④)**:`TaskService` 构造(`AppState` 装配,`Db::open` 之后)
   时把上次进程遗留的 running/planning 任务置 ended 终态 + error 文本
   「服务重启,任务中断」,并逐个发射 status 事件(先写库后发射);planned
   (待批准,approve 续跑语义可跨重启存活)、pending 与终态不动;幂等
   (重复执行零命中)。`db.rs::recover_orphan_tasks` 专测覆盖「插 running 行 →
   重开库 → 断言终结」。
5. **工具循环历史回灌上限(R3b,2026-09-02 实测)**:`run_tool_loop` 此前每轮
   全量回灌 assistant/tool 交替历史,3 步任务 prompt 3,119 → 26,679 token
   无界膨胀。现每轮 `execute_generation` 前执行 `trim_tool_history`
   (`engine/messages/trim.rs`),按「assistant(带 tool_calls)+ 后续连续 tool
   消息」分轮,keep-recent + 预算双闸门在「轮」粒度操作:完整轮数超出
   `tool_history_keep_rounds`(默认 4,钳 1..=32)时,最老轮的 tool 结果原地
   替换为短摘要(「(较早工具结果已省略:工具 "X" 原输出约 N 字符)」,幂等
   标记,已摘要轮凭前缀跳过、不占 keep 名额),该轮 assistant 的
   `reasoning_content` 一并清空;估算总 token 超 `tool_history_budget_tokens`
   (默认 16384,钳 1024..=1048576;0 = 禁用预算闸门、仅 keep-recent 生效)
   时从最老完整轮起继续摘要直到达标。保底最近 1 轮始终完整(模型必须看到
   最新工具结果才能续推)。`tool_calls` 与 `tool_call_id` 全部保留,
   assistant(tool_calls)↔tool 配对不破坏(严格后端不报 400);system/首条
   user/非工具循环消息不动。仅存在 tool 消息时触发;参数读 `engine.settings`
   扁平值(与 `bypass_mode` 等消费同款,任务模式经设置快照同链路生效)。
   与聊天主链路 compaction(`compaction.rs`)不同层:不动摘要槽/记忆槽/
   `protected_tail` 注入语义。触发时落 info 日志(newly_summarized/
   keep_rounds/budget_tokens),调用追踪 `prompt_summary` 可见摘要行。
   集成用例:`tests/tasks.rs::task_solo_tool_history_trimmed_in_prompt_summary`
   (solo 连续 6 轮 `[[tool_loop:read|6]]`,keep=1,断言落库 `prompt_summary`
   含 5 处省略标记、最近轮工具结果原文保留且仅此一份)。

mock 连接器配套新增 `[[tool_raw:name {...}]]` 钩子(模拟 `max_tokens` 把
tool_call 参数 JSON 切成半截,按预算门限 2048 区分截断/完整两轮)与
`has_tool_result` 分支内的回复钩子守卫(侦察轮回填工具结果后 `[[reply:]]`/
`[[reply_if:]]` 仍生效,产出计划 JSON)。

### 八、批次 R2a:终态追加输入(followup,2026-09-03)

任务完成后允许用户继续追加指令,任务以「原目标 + 上轮 result + 追加指令」
solo 续跑(`run_agent_loop` 单轮工具自循环,不重新规划),产出追加进成果。

1. **L1 契约**:新表 `task_messages`(id / task_id FK ON DELETE CASCADE /
   role(user|assistant CHECK)/ kind(normal|followup|plan_chat,默认 normal)/
   content / created_at,索引 (task_id, created_at));`schema.rs` 建表 +
   `migration.rs` 的 `ensure_task_messages_table` PRAGMA 探测幂等(`merge_databases`
   两侧补齐);`types.rs` 的 `TaskMessageRecord`(仅 Serialize,snake_case)。
   `GET /api/tasks/{id}` 响应新增 `messages` 数组(created_at 升序;无消息任务为
   空数组,前端零特判)。
2. **L2 后端**:`POST /api/tasks/{id}/followup`(body `{content}`)。校验顺序:
   空指令 400(VALIDATION)→ 不存在 404(NOT_FOUND)→ 非终态 409(CONFLICT;
   仅 done/partial/error/ended 可追加,running/planning/planned/pending 拒绝)
   → service 层复核(竞态失败同样 409)。链路:用户指令落 `task_messages`
   (kind=followup, role=user)→ 任务回 running → spawn 续跑(不经 `TaskEngine`
   派发:收尾语义为 append 而非覆盖)→ 产出落 assistant 消息并 append 进
   result(分隔段 `\n\n---\n\n**追加 N:**指令概要(截 80 字符)\n\n` + 产出;
   N = followup 序号,即 kind=followup 的 user 消息计数)→ 终态映射:原
   partial 保持 partial(失败步骤历史不抹平),done/error/ended 追加成功后
   回 done。取消/失败与 legacy 同款 `finalize_run` 收尾(stop →
   ended,失败 → error 文本)。事件纪律:消息落库不发射独立事件,生命周期
   复用既有 status 事件(running → done/partial),前端经事件驱动重拉详情
   携带 messages;不引入新事件 kind。
3. **L3 前端**:TaskBoard 详情底部「追加指令」输入条(textarea + 发送按钮,
   终态可用;running/planned/pending 禁用并按状态给出下一步提示)+「用户
   指令历史」区(user 气泡搬 `ChatMessageItem` 的 sv-msg user/sv-msg-bubble,
   assistant 行显示 160 字截断概要,title 悬停见全文;kind 小标签区分
   追加/规划对话)。`api/tasks.ts` 的 `followupTask`、`stores/task.ts` 同名 action
   (成功后 `loadTaskDetail` 对齐,仿 `approveTask` 模式)、`types.ts` 的 `TaskMessage`
   与 `TaskDetail.messages`(可选,旧服务端容错)。

测试:`tests/tasks.rs` 的 `task_followup_appends_to_result_and_messages`(全链路:
追加段序/段标/messages 两行/第二轮序号递增/删除级联)、
`task_followup_state_gate`(400/404/409 门禁与 code;ended 可追加)、
`task_followup_keeps_partial_status`(partial 不抹平;title 用 `reply_if` 钩子
防续跑 user 消息命中首轮 reply 钩子——reply 钩子按首个出现命中,title 带
无条件 `[[reply:]]` 会污染追加轮);`tests/task_events.rs` 的
`task_followup_emits_status_events`(status running→done 保序,无新 kind);
`migration.rs` 的 `ensure_task_messages_table_creates_and_is_idempotent`;
前端 `taskMessagesUi.test.ts`(SSR:输入条门控 + 历史区 + 无 messages 容错)、
`stores/task.test.ts` 与 `api/tasks.test.ts` 各 1 用例。

### 九、批次 R2b:批准环节规划对话(plan-chat,2026-09-03)

planned 待批准态允许用户与规划器多轮对话改计划:反馈 → 规划器携带上下文
修订 → 计划替换 → 重新待批准;任务全程保持 planned。

1. **后端**:`POST /api/tasks/{id}/plan-chat`(body `{message}`)。校验顺序:
   空反馈 400(VALIDATION)→ 不存在 404(NOT_FOUND)→ 非 planned 409
   (CONFLICT)→ LLM 上游/解析失败 502(UPSTREAM);修订期间 stop 竞态中断
   按 409。链路:用户反馈落 `task_messages`(kind=plan_chat, role=user)→
   规划器修订(同步等待;`register_cancel` 登记使 stop 可中断)→ `set_plan`
   替换(发射 plan 事件)→ planned 态 result 计划清单文本同步刷新(「计划
   已修订,共 N 步:…」,`set_planned_result`)→ assistant 修订说明落库
   (「计划已修订,共 N 步:步骤名清单」,名清单截 200 字符)→ 重发
   `approval_required` 事件(前端据此刷新批准区)。
2. **修订提示词**:system = `PLANNER_PROMPT` + 世界书 + 修订指引段(新常量
   `PLANNER_REVISE_GUIDANCE`,含独特词「计划修订指引」,mock 测试据此经
   `[[reply_if:]]` 区分首轮/修订轮);user = 原始目标 + 当前计划 JSON +
   plan_chat 对话历史 + 本轮反馈。历史截断参照 `record_llm_call` 摘要口径:
   每条内容截 800 字符、历史段整体截 4000(多轮体积封底)。
3. **实现结构**:`plan_task` 的侦察循环抽为 `plan_scout_loop`(`plan_task` /
   `plan_revise` 共用:白名单工具侦察 ≤2 轮、回填、取消检查口径不变);
   `plan_revise_retry` 与 `plan_task_retry` 同款解析 + 分级重试骨架
   (`PLAN_MAX_ATTEMPTS`,length/空输出翻倍 `max_tokens` 上限 `RETRY_MAX_TOKENS_CAP`)。
   与 followup 不同:plan-chat 是同步 handler(planned 态无并发执行,
   响应直接携带修订后计划),不经 `tokio::spawn` 后台化。
4. **前端**:批准区加「与规划器对话」区(plan_chat 对话记录列表 +
   反馈输入框;发送后详情刷新带出修订计划与记录);底部「用户指令历史」
   在 planned 态过滤 plan_chat(批准区专属渲染,防同屏重复),其他状态
   全量显示(含「规划对话」kind 标签)。`api/tasks.ts` 的 `planChatTask`、
   `stores/task.ts` 同名 action(成功后 `loadTaskDetail`)。

测试:`tests/tasks.rs` 的 `task_plan_chat_revises_plan_and_keeps_planned`
(修订链路:计划替换/保持 planned/清单文本刷新/messages 落库/第二轮
历史经 GET calls 的 `prompt_summary` 断言携带)、`task_plan_chat_state_gate`
(400/404/409 门禁);`tests/task_events.rs` 的
`task_plan_chat_reemits_plan_and_approval_events`(plan → approval_required
重发保序,无终态事件);前端 `taskMessagesUi.test.ts` 批准区对话区 2 用例
(planned 专属渲染防重复 / done 态回底部历史区)、`stores/task.test.ts` 与
`api/tasks.test.ts` 各 1 用例。

### 十、team 纯生成点截断自愈(2026-09-03 真实模型复验驱动)

实测 team 全链路(DeepSeek 推理模型,`default_max_tokens=1024`)发现:审计
agent 的 reasoning 烧光预算,`finish_reason=length` 产出腰斩 JSON
(`{"通过":false,"打回":[{"main":2`),腰斩原文直接进最终 result 的
「## 审计结论」卡——既难看又误导(`parse_audit` 虽按通过兜底不沉任务,
但结论=原文的旧语义把协议残片暴露给了用户)。

根因:solo/custom 的截断自愈在 `run_tool_loop` 层,而 team 的审计/终审/汇总
是 `generate_text` 直调点,不在自愈覆盖内。修复(`team.rs`):

1. **`generate_text_healed` 包装**:审计/终审/汇总三处统一走新出口——先按
   原预算调 `generate_text`,`finish_reason=length` 时预算翻倍重发一次
   (纯函数 `trunc_heal_budget` 决策,封顶 `TEAM_RETRY_MAX_TOKENS_CAP`)。
   两次调用均经 `generate_text` 落 `task_llm_calls`,调用情况面板可见
   「截断 → 提高预算重发」链路;token 用量与 `record_usage` 两次都入账
   (与 solo 自愈留痕同口径),并发 `agent_status` 事件告知前端。
2. **腰斩 JSON 末层兜底**:`parse_audit` 对「以 `{` 开头但解析失败」的
   截断形态不再把原文塞进结论段,改为干净说明「(审计输出不完整,按通过
   兜底)」,原文前 120 字符记 warn 日志备查;纯文本回复维持旧语义
   (结论=原文)。

测试:`team.rs` 的 `trunc_heal_budget_only_on_length`(仅 length 触发/翻倍/
封顶/越顶不触发)、`audit_truncated_json_fallback_clean_conclusion`
(腰斩 JSON 兜底通过、结论段不含 `{`)。

### 十一、2026-09-10 六模式实跑修复(F1~F8)

隔离数据目录 + 真实 `deepseek-flash` 对六模式各跑「创建 → run → 多轮 followup」,
plan 补跑 plan-chat → approve → followup,依据实测暴露的 8 个问题修复:

1. **F1 custom 非生成步骤产出正文**(`custom.rs`):`generates=false` 的 direct 步骤
   此前复用 `EXECUTOR_PROMPT`(「直接输出最终结果」),无视步骤自身「只做内部规划」
   而吐出完整正文,并被下一步原样回显。现改用新常量 `TASK_INTERNAL_PLAN_PROMPT`
   (只做分析规划、不产出正文),其产出加「(内部规划)」标注、仅供后续步骤参考、
   不计入最终成果(`draft` 仅由 `generates=true` 步骤产出)。
2. **F2 任务模式注入污染**(`task_prompt_inject_enabled`,默认 false = 隔离):
   角色扮演的 `prompt_floors.json` 文章要求(1200 字/第三人称/禁词表)此前注入任务
   执行/汇总/followup 轮次,与任务目标对抗(实测「压缩到 200 字」后结果反涨)。
   三处消费点(legacy 步骤/汇总、solo 主循环)加开关守卫;预览层同步门控。
3. **F3 记账缺口**(一致性口径):凡落 `task_llm_calls` 的调用都补落 `task_usage`——
   规划器侦察轮、截断自愈 heal 行、规划失败重试轮、步骤/汇总首调空输出轮次。
   新不变量(集成测试锁定):`/calls` 各行 token 求和 == 详情 `usage_total`。
4. **F4 plan 续跑步数矛盾**:approve 组 goal 追加约束句「步骤数量与内容一律以本
   清单为准」,修用户经 plan-chat 改步数后汇总仍沿用原始目标旧步数的矛盾
   (实测「标题三步、正文两步」)。
5. **F5 followup 重写模式**(`mode: append|replace`,缺省 append 向后兼容):
   append 无法表达「压缩/重写/改前面」;replace 用新产出整体替换 result
   (段标「修订 N」),前端追加条加模式选择。
6. **F6 带工具轮 `finish_reason` 落空串**(`sse_parser.rs`):`finish_reason=tool_calls`
   时在 flush 工具调用后同样产出 `Finish{tool_calls}`,调用面板 finish 列不再空白;
   截断自愈只认 length,行为不受影响。
7. **F7 legacy 前端子任务区重复**(`TaskBoard.vue`):legacy 的 subtasks 与计划步骤
   同源,前端两区各渲染一次;现 legacy 不渲染子任务区(multi 照常)。
8. **F8 tools 空数组陷阱**(前端提示):`tools: []` 语义是「全部工具」而非「不使用」,
   `AgentFlowSection` 在「全部工具」及「不生成正文 + 全部工具」组合下加警示
   (纯函数 `stepToolsWarnings`,单测覆盖)。

### 十二、存储六件套与事件链(任务侧现状)

`tasks`(`task_mode` 列)/ `task_subtasks` / `task_usage`(token 口径)/
`task_llm_calls`(含 `finish_reason` 可观测性)/ `task_messages` / `agent_subtasks`,
均 `task_id` 外键 CASCADE。任务引擎 DB 写入成功后经 broadcast 通道发 `SseEvent::Task`,
`GET /api/tasks/events` 转 SSE 推送前端,前端事件驱动刷新(无轮询);
legacy 三段式状态为 planning → running(逐步派子智能体)→ done,全程可中断、可重跑;
任务模式跳过 `llm_requests` 落库(`FK` 指向 sessions),也**不接契约系统**。

**权威来源**:`server-rs/src/services/task_engine/`(mod / sink / executor / context /
solo / multi / plan / team / custom / legacy / followup / tool_policy)、
`server-rs/src/services/task_core/`、`server-rs/src/services/task_service/`、
`server-rs/src/models/{db/schema.rs,types.rs,migration.rs}`、
`server-rs/src/tools/{tool_sets.rs,agent_tools_agent.rs}`、
`web/src/components/TaskBoard.vue`、`web/src/stores/task.ts`;
文档小节 [任务引擎六模式](archive/2026-09-16-consolidation/任务引擎六模式.md)(本文移植源)、
[优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 6 章、
`../MAINTENANCE.md` §14「任务工作台」。
未实现项(手动拓扑、每子模式独立覆盖字段)见 `计划.md`。

**关联契约**:`契约.md`(任务端点与响应形状)、`契约-协议与配置.md`(任务事件 SSE 与设置字段)、
`契约-架构与数据.md`(任务六表与事件流)。

---

## 工具系统与授权

工具系统是「模型能做什么」的唯一出口,授权层是「允许它做什么」的唯一闸门。

**现状能力**

- **工具注册表**:标准化 Tool 接口(OpenAI Function Calling 格式);定义顺序按 name 稳定
  (有跨构建序列化一致性测试,**保前缀缓存**);工具定义有快照缓存 + 写路径失效(注册/注销);
  注册表错误文案带「下一步怎么做」指引;插件工具重名不得覆盖内置工具(此前已修复)。
- **内建工具**:`calculator`(白名单解析,不使用 eval)、`memory_read`/`memory_write`
  (会话长期记忆)、`bash`(命令执行)、文件工具(`read` / `write` / `replace` / `create`,
  路径经 `safe_rel_path` 收口在 `data/character_files/{角色}/`)、`search`、`todo`、
  `sleep`、`censor_text`(禁词同义替换兜底)、编排类(`agentgo` / `agentend` / `run_subtask`)、
  正文元工具(`get_state` / `apply_patch`,**永不进正文列表**)、变量工具
  (`update_variables` / `update_status_bar`,两步生成用)。
- **三档授权矩阵**:判定从「一个布尔开关」改为「**操作类型 × 路径区域**」——
  `ToolOp`(`ReadFile`/`WriteFile`/`DeleteFile`/`Exec`/`Other`)× `PathZone`
  (`Sandbox`/`SystemPath`/`Opaque`),按 `strict` / `loose`(默认)/ `bypass` 三档裁定;
  **系统路径的写/删是三档共同的硬底线**;文件规则只作用于文件目标
  (`write(target=bubble)` 写正文不受影响);混合操作取最高危项。
- **判定链顺序(关键)**:未注册 → 拒绝 → **命令执行高危硬门**(破坏性/提权命令,
  不受任何授权豁免)→ 白名单命中 → 显式授权(会话/角色)→ 「始终需授权」清单 →
  三档文件规则 → 命令执行(非高危)→ 其余按原风险裁决。命令级硬门排在显式授权之前,
  是因为 `bash` 是单一工具名,工具级授权无法区分「这次跑的是 `ls` 还是 `rm -rf`」。
- **授权生命周期**:弹窗按钮为「仅允许本次 / 允许当前会话 / 允许当前角色 / 拒绝」;
  无角色会话隐藏「允许当前角色」;授权落盘失败降级为「仅本次允许」并记 warning,
  不再中断整轮生成;删除会话清理 `session_grants`;启动清理孤儿会话授权;
  匿名会话不查角色授权(避免 `role_grants[""]` 串权);前端「授权管理」面板可查看并撤销已授权限;
  `tool_permissions.json` 以 tmp + Windows `ReplaceFileW` 原子替换,未知工具默认按 `Dangerous`。
- **命令执行层(`bash` 工具,四道闸)**:① 总开关 `exec_enabled`(默认关,关闭时一律拒绝并留审计);
  ② 工具级风险恒 `Dangerous`;③ **命令级**分四级——`safe`(只读检视)/ `sensitive`(工作区内写)/
  `destructive`(`rm -rf`/`dd`/`mkfs`/`shutdown`/`chmod 777` 等)/ `admin`
  (`su`/`sudo`/`systemctl`/`pm`/`am`/包管理等),后两级**逐条确认、不受任何豁免**,
  多命令串联(`&&` `||` `;` `|`)逐段判定取最高危;④ 执行级强制超时(默认 60s、上限 300s)
  + 超时强杀 + stdin 置 null 禁交互 + 输出截断 32K。
- **执行抽象层 `services/exec/`**:`ShellTier{Root, Shizuku, Sandbox, Disabled}`、
  `ExecRequest`/`ExecResult`、`execute(req, allowed_tiers)`(等级放行校验)、`detect_tier()`、
  超时夹取与输出截断;`desktop.rs` 用 `tokio::process` 直派(`kill_on_drop` + 显式强杀);
  `android.rs` 经 JNI 调 Kotlin 桥,入参 `command␟cwd␟timeoutMs` 返回
  `exitCode␟stdout␟stderr`(沿用 `\u{1f}` 分隔约定)。**等级放行**是执行侧落点:
  探测到的等级必须被用户显式放行(`exec_allow_*`),否则拒绝——不会「探测到 ROOT 就悄悄高权限执行」。
- **Android 三档执行器**:`detectTier()` 按 ROOT(`su -c id` 3s 超时探测)→ Shizuku(binder ping + version)
  → 沙箱探测;ROOT 用 `su -c`,Shizuku 用 `Shizuku.newProcess("sh","-c",cmd)`,沙箱用系统 `sh -c`;
  Shizuku 经**反射**访问(不引入编译期依赖),未安装/未授权时静默降级为沙箱档,不崩溃;
  `requestShizukuPermission()` 经动态代理触发系统授权弹窗;`AndroidManifest.xml` 有
  `moe.shizuku.manager.permission.API_V23` + `<queries>` 声明;`proguard-rules.pro` 整类 keep 新桥类。
  Rust 侧 Android 交叉编译与 Kotlin 编译均已通过;ROOT / Shizuku **运行时**需真机与
  ADB 激活的 Shizuku 服务验证(模拟器无法验证),沙箱档可在模拟器验证。
- **审计**:`exec_audit` 表(幂等迁移 + 基线 DDL 双份)记录每次尝试**含被拒绝的**
  (命令原文/风险级/等级/裁决/退出码/输出摘要,摘要截断 2000 字符),
  经 `GET /api/exec/audit` 查询、`DELETE /api/exec/audit` 清空、`GET /api/exec/tier` 查当前等级。
- **任务模式工具策略**:`task_tool_policy` 三档 `all` / `deny_dangerous`(默认)/
  `allowlist`;任务侧**恒不等待授权**(未放行工具立即拒绝为 `tool_policy_denied`);
  `deny_dangerous` 按**工具名精确匹配**对 `bash` 开例外(含 `bash2`/`mcp_x_bash` 不外溢的护栏测试);
  custom 步骤白名单在此基础上取交(只能收窄);子智能体沿用 `tool_sets::SUBAGENT` 只读白名单。
- **前端入口**:输入框底部三档切换(切换即持久化)、设置 → Agent 设置 → 授权管理、
  `AndroidExecSection`(总开关 / 三档放行 / 当前等级与刷新 / Shizuku 授权请求 / 审计列表),
  Android 专属档位按 `isAndroidTauri` 条件显示。

**权威来源**:`server-rs/src/tools/{registry,permissions,action_class,command_risk,tool_sets}.rs`、
`server-rs/src/services/exec/{mod,desktop,android,audit}.rs`、
`server-rs/src/services/task_engine/tool_policy.rs`、`web/src/components/settings/{AuthorizationSection,AndroidExecSection}.vue`、
`web/src/api/exec.ts`;
文档小节 [授权模式](archive/2026-09-16-consolidation/授权模式.md)(判定矩阵与设置字段)、
[命令执行层与Android三档执行器-变更说明](archive/2026-09-16-consolidation/命令执行层与Android三档执行器-变更说明.md)、
[0.3.0-beta-后续修复-变更说明](archive/2026-09-16-consolidation/0.3.0-beta-后续修复-变更说明.md) §二与 §六。

**关联契约**:`契约-协议与配置.md`(exec 端点、授权与任务策略设置字段)、`契约.md`(`tool_policy_denied` 等错误码)。

---

## 变量与契约引擎

变量系统是「故事状态」的载体,契约引擎(Kaleido)是其唯一事实源与门控。

**现状能力**

- **mvu 变量系统**:直接操作 JSON 树,无独立 IR;`AssistantVars{tree: Value}` 根即 `stat_data`;
  路径归一化剥 `stat_data.` 前缀、点/斜杠双分隔、**过滤 `__proto__`/`constructor`/`prototype`
  防原型污染**;`PatchOp::{Replace,Delta,Remove,Move}` 四变体各带 `reason`。
- **双协议解析**:`<UpdateVariable><JSONPatch>[...]</JSONPatch></UpdateVariable>`
  (RFC 6902 子集 + `delta` 扩展;`insert`/`set` 并入 Replace;delta 非数字整条跳过;
  未知 op 跳过)与 MagVarUpdate `_.set('path', old, new);//原因`
  (手写平衡括号扫描 + 顶层逗号切分;reason 只出自这里,行注释优先、块注释次之;
  JS 字符串转义与前端 `parseJsString` 逐字对齐)。
- **容错**:顺序应用、首个失败返回 Err 不回滚;Move 源缺失整条跳过;引擎侧应用失败仅记 warn。
- **七作用域**:`Global` / `Chat` / `Character` / `Preset` / `Message` / `Script` / `Extension`。
  读优先级 `message → chat树 → chat扁平 → character → preset → global`
  (script/extension 不进读链;message 未命中强制回退 chat 兼容存量卡);
  写路由 chat 按「`stat_data.*` 前缀或已在树中」分流树/扁平层;回合内树写以 `env.vars` 实时权威;
  回合结束 `take_others()` 批量落 `scope_variables` 表。持久化三层:
  `session_assistant_vars`(树)+ `session_vars`(扁平)+ `scope_variables`(PK(scope, scope_id));
  SSE `Vars` 事件推最新树。message 作用域 = `extra` 镜像 + `scope_variables` 双写,引擎为唯一写者。
- **两步生成**:正文后追加一次独立 function-calling(工具 `update_variables`/`update_status_bar`,
  独立温度档 `mvu_temperature` 默认 0.3,空输出以 0.7 重试一次);模型不调工具时回退旧文本协议;
  仍无状态栏用 `diff_status_bar`(以本轮初始树为基准)兜底。**首楼纪律**:含
  `<StatusPlaceHolderImpl/>` 占位符时服务端绝不改首楼;否则用 `extra.status_slot.last` 锚点就地替换并落库。
- **HTTP 出口**:`GET/PUT/PATCH /api/variables`(读整树 / 整树覆写 / JSON Patch 子集)、
  `PUT /api/chat/sessions/{id}/assistant-vars`(写树)、
  `PATCH /api/chat/messages/{id}/variables`(消息级)、`GET /api/chat/init-vars`(初始化)。
- **Kaleido 契约系统(万花筒契约 DSL)**:三结构 `Contract`
  (version/schema/updateRules/displayRules/guardrails/invariants)、`FieldDef`
  (updateMode/everyN/dependencies/ownership{owner,writers,priority,merge,audit}/persist/stability)、
  `KaleidoState`(stat_data + revision{seq,hash} + meta + changelog + checkpoints)。流程:
  1. **提取**:角色卡 `extensions.nlkaleido` 优先(兼容 `{contract:{...}}` 包装),
     世界书 `[nlkaleido_contract]` 条目兜底;`ContractRegistry` 按 character_id 惰性加载 +
     Mutex 缓存,写路径 `invalidate`。
  2. **渲染注入**:`build_state_prompt` = `due_fields` 本地确定性调度
     (every_turn 每轮、every_n 按 `turn_id % N == 0`)+ `observe` 投影
     (仅 due + 依赖传递闭包深度 ≤3 + pending;viewer 不可写或 `display:hidden` 剔除;
     top_k 折叠摘要;敏感脱敏);无到期字段不注入状态块。
  3. **门控**:`gate_assistant_patches_detailed` 做路径归一点分 → `validate_ops` 所有权检查
     (未声明字段 `unknown_field`、写者不在白名单 `not_owner`)→ 同 path 冲突按 MergeRule
     (last_write/sum/max/min;聚合置信度取组内最低防绕过)→ 置信度门控
     (`minConfidence` 低置信入 pending,未声明视为 High);应用复用 `AssistantVars::apply_patches`,
     **避免两套寻址语义分叉**。
  4. **审计与恢复**:`entries_from_applied` 由前后树生成 old/new;checkpoint 三条件
     (50 轮 / 24h / 100KB);`recover` = checkpoint + seq 更大的 log 按序重放。
  5. **落库**:engine 收尾 `commit_turn` → `kaleido_state` + `kaleido_changelog`;
     pending 经 `merge_pending`(`op_hash` 去重,超 `MAX_PENDING=32` 丢最旧)。
  6. **校验**:键=path 一致、everyN≥1、不变量引用已声明、**依赖环拓扑拒绝**、路径前缀互斥。
  HTTP 出口:`POST /api/variable/update`(统一写入)、`GET /api/variable/state`(运行态整行)、
  `GET /api/variable/changelog`(逐 op 变更流水)。
- **多步熔断**:`contracts/multi_step.rs` 用 FNV 风格 `op_hash`/`failure_hash` 与近 8 步环形缓冲,
  同一哈希出现 ≥3 次判 `loop_broken`,防契约路径工具调用死循环。
- **契约与任务模式零交集**(`task_engine`/`task_service` 全文无 contract 引用)——这是刻意的边界。
- **前端**:`ContractsModal.vue` + `contracts/contractDiff.ts`;
  `mvu/` 提供 parser / variables / host / mvuStore 与 `unwrap.ts`(解包逻辑收敛,沙箱侧以
  源函数哈希注释保持同步可见性)。

**权威来源**:`server-rs/src/parsing/{scopes.rs,assistant/}`、`server-rs/src/agents/engine/mvu.rs`、
`server-rs/src/contracts/`(op / field / due_fields / observe / changelog / invariant / multi_step /
render / state / validation / registry / extract / meta)、`server-rs/src/services/{kaleido_state_service.rs,variable_apply.rs}`、
`web/src/mvu/`、`web/src/components/ContractsModal.vue`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 8、9 章、
`../MAINTENANCE.md` §14「万花筒契约 DSL」、
[mvu-protocol](archive/2026-09-16-consolidation/mvu-protocol.md)。
契约不变量当前只落地 mutex 类型(range / require_if 未实现)属遗留项,见 `遗留.md`
(登记口径见 [known-limitations](archive/2026-09-16-consolidation/known-limitations.md) L2)。

**关联契约**:`契约-协议与配置.md`(变量与契约端点)、`契约-架构与数据.md`(七作用域与三层持久化)。

---

## 记忆

**现状能力**

- **跨会话记忆蒸馏**:`services/memory_service.rs` 的 `distill_session`(LLM 以闭包注入,mock 可测)
  把会话历史蒸馏为结构化记忆条目(人物关系/事件/伏笔取向);表 `memory_entries` 按
  character_id 维度,`kind` CHECK(`distilled|tool|manual`),含 `usage_count` / `last_usage` /
  `selected` / `pinned` 与索引。
- **精选与注入**:按 `usage_count DESC → last_usage DESC → id DESC` 精选,注入摘要槽之后的
  **记忆槽**;使用后 `step_loop` 成功路径 `touch`。
- **双写入口**:agent 主动写记忆(`memory_write` 工具)与蒸馏落**同表同策略**。
- **七端点**:`POST /api/memory/distill`(需开启 `memory_distill_enabled`)、
  `GET /api/memory`(按角色列出)、`GET /api/memory/search`(检索)、
  `POST /api/memory/prune`(硬删除该角色 `selected=0` 的归档条目)、
  `POST /api/memory`(手动添加,kind='manual')、`PATCH /api/memory/{id}`(编辑 content/selected/pinned)、
  `DELETE /api/memory/{id}`;配套 `GET /api/memory/embedding-status`(向量索引状态)。
- **向量检索**:`services/embedding_service.rs` 提供语义检索与向量索引,支持
  `POST /api/memory/rebuild-embeddings`(手动重建)与 `POST /api/settings/embedding/test`
  (测试向量化连接);索引构建使用批量写入语义(有集成用例)。
  检索形态为精确 KNN、无 ANN 索引(有意裁剪,见 `遗留.md`)。
- **前端**:`MemoryPanel.vue` + `memoryPanel.ts` + `composables/useMemoryPanel.ts`,
  记忆库面板可查看 / 编辑 / 手动蒸馏 / 精简。

**权威来源**:`server-rs/src/services/{memory_service.rs,embedding_service.rs}`、
`server-rs/src/api/memory.rs`、`server-rs/src/tools/memory.rs`、
`web/src/components/MemoryPanel.vue`;
文档小节 `../MAINTENANCE.md` §14「跨会话记忆蒸馏」、§11(集成用例范围),
[优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 2 章(记忆槽插入)。

**关联契约**:`契约.md`(记忆七端点与响应形状)、`契约-架构与数据.md`(`memory_entries` 与索引)。

---

## 技能与 MCP

**现状能力**

- **技能渐进披露**:`services/skill_service.rs` 维护 manifest 固定格式清单(按 name 字节序稳定);
  预载**仅 `name + description`**(每技能几十 token),正文经 `read(type=skill)` 按需加载;
  设置 `skill_progressive_disclosure` 默认开,关闭即回退零注入;
  skills 表带 `allowed_tools` / `run_as_subagent` / `model` 三列元数据。
- **技能库管理**:`GET/POST/PUT/DELETE /api/skills*`(列表 / 导入 / 更新 / 删除)。
- **子代理调度守卫**:`tools/agent_tools_agent.rs` 深度守卫 `subagent_max_depth`(默认 2)、
  并发守卫 `subagent_max_concurrency`(默认 6)、结果截断 `subagent_result_max_chars`
  (默认 2000,截断附尾注);`run_subtask` 在 engine 就绪时走
  `run_subtask_with_tools`(`run_tool_loop` + `SUBAGENT_TOOL_WHITELIST`),缺省回退纯生成。
- **自定义工具插件**:`<数据目录>/plugins/tools/*.json` 白名单脚本工具,经
  `GET/POST/DELETE /api/plugins/tools*` 管理(列表 / 重载 / 上传 / 删除);
  脚本为白名单表达式求值器(不使用 eval),必须以 `return`/`result =` 开头、唯一变量 `args`;
  上传/删除留痕业务数据(`ok`/`loaded`/`errors`)并带 `code`;
  `examples/plugins/tools/` 提供示范模板与语法边界说明。
- **MCP 客户端**:`server-rs/src/mcp/`(`client.rs` / `process.rs` / `mod.rs`)实现
  **stdio transport + tools**(v1 边界:不做 SSE/HTTP,不做 resources/prompts);
  设置 `mcp_enabled`(默认关)+ `mcp_servers` 列表,开启后启动时装配,工具以 **`mcp_` 前缀**注册;
  运行期改设置不回溯重连,重启后生效;`mcp_enabled=false` 时完全跳过(零进程、零注册)。
  服务器句柄表持 `Arc<McpClient>` 与进程句柄(Drop 即杀,防孤儿),关停经 `shutdown` 主动 close
  (避免残留注册项调用悬挂);单工具超时 120s(客户端内部 `tools/call` 超时 110s 略小,
  保证先拿到可读错误)。
- **MCP 权限归属**:`mcp_` 前缀不命中任何内建分类,由 `permissions.rs` 的
  「未知工具按 Dangerous 处理」自动全覆盖,无需额外分类工作。
- **MCP 代际纪律**:该模块**只依赖 L1**(配置词汇 `models::tool_policy::McpServerConfig`、
  窄接口 `models::types::{ToolDefinition, ToolExecutor, ToolRegistrar}`),
  不 `use crate::services::` / `use crate::tools::`;配置由宿主读设置后**传参**进来,
  注册经 `&dyn ToolRegistrar` 注入——与 `task_core::TaskBackend` 断开环依赖同一手法。
- **前端**:设置弹窗 `Mcp` section 维护服务器列表;技能库面板管理技能。

**权威来源**:`server-rs/src/services/skill_service.rs`、`server-rs/src/tools/agent_tools_agent.rs`、
`server-rs/src/tools/registry.rs`、`server-rs/src/mcp/{mod,client,process}.rs`、
`server-rs/src/services/settings_service/mod.rs`(`mcp_enabled` / `mcp_servers`)、
`web/src/components/settings/McpSection.vue`;
文档小节 `../MAINTENANCE.md` §14「技能渐进披露与子代理守卫」、
[优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 7 章(工具期能力边界)。

**关联契约**:`契约-协议与配置.md`(技能/插件/MCP 设置字段)、`契约.md`(技能与插件端点)。

---

## 可观测性与错误面

**现状能力**

- **结构化错误码**:`api/errors.rs` 的 `ErrorCode` + `err_with_code`;`api/util.rs::db_err`
  的数据库原文改由 `tracing::error!` 记录,响应统一固定文案 + `code: "DB"`(签名不变,调用方零改动);
  settings / prompt_inject / repo_index / resource 等出口的「内部细节 + 原文」一律收敛为
  日志 + 固定文案 + `code`。
- **状态码与形状**:目录穿越拒绝与头像不存在改为真实 `404` + `code: NOT_FOUND`(此前是
  「错误体 + 200」的假成功);未知 `/api/*` 与 405 由空 body 改为 JSON + `code`;
  `resource` 8 个出口统一 `{error, code}` + 真实状态码;plugins 保留业务数据并补 `code`;
  embedding 测试保留「200 + `ok:false`」语义(测试未通过不是异常)并补 `code`;
  413 为已登记例外(统一形状成本远高于收益)。
- **JSON 体提取器**:`api/json_body.rs` 的 `JsonBody<T>` 把 `JsonRejection` 收口为
  `{error, code: VALIDATION}` + 400;已接入 chat / tasks / settings / characters / sessions;
  `tools/check-arch.mjs` 规则 K(未接入提取器数量的 ratchet,只降不升)防新增漏接点。
- **请求关联 ID**:`api/request_id.rs` 读/生成 `X-Request-Id`(白名单净化:可见 ASCII 且 ≤64 字符)、
  回写响应头、建 `http_request` span、请求结束打 1 行 INFO 访问日志
  (`method`/`path`/`status`/`duration_ms`);注册在 `build_router` 链尾(CORS 之后)成为**最外层**,
  使 `security::guard` 的 401/403 早退响应同样带 `X-Request-Id`(有回归护栏)。
- **span 穿透**:`utils/logging.rs` 的 `RequestIdLayer` 在 `on_new_span` 把 `requestId` 存为
  **span extension**,`PinoFormat` 经 `event_scope()` 合并进 JSON 行——
  请求生命周期内所有日志(含 service 层、spawn 出去的引擎/任务)自动带 `requestId`,
  无需逐处传参;引擎 run 入口与任务执行器 spawn 补 `.instrument(span)`。
  访问日志字段组装抽为纯函数(`AccessLog`)以便单测。
- **health 依赖探测**:`/api/health` 返回
  `{ok, ts, version, build_id, build_time, data_dir, dependencies:{db}}`——`db` 由 `SELECT 1` 探活,
  `?deep=1` 才跑 `PRAGMA quick_check`(深检放 `spawn_blocking`);
  **`ok` 恒为 `true`(liveness 冻结点)**,`src-tauri` 的 `health_ok()` 依赖它。
- **错误分类类型化**:`models/llm_error.rs`(L1,零 HTTP crate 依赖)定义
  `LlmErrorKind{Timeout, RateLimited, AuthFailed, Upstream, Generation}`、
  `TransportFailure{Timeout, Connect, Body, Request, Other}`、`LlmError{kind, message}`,
  提供纯映射 `from_http_status` / `from_transport` / `code()` / `retryable()`;
  `connectors/openai_compatible` 是**唯一分类边界**(生产者侧),
  `agents/engine/types.rs` 的 `EngineError{Interrupted, Llm(LlmError), Internal(String)}` 消费它;
  **禁止**回退到「扫错误文案子串」判分类(字符串回退路径已整体删除)。
- **静默吞错清理**:全仓 `map_err(|_| ...)` 逐处判定,该补回原错的补回(10 处),
  保持不动的 9 处全部补中文理由注释;`finalize_run` 不再 `let _ =` 丢弃 `set_status`/`set_error`
  返回值(为 `false` 时打 `tracing::error!` 并写明「任务可能停留在 Running」);
  `RecvError::Lagged` 由 `debug` 升为 `warn`。
- **DB schema 版本化与升级事务化**:`models/db/schema.rs` 的 `SCHEMA_VERSION`;
  `Db::open` 读 `PRAGMA user_version`,**库版本高于应用则拒绝启动**(文案含两个版本号与处置建议);
  建表批 + 全部 `ensure_*` 用 `BEGIN IMMEDIATE … COMMIT` 包裹,失败统一回滚
  (双层闭包保证连接绝不以打开事务状态返回);`backfill_*` 与 FTS 回填留在 COMMIT 之后靠
  `backfill_meta` 幂等;合并产物经 `align_schema_version` 对齐版本号。
  升级一处 schema 需同步的四处(schema.rs / migration/ddl.rs / SCHEMA_VERSION / 迁移元测试)
  见 `../MAINTENANCE.md` §14。
- **前端错误口径**:`api/client.ts` 的 `ApiError` 结构化(code/status/detail)+
  `apiErrorMessage` 按 code 出中文用户口径(NOT_FOUND/CONFLICT/VALIDATION 透传,
  DB/INTERNAL/UPSTREAM 统一「服务端错误」,UNAUTHORIZED 提示重载);token 惰性单飞
  (失败不缓存)、仅驻内存、401 重引导重试一次(针对服务端重启 token 轮换)。
- **前端响应形状闸门**:`web/src/api/shape.ts` 的形状校验原语(`requireObject` /
  `requireObjectField` / `requireArrayField` / `requireStringField` / `requireNumberField` /
  `requireNullableObjectField` / `requireStringMapField`),失败优先透出服务端 `error` 原文;
  已接入 settings / worldbooks / memory / sessions / tasks;纪律是**只做最小存在性校验**
  (避免把「后端加字段」误判为故障),仅用于「`request<T>()` 后直接解构」的封装。

**权威来源**:`server-rs/src/api/{errors.rs,util.rs,json_body.rs,request_id.rs,mod.rs,resource.rs,static_files.rs}`、
`server-rs/src/models/{llm_error.rs,db/schema.rs,db/mod.rs}`、`server-rs/src/utils/logging.rs`、
`server-rs/src/connectors/openai_compatible/`、`web/src/api/{client.ts,shape.ts}`;
文档小节 [工程韧性修复-变更说明](archive/2026-09-16-consolidation/工程韧性修复-变更说明.md) §2.1–§2.4(逐批改动点与验收证据)、
[0.3.0-beta-后续修复-变更说明](archive/2026-09-16-consolidation/0.3.0-beta-后续修复-变更说明.md) §八,
`../MAINTENANCE.md` §14「可观测性与错误面收口」、§0(门禁纪律)。

**关联契约**:`契约-协议与配置.md`(错误体形状、错误码取值与 SSE Error 五个 code)、
`契约-架构与数据.md`(schema 版本与升级链)。

---

## 前端

**现状能力**

- **技术栈与结构**:Vue 3.5 + Vite + Tailwind v4 + Pinia 2.3(strict TS);
  目录分为 `api/`(按域拆分 + index 聚合)、`stores/`(七子 store)、`mvu/`、`components/`
  (Sidebar / ChatWindow / ChatInput / AgentDock / TaskBoard + 弹窗)、`components/settings/` 各 section。
- **store 门面**:单一 `useAppStore('app')` 用 `storeToRefs` 转发七子 store
  (chat / character / modelConn / genSettings / task / uiPrefs / resources),调用方零改动;
  **键名全局唯一纪律**(有 `storeFacade.test.ts` 兜底);跨 store 引用都在动作内运行时解析(防初始化环);
  `storeBridge` 类型化注册(owner 必填 + 显式降级 + 配对测试)。
- **SSE reducer**:`sseReducer.ts` 的 `reduceSseEvent` 是**纯函数**(不碰响应式),
  返回 `SseStateChanges`(含 `applyMvuOn`/`reloadHistory` 钩子)由 store 应用,可单测性高;
  处理全部事件类型(token 负 id 临时气泡、step 阶段与 flowProgress、tool_call 入队、
  tool_authorization_required 逆序按 call_id 匹配、tool_result 配对、vars 镜像 display_data、
  interrupted/error 清理草稿、finish 落 usage 并 reloadHistory、task 透传)。
- **流式体验**:`chatStreamService` abort 旧流 → `api.streamChat`(fetch + ReadableStream)
  → `createSseParser`(增量解析三种分帧)→ `store.onSseEvent` → 消息 ref →
  `ChatMessageItem` 缓存 computed(键=content/content_display/extra/scriptHash/renderHtml)→ v-html;
  **必发终态纪律**:HTTP 非 2xx / 流提前 EOF / 网络异常三条路径都合成 step + 空 finish,
  `generating` 一定复位,UI 永不卡生成态;**负 id 临时气泡协议**统一弹掉,
  重发/重生成前强制 `loadHistory` 换正 id。
- **任务事件 SSE**:`api/tasks.ts` 的 `streamTaskEvents`(自写帧解析器)→ `stores/task.ts` 的
  `connectTaskEvents` → `onTaskEvent` 按 kind 分发(created / status / plan / subtask / usage /
  llm_call / **delta→`appendLiveDelta` 就地追加 `liveBuffers` 绕开全量重拉** / approval_required /
  agent_status / deleted)。可靠性:settle 窗口(连接存活 >1.5s 视为成功 → 复位退避、停兜底轮询、
  全量补偿刷新)、指数退避 1s 起步封顶 15s、断开期间 5s 兜底轮询(仅列表 + 当前详情)、
  **in-flight 合并**(进行中则记 pending,结束补一次)、**内容签名去重**(JSON 全量序列化作签名,
  未变保持原引用 → 面板零重渲染);所有事件同时进事件监控面板。
- **沙箱渲染**:三宿主文档三 CSP(服务端下发):`/sandbox.html`(角色脚本,
  `sandbox="allow-scripts allow-modals"` + no-referrer,opaque origin)、`/resource-frame.html`、
  `/render-frame.html`;前端**不能用 srcdoc**(会继承父 CSP 无 `unsafe-inline`)。
  通信 `postMessage` + channel 常量 + 每实例 `crypto.randomUUID()` nonce 双校验
  (WebView2 下 source 不可靠);消息体超 1MB 跳过广播。
  沙箱模块按五职责分层:① iframe 注入与生命周期(ready→boot 双向握手、30s 超时带脚本指纹、
  cleanup 注销监听与注册表);② boot 脚本生成(变量/globals/localStorage/几何/idMap JSON 内联,
  `<`→`\u003c` 防 `</script>` 逃逸;断网:fetch/XHR/WebSocket/EventSource/indexedDB 置 undefined;
  `window.parent` Proxy 门面);③ **mini-jquery DOM RPC**(操作入队 → 微任务批量 postMessage →
  宿主回包附状态镜像 → 未命中发 probe → setter 乐观回写;游离元素记 spec 在 append 时统一落 DOM;
  事件经 `jq-event` 回沙箱);④ HTML 白名单清洗(sanitize-html;事件处理器一律剥离,
  交互走 `on()` RPC;safeText ≤512KB、safeSelector ≤1KB);⑤ 通信协议(9+6 种消息类型;
  inline 事件降级桥把 `onclick` 剥成 `data-kd-onclick`,触发时代码串回沙箱求值)。
- **渲染管线**:`render.ts` 的 `renderScopedScripts`——剥 mvu 协议块 → 隐藏类脚本剥离 →
  渲染脚本排序(防占位符被围栏吞)→ 占位符化(replace_string 经 `$1` 展开 + `{{user}}`/`{{char}}` 宏)
  → 逐占位符构建 scoped HTML(剥围栏 / 提取样式脚本 / `demoteInlineHandlers` /
  `sanitizeVisibleHtml` 33 标签白名单 / CSS `sanitizeScopedCss` + `scopeCss` 加 `[data-kd-scope]` 前缀
  + `@keyframes` 重命名)→ 非脚本段 markdown-it(`html:false`,链接新窗口)→ `{html, blocks}`。
  **白名单三层**:消息可见(33 标签,`data-*` 放行,协议 data+https)> 脚本运行时
  setHtml/append(更宽,含表单控件)> RPC setHtml(最严,7 个内联标签无协议)。
- **稳定引用防重建**:`stableFrameNonce`(FNV-1a)+ seed 化 scopeId(`msg-${id}`),
  同消息重渲染产出相同 HTML → v-html 不动 → iframe 与沙箱存活;四级缓存叠加
  (ChatMessageItem computed / scriptBlocksCache / ScriptRunner executedContainers+inFlight /
  useVirtualMessages:阈值 80、尾部 30 常驻、overscan 1200px)。
- **消息渲染性能**:流式逐 token 不再全量重渲染(此前已修复);核心渲染组件已有测试覆盖
  (此前缺口已补)。
- **弹窗体系**:弹窗**注册表单点定义**(App.vue 与 `check-arch` 白名单派生,
  有「flag 无重复 / label / 组件已定义 / MODAL_FLAGS 对应」三方一致元测试);
  `lazyModal(defineAsyncComponent)` 懒加载,`asyncModal.ts` onError 自动 retry 一次,
  仍失败写 `modalLoadError` 由全局错误条手动重试;设置中心各 section 按 tab 二次懒加载
  (visitedSections 门禁);`manualChunks` 分组、生产禁 sourcemap;
  深链接 `#settings` / `?settings=1` 手写实现(Tauri 下跳过 hash 写入);
  启动 4 个 load 并行(`Promise.allSettled`)+ `loadSettings` 串行先于 `loadTasks`
  (消除 appMode 依赖竞争)。
- **窄屏适配**:设置面板在 Android 窄屏的 CSS 塌缩已修复(CSS 网格 / 输入框宽度 /
  下拉箭头三处),以三档视口实测 + 自动化校验。
- **本地偏好持久化**:`character` store 的脚本授权(localStorage,characterId + scriptHash SHA-256 对)、
  `uiPrefs`(13+ 弹窗开关 / 错误条 / HTML 渲染开关按卡记忆 / callTrace tab,300ms 节流写)、
  `task`(`kedai.appMode` 等)、`chat` 的 last-character 映射。
- **类型门禁**:`npm run typecheck -w web`(vue-tsc)为**硬门禁**(历史存量错误已清偿归零);
  类型逃逸 ratchet `node tools/check-frontend-lint.mjs`(as never / as unknown as / 非空断言 / any,只降不升)。

**权威来源**:`web/src/{store.ts,sseReducer.ts,api/,stores/,mvu/,components/,sandbox/}`、
`web/src/rendering/render.ts`、`web/src/composables/`、`web/vite.config.ts`;
文档小节 [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇第 12–15 章,
[手机端设置面板窄屏适配-变更说明](archive/2026-09-16-consolidation/手机端设置面板窄屏适配-变更说明.md),
[前端结构债收敛-变更说明](archive/2026-09-16-consolidation/前端结构债收敛-变更说明.md),
`../MAINTENANCE.md` §0(渲染性能纪律、样式分层纪律)、§11(前端用例范围)。

**关联契约**:`契约-协议与配置.md`(前端消费的 SSE 事件与设置字段)、
`契约-架构与数据.md`(store 分层与沙箱宿主文档)。

---

## 工程与质量基线

**现状能力**

- **测试套件**:后端 cargo 单测 + API 集成测试,前端 Vitest;
  覆盖范围见 `../MAINTENANCE.md` §11 的用例清单(状态机、planner/reflector、世界书注入与
  `scan_depth` 窗口、mvu/EJS、角色卡解析、正则脚本、@INJECT、GENERATE 注入、结构化错误码、
  任务编排工具契约、命令分级与权限矩阵、截断自愈、密钥保留、pending_runs RAII、DB 锁中毒回滚、
  世界书概率门控不变式、用量落库事务化、migrate 元测试、脚本沙箱 e2e 等)。
  **数量以 `../MAINTENANCE.md` 为准**,取得命令:

  ```bash
  node tools/count-tests.mjs          # 当前计数
  node tools/count-tests.mjs --check  # 校验文档描述是否漂移
  npm run count:tests -w web          # 等价 npm script 别名(以根 package.json 为准)
  ```

- **门禁清单(本地 CI 单一入口)**:`tools/check-all.ps1`(`npm run check`),阶段依次为
  fmt → clippy → cargo test → **cargo audit(硬门禁)** → lock-sync(双锁漂移)→
  check-contract(前后端契约映射)→ check-arch(分层规则 C/D/E/G/H/I/J)→ 类型逃逸 ratchet
  → `npm audit --omit=dev`(警告档)→ **vue-tsc(硬门禁)** → vitest → vite build。
  接三处触发:`build.ps1` 构建前跑 `-Quick`(失败即中止构建)、`tools/hooks/pre-push`
  (推送前,`npm run hooks:install` 安装)、`.github/workflows/ci.yml`(**纸面 CI**——
  仓库当前未配置 git 远端,从未运行;实际生效的是前两条)。
- **架构门禁**:`tools/arch-layers.json` 是代际归属与跨代依赖方向的唯一机器可读事实源(SSOT),
  `tools/check-arch.mjs` 的规则 I(每个顶层模块/目录必须登记代际,未登记即 FAIL)与
  规则 J(按登记代际校验 import 图,新增未登记越代边即 FAIL;存量越代边登记
  `reason` + `remediation`,只减不增)硬门禁锁定;另有规则 K(未接入 `JsonBody` 的提取器
  ratchet,只降不升)。
- **依赖供应链**:`check-lock-sync.mjs`(双 `Cargo.lock` 漂移,0 漂移为基线)、
  `cargo audit`(硬门禁,advisory DB 拉取失败降级 WARN)、`npm audit --omit=dev`(警告档)。
- **性能基线方法**:`tools/perf-baseline.mjs` 压测端点(`npm run perf`,输出 p50/p95/吞吐),
  基线存 `tools/perf-baseline.json`;阈值门禁口径为 **`当前 p95 <= 基线 p95 × factor` 且
  `errors == 0`**,`p95 < 5ms` 的端点跳过(小数据集抖动),经 `check-all.ps1 -Perf` 并入总门禁
  (**默认关闭**——需服务已在运行;服务不可用时 fail-closed 而非静默跳过)。
  基线是**本机 release 口径**,只作回归对比基准、不是跨机 SLA,换机器或数据量变化后须重采样。
  用法与实测定量见 [perf-baseline](archive/2026-09-16-consolidation/perf-baseline.md) §六与 §三。
- **性能治理已落地项**:工具参数计入 token 计数、裁剪回收工具参数(占位保持合法 JSON)、
  重复调用熔断(`utils/loop_guard.rs`,FNV 指纹 + 环形窗口 N=8/K=3)、世界书正则预编译
  (进程级缓存 + 小写化外提)、token 计数缓存、推理感知自愈、性能门禁、工具定义快照、
  世界书条目缓存、连接器读锁快照化(改 `read().await.clone()` 后立即释放,修复
  「生成期间保存设置阻塞到整轮生成结束」)、索引补齐
  (`idx_sessions_character` / `idx_tasks_character` / `idx_tasks_status`,新库建表即带、
  旧库经幂等迁移补建)、`trim` 量纲修正(极端兜底分支不再把 token 数当字符数用)。
- **构建指纹与双端同步**:测试版 `dist\kedai-server.exe` 与便携版
  `dist\Kedai-portable\Kedai.exe` 为两条独立编译链,各冻结编译时刻的 `web/dist`;
  `build.ps1` 双端同步产出并写 `<exe>.build.json` 指纹(`version/build_time/dist_hash`),
  启动器与 `start.ps1` 比对,不一致自动重建;设置中心底部可见两端指纹。
- **构建与测试前置**:**改完代码后 `dist\` 与项目根 `Kedai.exe` 不会自动更新**——
  交付/试用 exe 版前必须跑一次 `.\build.ps1`(快速迭代可用 `-TestOnly`,但便携版会因此未同步)。
  `cargo test` **必须带 `-j 2`**(原因此处不抄,见 `../MAINTENANCE.md` §10);
  本机裸 shell 可能没有 cargo 环境,需先加载 vcvars64。
- **文档事实权威**:代际归属以 `tools/arch-layers.json` 为准;数字(测试数量、构建步骤、门禁清单)
  以 `../MAINTENANCE.md` 为准,由 `tools/count-tests.mjs --check` 守护;叙述以
  `ARCHITECTURE-3H.md` 为准(须与 `arch-layers.json` 对账)。

**权威来源**:`tools/check-all.ps1`、`tools/check-arch.mjs` + `tools/arch-layers.json`、
`tools/check-contract.mjs`、`tools/check-lock-sync.mjs`、`tools/count-tests.mjs`、
`tools/check-frontend-lint.mjs`、`tools/perf-baseline.mjs` + `tools/perf-baseline.json`、
`build.ps1`、`tools/hooks/pre-push`;
文档小节 `../MAINTENANCE.md` §0(门禁与治理纪律)、§2(命令速查)、§4(构建)、§11(测试),
[perf-baseline](archive/2026-09-16-consolidation/perf-baseline.md) §六,
[优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇小结与附录 C 的度量方法。

**关联契约**:`契约-架构与数据.md`(模块归属与分层)、`../MAINTENANCE.md`(数字权威)。

---

## 来源与溯源

本文件由以下源材料整合而成(均为**已实现能力**的取证材料):

| 源材料 | 在本文件中的落点 |
|---|---|
| `../README.md`「核心特性」「Roadmap(已勾选项)」 | 各章能力清单的总纲(用户向口径);未勾选条目一律未收录 |
| [任务引擎六模式](archive/2026-09-16-consolidation/任务引擎六模式.md) | 「六模式任务引擎」章(全文移植;§六能力晋升预留改为交叉引用) |
| [优化实施方案-2026-09](archive/2026-09-16-consolidation/优化实施方案-2026-09.md) 第一篇(第 1–16 章) | 「聊天主链路」「上下文工程与压缩」「世界书与提示词注入」「Agent 引擎」「变量与契约引擎」「工具系统与授权」「前端」「可观测性与错误面」的机制描述 |
| [perf-baseline](archive/2026-09-16-consolidation/perf-baseline.md) §五「本轮落地的修复」 | 「工程与质量基线」的性能已落地项与门禁方法 |
| [工程韧性修复-变更说明](archive/2026-09-16-consolidation/工程韧性修复-变更说明.md) §1/§2 | 「可观测性与错误面」(错误面收口 / requestId 与访问日志与 health 依赖探测 / DB schema 版本化与升级链事务化 / 静默吞错与错误分类类型化) |
| [0.3.0-beta-后续修复-变更说明](archive/2026-09-16-consolidation/0.3.0-beta-后续修复-变更说明.md) §一–§十 | 「工具系统与授权」(bash 能力与命令级硬门)、「可观测性与错误面」(密钥保留 / pending_runs RAII / 锁中毒回滚 / 保留策略 / 迁移元测试)、「上下文工程与压缩」(概率门控不变式、trim 量纲) |
| [授权模式](archive/2026-09-16-consolidation/授权模式.md) | 「工具系统与授权」的三档矩阵、判定链顺序、设置字段与生命周期 |
| [命令执行层与Android三档执行器-变更说明](archive/2026-09-16-consolidation/命令执行层与Android三档执行器-变更说明.md) | 「工具系统与授权」的命令执行四道闸、Exec 抽象层、Android 三档执行器 |
| [角色卡世界书扫描窗口修复-变更说明](archive/2026-09-16-consolidation/角色卡世界书扫描窗口修复-变更说明.md) | 「世界书与提示词注入」的 `scan_depth` 语义归位与两条路径收敛 |
| [手机端设置面板窄屏适配-变更说明](archive/2026-09-16-consolidation/手机端设置面板窄屏适配-变更说明.md) | 「前端」的窄屏适配 |
| [前端结构债收敛-变更说明](archive/2026-09-16-consolidation/前端结构债收敛-变更说明.md) | 「前端」的 storeBridge 类型化与弹窗注册表单点派生 |
| [mvu-protocol](archive/2026-09-16-consolidation/mvu-protocol.md) / [inject-protocol](archive/2026-09-16-consolidation/inject-protocol.md) / [generate-render-protocol](archive/2026-09-16-consolidation/generate-render-protocol.md) / [模式提示词边界](archive/2026-09-16-consolidation/模式提示词边界.md) | 各章协议细节的指向(此处不重复正文) |
| [known-limitations](archive/2026-09-16-consolidation/known-limitations.md) | 未实现/有意裁剪项的分流去向(`遗留.md`) |
| `../MAINTENANCE.md` §1、§14、§0、§11 | 「架构与数据流」概况、各新模块速览、门禁与治理纪律、测试范围;一切计数事实的权威 |
| `../API.md` 分组结构 | 能力面完整性核对(聊天 / 会话 / 角色卡 / 设置 / Token / 提示词注入 / Agent 流程 / 导入导出 / 世界书 / Agent 控制 / 任务 / 记忆 / 插件技能 / 变量与契约 / exec / 健康检查);端点字面不在此重复 |

**未收录内容说明**(属其他类别,不在本文件):

- **未实现的计划**:README Roadmap 未勾选项(Oobabooga/KoboldAI 连接器、工具执行沙箱隔离、
  知识库向量检索工具)、六模式文档 §六 的能力晋升预留方向与 `ModeSettings` 每子模式独立覆盖字段、
  team 手动拓扑、契约不变量的 range / require_if 谓词、`docs/优化实施方案-2026-09.md` 第二篇的
  22 项优化方案——归 `计划.md` / `展望.md`。
- **已知缺陷与有意裁剪**:`sticky/cooldown` 不参与匹配、无 ST 式递归激活、世界书 `role=system`
  钳制为 user、MCP 孙进程不保证回收、记忆检索无 ANN 索引、无 metrics/OTLP、无 down 迁移、
  事件流无持久化回放、413 未统一形状、`deep` 主生成不启用只读工具循环等——归 `遗留.md`
  (逐项体例见 `known-limitations`)。
