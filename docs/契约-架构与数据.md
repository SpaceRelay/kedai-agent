# 契约 —— 架构分层与数据

> **本文件归属**：**契约**类；**架构叙述权威**（代际归属事实源仍是 `../tools/arch-layers.json`，本文须向其、向代码实测对账）。
>
> **一句话定位**：本文锁定 Kedai 的两条结构性契约——①「老中青三结合」分层代际契约（模块归哪一代、谁允许依赖谁、怎么晋升、哪些偏差已被知情接受）；②`kedai.db` 的 schema 版本与降级/合并契约（库比代码新怎么办、旧库怎么升、新版库被旧版打开会怎样、双库怎么合）。
> **任何结构性改动先对照本文判断落点与代际影响。**
>
> **整合说明（2026-09-16）**：本文由两份原文**全文、不可裁剪**地移植合并而成——
> `archive/2026-09-16-consolidation/ARCHITECTURE-3H.md`（301 行，架构叙述权威）与
> `archive/2026-09-16-consolidation/数据库版本与降级行为.md`（330 行，含其既定的「原文 + 变更标注」体例）。
> 原节号、判据、判定程序、允许/禁止出边表、三种结合机制、模块代际清单、晋升台账、偏差登记、
> 结构性遗留、行为矩阵、风险条目编号一律保留；仅按整合需要做两处机械处理：
> ①原文中指向 `docs/` 内文档的引用改写为 `archive/2026-09-16-consolidation/<原文件名>`（相对 `docs/`）；
> ②节标题层级下移一级以适配本文的目录结构（节号不变）。原文中的自称「本文件」在本文内一律理解为**本文的「三结合分层架构」一节**。
> `../tools/arch-layers.json` 的 `$comment` 中「权威叙述」一行**将**改为指向本文（现仍指向已归档的 `ARCHITECTURE-3H.md`，属本次范围外的文件编辑，见「代际归属事实源对账」一节的【待核实】待改项）。

---

## 契约登记表

| 契约条目 | 约束要点 | 权威来源 | 机检状态 |
|---|---|---|---|
| **代际定义与判据**（L1 老层·稳 / L2 中层·干 / L3 青层·活 / entry 组合根） | 四代际各有 `criterion`（一句话判据）与 `discipline`（纪律），判定争议按 P1–P5 程序先命中先定 | `../tools/arch-layers.json` 的 `generations`；判据详解见本文「三结合分层架构」§2.1/§2.2 | 规则 I + J（`../tools/check-arch.mjs`）实测执行；无独立规则校验判据文本本身 |
| **模块代际归属完整性** | 每个后端顶层目录、每个后端根 `.rs`、每个前端顶层目录、每个前端根 `.ts`/`.vue` 都必须登记代际与职责 | `../tools/arch-layers.json` 的 `backend` / `entryFiles` / `frontend` / `frontendEntryFiles` | **规则 I 硬 FAIL**：未登记即失败，不静默放过；配置文件读取失败亦 FAIL |
| **跨代依赖方向（后端 = 隔离模型）** | 允许 `L1→L1` `L2→L1` `L2→L2` `L3→L1` `L3→L3`；禁止 `L1→L2` `L1→L3` `L2→L3` `L3→L2` | `dependencyRules.backendAllowed` / `forbidden` | **规则 J 硬 FAIL**（未登记的新增越代边） |
| **跨代依赖方向（前端 = 层次模型）** | 在后端允许集之外**额外允许 `L3→L2`**（组件读 store、store 调 composable 是常态） | `dependencyRules.frontendAllowed` | **规则 J**（前端桶独立报告，与后端桶分开打印后统一退出） |
| **组合根（entry）豁免** | entry 不参与方向校验；但**必须登记**。后端 entry 仅作依赖发起方；前端 entry 同时是**被广泛引用的公共装配面**（`store.ts`/`modals.ts`），作为接收方也豁免 | 规则 I 强制登记；豁免语义见规则 J 实现注释 | 规则 I 登记项 + 规则 J 跳过项 |
| **越代债务登记（只减不增）** | `registeredEdges` / `frontendRegisteredEdges` 登记已核实存量债务，`class` 必须二选一：`wiring`（正常接线，设计上接受）或 `debt`（应还的债，须写 `remediation`）。还清一条删一条，**禁止为过检查而加条目** | `../tools/arch-layers.json` | 规则 J：已登记边只出 note 不 FAIL；新增未登记边立即 FAIL；**登记陈旧检测**（已登记但实测不存在的边 → note 要求删除） |
| **L3 冻结项：EJS 自研解释器** | `parsing/assistant/ejs/` 只接受安全修复，新模板能力一律走 `scripts/runtime.rs` 的 rquickjs 沙箱；能力面由字面量条数 **ratchet 只降不升**钉死 | 本文「三结合分层架构」§2.6；ratchet 基线在 `../tools/check-arch.mjs` 规则 H | **规则 H**：`env.rs` 三个能力面（`builtin_global` 64 / `get_prop` 26 / `set_prop` 0）任一超基线即 FAIL |
| **schema 版本单调契约** | `models/db/schema.rs` 的 `pub const SCHEMA_VERSION: i32 = 1`（`0` 留给「从未标记」的旧库）；`Db::open` 在建表批**之前**读 `PRAGMA user_version`，`db_version > SCHEMA_VERSION` 即**拒绝启动**；版本在全部升级动作成功**之后**才写 | `server-rs/src/models/db/schema.rs`、`server-rs/src/models/db/mod.rs` | 元测试 `server-rs/tests/schema_migration_meta.rs`（§四 4.3 验收断言 1/2/3/4/5/6）；**覆盖率见风险 3/5** |
| **schema 只增不改 + `normalize_sql` 逐字节一致** | 升级靠幂等 DDL（全量 `CREATE TABLE IF NOT EXISTS` 建表批 + 增量 `ensure_*`）；`migration/ddl.rs` 的补列/补表/补索引文本必须与 `models/db/schema.rs` 的 `CREATE_TABLES` 在 `normalize_sql` 后**逐字节一致**，否则跨库合并判 schema 冲突 | `server-rs/src/migration/ddl.rs`、`server-rs/src/models/db/schema.rs`、`server-rs/src/migration/merge.rs:264` | 元测试「逐表结构指纹比对」（`schema_migration_meta.rs:78-125`）；文本一致性护栏见 `../MAINTENANCE.md:458-461` |
| **升级链原子性** | 建表批 + 9 个 `ensure_*` 由 `BEGIN IMMEDIATE … COMMIT` 包裹，任一步失败统一 `ROLLBACK`；`ensure_memory_entries_fts_backfill` 与 `backfill_scope_variables` **有意留在提交之后**（靠 `backfill_meta` 幂等，避免长写事务膨胀 WAL） | `server-rs/src/models/db/mod.rs` | 元测试 `failed_ensure_rolls_back_whole_upgrade_chain_and_keeps_version`（断言失败后 `user_version` 保持旧值） |
| **跨库合并保守语义** | 合并前对两侧各补一遍 `ensure_*` + 新增表 DDL 拉到同款 schema；schema 冲突或缺表时**停止合并并保留两份原数据**，绝不写坏任何一侧 | `server-rs/src/migration/merge.rs`、`server-rs/src/bin/kedai-data-merge.rs` | 离线 CLI，无自动门禁；合并产物的 `user_version` 对齐属 §四 4.3 断言 6 |
| **双库并存不自动检测** | 项目 `data\` 与 `%APPDATA%\com.kedai.app\data` 可能长期并存两份库；唯一处置是**手动** CLI 合并 | `server-rs/src/config.rs:73-105`、本文「数据库 schema 与降级契约」§2.4/§2.5 | **无机检**（风险 7，知情接受） |
| **新增内置工具的 6 处登记点** | 新增工具须同时出现在注册器、风险级、场景白名单、调用时机、渲染意图、前端流程白名单六处，漏改**静默降级** | **该清单正文见 `契约-协议与配置.md` §新增内置工具登记清单**（原出处 `archive/2026-09-16-consolidation/威胁模型与残余风险.md` §四） | **无机检**；收敛方向（元测试穷尽性检查）登记为该节待办 |
| **前端 `entry` 门面白名单** | `store.ts` 门面与 `modals.ts` 注册表是唯一事实源，`check-arch.mjs` 的组件直改 state 白名单**由此派生** | `frontendEntryFiles` 的 `responsibility` 字段 | 规则 A/B：`组件直改 state` 计数 + 白名单（实测 44 处 / 白名单 44） |

> **交叉引用声明（新增内置工具登记清单）**：上表末两行涉及的「6 处登记点」与 `tools/` 代际纪律直接相关（新增工具属 L2 设施变更），但其**正文不在此重复**——**该清单正文见 `契约-协议与配置.md` §新增内置工具登记清单**。本文只声明交叉引用关系，避免两处正文分叉。

---

## 三结合分层架构

> 本节为 `archive/2026-09-16-consolidation/ARCHITECTURE-3H.md` 的**全文移植**，是仓库的**架构叙述权威**。
> 借鉴我国干部队伍/领导班子建设中长期沿用的「老中青三结合」组织原则，将其四条精神
> （优势互补 / 传帮带 / 梯队衔接 / 动态循环）映射为代码治理模型，作为 kedai 演进的总纲。
> **本节是架构决策依据：任何结构性改动先对照本节判断落点与代际影响。**

### 0. 文档地位与权威边界（先读这一节）

本仓库的架构事实有三个权威，**各管一段、不得互相顶替**（2026-09-14 确立，源于「两份互相
冲突的权威」的教训）：

| 权威 | 管什么 | 位置 |
|---|---|---|
| **代际归属权威** | 哪个模块属 L1/L2/L3/entry、允许的跨代方向、已登记的越代债务 | `../tools/arch-layers.json`（机器可读 SSOT，**受 `../tools/check-arch.mjs` 规则 I/J 强制**） |
| **叙述权威** | 为什么这样分层、判据是什么、纪律与晋升规则 | **本节（原 `ARCHITECTURE-3H.md`，现由 `契约-架构与数据.md` 承载）**（必须向 `arch-layers.json` 对账，不得单方面改代际） |
| **数字权威** | 测试数量、命令、构建步骤等一切可计数事实 | `../MAINTENANCE.md`（受 `../tools/count-tests.mjs --check` 守护） |

**冲突裁定顺序**：`arch-layers.json` > 本节 > 其他文档。本节如与代码实测不符，
以代码为准并**必须先修本节**再动代码（见 §7「叙事与实现偏差登记」的既有体例）。
本节**不再硬编码测试数字**——那类事实一律引用 `../MAINTENANCE.md`。

---

### 1. 「老中青三结合」的含义与内容

「老中青三结合」是干部队伍与领导班子配备的指导原则：以**老（经验者）**把关定向、
**中（骨干）**承上启下、**青（后备）**注入活力，三部分按合理梯次组合，保证事业平稳
交接、后继有人、不断层、不僵化。

| 组成部分 | 特点与作用 |
|---|---|
| 老（经验者） | 经验丰富、把关定向、原则性强；「传帮带」的主要承担者，防风险的压舱石 |
| 中（骨干） | 年富力强、业务成熟，处于干事创业黄金期；承上启下、攻坚执行的中坚 |
| 青（后备） | 朝气蓬勃、学习力强、可塑性强；队伍的活水与未来，敢闯敢试 |

**四条精神内核**：
1. **优势互补**：经验 + 成熟 + 活力互补，避免「全老则僵化保守、全新则轻率失稳」的单结构缺陷；
2. **传帮带**：老带中青、经验传承，在实践中培养，防止能力断层；
3. **梯队衔接**：合理梯次、后继有人，保证事业平稳交接、不断层；
4. **动态循环**：青年成长为骨干、骨干沉淀为经验者，队伍新陈代谢、永续发展。

---

### 2. 分层模型：四代际（3 层 + 组合根）

**澄清（2026-09-14）**：本模型是 **3 层 + 1 个组合根 = 4 个桶**，不是「四层」。
`entry` 不是第四层，而是组合根（Composition Root），其判据与纪律与前三个代际不同。

#### 2.1 判据（把比喻降维成可裁决的标准）

| 代际 | 精确判据（一句话） | 对应的机器门禁 |
|---|---|---|
| **L1 老层 · 稳**（Anchored Core / 基石层） | 承载**兼容契约**：形状一变即破坏既有用户数据或外部生态（SillyTavern 角色卡/世界书/正则脚本/酒馆助手协议），且**自身不依赖任何上层** | 规则 D + I + J |
| **L2 中层 · 干**（Orchestration / 骨干层） | 依赖 L1 完成**业务流程**，自身不被 L1 感知；承担**协议翻译**（V2/V3、新旧字段、注入合成） | 规则 C + J |
| **L3 青层 · 活**（Frontier / 探索层） | **失败必须被隔离**（沙箱 / 设置开关 / 注册表三者至少有一），不反向定义上层语义 | 规则 H + J |
| **entry 入口 · 装配**（Composition Root） | 只做**拼装与配置解析**，不承载业务语义（业务逻辑沉淀于此则无法被测试与复用） | 规则 I（并按约定豁免方向校验） |

`arch-layers.json` 的 `generations` 另给每个代际一条 `discipline`（可机读的纪律原文，规则编号落
在 `guardRules` 字段）：

| 代际 | `criterion`（JSON 原文） | `discipline`（JSON 原文） | `guardRules` |
|---|---|---|---|
| L1 | 长期验证的兼容契约：数据/协议形状一旦改变即破坏既有用户数据或外部生态兼容。改动成本最高，且不依赖任何上层。 | 不可变契约优先；改动须先写失败测试 + 声明兼容影响 + 过评审；禁止 `use crate::services / agents / api / tools`。 | D、I、J |
| L2 | 业务编排与协议翻译：依赖 L1 契约完成业务流程，自身不被 L1 感知。 | 不得绕过 L1 契约直接改数据层语义；协议翻译集中在本层；禁止 `use crate::tools / mcp / plugins / scripts`（青层能力需经显式接缝注入）。 | C、J |
| L3 | 新能力、实验与隔离执行：失败必须被隔离（沙箱/开关/注册表），不得反向定义 L1/L2 的语义。 | 新能力默认隔离；成熟后按晋升通道升级；禁止 `use crate::services`（core 能力应由宿主经接口注入，参照 `TaskBackend` 先例）。 | H、J |
| entry | 进程入口与最终装配：负责把各层拼装成可运行系统，不承载业务语义。 | 只做装配与配置解析；业务逻辑不得沉淀于此（否则无法被测试与复用）。 | I |

#### 2.2 代际判定程序（新增：用于裁决争议，按顺序适用，先命中先定）

旧叙述最大的缺口是**没有可操作步骤**，导致 `tools/` 这类模块长期卡在「名义 L3 / 实测 L2」。
判定任何模块时依次过筛：

- **P1 契约性**：改动是否会破坏外部生态兼容或既有用户数据？**是 → L1**。
- **P2 依赖方向**：是否存在指向 L2 或 L3 的出边？**是 → 不可能是 L1**。
- **P3 隔离性**：失败是否被沙箱 / 开关 / 注册表三者之一隔离？**否 → 不可能是 L3**。
- **P4 装配性**：是否只做拼装与配置解析、不承载业务语义？**是 → entry**。
- **P5 复用度**：被 ≥2 个调用点复用的业务逻辑 → **L2**；仅直接服务用户交互 → **L3**。

#### 2.3 名义代际 vs 实测代际（核心纪律）

> **名义代际**由设计意图决定，**实测代际**由依赖图决定。两者不一致时，**以实测为准**，
> 并把差异登记为债务（`arch-layers.json` 的 `registeredEdges`）。
> **不允许**用「名义上是 L3」为越代依赖辩护。

规则 J 已把这套裁决机器化；本节只保证人和机器用同一把尺子。
**典型案例**：`tools/` 名义 L3，但存在 `→services`（违反 P2、不满足 P3），故实测为 L2——
修法见 `archive/2026-09-16-consolidation/架构分析-2026-09-14.md §5`。

#### 2.4 单值代际纪律（新增）

**代际是单值**。一个模块若内部存在两种纪律，**按子目录拆分**，不得给模块贴双标签
（此前 `web/src/mvu/` 被标为「L1+L3」，无法按单一纪律评审，已修正为 L1）。

#### 2.5 前后端的方向模型不同（务必区分）

同一套 L1/L2/L3 词汇，在两端的**依赖位置不同**，因此允许方向也不同
（配置见 `arch-layers.json` 的 `dependencyRules.backendAllowed` / `frontendAllowed`）：

- **后端 = 隔离模型**：L3（工具/沙箱）与 L2（编排）**双向互斥**。青层反向依赖骨干层会使
  「隔离」名存实亡，故 `L2→L3` 与 `L3→L2` 都禁止。
- **前端 = 层次模型**：`L3（展示）→ L2（状态编排）→ L1（纯契约）` 是**严格向下的正常依赖**。
  组件读 store、store 调 composable 是该架构的常态，故前端**额外允许 `L3→L2`**；
  真正禁止的是 `L2→L3`（编排依赖 UI/沙箱）与 `L1→L2/L3`（纯逻辑依赖状态）。
- **前端 `entry` 的双重身份**：`store.ts`（7 个 store 的门面）与 `modals.ts`（弹窗注册表）是
  **被广泛引用的公共装配面**，故组件/composable 引用它们是**豁免**的——门面存在的意义就是
  让组件不必直接引 7 个 store。这与后端 `entry` 只作依赖发起方不同。

kedai 三层落点：
- **L1**：`parsing/`（角色卡 V2/V3、世界书、mvu 协议兼容解析、EJS 渲染契约）、`models/`（SQLite 表结构、types 契约）、
  `contracts/`（万花筒契约 DSL）、`migration/`（迁移幂等，决定老层经验能否跨版本延续）、`utils/`（基础纯函数）、
  **既有测试套件**（数量见 `../MAINTENANCE.md`，是「传帮带」的第一载体）、SillyTavern 生态兼容承诺。
- **L2**：`agents/`（engine 编排、状态机、planner/reflector）、`services/`（角色/会话/世界书/注入/任务引擎/undo）、
  `api/`（路由/鉴权/SSE）、`connectors/`（LLM 后端适配）、`slash/`（命令系统）。
- **L3**：`scripts/`（rquickjs 沙箱，新模板能力的唯一合法落点）、`mcp/`（默认关）、
  `plugins/`（受控指令脚本，白名单求值器）、`services/exec/`（命令执行，默认关）、
  前端 `components/` 与 `sandbox/`。
- **`tools/` 的归属修正（2026-09-14，L3 → L2）**：`tools/` 曾按设计意图标为 L3，但按 §2.2 判据实测为 **L2**：
  ① P3 隔离性**不满足**——目录内无沙箱、无设置开关、无实验隔离，仅有 `ToolRegistry` 注册表，
  而注册表是 L2 骨干设施、不是 L3 的失败隔离手段；② P5 复用度**命中**——被 `agents`/`services`/`api`
  共 32 处复用，是**核心必需设施**（它失败则整个应用不可用），与 L3「失败必须被隔离」的语义相反。
  故原先登记的 5 条 tools 相关越代「债务」实为**归属画错**，已随本次修正全部消除。
  真正的 L3 隔离能力在其相邻模块：`scripts/`（rquickjs 沙箱）、`mcp/`（默认关）、`plugins/`（受控求值器）、
  `services/exec/`（默认关）。**教训**：不要用「名义上是 L3」为越代依赖辩护，见 §2.3。

#### 2.6 L3 的冻结项：EJS 自研解释器（`parsing/assistant/ejs/`）

**只接受安全修复，新模板能力一律在 `scripts/runtime.rs` 的 rquickjs 沙箱侧实现**（rquickjs 自带
内存/中断/栈上限）。该解释器已加固「循环步数 + 墙钟预算 + 解析深度守卫」，但不再扩展——自研引擎缺
引擎级沙箱限额，继续加功能会扩大不可信输入的攻击面。能力面由规则 H 的**字面量条数 ratchet** 钉死
（只降不升）。详见 `../MAINTENANCE.md §0`。

---

### 3. 每代际的允许/禁止出边与违例后果

| 代际 | 允许出边 | 禁止出边 | 违例后果 |
|---|---|---|---|
| L1 | `→L1` | `→L2` `→L3` `→entry` | 规则 D + J **硬 FAIL**，无登记豁免 |
| L2 | `→L1` `→L2` | `→L3` `→entry` | 规则 J FAIL；确无法消除须登记 `class: debt` |
| L3 | `→L1` `→L3` | `→L2` `→entry` | 规则 J FAIL；装配类可登记为 `class: wiring` |
| entry | 任意 | 无 | 不参与方向校验，但**必须登记**（规则 I） |

**`debt` 与 `wiring` 的区别**（登记时必须二选一）：
- `wiring` = 组合根/兼容桥的**正常接线，设计上接受**（如 `api→tools` 的工具注册表装配）；
- `debt` = **应还的债**，必须写明 `remediation`（收口路径）。
两者都**只减不增**：还清一条删一条，禁止为过检查而加条目。


---

### 4. 三种「结合」机制

#### 4.1 传帮带 · 晋升通道（动态循环）
青层创新成熟 → 过晋升门槛 → 升入中层打磨 → 沉淀为老层契约。
**晋升门槛（三关）**：① 通过既有测试套件（经验库）；② 不破坏兼容协议；③ 经评审确认边界。
- 新能力一律先进 L3 验证（GENERATE/@INJECT/EJS API 即走此路）；
- 反例（禁止）：新代码直接改写 L1 契约而不带测试 → 破坏经验传承。

#### 4.2 优势互补 · 边界
- **老层**：不可变契约，防青层误伤、防中层漂移；
- **中层**：翻译适配（V2/V3、新旧字段、协议转义），是「经验 → 实现」的桥梁；
- **青层**：隔离试探（沙箱/功能开关），防架构僵化。
三层缺一不可：全老 = 僵化不可演进；全新 = 失稳无锚点。

#### 4.3 防断层 · 兼容承诺
- 兼容协议是「老层经验」的载体：演进可迁移、不破坏（数据迁移幂等、v3fix 重刷、SQLite 迁移）；
- 数据结构演进必须带迁移路径（如旧字段兼容解析、`serde(default)` 缺省）；
- 跨端协议（前后端 mvu）以协议文档锁定双端一致，防「代际交替」时行为漂移
  （见 `archive/2026-09-16-consolidation/mvu-protocol.md`）。

---

### 5. kedai 模块代际归属清单

> **本节是叙述性对照表，权威是 `../tools/arch-layers.json`**（规则 I 强制每个顶层模块/根文件登记）。
> 本节与 JSON 不一致时，以 JSON 为准并回填本节。
> **实测对账结果见本文「代际归属事实源对账」一节**（该节列 JSON 原文与当前 5 条已登记越代债务）。

| 模块 | 代际 | 说明 |
|---|---|---|
| `parsing/character_card.rs` | L1 | 角色卡 V2/V3 解析（兼容契约） |
| `parsing/world_book.rs` | L1 | 世界书解析 + 条目匹配（含 @@ 装饰器） |
| `parsing/macros.rs` / `preset.rs` / `regex_script.rs` / `scopes.rs` | L1 | 宏、预设、正则脚本、作用域变量解析 |
| `parsing/assistant/` | L1 | mvu 变量树 / 输出协议 / EJS 解释器（酒馆助手兼容层） |
| `models/` | L1 | SQLite 表结构 + 类型契约（`types.rs` 混装传输类型为已登记知情接受项） |
| `contracts/` | L1 | 万花筒契约 DSL：定义/注册表/操作/求值（registry 已改闭包注入，见 §7） |
| `migration/` | L1 | 快照备份、双库合并、幂等 DDL 升级、冲突处理（零 `crate::` 出边） |
| `utils/` | L1 | 结构化日志、原子文件写入、截断重试纯函数（零 `crate::` 出边） |
| `agents/engine/` | L2 | Agent 编排（run 流程、消息构建、mvu 应用、工具循环） |
| `agents/planner.rs` / `reflector.rs` / `state_machine.rs` | L2 | 计划/反思/状态机 |
| `services/` | L2 | 业务服务（角色/会话/世界书/注入/设置/密钥/undo） |
| `services/task_engine/` | L2 | 六模式任务引擎（`ModeExecutor`/`TaskRunContext`/事件桥 + solo/multi/plan/team/custom/**legacy** 执行器）——语义见 `archive/2026-09-16-consolidation/任务引擎六模式.md` |
| `services/task_service/` | L2 | 任务宿主（DB/取消/事件/legacy 三段式历史路径） |
| `api/` | L2 | 路由 + 鉴权 + SSE |
| `connectors/` | L2 | LLM 后端适配 |
| `slash/` | L2 | slash 命令系统（注册表 + 内置命令 + 参数解析） |
| `tools/` | **L2** | 工具系统：注册表、内置工具集（读取/写入/搜索/agent 域）、授权裁决与命令分级。**2026-09-14 由 L3 修正为 L2**（判据 P3/P5，见 §2.5 末段）；真 L3 隔离能力见 `scripts/` `mcp/` `plugins/` `services/exec/` |
| `services/undo_service.rs` + `api/undo.rs` | L3 | 回退快照/undo：写工具执行前取逆操作负载落 `undo_snapshots` 表；默认开，`undo_enabled` 开关隔离 |
| `mcp/` | L3 | MCP stdio 客户端；默认关，`mcp_enabled` + `mcp_servers` 隔离，仅启动时装配 |
| `plugins/` | L3 | 工具插件加载器：`data/plugins/tools/*.json` 受控指令脚本工具 |
| `scripts/` | L3 | rquickjs 运行时沙箱 + TavernHelper 兼容桥 + 脚本树装载（**新模板能力的唯一合法落点**） |
| `services/exec/` | L3 | 命令执行层（bash 四道闸 + Android 三档执行器）；默认关 |
| `web/src/` 的 `api/contracts/mvu/utils` | L1 | 线格式类型、契约 diff、mvu 协议、纯工具（**单值代际**：`mvu/` 整体为 L1） |
| `web/src/stores/` + `composables/` | L2 | Pinia 状态编排（唯一状态变更入口）+ 组合式业务流程 |
| `web/src/components/` + `sandbox/` | L3 | UI 组件（展示层）+ 角色卡脚本沙箱宿主 |
| 测试套件（`tests/` + `*.test.ts`） | L1 | 经验库，晋升门槛的裁判 |

**前端根文件**（`web/src` 顶层 `.ts`/`.vue`）同样必须登记代际，判定见 `arch-layers.json` 的
`frontendEntryFiles` 与其 `responsibility` 字段。

---

### 6. 代际流动规则与评审纪律

1. **改 L1**：先写失败测试 → 最小实现 → 声明兼容影响（`../MAINTENANCE.md` 架构治理节）→ 评审；
2. **改 L2**：不得绕过 L1 契约直接改数据层语义；协议翻译集中在中层；
3. **进 L3**：新能力自由，但必须默认隔离（沙箱/开关/注册表），且纳入晋升跟踪；
4. **晋升**：L3 → L2：能力被 2 个以上调用点复用且测试覆盖；L2 → L1：协议稳定、外部依赖成型；
5. **Mutex/并发纪律**：禁止持 `std::sync::Mutex` guard 跨 `.await`（Clippy `await_holding_lock` 防护）；
   锁中毒一律恢复（`unwrap_or_else(|e| e.into_inner())`），不 panic；
6. **文档先行**：结构性改动先更新本节或对应协议文档，再动代码（传帮带：先稳基座再动骨干）；
7. **代际变更须同步 SSOT**：新增/改名/删除顶层模块时，先更新 `arch-layers.json` 再动代码，
   否则规则 I 直接 FAIL（不静默放过）。

#### 6.1 晋升台账（新增：让「动态循环」有据可查）

「传帮带」的门槛此前只有文字描述、无记录。登记每次代际晋升：

| 日期 | 能力/模块 | 代际变化 | 复用调用点 | 测试 | 评审要点 |
|---|---|---|---|---|---|
| 2026-09-14 | `web/src/cardScripts.ts` | L3 → L1 | 2（`stores/character.ts` 授权哈希 + 前端授权台账） | `cardScripts.test.ts` 6 例 | 纯数据函数 + 与后端授权门共享哈希契约，属契约层而非沙箱层 |
| 2026-09-14 | **后端 `tools/`** | **L3 → L2（归属修正）** | **32**（agents/services/api） | 既有套件 1022 例 | 按判据 P3/P5 实测：无沙箱/开关隔离、被 32 处复用为核心必需设施。修正后原有 5 条「越代债务」被确认为**伪债务**（归属画错）而消除，非靠豁免 |
| 2026-09-14 | `plugins/`、`mcp/` 对 L2 的依赖 | 倒置（依赖消除，代际不变） | — | 既有套件 + MCP 装配测试 | L3 经 L1 窄接口（`ToolRegistrar`）与配置入参（`McpServerConfig`）与宿主交互，不再直接依赖 L2 |
| 2026-09-14 | `models/tool_policy.rs`（新） | L1 新增 | 2+（services 设置/审计、tools、mcp、plugins） | 5 例线格式冻结与判据测试 | 承接 `AuthorizationMode`/`ToolRisk`/`CommandRisk`/`ToolOrigin`/`McpServerConfig` 五个共享词汇 |
| 2026-09-14 | 后端 `migration/`、`utils/`、`slash/`、`plugins/`、`scripts/` | 补登记（此前无归属） | — | 既有套件 | 补齐代际归属，消除分层叙事的静默失效点 |

> 新增条目请在合并前补入本表；「动态循环」靠这张表才可审计。


---

### 6. 本轮重构的三层执行对照

| 阶段 | 层 | 内容 |
|---|---|---|
| 阶段 0 | 全局 | 本节（定调） |
| 阶段 1 | L1 加固 | Mutex 76 处中毒恢复（经验库更可靠） |
| 阶段 2 | L2 清障 | assistant 拆 6 模块、agent_tools 拆 4 模块、engine run 拆分 |
| 阶段 3 | L3 放活 | mvu 语义对齐 + 协议文档（传帮带经验文本） |
| 阶段 4 | 落实 | EJS 加固+冻结、任务引擎依赖倒置（`task_core`/`TaskBackend`）、门禁接线（build/pre-push）、L1 渗透清理 |

> **注**：上文第二处 `### 6.` 是原 `ARCHITECTURE-3H.md` 的既有节号重复（两份文档合并编辑时遗留）；本文**照原文保留两处 §6** 以示溯源。前一 §6 = 「代际流动规则与评审纪律」，后一 §6 = 「本轮重构的三层执行对照」。

---

### 7. 叙事与实现的偏差登记（2026-09-13 核查）

> 本章如实记录「本节描述的架构」与「代码实际状态」曾经的差距,以及核查后的处置。
> **用途**:防止后续 agent 照着旧叙事改错位置;也是「传帮带」的经验留存。
> 核查方法:`grep` 交叉验证 + 全量测试(当时 975 后端 / 733 前端)+ 依赖图分析。

| # | 叙事曾声称 | 代码实际(核查时) | 处置 |
|---|---|---|---|
| 1 | L1 是「不可变契约、上层不得渗透」 | **被 L2 反向渗透**:`contracts/registry.rs:11-12` 引 `services::character_service/world_book_service`;`parsing/preset.rs:24` 引 `services::prompt_inject_service` | 已修:`PromptFloor` 等类型下沉 `models/types.rs`;registry 改为闭包注入(见 §4)。check-arch 规则 D 锁死 |
| 2 | L2 内部单向分层 | `services/task_engine/**` ↔ `services/task_service/**` **双向 import**(引擎侧 10 个文件引用 TaskService) | 已修:抽 `task_core`(共享类型/常量)+ `TaskBackend` trait(宿主能力),`task_engine` 对 `task_service` 零引用。check-arch 规则 C 锁死 |
| 3 | legacy/followup「上 ModeExecutor 缝」 | 上了缝,但经 `OnSuccess::SelfFinalized` **自行写库**,绕过统一收尾(4 个收尾入口重复同构) | 已修:终态值化(`TaskTerminal` 五变体),执行器只返回值;4 入口收敛为 `finalize_terminal` 单一出口 |
| 4 | EJS 为 L3 探索层「新能力」 | 自研解释器缺引擎级沙箱限额:循环无迭代上限、解析无深度守卫(单卡可挂死/栈溢出) | 已修:加迭代+墙钟预算与 3 处解析深度守卫;**并冻结**(新能力一律走 rquickjs 沙箱) |
| 5 | 测试套件是 L1「经验库/晋升门槛」 | 数字长期手抄(文档 923/697,实际 975/733);且**从未被任何自动化调用**(无 CI、build.ps1 不跑测试) | 已修:`build.ps1` 硬门禁 + pre-push hook;测试数改 `../tools/count-tests.mjs` 自动统计 |
| 6 | 「新能力默认隔离」 | 角色卡脚本桥(`scripts/bridge.rs` 写变量/导入)**不经授权裁决**却默认自动执行 | 登记为 L12 待办(见 `archive/2026-09-16-consolidation/known-limitations.md`),补齐路径已写明 |
| 7 | 六模式「由 ModeExecutor 统一驱动」 | 仅**派发点**统一;`multi.rs` 逐行复制 `solo.rs`;步进循环三份且取消口径不一致 | 已修:multi 委托 solo;`StateMachine` 静默吞错改为告警+迁移表修正;步进循环收敛见 `archive/2026-09-16-consolidation/架构分析-2026-09-14.md §6 批次5` |

**核查结论**:架构叙事本身方向正确(分层、隔离、晋升),但此前**停留在文档层**——代码里存在环依赖、重复实现与死抽象。本章第 1-5、7 项的处置使分层首次成为**结构 + 机器可验证护栏**;第 6 项与步进循环收敛仍为待办,已在对应文档登记。

#### 7.1 第二轮核查(2026-09-14,全量架构审计)

> 方法:全量静态侦查(依赖图 grep 交叉验证 + 文件规模统计 + **门禁实测执行**)+ 文档叙事与代码逐条对拍。
> 完整报告:`archive/2026-09-16-consolidation/架构分析-2026-09-14.md`。以下为「叙事曾声称」与「代码实际」的新增偏差。

| # | 叙事曾声称 | 代码实际(核查时) | 处置 |
|---|---|---|---|
| 8 | 规则 I/J「已落地为机器可验证护栏」 | 规则 I 把前端违规写进后端失败桶,而汇总在后端违规时立即 `exit(1)`,**前端 J 结论永不打印**;且 26 个前端根文件未登记 | **已修**:失败分桶 + 两桶打印后统一退出;补登记 26 个根文件并新增 `frontendEntryFiles` 全量代际 |
| 9 | `arch-layers.json` 是「唯一机器可读事实源」 | 该文件**未被 git 跟踪**(`git ls-files` 为空),且 7 处引用**不存在的** `docs/架构分析-2026-09-14.md` | **已修**:纳入版本控制;补齐被引用的分析文档(本仓库新增该文件) |
| 10 | 前端分层护栏「校验跨代方向」 | 规则 J 的 `!feGenOf(to) → continue` 使 69 条跨目录边中 **49 条(71%)被静默跳过**;唯一真实越代边 `components→composables`(20 处)从未被审计 | **已修**:根文件两侧都参与代际解析,前端护栏有效率恢复;新增真实违规 `stores→characterScriptSandbox` 已登记为 debt |
| 11 | `web/src/mvu/` 是「L1+L3」 | 双标签使该模块无法按单一纪律评审,而 SSOT 只接受单值 | **已修**:确立单值代际纪律,`mvu/` 归 L1;模块内两种纪律须按子目录拆分 |
| 12 | 前后端共享同一套「三结合」方向规则 | 两者同名 L1/L2/L3 但**依赖位置不同**:后端是隔离模型(L2↔L3 互斥),前端是层次模型(L3→L2 是正常依赖)。此前用一份 `allowed` 校验两端 | **已修**:拆分 `backendAllowed` / `frontendAllowed`;前端 `entry`(store 门面/modals 注册表)作为被引用方豁免 |
| 13 | 插件与 MCP 工具「默认隔离」 | 插件注册用 `g.tools.insert` **无重名校验**,名为 `bash`/`write` 的插件可**劫持内置工具**;`scripts/runtime.rs` 有内存/中断限额但**无栈上限**;后端角色卡脚本(`bridge.rs` 写变量/导入/生成)**零授权裁决**(L12) | **已修**(2026-09-14):插件注册前置重名校验 + 名称格式校验([known-limitations.md](archive/2026-09-16-consolidation/known-limitations.md) L19);rquickjs 加栈上限(`EvalOptions.stack_limit`,实测确认「未显式设置=崩进程」,L20);角色卡脚本后端授权路径打通(L12)。三项状态见各自条目 |

**核查结论(第二轮)**:分层在 `use` 层依然真实成立(L1 无上行依赖、任务引擎断环属实、终态值化与 multi 委托 solo 属实),但**本轮新增的规则 I/J 落地未完成,且以「自我遮蔽」方式失败**——门禁失败被早退吞掉、SSOT 未纳管、7 处悬空引用、前端护栏 29% 有效率。第 8–12 项的处置使规则 I/J 首次真正可用;第 13 项为安全缺口,已登记待修(后于 2026-09-14 修复,见上表)。

#### 7.2 第三轮核查(2026-09-15,韧性修复批次 0–4 落地)

> 方法:按 `archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md` 实施时**逐项核实代码现状**;结论以实测为准。
> **本轮结论:ARCHITECTURE-3H 的分层叙事未发现新的偏差**——L1/L2/L3 方向、
> `utils/` 的「零 `crate::` 出边」纪律在新增可观测性代码后**仍然成立**(`RequestIdLayer`
> 只依赖 tracing,`api/request_id.rs` 归 L2,方向正确,规则 D 实跑通过)。
> 发现的 4 处偏差**全部在修复计划文档自身**(非本架构文件),已逐条登记于
> [工程韧性修复-变更说明.md](archive/2026-09-16-consolidation/工程韧性修复-变更说明.md) §4。此处仅记录与本叙事相关的现实变化:

| 叙事相关面 | 2026-09-15 后的现实 |
|---|---|
| 错误面契约 | 由「增量接入 `code`,未覆盖处裸 `{error}`」推进为**统一 JSON 错误体**:新增 `api/json_body.rs` 收口提取器拒绝、`method_not_allowed_fallback` 收口 405、抽查 128 处错误体出口后修正 `static_files` 的「错误体 + 200」;`[G]` 裸 `{error}` **117 → 105** 并新增**规则 K** ratchet(未接入 `JsonBody` 的提取器基线 32) |
| 可观测性 | 由「有结构化日志,无 span/requestId/访问日志」推进为**每请求可定位**:`api/request_id.rs`(最外层)+ `utils/logging.rs` 的 span extension 穿透 + 访问日志 + health 依赖探测。`utils/` 仍为 L1 零出边 |
| 数据库迁移 | `Db::open` 的「建表批 + 9 个 `ensure_*`」由顺序裸执行改为 **`BEGIN IMMEDIATE … COMMIT` 事务包裹**,新增 `SCHEMA_VERSION` + `user_version` 读写与「库比代码新则拒绝启动」(L18/L21 均已修复) |
| 错误分类 | 新增 `models/llm_error.rs` 作为 **L1 共享词汇**(连接器与引擎共同依赖),取代原 `agents/engine/util.rs` 的**文案子串匹配**分类路径 |


---

### 8. 偏离纪律的结构性遗留(待办,知情接受)

| 遗留 | 现状 | 为何暂不动 |
|---|---|---|
| 步进循环三份 | legacy/plan/custom 各写一遍「置 running→写库→失败继续」;取消兜底三种口径(plan 扫 Pending、team 扫 Running、custom 直接 return) | 抽 `step_runner` 会同时改动三个已稳定运行的模式,收益(一致性)低于回归风险;已列入 `archive/2026-09-16-consolidation/架构分析-2026-09-14.md §6 批次5`,在 tools 收口后执行 |
| `models/types.rs` 混装传输类型 | `LlmMessage`/`LlmStreamChunk`/`SseEvent`/`GenerationParams`/`ToolContext` 与数据契约同处一文件 | 拆分不改线格式但会大范围改动 import,收益仅为文件整洁;已用 check-arch 规则 D 的豁免项显式标注 |
| 契约不变量仅 mutex | range/require_if 恒判通过(见 `archive/2026-09-16-consolidation/known-limitations.md` L2) | 依赖未实现的谓词引擎,属独立设计,不宜顺手发明语法 |

---

## 代际归属事实源对账

**定位**：`../tools/arch-layers.json` 是**代际归属事实源（SSOT）**，本文是**叙述权威**。
两者分工与冲突裁定顺序见上文 §0：**`arch-layers.json` > 本文 > 其他文档**。
本节的作用是把 SSOT 的现状原样对账出来（JSON 原文 + 门禁实测输出），使读者不必打开两份文件即可
判断「当前归属是什么、哪些越代边已被知情接受」，并让任何一次归属改动都能被本节复查。

### 事实源 `$comment`（实测原文）

> Kedai「三结合」代际归属与依赖方向的唯一机器可读事实源(SSOT)。
> 消费方:tools/check-arch.mjs 规则 I(归属完整性)、规则 J(跨代依赖方向)。
> 权威叙述:docs/ARCHITECTURE-3H.md;分析依据:docs/架构分析-2026-09-14.md。
> 纪律:新增/删除/改名顶层模块必须同步本文件——否则规则 I 直接判 FAIL(不静默放过)。
> registeredEdges 是「已知偏离登记」:登记在案的边不判 FAIL,但新增未登记的越代边立即 FAIL。
> 只减不增:偿还一条债务就从 registeredEdges 删除一条,并同步下调计数;绝不允许为了过检查而加条目。

> **⚠️【待核实】待改项（本次整合批注）**：上引 `$comment` 第 3 条目前仍写「权威叙述:`docs/ARCHITECTURE-3H.md`」，而 `ARCHITECTURE-3H.md` 已归档。按本次整合分工，该行**应改为指向本文**（`docs/契约-架构与数据.md`），分析依据一行应同步改为 `docs/archive/2026-09-16-consolidation/架构分析-2026-09-14.md`。**该改动属 `tools/arch-layers.json` 的编辑，不在本次「只写一个文件」的范围**，故本文只能如实记录「现状仍是旧路径 + 应改为什么」，不敢代为宣称已改。**下次改 `arch-layers.json` 时请一并落实此处。**

### SSOT 结构清单（`version: 1`）

| 键 | 内容 | 当前规模（2026-09-16 实测） |
|---|---|---|
| `$comment` | 定位、消费方、权威叙述指向、纪律声明 | 6 条（见上） |
| `version` | SSOT 自身格式版本 | `1` |
| `generations` | 四个代际的定义：`name` / `alias` / `criterion` / `discipline` / `guardRules` | `L1`、`L2`、`L3`、`entry` 共 4 键 |
| `backend` | 后端每个顶层目录的代际归属：`generation` / `responsibility` / `notes` | **15 个模块**：`parsing` `models` `contracts` `migration` `utils` `agents` `services` `api` `connectors` `slash` `tools` `mcp` `plugins` `scripts` `bin` |
| `entryFiles` | 后端 `server-rs/src` 顶层 `.rs` 根文件的代际：`generation` / `responsibility` | **3 个**：`config.rs`(entry)、`lib.rs`(entry)、`main.rs`(entry) |
| `frontend` | 前端每个顶层目录的代际：`generation` / `responsibility` | **8 个目录**：`api` `contracts` `mvu` `utils`(L1)、`stores` `composables`(L2)、`components` `sandbox`(L3) |
| `frontendEntryFiles` | 前端 `web/src` 顶层 `.ts`/`.vue` 根文件的代际：`generation` / `responsibility` | **30 个根文件**（`entry` 4 / `L1` 18 / `L2` 4 / `L3` 4） |
| `dependencyRules` | 允许方向集：`backendAllowed`(5) / `frontendAllowed`(6) / `allowed`(5) / `forbidden`(4) + `$backendModel` / `$frontendModel` 两段模型说明 | 见下表 |
| `registeredEdges` | 后端已登记越代债务：`from` / `to` / `class` / `reason` / `remediation` | **3 条** |
| `frontendRegisteredEdges` | 前端已登记越代债务：同上字段结构 | **2 条** |

### 方向集原文（`dependencyRules`）

| 键 | 值 |
|---|---|
| `backendAllowed` | `L1->L1`、`L2->L1`、`L2->L2`、`L3->L1`、`L3->L3` |
| `frontendAllowed` | `L1->L1`、`L2->L1`、`L2->L2`、`L3->L1`、**`L3->L2`**、`L3->L3` |
| `allowed` | `L1->L1`、`L2->L1`、`L2->L2`、`L3->L1`、`L3->L3` |
| `forbidden` | `L1->L2`、`L1->L3`、`L2->L3`、`L3->L2` |

> `$backendModel`（原文）：隔离模型：L3(工具/沙箱)与 L2(编排)互斥——青层反向依赖骨干层会使「隔离」名存实亡，故 `L2->L3` 与 `L3->L2` 双向禁止。
> `$frontendModel`（原文）：层次模型：`L3(展示)-> L2(状态编排)-> L1(纯契约)` 是严格向下的正常依赖(组件读 store、store 调 composable)，故额外允许 `L3->L2`；真正禁止的是 `L2->L3`(编排依赖 UI/沙箱)与 `L1->L2/L3`(纯逻辑依赖状态)。
> `allowed` 键是历史遗留的「单一方向集」，规则实现里取值为 `backendAllowed ?? allowed`、`frontendAllowed ?? [...allowed, 'L3->L2']`——即**消费方优先读分开的两份**，`allowed` 仅在分开键缺失时兜底。

### 后端模块代际归属（JSON 实测原文摘要）

| 模块 | `generation` | `responsibility`（原文） |
|---|---|---|
| `parsing` | L1 | 角色卡 V2/V3、世界书、宏/预设/正则脚本、mvu 协议与 EJS 解释器的兼容解析 |
| `models` | L1 | SQLite 表结构与全部数据契约类型 + 工具策略领域词汇(`tool_policy`) |
| `contracts` | L1 | 万花筒契约 DSL：定义、注册表、操作与求值语义 |
| `migration` | L1 | 数据迁移：快照备份、双库合并、幂等 DDL 升级、冲突处理 |
| `utils` | L1 | 基础工具：结构化日志(tracing)、原子文件写入、截断重试纯函数 |
| `agents` | L2 | Agent 引擎编排：run 流程、消息构建、工具循环、planner/reflector/状态机 |
| `services` | L2 | 业务服务：角色/会话/世界书/注入/设置/密钥/任务引擎/undo |
| `api` | L2 | HTTP 路由层、鉴权、SSE 流、请求/响应契约 |
| `connectors` | L2 | LLM 后端适配(mock / openai 兼容协议的请求与流式解析) |
| `slash` | L2 | slash 命令系统：注册表 + 内置命令 + 参数解析；经 API 与脚本桥两条链路暴露 |
| `tools` | **L2**（2026-09-14 由 L3 修正） | 工具系统：注册表、内置工具集(读取/写入/搜索/agent 域)、授权裁决与命令分级 |
| `mcp` | L3 | MCP stdio 客户端：JSON-RPC 2.0 托管子进程，工具以 `mcp_{server}_{tool}` 前缀注册 |
| `plugins` | L3 | 工具插件加载器：`data/plugins/tools/*.json` 定义的受控指令脚本工具 |
| `scripts` | L3 | 脚本系统：rquickjs 运行时沙箱 + TavernHelper 兼容桥 + 脚本树装载 |
| `bin` | entry | `kedai-server` 命令行入口 |

`notes` 字段里与本契约直接相关的三条（原文摘要）：
- `models`：「`types.rs` 混装传输类型(`LlmMessage`/`SseEvent` 等)为已登记知情接受项，已由规则 D 豁免；拆分只改 import 不改线格式。`tool_policy.rs` 于 2026-09-14 承接自 `tools/` 下沉的 `AuthorizationMode`/`ToolRisk`/`CommandRisk`(消除 `services→tools` 的 2 条越代边)」。
- `tools`：「**2026-09-14 归属修正：L3 → L2(名义 L3、实测 L2)**……故原登记的 5 条 tools 相关 `debt` 属**归属画错**，不是需要偿还的债——已全部删除」。
- `agents` / `services`：「`engine/mod.rs` 依赖 9 个 services，同时 `services/task_engine` 反向依赖 `agents::engine`——L2 内部双向」「`task_engine` 已对 `task_service` 断环(规则 C)；但整体仍与 `agents`、`tools` 双向」——**L2 内部的双向不算越代，不登记为债务**。

### 前端归属（JSON 实测原文）

**目录级（`frontend`，8 个）**：

| 目录 | `generation` | `responsibility`（原文） |
|---|---|---|
| `api` | L1 | 后端接口契约封装与类型定义(线格式的单一来源) |
| `contracts` | L1 | 契约差异计算等纯函数 |
| `mvu` | L1 | mvu 变量树协议解析/回放(与 Rust 侧同读 docs/fixtures 对拍) |
| `utils` | L1 | 纯工具函数 |
| `stores` | L2 | Pinia 状态编排：唯一的状态变更入口(action) |
| `composables` | L2 | 组合式编排逻辑(可复用业务流程) |
| `components` | L3 | UI 组件(展示层；不承载业务规则) |
| `sandbox` | L3 | 角色卡脚本沙箱宿主(iframe 生命周期/协议/净化) |

**根文件级（`frontendEntryFiles`，30 个）**：

| 代际 | 根文件 |
|---|---|
| `entry`（4） | `App.vue`（根组件与弹窗装配）、`main.ts`（应用引导）、`modals.ts`（弹窗注册表，单一事实源，check-arch 白名单由此派生）、`store.ts`（store 门面聚合） |
| `L1`（18） | `cssSanitize.ts` `markdown.ts` `platform.ts` `render.ts` `renderHints.ts` `renderPanel.ts` `sseReducer.ts` `taskResult.ts` `taskStatus.ts` `contextStats.ts` `devTools.ts` `exportFile.ts` `lastPosition.ts` `renderHtmlPreference.ts` `scriptAuthorization.ts` `scriptTreeIO.ts` `plugins.ts` `cardScripts.ts` |
| `L2`（4） | `asyncModal.ts`（弹窗懒加载包装）、`audioController.ts`（音频控制器注册表；宿主能力桥）、`chatStreamService.ts`（聊天流服务编排）、`resourceStore.ts`（资源界面存储快照桥） |
| `L3`（4） | `cardScriptHost.ts`（卡级脚本长驻沙箱宿主）、`characterScriptSandbox.ts`（角色卡脚本沙箱门面）、`chatMessageScriptScheduler.ts`（消息脚本执行调度）、`scriptRunner.ts`（消息脚本受控执行器，授权硬门） |

其中 `cardScripts.ts` 的 `responsibility` 原文点明其属契约层而非沙箱层：「角色卡卡级脚本收集与规范化(纯数据函数：递归拉平扩展形态 + `cardScriptCanonical` 哈希规范；与后端脚本授权门共享哈希契约，故属契约层而非沙箱层)」——与 §6.1 晋升台账第一行（L3 → L1）互为事实源与叙述。

### 已登记越代债务（实测原文）

以下 5 条为当前 `../tools/check-arch.mjs` 实测输出的**待偿还债务**原文（后端 3 + 前端 2），
均已登记在 `arch-layers.json`，**只出 note 不阻塞**；还清一条须删除一条对应条目。

```
[债务] 后端越代依赖 api(L2) → mcp(L3) 例:server-rs\src\api\app_state.rs:4(已登记于 arch-layers.json;还清后请删除该条目)
[债务] 后端越代依赖 api(L2) → plugins(L3) 例:server-rs\src\api\plugins.rs:8(已登记于 arch-layers.json;还清后请删除该条目)
[债务] 后端越代依赖 scripts(L3) → slash(L2) 例:server-rs\src\scripts\bridge.rs:11(已登记于 arch-layers.json;还清后请删除该条目)
[债务] 前端越代依赖 composables(L2) → components(L3) 例:composables/useMemoryPanel.ts(已登记于 arch-layers.json;还清后请删除该条目)
[债务] 前端越代依赖 stores(L2) → characterScriptSandbox(L3) 例:stores/chat.ts(已登记于 arch-layers.json;还清后请删除该条目)
```

登记表原文（`from → to` / `class` / `reason` / `remediation`）：

| `from → to` | `class` | `reason`（原文） | `remediation`（原文） |
|---|---|---|---|
| `scripts → slash` | `wiring` | 「`TavernHelper.triggerSlash` 兼容桥需要 `slash` 注册表。属兼容桥的显式接线，语义上是「外部协议 → 内部命令」的翻译，登记为可接受的装配依赖。」 | 「如需彻底解耦，可改为由宿主注册 slash 执行回调」 |
| `api → mcp` | `wiring` | 「`app_state` 持有 MCP 客户端句柄以支持启动装配与关闭。属组合根装配。」 | 「无(装配点)」 |
| `api → plugins` | `wiring` | 「插件管理 API 路由直接暴露插件解析器(列出/重载/导入)，注册动作由本层执行。属 HTTP 面暴露青层能力 + 组合根装配。」 | 「无(暴露面)」 |
| `composables → components` | `debt` | 「唯一回边：`composables/useMemoryPanel.ts` 从 `components/memoryPanel` 导入 `pruneSuccessText`/`nextPendingPrune`(值导入，非纯类型)。`components/` 是 L3 展示层，不应向 L2 提供业务函数。」 | 「把 `memoryPanel` 的纯逻辑函数从 `components/` 移入 `composables/` 或 `utils/`，组件只保留渲染」 |
| `stores → characterScriptSandbox` | `debt` | 「`stores/chat.ts:11` 从 `characterScriptSandbox`(L3 沙箱)导入 `broadcastCardEvent`/`broadcastMvuUpdate`——L2 状态编排反向依赖 L3 沙箱宿主。2026-09-14 修好门禁分桶与根文件代际后首次暴露(此前因 `characterScriptSandbox.ts` 未登记代际而被静默跳过)。语义上是「状态变更后通知沙箱」，属事件通知而非能力调用。」 | 「反转依赖：把事件广播改为 store 侧 emit 到一个 L2/L1 事件总线，由沙箱宿主(`cardScriptHost`/`scriptRunner`)订阅，store 不再 import 沙箱；或把两个纯广播函数下沉到 L1(它们只是事件发射，无沙箱实现依赖)」 |

> **读法提醒**：`wiring` 是「设计上接受的正常接线」（不还），`debt` 是「应还的债」（须按 `remediation` 收口）。
> 两条 `debt` 都在前端；后端 3 条全是 `wiring`。

### 规则 I / J 的执行语义（`../tools/check-arch.mjs` 实现）

规则 I 与 J 的**单一事实源**都是 `../tools/arch-layers.json`（该文件读取失败即记一条 FAIL：「该文件是分层的单一事实源，缺失即护栏失效」）。为什么需要这两条规则：`ARCHITECTURE-3H.md` 定义了「老中青三结合」分层（L1 稳 / L2 干 / L3 活），但 2026-09-14 全仓实测发现两处护栏盲区——① 5 个顶层模块（`migration`/`scripts`/`slash`/`utils`/`plugins`）从未被列入任何代际清单，分层叙事对它们完全失效；② 存在 10 条真实越代依赖边（`tools` 名义 L3、实为被 L2 依赖的核心设施），此前无任何机器约束。

**规则 I —— 归属完整性**（把「每个模块都有明确代际」变成硬约束）

- **判据是「未登记即 FAIL」**，不问理由、不静默放过：
  - 遍历 `server-rs/src/` 下每个**目录** → 必须在 `backend` 中有键，否则 FAIL「后端模块未登记代际……（新增模块必须先声明 L1/L2/L3 归属与职责，否则无从按代际纪律评审）」；
  - 遍历 `server-rs/src/` 下每个 **`.rs` 根文件** → 必须在 `entryFiles` 中有键，否则 FAIL「后端根文件未登记」；
  - 遍历 `web/src/` 下每个**目录** → 必须在 `frontend` 中有键，否则 FAIL「前端目录未登记代际」；
  - 遍历 `web/src/` 下每个**非测试 `.ts`/`.vue` 根文件** → 必须在 `frontendEntryFiles` 中有键，否则 FAIL「前端根文件未登记：根文件同样要声明代际:它既是规则的被依赖方,也是依赖的发起方」。
- 统计项在 `VERBOSE` 下打印「`[I]` 代际归属完整性:未登记 N 项」。
- **根文件为何必须登记**（实现注释原文要点）：`web/src` 根下约 30 个模块此前既未登记、也不参与方向校验，造成 `components→render`、`stores→sseReducer` 这类边整体被跳过（实测 69 条跨目录边中 49 条因一端无代际而静默略过）。根文件既可能是被依赖方，也可能是依赖发起方，故两侧都要按代际校验。

**规则 J —— 跨代依赖方向**（把方向变成 ratchet 门禁）

- **出边采集（后端）**：遍历 `server-rs/src/**/*.rs`，按行匹配 `^\s*(?:pub(?:\([^)]*\))?\s+)?use\s+crate::([a-z_]+)`；`entry` 作为发起方跳过；自指（`to === from`）与指向 `entry` 的边跳过；任一端口未登记代际则跳过（**由规则 I 单独报错，此处不重复报**）。采集前用 `productionSource()` 剔除 `#[cfg(test)]` 及其后——「测试里的跨模块引用不是架构依赖」。
- **出边采集（前端）**：遍历 `web/src/**/*.ts|vue`（跳过测试文件），用 `from '…'` 匹配；`@/x` 取首段，`./x` 解析为同层根文件（`../x` 等上跳路径不视作同层依赖）。模块代际用 `feModuleGen()` 双侧解析：**先查目录（`frontend`），再查根文件（`frontendEntryFiles`，键带扩展名）**。
- **判定**：`ga === gb`（同代内）或方向在允许集中 → 跳过；否则计入跨代边。
  - 边**已在登记表**（`registeredEdges` / `frontendRegisteredEdges`）→ 出 `note`「`[债务]` ……（已登记于 `arch-layers.json`;还清后请删除该条目）」，**不阻塞**。设计取舍原文：「`registeredEdges` 登记的是**已核实的存量结构债务**，只报债务不阻塞（与 `KNOWN_CYCLES` / `BASELINE_BARE_ERROR` 同模式）——若不做登记，门禁将永久红而失去信号价值；一旦登记，**任何新增**未登记越代边立即 FAIL。还债即删条目，禁止为过检查而加条目。」
  - 边**未登记** → **FAIL**，文案含方向与样例位置，并要求「确属本轮无法消除，须在 `arch-layers.json` 的 `registeredEdges` 登记理由与收口路径」。
- **登记陈旧检测（防止登记表变成历史沉积）**：遍历登记表，若某条 `from->to` 实测已无该依赖 → 出 `note`「`[登记陈旧]` ……请删除该条目」。
- **组合根豁免**：`entry` 代际的模块**不参与方向校验**（「组合根被允许依赖一切……它的职责就是把各层拼装起来」）。差异在两端：
  - 后端 `entry`（`lib.rs`/`config.rs`/`main.rs`）**只作依赖发起方**时豁免；
  - 前端 `entry` 作为发起方与被依赖方**都豁免**——`store.ts`（门面聚合 7 个 store）与 `modals.ts`（弹窗注册表）是「**被广泛引用的公共装配面**」，组件/composable 经门面访问状态正是该架构的核心模式，若禁止则门面不可用；登记仍是硬要求（由规则 I 负责）。这**不是**为过检查而放宽：若禁止 `L3->L2`，几乎每个组件都会「违规」，护栏会因噪声过载而失去信号价值。
- **失败分桶与退出纪律（2026-09-14 修复）**：后端违规写 `backendFailures`、前端违规写 `frontendFailures`；汇总段**两桶都必须先打印再退出**，任一侧非空即 `exit(1)`。修复背景：此前规则 I 的前端分支写进 `backendFailures`，而汇总在后端违规时立即 `exit(1)`，使前端规则 J 的结论被静默吞掉（偏差登记 §7.1 第 8 项）。

**当前实测（2026-09-16 运行 `node ../tools/check-arch.mjs`）**：

```
========== Kedai 后端分层与冻结护栏(C/D/E/G/H/I/J/K) ==========
[ OK ] 后端分层无违规

========== Kedai 前端架构护栏(A/B/I/J) ==========
[ OK ] 前端架构无违规
store 循环依赖:0 个(新增 0)
组件直改 state:44 处(白名单 44)

待偿还债务 5 条:（即上文 5 条已登记越代边）
[ OK ] 无新增架构违规
```

---

## 数据库 schema 与降级契约

> 本节为 `archive/2026-09-16-consolidation/数据库版本与降级行为.md` 的**全文移植**。
>
> **用途**：写清 Kedai 的 `kedai.db` 的 schema 版本机制，以及由此推导出的
> 「旧库→新版」「新版库→旧版」「双端/多入口共用库」「双库合并」四类场景的实际行为与应对。
> 对齐 `archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md` §六 6.2：
> 本节是该项目的**文档部分**。
>
> **⚠️ 状态更新(2026-09-15)**：本节 §一 / §三 记录的是「**无 `user_version`**」时期的现状与风险，
> 已由批次 3 落地改变——`SCHEMA_VERSION = 1` 已引入，`Db::open` 会在建表批前读版本、
> 库比代码新时**拒绝启动**，并用事务包裹「建表批 + 9 个 `ensure_*`」。
> **§一 / §三 请按各节开头的「2026-09-15 已变更」标注对照阅读**（原文保留以记录演进过程）；
> **§四是该方案的设计稿，已完整兑现，现作为历史索引保留**。
> 落地细节与验收证据见 `archive/2026-09-16-consolidation/工程韧性修复-变更说明.md` §2.3，
> 缺口登记状态见 `archive/2026-09-16-consolidation/known-limitations.md` L18 / L21（均已修复）。
>
> **体例说明（2026-09-16 整合批注）**：本节有意**逐节保留「原文 + 2026-09-15 变更标注」的双层写法**——第一层是引入版本号之前的原始描述（历史溯源依据），第二层是各节开头的「2026-09-15 已变更」标注（现状）。**这是该文档的既定体例，不得为了「读起来整齐」而合并或删除任一层**。
>
> **约定**：凡标注「推断」的结论来自代码与 SQLite 语义的推导，未在本机实跑复现；改动相关
> 行为前应先按 §四的验收断言做实跑验证。`文件:行号` 引文以本节落盘时的代码为准
> (2026-09-15 之后的行号可能已随批次 3 的改动漂移)。

### 一、现状

> **2026-09-15 已变更**：本节的 1.1 / 1.3 / 1.4 描述的是**引入版本号之前**的现状。
> 新增的现实：`models/db/schema.rs` 现有 `pub const SCHEMA_VERSION: i32 = 1`（0 留给
> 「从未标记」的旧库）；`Db::open` 在建表批**之前**读 `PRAGMA user_version`，库版本高于
> 应用即 `Err`；建表批 + 9 个 `ensure_*` 已用 `BEGIN IMMEDIATE … COMMIT` 包裹，
> 失败统一 `ROLLBACK`（`ensure_memory_entries_fts_backfill` 与 `backfill_scope_variables`
> 仍留在提交之后）；全部成功后才**最后**写 `user_version`。
> 故 1.1 的「代码零引用 `user_version`」与 1.4 的「升级链不是单一事务」**均已不成立**。

#### 1.1 没有 schema 版本号,也没有迁移台账(**2026-09-15 起前半句已不成立**)

- **2026-09-15 前**：全仓搜索 `user_version` 只命中残余计划 6.2 一处，**代码零引用**：数据库文件的
  `PRAGMA user_version` 始终是 SQLite 默认值 `0`。
  **2026-09-15 起**：已引入 `SCHEMA_VERSION`（见本节开头标注），`user_version` 由 `Db::open` 读写。
- 没有版本记录表(如 `schema_migrations` / `migration_history`)。`backfill_meta`
  (`server-rs/src/models/db/schema.rs:366`)不是迁移台账：它只存两件事——FTS 回填标记
  `memory_fts_rebuilt` 与 message 作用域回填游标 `message_scope_last_id`
  (`server-rs/src/migration/ddl.rs:314`、`server-rs/src/models/db/backfill.rs:19-24`)。
  **这一点至今未变**（本轮只加了单值的 `user_version`，未建台账表）。
- **2026-09-15 前**：因此**无法在启动时判断「这个库是哪个版本建的/被哪个版本写过」**，也无法判断
  「库比当前代码新还是旧」。
  **2026-09-15 起**：以单值 `user_version` 与 `SCHEMA_VERSION` 比较即可判「库比代码新」并拒绝启动。

#### 1.2 升级靠幂等 DDL(schema 只增不改)

升级不靠版本比较,而靠「缺什么补什么」的幂等 DDL,分两层:

1. **全量建表**：`Db::open` 每轮启动都执行 `CREATE TABLE IF NOT EXISTS` 建表批
   (`server-rs/src/models/db/mod.rs:62`，SQL 常量在 `schema.rs` 的 `CREATE_TABLES`，
   共 30 张表 + FTS 虚拟表 + 索引)。已存在的表原样跳过，不比对、不改写。
2. **增量 `ensure_*`**：对无法用 `CREATE TABLE IF NOT EXISTS` 触达的「已有表补列 / 补表 /
   补索引」，逐个函数探测后补齐(`server-rs/src/migration/ddl.rs`)：
   - 补列：`PRAGMA table_info(<表>)` 读现存列名 → 缺失才 `ALTER TABLE ... ADD COLUMN`
     (`ensure_llm_requests_usage_columns:69`、`ensure_skills_progressive_columns:107`、
     `ensure_tasks_task_mode_column:135`、`ensure_task_llm_calls_finish_reason_column:171`、
     `ensure_memory_entries_pinned_column:286`)。列一律追加在**表尾**，且带常量默认值
     (如 `task_mode TEXT NOT NULL DEFAULT 'legacy'`)，旧行零迁移成本。
   - 补表：`PRAGMA table_info` 探测不到列(表不存在返回零行)才建
     (`ensure_task_messages_table:358`、`ensure_exec_audit_table:406`，DDL 全 `IF NOT EXISTS`)。
   - 补索引：`CREATE INDEX IF NOT EXISTS`(`ensure_perf_indexes:425`)。
   - 一次性回填：`ensure_memory_entries_fts_backfill:313` 用 `backfill_meta` 标记保证
     `rebuild` 只跑一次(全表重建代价高,不进热启动路径)。

   **不变式(跨库合并依赖)**：这些补列/补表/补索引的文本必须与 `schema.rs` 的
   `CREATE_TABLES` 在 `normalize_sql` 后逐字节一致，否则跨库合并的 schema 比对会判冲突
   (`server-rs/src/migration/merge.rs:264`)。新增/改动索引时 `ddl.rs` 与 `schema.rs`
   两处都要改。

#### 1.3 执行时机与顺序

全部发生在 `Db::open` 内部(`server-rs/src/models/db/mod.rs:49`)，即**进程装配期、监听端口之前**：

| 步骤 | 内容 | 位置 |
|---|---|---|
| 1 | 注册 sqlite-vec 扩展、建数据目录、打开写连接 | `mod.rs:51-55` |
| 2 | `journal_mode=WAL`、`foreign_keys=ON`、`busy_timeout=5s` | `mod.rs:56-61` |
| 3 | `CREATE TABLE IF NOT EXISTS` 全量建表批 | `mod.rs:62` |
| 4 | 8 个 `ensure_*` 按固定顺序补列/补表/补索引 | `mod.rs:65-87` |
| 5 | FTS 一次性回填 + scope_variables 幂等回填 | `mod.rs:89-91` |
| 6 | 建只读连接池 | `mod.rs:93-109` |

顺序为：`llm_requests usage 列 → skills 渐进披露列 → tasks.task_mode →
task_llm_calls.finish_reason → task_messages 表 → memory_entries.pinned →
exec_audit 表 → 性能索引 → FTS 回填 → scope_variables 回填`。

> **口径提示（2026-09-16 批注）**：上表第 4 步的「8 个 `ensure_*`」是引入版本号之前的实测值；2026-09-15 起事务包裹的是「建表批 + **9** 个 `ensure_*`」。两处数字并存是「原文 + 变更标注」体例的直接体现，**不是笔误，不要统一**。

#### 1.4 失败处理:启动失败即退出,不留半可用状态

- 任何一步返回 `Err`，`Db::open` 即返回 `Err`；`AppState::new` 用 `?` 上抛
  (`server-rs/src/api/app_state.rs:113-116`)；`run_server` 再包成
  `初始化失败: <细节>`(`server-rs/src/lib.rs:99-102`)。
- 命令行版：打印 `[错误] ...` 后 `std::process::exit(1)`(`server-rs/src/main.rs:19-22`)。
- 桌面版：启动阶段收到后端 Err 后弹错误并退出，错误文本会落到
  `%APPDATA%\com.kedai.app\logs\tauri-start-error.log`(`src-tauri/src/lib.rs:339-380`、
  `:739-740` 的错误提示文案)。
- **升级链不是单一事务(2026-09-15 起已不成立)**：原 `Db::open` 没有用事务把整条链包起来，
  建表批与各 `ensure_*` 的 DDL 各自提交。**2026-09-15 起**：建表批 + 9 个 `ensure_*` 已用
  `BEGIN IMMEDIATE … COMMIT` 包裹，任一步失败统一 `ROLLBACK`，**不会**留下「部分升级」的
  中间态；`ensure_memory_entries_fts_backfill` 与 `backfill_scope_variables` 仍留在提交之后
  (二者靠 `backfill_meta` 幂等，不进长事务以避免膨胀 WAL)。
  回滚断言见 `tests/schema_migration_meta.rs` 的
  `failed_ensure_rolls_back_whole_upgrade_chain_and_keeps_version`。

#### 1.5 元测试与冻结基线的护栏作用

- `server-rs/tests/schema_migration_meta.rs` 的做法：用冻结基线
  `tests/fixtures/schema_baseline_v0_3_0_beta.sql` 建一个「老库」→ 跑 `Db::open`
  (真实启动路径，执行全部 `ensure_*`)→ 另建全新库 → **逐表比对结构指纹**
  (`PRAGMA table_info` 的列名/类型/NOT NULL/默认值/主键 + `PRAGMA index_list` 的索引清单)，
  两侧必须完全一致(`schema_migration_meta.rs:78-125`)。
- 作用：防「`schema.rs` 加了列/表/索引却忘了写 `ensure_*`」——这种 bug 只在老用户机器上
  以 `no such column` 炸，新装机与其它全量测试都发现不了(踩坑记录见
  `../MAINTENANCE.md:458-461`)。
- **纪律：不要更新基线文件**。它是 0.3.0-beta 发布时的 `CREATE_TABLES` 历史快照
  (`schema_baseline_v0_3_0_beta.sql:1-4`)，更新它等于关掉守卫。
- **覆盖边界(重要,别高估)**：基线快照已经包含当前绝大部分表/列(实测：基线 30 张表
  与当前一致，仅差 `idx_sessions_character`/`idx_tasks_character`/`idx_tasks_status`
  三个性能索引)。因此**从基线建出的「老库」升级后能与新库一致，主要靠 `ensure_perf_indexes`**；
  其余 `ensure_*` 对应的列/表在基线里已存在，该测试**当前并没有实际锻炼到它们**——
  它们的价值是拦住**基线之后新增**的结构。此外基线只有 0.3.0-beta 一个版本，
  **早于 0.3.0-beta 的库没有 fixture 覆盖**，只能靠代码上的 `ensure_*` 兜底。

#### 1.6 「三处迁移逻辑」里只有一处是 schema 迁移

残余计划 §五登记的「三处迁移并存」指的是下面三种，职责不同：**只有第 3 种动 schema**。

| # | 位置 | 做什么 | 是否动 schema |
|---|---|---|---|
| 1 | `launcher/src/main.rs:251`(启动器，`main:708` 调用) | 命令行版 `<root>\data` → 桌面版 `%APPDATA%\com.kedai.app\data` 的**整目录搬迁**；交互确认、目标非空则不动、覆盖前备份、先清陈旧 `-wal`/`-shm`；头像绝对路径借系统 `sqlite3` 改写(无 `sqlite3` 则如实报告不修) | 否(只搬运文件) |
| 2 | `src-tauri/src/lib.rs:474`(桌面壳，`#[cfg(desktop)]`) | 项目 `data` → `%APPDATA%` 的**首启搬迁**，仅在目标 pristine 时执行；用 SQLite Online Backup API 生成一致性快照(`snapshot_database`，`server-rs/src/migration/backup.rs:7`)，写 `.migration-v1` 标记，源目录保留 | 否(整库快照) |
| 3 | `server-rs/src/migration/`(`ddl.rs`/`merge.rs`/`backup.rs`/`conflict.rs`) | **schema 幂等升级**(§1.2)、跨库合并(§2.5)、快照备份 | 是 |

- 三者**不会互相冲突**：1/2 各自有「目标非空则跳过」的门槛(启动器 `looks_pristine`，
  `launcher/src/main.rs:368-370`；桌面壳 `target_is_pristine`，`src-tauri/src/lib.rs:510-527`)，
  且搬迁的是整库快照/文件，不解析也不改写表结构。收敛方向见残余计划 §五(收敛为 server-rs
  单一实现，注意 launcher 零依赖约束)。
- 副作用：1/2 留下的是**两份数据**(源目录不删 + 目标目录新拷贝)，这正是 §三「双库分叉」
  的成因。

### 二、行为矩阵

先给结论速查,再逐场景展开。

| 场景 | 会发生什么 | 用户/开发者该怎么办 |
|---|---|---|
| 旧库 → 新版(升) | 启动时幂等补齐；当前全部为「只增」变更，预期无痛 | 直接升级;**2026-09-16 起升级前自动留档**（见 §2.1），手工备份不再是必需 |
| 新版库 → 旧版(降) | 无版本门禁，旧版**静默打开**；多余表/列被忽略，预期能跑 | 无官方回滚路径；要回退用升级前自动快照或手工备份 |
| 桌面 + Android 双端 | 两个**物理隔离**的库，互不可见 | 用导入/导出或 `kedai-data-merge` 手工合并 |
| 测试版 + 便携版 | 同一 `%APPDATA%` 库 + 同一端口 | 勿同时运行；二者抢库会互相阻塞 |
| 项目 `data\` + `%APPDATA%` | 可能长期并存两份库(双库分叉) | 用合并工具；别再手动各写一边 |
| 双库合并 | CLI 工具；schema 冲突/缺表时**停止并保留两份原数据** | 先让两端都升到同版本再合 |

> **速查表口径（2026-09-16 批注）**：上表「新版库 → 旧版(降)」行的「无版本门禁」是引入 `user_version` **之前**的实测行为；2026-09-15 起已有版本门禁（§2.2「风险管理」段即为此设计）。原文保留，现状以 §2.2 变更标注与 §三 风险 1 为准。

#### 2.1 旧库 → 新版(升级)

- **会发生什么**：`Db::open` 跑建表批 + `ensure_*`，缺的列/表/索引补齐(§1.2)；
  schema 变化至今全部是**追加**(新增表、表尾加列、加索引)，没有重命名或删除列。
  补列带常量默认值，旧数据行的新列取默认值(如 `tasks.task_mode='legacy'`、
  `task_llm_calls.finish_reason=''`、`memory_entries.pinned=0`)。
- **失败时**：应用启动即失败退出(§1.4)，不会带着半残 schema 继续服务。
- **升级前自动备份（2026-09-16 新增，DB-2 封堵）**：`Db::open` 在**建表批之前**判定
  「本次是否为既有库的升级」，若是则先留一份快照再动 schema。落点与命名：
  `<data_dir>/backups/kedai-preupgrade-v<旧版本>-<YYYYMMDD-HHMMSS>.db`，保留最近 **3** 份。
  - **判据（易写错，实现须照此）**：`user_version >= SCHEMA_VERSION` → 不备份（已最新）；
    `> 0` → 备份；**`== 0` 时再看库里有无业务表**——无表 = 全新库（不备份）、
    有表 = 未标记的旧库（备份）。只判 `> 0` 会漏掉最后一档，而那正是真实用户的升级主路径。
  - 命名带时间戳是**必需**的：底层 `snapshot_database` 会先删同名目标文件，固定名无法留多份历史。
  - **失败不阻断启动**：快照失败只记 warn 并继续升级（备份是「多一份保险」，不能因它失败
    反而让库打不开）。
- **该怎么办**：
  1. 通常无需手工操作——升级前快照已自动生成于 `<data_dir>/backups/`（保留 3 份）；
     如需长期留存某次升级前的状态，请自行复制该文件。
  2. 升级失败看启动错误文本与 `tauri-start-error.log`；修掉原因后重启即可续补(幂等)。
- **推断**：`scope_variables` 回填是增量的、FTS `rebuild` 只跑一次，大库升级耗时主要在
  首次 FTS rebuild；重复启动不会重复付这个代价。

#### 2.2 新版库 → 旧版(降级)

- **会发生什么**：因无 `user_version` 门禁，旧版 exe **不会察觉库更新，直接打开**。
  - 多余的表：`CREATE TABLE IF NOT EXISTS` 原样跳过，旧代码不引用即无影响。
  - 多余的列：旧代码的查询多按显式列名取列，不认识的列不参与；旧代码 `INSERT` 不写这些列时
    走常量默认值。**推断**：因此能正常读写。
  - 缺列：当前 schema 只增不减，**旧代码需要的列在新库都存在**，不存在「缺列」问题；
    真正会断的是未来引入「重命名列/删除列/改类型或约束」之后——那时旧版会在运行时
    报 `no such column` 或读到语义错位的数据。
  - 新增 trigger(如 `memory_entries_*` FTS 触发器)：SQLite 自动执行，旧代码不感知，
    但若**旧版本对相关表的结构假设与新 trigger 不兼容**，属未验证区(推断：现阶段无不兼容)。
- **该怎么办**：项目**没有官方降级/回滚路径**。要回退到旧版，只能：
  1. 关闭应用；
  2. 用**升级前自动快照**(`<data_dir>/backups/kedai-preupgrade-v*.db`，2026-09-16 起自动生成)
     或自己的手工备份覆盖 `kedai.db`；
  3. 再启动旧版。
  若已用新版写入过数据，这些数据在回退后丢失。**不要假设「装回旧版 exe 就能无损降级」。**
  注意自动快照只保留最近 3 份，且**升级成功时才会写下**——它只覆盖「升级前」这一个时点，
  不是随时的备份手段。
- **风险管理**：§四的 `user_version` 检查就是为了让这条「静默打开」变成**显式提示**。**2026-09-15 已落地**：该检查已在批次 3 生效（库比代码新即拒绝启动，见 §四状态说明与 §三 风险 1）。

#### 2.3 桌面端 + Android 端

- **会发生什么**：两个**物理隔离、各自独立**的库，不存在共享：
  - Windows 桌面(便携版/`Kedai.exe` 桌面壳，以及被其复用数据目录的测试版)：
    `app_data_dir()` = `%APPDATA%\com.kedai.app`，再 `.join("data")`
    (`src-tauri/src/lib.rs:176`；identifier `com.kedai.app` 见 `src-tauri/tauri.conf.json:5`)，
    即 `%APPDATA%\com.kedai.app\data\kedai.db`。桌面壳启动时把该路径注入
    `DATA_DIR`(`src-tauri/src/lib.rs:188`)。
  - Android：`app_data_dir()` 映射到应用私有目录 `/data/data/<pkg>/files`
    (`src-tauri/src/lib.rs:170-171`，注释明示)，无 `%APPDATA%`，解析失败直接报错退出
    (`:74-80`)。
- **该怎么办**：两端数据不自动同步。需要在两端之间搬运时，走应用的会话/数据导入导出，
  或把一端的 `kedai.db` 取回后用 `kedai-data-merge` 合并(§2.5)。
- **注意**：Android 端 `Db::open` 的升级路径与桌面完全相同(同一 `kedai_server` crate)，
  所以「旧库→新版」的 §2.1 结论在 Android 上同样成立，只是失败日志位置不同。

#### 2.4 测试版与便携版共用同一库

- **会发生什么**：`start.ps1` 默认启动测试版(`dist\kedai-server.exe`，浏览器模式)，
  便携版是 `dist\Kedai-portable\Kedai.exe`(Tauri 桌面壳)。两者**共用端口 3001 与同一数据
  目录**(`../MAINTENANCE.md:220`，原文「两版共用端口 3001 与同一数据目录，勿同时运行」)。
- **数据目录解析(测试版为何也会落到 `%APPDATA%`)**：`AppConfig::from_env`
  的顺序是 `DATA_DIR` 环境变量 > 用户级统一目录 > 项目根 `data\`
  (`server-rs/src/config.rs:138-144`)。其中用户级目录
  `%APPDATA%\com.kedai.app\data` **仅在该目录已含用户数据时才被采用**
  (`canonical_user_data_dir`，`config.rs:73-83`；判据 `dir_has_user_data:85-105`：
  存在 `settings.json`，或库里有非内置角色/任何会话)。桌面版则**无条件**指向
  `%APPDATA%`(§2.3)。
- **后果**：桌面版用过的机器上，测试版随后启动会命中「已有用户数据」→ 也切到 `%APPDATA%`，
  两版即共用同一库，**同一时刻只能跑一个**(第二个实例要么被健康检查判定为「已在运行」
  而复用，要么抢端口/抢写锁)。
- **该怎么办**：开发迭代时明确「同时只跑一端」；切换版本前先关掉另一端的进程。

#### 2.5 项目 `data\` 与 `%APPDATA%` 双库分叉与合并

- **会发生什么(双库分叉)**：在桌面版从未用过的机器上先跑测试版，它会用项目根 `data\`
  建库；之后跑桌面版会把项目数据**拷贝**(非移动)到 `%APPDATA%`，源目录保留
  (§1.6)。于是磁盘上出现两份库。此后测试版若再启动，会因 `%APPDATA%` 已有用户数据而切过去，
  但项目 `data\` 里那份旧库仍在，可能与新库内容分叉。
- **手工合并工具**：`server-rs/src/bin/kedai-data-merge.rs`，用法
  `kedai-data-merge <基线数据目录> <源数据目录> <工作目录>`(`:9-12`)，
  入口是 `migration::merge_data_dirs`(`server-rs/src/migration/merge.rs:54`)。
  注意这是**离线 CLI**，需要显式传三个目录，**不删源数据**、产出写在工作目录。
- **合并时的 schema 行为**：合并前会对基线库与源快照**各补一遍 `ensure_*` + 新增表 DDL**
  (`merge.rs:177-251`)，把两侧拉到同款 schema 再比对，因此「旧库 vs 新库」合并通常不会
  因新增表/列失败。若仍出现：
  - `表 <T> 的 schema 冲突,已停止合并并保留两份原数据`(`merge.rs:264-267`)；
  - `基线缺少源表 <T>,已停止合并并保留两份原数据`(`merge.rs:269-270`)。

  都是**保守停止**，不会写坏任何一侧；解决方式是把两端都升级到**同一版本**再合。
- **该怎么办**：合并前对两份库各做一次备份；合并失败时按报错表名定位是「哪端版本旧」。

### 三、已知风险与不支持项

> **2026-09-15 已变更**：第 1 / 4 / 6 条已由批次 3 处理，下方逐条标注现状；
> 第 3 / 5 / 7 / 8 条**仍然成立**；第 1 条部分已变更，第 2 条已修复(2026-09-16)，第 4 条已变更，第 6 条已修复。

1. **无官方降级/回滚路径**(**部分已变更**)：仍没有 down 迁移(不 `DROP COLUMN`、不重建表)，
   降级仍依赖用户自己的备份(已登记为 `archive/2026-09-16-consolidation/known-limitations.md` L25「有意不做」)。
   **但「无『库版本 > 代码版本』检测」已不成立**：批次 3 起 `Db::open` 会在建表批前读取
   `user_version`，库比代码新时**拒绝启动**并给出两个版本号(把静默继续改为启动点显式失败)。
2. **升级前不自动备份**(**已修复，2026-09-16**)：原状为 `Db::open` 的升级链没有任何前置快照，
   唯一自动快照(`snapshot_database`)只服务于**跨目录搬迁**(§1.6 第 2 种)与**合并**。
   现状：`Db::open` 在建表批之前对**既有库**留一份快照到 `<data_dir>/backups/`
   (命名 `kedai-preupgrade-v<旧版本>-<时间戳>.db`，保留 3 份)，失败不阻断启动；
   判据与细节见 §2.1「升级前自动备份」。**down 迁移仍不做**(L25 有意不做)，
   本项即 L25 指定的首选补齐路径。
3. **降级正确性未被测试锁定**：§2.2 的「旧代码能跑」是推断，没有「新库 + 旧 exe」的
   回归测试(也不现实，旧 exe 不在仓库)。一旦引入重命名/删除列，降级会静默损坏。
4. **`user_version` 缺失导致无法诊断**(**已变更**)：原指排查「用户库怎么长这样」时无库内标记可读，
   只能靠结构指纹反推。**批次 3 起 `user_version` 已是可读标记**，`= SCHEMA_VERSION` 时
   即可判定「库由本代或更新的代码写过」。
5. **元测试只覆盖 0.3.0-beta 之后的漂移**(§1.5)：早于该版本的库无 fixture；
   且基线已含当前绝大多数结构，当前真正被该测试锻炼的是新增的三个性能索引。
6. **升级链非原子**(**已修复**)：原「失败可停在部分升级的中间态」已由 `BEGIN IMMEDIATE …
   COMMIT` 事务包裹消除；失败即整体回滚，`user_version` 保持旧值(否则下次启动会误判「已最新」)。
7. **双库/多库并存无自动检测**：§2.4、§2.5 的分叉只有一个**手动** CLI 合并工具，
   没有启动期「检测到另一套库」的提示(桌面壳只在「复用运行中实例」时校验数据目录，
   见 `src-tauri/src/lib.rs:359`，不覆盖多份库并存的情况)。
8. **schema 是事实上的版本契约,却只写在代码里**：每个 `ensure_*` 的默认值语义
   (如 `task_mode='legacy'` 的含义)散在注释中,没有集中清单。

> **逐条现状汇总（2026-09-16 整合批注）**：**仍然成立**的是 **2、3、5、7、8**；第 **1** 条部分变更（「无 down 迁移」成立，「无库版本检测」不成立）；第 **4、6** 条已由批次 3 处理完毕（第 6 条为已修复）。权威来源：`archive/2026-09-16-consolidation/工程韧性修复-变更说明.md` §2.3、`archive/2026-09-16-consolidation/known-limitations.md` L18/L21/L25。

### 四、已兑现方案(历史索引):引入 `PRAGMA user_version` 的最小方案

> **状态(2026-09-15)**：本节原为**设计稿**，现已在批次 3 **完整落地**，保留作为
> 「为什么这样设计」的历史索引。落地实况与验收证据见
> `archive/2026-09-16-consolidation/工程韧性修复-变更说明.md` §2.3。
> 与下方原文的差异只有一处：实际包进事务的是「建表批 + 9 个 `ensure_*`」，
> 而 `ensure_memory_entries_fts_backfill` 与 `backfill_scope_variables` **有意留在提交之后**
> (避免长写事务膨胀 WAL;二者靠 `backfill_meta` 幂等)。


#### 4.1 最小实现

1. **定义单一版本常量**：在 `server-rs/src/models/db/schema.rs`(或 `mod.rs`)加
   `pub const SCHEMA_VERSION: i32 = 1;`，并导出；`0` 保留给「未标记的旧库/未知」
   (与 SQLite 默认值一致，天然区分「从未写过版本」)。
2. **启动时读取并分流**(`Db::open`，在建表批之前)：
   - `db_version > SCHEMA_VERSION` → 库比代码新，进入 §4.2 的策略分支；
   - 否则(含 `0`)→ 照常跑建表批 + 全部 `ensure_*` + 回填(**保持每轮幂等执行**，
     不因版本相等而跳过；这样可自愈「部分升级」的中间态，见 §1.4)；
   - 全部成功后再写 `PRAGMA user_version = SCHEMA_VERSION`。
3. **写入时机**：必须在所有 `ensure_*` 与回填**成功之后**写；否则失败时版本被提前抬高，
   下次启动会误判为「已最新」而跳过补迁。
4. **合并路径对齐**：`merge_databases` 的两侧预补 DDL 之后，把工作库(及合并产物)的
   `user_version` 设为 `SCHEMA_VERSION`；不满足时按合并既有风格保守停止。
5. **不改 `CREATE_TABLES` 的静态文本**：`user_version` 是连接级 `PRAGMA`，不进建表 SQL，
   对 `normalize_sql` 比对与元测试零影响。

#### 4.2 「库版本 > 代码版本」时的显式提示策略(需主智能体决策)

三选一,推荐第 1 种(与现有「升级失败即退出、宁可明确报错」的纪律一致):

1. **拒绝启动 + 明确文案(推荐)**：`Db::open` 返回 `Err`，
   文案含「数据库由更新版本的 Kedai 写入(库 vX > 应用支持 vY);请升级应用,
   或从升级前备份恢复」。桌面版会弹错误并写 `tauri-start-error.log`；
   命令行版 `exit(1)`。优点：杜绝旧代码写坏新库;代价：同机降级会直接打不开。
2. **警告但继续**：仅 `tracing::warn!` + 控制台提示，照常启动。优点：不阻断；
   代价：旧代码仍可能写坏新库数据。
3. **拒绝启动 + 引导用合并/备份恢复**：在文案里再给出 `kedai-data-merge` 与备份路径提示。

> 推荐策略下要同步评估：测试版与便携版**共用库**场景，若一端是新版、另一端是旧版 exe，
> 旧端会直接打不开(比现在的静默打开更早暴露问题，符合预期)。

> **落地结果（2026-09-16 批注）**：实际采用**策略 1（拒绝启动 + 明确文案）**，已在批次 3 生效（见 §2.2「风险管理」段与 §三 风险 1）。

#### 4.3 验收断言建议

在 `server-rs/tests/schema_migration_meta.rs` 扩测，至少覆盖：

1. **全新库**：`Db::open` 后 `PRAGMA user_version == SCHEMA_VERSION`。
2. **旧库(冻结基线，`user_version=0`)**：`Db::open` 后 `user_version == SCHEMA_VERSION`，
   且结构指纹与全新库一致(现有断言保留)。
3. **伪造「更新的库」**：在临时库上 `PRAGMA user_version = SCHEMA_VERSION + 1` 后调
   `Db::open`，断言返回 `Err` 且错误文案含两个版本号(按 §4.2 选定策略调整)；
   若选策略 2 则断言「成功打开且输出了警告」(需可观测点，如返回的告警字段或日志钩子)。
4. **幂等**：连续两次 `Db::open`，第二次不改动 `user_version`、结构指纹不变。
5. **失败不抬版本**：构造一个会让某个 `ensure_*` 失败的库(如手工建一张同名列型不兼容的表)，
   断言 `Db::open` 失败后文件的 `user_version` 仍是旧值(证明 §4.1 第 3 条)。
6. **合并后对齐**：合并产物的 `user_version == SCHEMA_VERSION`。
7. **静态一致性**：沿用现有「`schema.rs` 与 `ddl.rs` 文本 normalize 后一致」的护栏，
   新增表/列时仍必须同步 `ensure_*`(`../MAINTENANCE.md:458-461` 纪律不放松)。

### 五、相关文档

- 幂等迁移与元测试纪律：`../MAINTENANCE.md:458-461`(踩坑 25)、
  `server-rs/tests/fixtures/schema_baseline_v0_3_0_beta.sql:1-4`。
- 数据目录解析与双库分叉：`server-rs/src/config.rs:73-105`、`../README.md`「目录结构」节。
- 跨库合并实现与保守语义：`server-rs/src/migration/merge.rs`、
  `server-rs/src/bin/kedai-data-merge.rs`。
- 残余计划与风险登记：`archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md` §五、§六 6.2；
  `archive/2026-09-16-consolidation/known-limitations.md`。

---

## 来源与溯源

| 本文内容 | 原文件 | 原文件现状 |
|---|---|---|
| 「三结合分层架构」全节（§0–§8，含 §6.1 晋升台账、§7.1/§7.2 偏差登记） | `docs/ARCHITECTURE-3H.md`（301 行，原为架构叙述权威） | 已归档至 `archive/2026-09-16-consolidation/ARCHITECTURE-3H.md`；**叙述权威身份移交本文** |
| 「数据库 schema 与降级契约」全节（一–五，含 8 条风险与「原文 + 变更标注」体例） | `docs/数据库版本与降级行为.md`（330 行） | 已归档至 `archive/2026-09-16-consolidation/数据库版本与降级行为.md` |
| 新增内置工具 6 处登记点（本文仅交叉引用，不重复正文） | `docs/威胁模型与残余风险.md` §四 | 已归档；**清单正文见 `契约-协议与配置.md` §新增内置工具登记清单** |
| 代际归属事实源、方向集、5 条已登记越代债务 | `tools/arch-layers.json` | **未迁移**，仍是 SSOT；本文向其、向代码实测对账 |
| 规则 I/J 执行语义与失败分桶纪律 | `tools/check-arch.mjs`（规则 I/J 段） | **未迁移**；本文只作叙述，判据以脚本实现为准 |

**引用写法规约（本文统一执行）**：

- 指向已迁移的 `docs/` 内文档 → `archive/2026-09-16-consolidation/<原文件名>`（相对 `docs/`）。
- 指向未迁移文件 → 保持原样：`../MAINTENANCE.md`、`../tools/arch-layers.json`、`../tools/check-arch.mjs`、`../tools/count-tests.mjs`、`../README.md`。
- 代码路径与 `文件:行号` 引文 → 按原文照录（含 `server-rs/…`、`src-tauri/…`、`launcher/…`），行号以各自源文档落盘时为准，可能已随后续批次漂移。

**维护约定**：

1. 本文承载**架构叙述权威**，任何结构性改动先对照本文判断落点与代际影响；与代码实测不符时，**以代码为准并先修本文**，再动代码（§0 冲突裁定顺序）。
2. 代际归属一律以 `../tools/arch-layers.json` 为准；本文「三结合分层架构」§5 与「代际归属事实源对账」是与该文件对账的**对照面**，不一致时回填本文。
3. 「数据库 schema 与降级契约」保留「原文 + 变更标注」双层体例；新增变更时**追加标注**，不复写原文、不删除历史层。
4. 改本文（文档）后需跑 `node ../tools/check-arch.mjs` 与 `node ../tools/count-tests.mjs --check` 确认门禁不漂移（口径见 `../AGENTS.md`「Git 提交纪律」）。
