# 计划 —— 未实现但有明确改动方案的条目

> 本文件归属:**计划**类(有改动点 + 有验收断言 + 有人在跟踪);已知缺陷见 [`遗留.md`](遗留.md),
> 仅有方向见 [`展望.md`](展望.md)。
>
> 判据(见仓库六类体系说明):「计划」= **有批次号、有改动点、有验收断言、有人在跟踪**。
> - 与「遗留」的分界:条目本身是**问题/缺陷** → 属 `遗留.md`;条目是**改动方案** → 属本文。
>   二者用 ID 双向引用:本文相关条目写「关联遗留:L16」,遗留侧写「转计划」。
> - 与「展望」的分界:只有方向、没有改动点/验收断言的 → 属 `展望.md`。
> - 同一 ID 在本文只出现一次;若源文档状态不一致,以**代码实测**为准,并登记在
>   [§九 状态漂移裁定登记](#九状态漂移裁定登记)。
>
> **来源链接写法**:指向原 `docs/` 文档的引用一律写
> `archive/2026-09-16-consolidation/<原文件名>`(相对 `docs/`);指向 `MAINTENANCE.md`/`AGENTS.md`
> 写 `../MAINTENANCE.md` / `../AGENTS.md`。
>
> **整合背景**:仓库正把 68 份散落 md 整合为「契约/功能/计划/展望/经验/遗留」六类体系,
> 原文档移入 `docs/archive/2026-09-16-consolidation/`。本文是该体系中的「计划」类唯一出口。

## 条目模板(本文件逐条套用)

```
### <ID> <标题>
- **目标**：
- **现状证据**（`文件:行号` 或实测输出）：
- **改动点**：
- **验收断言**：
- **风险 / 回滚**：
- **依赖 / 需确认**：
- **状态**：未启动 / 进行中 / 部分完成（注明已完成部分）
- **来源**：archive/2026-09-16-consolidation/<原文件名> §X
```

**ID 纪律**:ID **沿用源文档编号**(`P-8`、`批次 5`、`4.5`、`L16`、`T1`、`D.3`、`B.4b` 等),
**不得重编号**——仓库存在跨文档按编号引用。新增条目应先向源文档编号体系靠拢。

**状态口径**:`未启动` = 代码中查不到任何落地痕迹;`进行中` = 部分改动已提交但批次未收口;
`部分完成` = 批次内部分项已落地并验证,余项未做(须注明已完成部分)。

**裁定依据的核对方法**:凡标注实测结论处,均为 2026-09-16 在本仓库执行
`grep` / `Read` / `node tools/count-tests.mjs --check` / `npm run lint -w web` 等命令得到,
具体命令与结果见 [§九](#九状态漂移裁定登记)。

---

## 一、活跃性能批次（P-8~P-13）

> 本章承自 `archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md`,
> 该文档**全文移植**于此(含 §0 基线事实、§1 总览与顺序、§8 全程纪律、§10 决策点),
> 并补充承接文档 `archive/2026-09-16-consolidation/后端性能批次P1-P7-变更说明-2026-09-16.md`
> 的 §4 未做缺口与 §5 后续顺序。**每批次独立可交付、独立可回滚。**

### 1.0 基线事实(2026-09-16 实测,均为本计划的前提)

| 项 | 实测值 | 证据 |
|---|---|---|
| `modal-settings` chunk | **188,565 B / 61,850 gz** | `web/dist/assets/modal-settings-DgSe9Odc.js` |
| 首屏被强制预载 | 是(含 modal-settings) | `web/dist/index.html` 有 `modulepreload …modal-settings-DgSe9Odc.js` |
| index chunk 静态依赖 modal-settings | 是 | `web/dist/assets/index-*.js` 含 `from"./modal-settings-…js"` |
| 其余 chunk | index 122,342 / vendor 187,492 / vue-vendor 84,517 / content-rendering 103,316 / CSS 84,731 | `web/dist/assets/` |
| chunk 总数 | 32 个 js | `ls web/dist/assets/*.js \| wc -l` |
| 虚拟滚动参数 | `tailKeep=30`、`overscanPx=1200` | `web/src/composables/useVirtualMessages.ts:39,117` |
| `sanitize-html` 单步成本 | 16.8k 字符 ≈ **3.057ms**(约 markdown-it 的 7 倍) | `docs/perf-baseline.md:118` |
| `SCHEMA_VERSION` | 1 | `server-rs/src/models/db/schema.rs:8` |
| 迁移基线 fixture | `server-rs/tests/fixtures/schema_baseline_v0_3_0_beta.sql` | 同上目录 |
| 测试计数门禁 | 后端 1154(930+224)/ 前端 834 | `node tools/count-tests.mjs --json` |

三条前置结论:

1. **P-12 依赖 P-8**:体积预算门禁若在 P-8 之前接入会立刻变红(现状 modal-settings 被预载),
   故顺序不可颠倒。
2. **P-11 是唯一有数据影响的批次**(schema 变更 + 迁移),单独确认后再动,建议最后做。
3. **本计划不新增 API 参数、不改 API 线格式**(沿用上一轮已确认的边界);P-11 的派生列属
   服务端内部存储优化,列表接口返回字段集合保持不变。

### 1.1 总览与顺序

| 序 | 批次 | 主题 | 工时 | 风险 | 依赖 |
|---|---|---|---|---|---|
| 1 | **P-8** | 首屏拆 `modal-settings` 反向耦合 | 1d | 中 | — |
| 2 | **P-9** | 虚拟滚动与渲染缓存生命周期对齐 | 1d | 中 | — |
| 3 | **P-12** | 前端体积预算门禁 + 性能门禁可见性 | 1d | 低 | **P-8** |
| 4 | **P-10** | AgentPanel 增长治理 | 2d | 中高 | — |
| 5 | **P-13** | `-j 2` 并发限制解除实验(允许失败) | 0.5d | 低 | — |
| 6 | **P-11** | 角色卡列表解析与启动 N+1(**需单独确认**) | 3d | **高** | — |

严格按序执行:P-8 → P-9 → P-12 → P-10 → P-13 → P-11。
P-12 必须紧随 P-8(否则门禁红);P-11 最后(风险最高且与其余无耦合)。

**承接文档的后续建议顺序**同样为 P-8 → P-9 → P-12 → P-10 → P-13 → P-11
(`archive/2026-09-16-consolidation/后端性能批次P1-P7-变更说明-2026-09-16.md` §5),两者一致。

### P-8 首屏拆 `modal-settings` 反向耦合

- **目标**:首屏不再加载 `modal-settings`;弹窗懒加载语义恢复。预期首屏少 188,565 B / 61,850 gz。
- **现状证据**(已实测):
  - `web/vite.config.ts:29` 的 `manualChunks` 把 `/components/SettingsHub` 与
    `/components/SettingsModal` 强制归入 `modal-settings`。
  - `web/src/components/SettingsModal.vue:11-17` 静态 import 了 `../store`(useAppStore)、
    三个 composable(`useApiSettings`/`usePromptInject`/`useDataManager`)、两个 section 组件。
  - `web/src/store.ts:21-27` 顶层实例化全部子 store。
  - 结果:被入口与弹窗**共享**的模块落进 `modal-settings`,于是 `index.js` 不得不静态
    import 该 chunk → `index.html` 发出 `modulepreload` → **首屏多拉 188KB / 62KB gz**。
  - vite.config 注释里「避免把首屏共享模块误划入异步 chunk 造成反向耦合」的护栏**没覆盖这条路径**。
  - **2026-09-16 复核**:`web/vite.config.ts:29` 该行仍在;`web/dist/index.html` 仍有
    `modal-settings` 预载;`modal-settings-DgSe9Odc.js` 仍是 188,565 B → 本批次未启动。
- **改动点**:
  1. **量基线**:`npm run build -w web` 后记录 `dist/index.html` 的 modulepreload 列表与
     各 chunk 体积,追加进 `docs/perf-baseline.md`(作为 P-12 预算与回归对照)。
  2. **主方案**:删除 `manualChunks` 中两条 app-code 归组(`SettingsHub`/`SettingsModal`),
     让 `modals.ts:40` 的动态 `import()` 自然产出异步 chunk。
     8 个 section 已由 `SettingsModal.vue:18-30` 的 `lazyModal` 各自成 chunk,不受影响。
  3. **验证是否真的解开**:重新构建后断言 `dist/index.html` **不含** `modal-settings`,
     且 `index-*.js` 不再出现 `from"./modal-settings-…js"`。
  4. **备选(仅当主方案产生反向耦合或碎片过多时)**:保留归组但对
     `/src/store.ts`、`/src/stores/`、`/src/composables/`、`/src/asyncModal.ts`
     返回固定 chunk 名(如 `'app-core'`),确保 `modal-settings` 内不再含被入口引用的模块。
  5. 复核 `web/src/modals.ts` 注册表在本改动后仍与 `modals.test.ts` 的元测试一致。
- **验收断言**:
  - `npm run build -w web` 通过;`dist/index.html` 无 `modal-settings` 预载;
  - `npm test -w web` 全绿(注意 `modals.test.ts` 可能断言 chunk 归属);
  - **手动**:打开综合设置,逐个 tab 切换,确认 8 个分区仍懒加载且功能正常;
  - 记录新体积到 `docs/perf-baseline.md`。
- **风险 / 回滚**:风险中——`manualChunks` 调整会改变 chunk 划分,可能出现循环 chunk 或某个
  chunk 变大。回滚 = `git revert` 单个 commit(vite.config.ts + 可能的 modals 调整)。
  **注意**:`web/dist` 被 `include_dir!` 编入 server 二进制(`server-rs/build.rs`),
  手动验证前必须重新 `npm run build -w web` 并重建 server,否则服务端仍发旧资源。
- **依赖 / 需确认**:无前置依赖;但 P-12 依赖本批次(顺序不可颠倒)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §2

### P-9 虚拟滚动与渲染缓存生命周期对齐

- **目标**:滚动卸载/重挂后不重复计算 markdown/sanitize/脚本渲染;缓存有界不泄漏。
- **现状证据**:
  - `useVirtualMessages.ts:147-154`:离开缓冲区即销毁组件(模板降级为占位 `div`)。
  - `ChatMessageItem.vue:213-234` 的 `html` 是**组件内 computed**,组件卸载即失效;
    其内部 `renderScopedScripts`(`render.ts:377`)与 `renderMarkdown`(`markdown.ts:59`)
    都是无外部缓存的直接调用。
  - 代价参照:`sanitize-html` 处理 16.8k 字符 ≈ 3.057ms(`docs/perf-baseline.md:118`),
    含 HTML 卡片的长会话来回滚动会反复付这份成本。
  - **2026-09-16 复核**:`web/src/renderCache.ts` 不存在;
    `useVirtualMessages.ts` 仍为 `tailKeep ?? 30` / 固定 `overscanPx=1200` → 本批次未启动。
- **改动点**:
  1. **先写失败测试**:加渲染计数(mock 或 spy 统计 `renderMarkdown`/`renderScopedScripts`
     调用次数),断言「同一消息内容二次渲染不重算」。
  2. **缓存提到模块级**:新增 `web/src/renderCache.ts`(或并入现有渲染模块),
     提供有容量上限的 Map(建议容量 500,超出按 FIFO/LRU 淘汰)。
  3. **缓存键沿用现有脚本缓存口径**(`ChatWindow.vue:61-91` 的
     `${scopeId}|${scriptHash}|${depth}|${text}` 形态),并加入 `renderHtml` 开关与
     `scripts` 版本(复用 `props.scriptHash`),保证:
     - 内容变更 → key 变更 → 必 miss(响应式正确);
     - `scripts` 引用或字段变化 → key 变更 → 必 miss(脚本热更新即时生效)。
  4. `ChatMessageItem.vue` 的 `html` computed 改为「先查模块级缓存,未命中再算并写回」,
     命中路径不得触发 `sanitize`。
  5. 顺带收敛 `overscanPx=1200` 固定像素对超高卡片覆盖不足的问题:改为按**条数窗口**
     (建议上下各 8 条)与像素窗口取较大者,减少挂载/卸载抖动。
  6. **不要**把缓存做成全局无界(内存风险);淘汰策略写进注释与测试。
- **验收断言**:
  - `npm test -w web` 全绿,**含新增的缓存命中用例**(证明二次渲染零重算);
  - 长会话(>80 条、含 HTML 卡片)滚动时,Performance 面板无重复 `sanitize-html` 长任务;
  - `npm run typecheck -w web` 与 `node tools/check-frontend-lint.mjs` 通过(类型逃逸只降不升)。
- **风险 / 回滚**:风险中——key 设计错误会渲染**陈旧内容**(显示 bug)。缓解:key 必须包含内容
  本身(或内容哈希),而非仅消息 id;测试覆盖「内容变更必重渲」。
  回滚 = revert 单个 commit(缓存模块 + 组件调用点)。
- **依赖 / 需确认**:无前置依赖。缓存容量取值(建议 500)可在实施时确认。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §3

### P-12 前端体积预算门禁 + 性能门禁可见性

- **目标**:首屏体积与 chunk 划分回归可被自动拦截;被跳过的门禁必须显式可见。
- **现状证据**:
  - `tools/check-all.ps1:235` 有 `vite build`,但**无产物体积断言**。
  - `tools/check-all.ps1:242-247`:性能门禁默认关闭(需 `-Perf` 且要求服务在跑),CI 不跑;
    跳过时**无任何提示**,属静默跳过。
  - 仓库既有护栏体例为「纯 Node 零依赖 + ratchet 只降不升」(`tools/check-frontend-lint.mjs`)。
  - **2026-09-16 复核**:`tools/check-bundle.mjs` 不存在;`check-all.ps1` 的性能门禁段
    仍为 `if ($Perf) { … }` 且收尾无「未运行性能门禁」提示 → 本批次未启动。
- **改动点**:
  1. **新增 `tools/check-bundle.mjs`**(纯 Node 零依赖,风格对齐 `check-frontend-lint.mjs`):
     - 断言 `web/dist/index.html` 的 modulepreload **不含** `modal-*`(P-8 的固化护栏);
     - 断言首屏集合(index + 其预载的 vendor/vue-vendor/content-rendering/CSS)的
       gzip 总量不超过预算;
     - 断言各 chunk gzip 体积不超单项预算;
     - 预算取 P-8 实测值 + 10% 余量,写为脚本内 `BASELINE` 常量,**只降不升**;
     - `web/dist` 缺失时给出明确提示并失败(与仓库「门禁 fail-closed」纪律一致);
     - 支持 `--json` 与 `--verbose`(与既有脚本一致)。
  2. **接入 `check-all.ps1`**:在 `web: vite build` 之后加
     `Invoke-Stage 'web: bundle budget' { node tools/check-bundle.mjs }`;
     `-Quick` 模式跳过它(与 vite build 同步跳)。
  3. **性能门禁可见性**:`check-all.ps1` 收尾打印一行显式提示,
     说明「未运行性能门禁(需服务在运行,用 -Perf 开启)」,避免静默跳过被误读为通过。
     不改 `-Perf` 的默认关闭语义(该门禁依赖外部服务,不适合默认开)。
  4. 更新 `MAINTENANCE.md` §11 或 §0 门禁清单,登记新增门禁项。
- **验收断言**:
  - 正常状态 `node tools/check-bundle.mjs` exit 0;
  - **负向验证**:手工把某 chunk 撑大 / 把 `modal-settings` 加回 `index.html` 预载,
    脚本必须 exit 1(证明门禁有效,不是空断言);
  - `npm run check` 全绿。
- **风险 / 回滚**:风险低。回滚 = revert(新增脚本 + check-all 两行 + 文档)。
  若预算过紧导致正常功能增长即红,把余量调到实测值**再收紧**,不得直接删除门禁。
- **依赖 / 需确认**:**依赖 P-8**(P-8 未合入前接入会立刻变红)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §4

### P-10 AgentPanel 增长治理

- **目标**:长 agent 会话下 DOM 节点数与渲染耗时不再线性膨胀;裁剪行为**显式可见**,不静默丢信息。
- **现状证据**:
  - `web/src/sseReducer.ts:105,121,123,138,153,168,197`:`chain` 与
    `toolCalls` 每次事件**无条件 push,从不裁剪**。
  - `web/src/components/AgentPanel.vue:392,408,426,456,488` 全量 `v-for`,**无虚拟化**,
    且 key 用 index(`:key="i"` / `:key="'r'+i"`)。
  - `AgentPanel.vue:86` 的 `ioText` 在渲染路径上做 `JSON.stringify(v, null, 2)`
    (展示与复制共用)。
  - `web/src/stores/chat.ts:123-144` 的 `restoreAgentTrace` 会把后端历史
    **全量**回填进 `agent.value`。
  - **2026-09-16 复核**:`web/src/sseReducer.ts` 无任何裁剪/上限常量
    (`grep -n "slice|prune|省略|MAX_" web/src/sseReducer.ts` 零命中)→ 本批次未启动。
- **改动点**:
  1. **先写失败测试**:派发 N(建议 500)次 step/tool 事件后,断言
     `chain`/`toolCalls` 被裁剪到上限,且 AgentPanel 渲染的 DOM 节点数受限。
  2. `sseReducer` 加有界裁剪:保留最近 N 条 + 在列表首部渲染「已省略 M 条」提示
     (**不静默丢弃**;`agent_sessions` 落库侧不受影响,面板只是展示层上限)。
  3. **需确认**:裁剪上限取值(建议 `chain` 500 / `toolCalls` 200)。
     `restoreAgentTrace` 走同一上限,保证刷新前后口径一致。
  4. `AgentPanel` 的工具卡列表接入 `useVirtualMessages` 或改为「仅抽屉打开时渲染」;
     key 改稳定 id(`toolCalls` 已有 `callId`,优先用它;`chain` 需补稳定 id)。
  5. `ioText(v)` 结果改为 computed 预计算(按 tool call id 缓存),避免模板每次重渲染
     重复 stringify 大对象。
  6. **先读清 `restoreAgentTrace` 与 `agent.value` 的整体替换语义**(`chat.ts:140`
     `agent.value = base`),确保裁剪不破坏「刷新后重建」与流式追加的共存。
- **验收断言**:
  - `npm test -w web` 全绿 + 新增裁剪用例;
  - 跑一轮 agent 模式(几十次工具调用)观察渲染耗时与 DOM 节点数**不再线性膨胀**;
  - `npm run typecheck -w web`、`node tools/check-frontend-lint.mjs` 通过。
- **风险 / 回滚**:风险中高——裁剪是**可见行为变更**(用户会看到「已省略 M 条」)。
  缓解:上限取值保守 + 提示显式 + 变更写入 `docs/known-limitations.md`(若降级为已知限制)。
  回滚 = revert(裁剪逻辑 + 面板改动)。
- **依赖 / 需确认**:**§10 决策点 1**:裁剪上限默认建议 `chain` 500 / `toolCalls` 200;
  它是**可见行为变更**,确认后再落地。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §5 与 §10-1

### P-13 `-j 2` 并发限制解除实验(**允许失败,产出入档**)

- **目标**:给出「本机能否放开 `-j 2`」的**实测结论**;无论成功或失败,把矩阵入档,避免下次重复试错。
- **现状证据**:
  - `tools/check-all.ps1:108-109` 强制 `cargo test --workspace -j 2`。
  - `MAINTENANCE.md:434`(条目 17):16GB 内存 + 11GB 页面文件下全并行 debug 链接
    `libkedai_server-*.rlib`(带 debuginfo 约 665MB)触发 `os error 1455 页面文件太小`。
  - `MAINTENANCE.md:539-544`(条目 29):默认并行度下 **rustc 自身栈溢出崩溃**
    (`0xc0000409 STATUS_STACK_BUFFER_OVERRUN`),伪装成「rlib 缺失」。
  - **用户已明确:不通过削减 debuginfo 换并发**。故本批次只走「参数与栈」两条路。
  - **2026-09-16 复核**:`.cargo/config.toml` 仅有 Android 16KB page-size rustflags,
    **无 `[build] jobs = N`**;`check-all.ps1` 仍强制 `-j 2` → 本批次未启动。
- **改动点**:
  1. **记录基线**:不带 `-j` 复跑一次,捕获退出码与崩溃文本(确认当前是否仍复现)。
  2. **并发矩阵**:依次 `-j 4` / `-j 6` / `-j 8` 各跑一次 `cargo test --workspace`,
     记录成功/失败与失败形态(条目 17 的内存型 vs 条目 29 的栈型,处置不同)。
  3. **栈假设验证**:若崩溃形态属条目 29,试 `RUST_MIN_STACK=33554432`(32MB)后
     再测默认并行度,验证是否与 rustc 线程栈相关。
  4. **若仅高并发崩** → 取稳定上限 N 固化(`.cargo/config.toml` 的 `[build] jobs = N`,
     或改 `check-all.ps1` 与 `AGENTS.md` 的验证命令)。
  5. **若全手段仍崩** → 结论「维持 -j 2」,把矩阵写入 `MAINTENANCE.md` 条目 29/30/31 附近。
  6. **约束**:测试期间**不得**与 `build.ps1` 并发(`MAINTENANCE.md:434` 明确警告内存争用
     会让链接器崩 `0xc0000409`);每轮之间清一次孤立 `.rmeta`(条目 30)。
- **验收断言**:
  - 实测矩阵(并发度 × 结果 × 失败形态)入档 `MAINTENANCE.md`;
  - 若放开:门禁脚本与 `AGENTS.md` 同步更新,并复跑一次全量确认;
  - 若未放开:结论与依据入档,`-j 2` 维持现状并注明「已实测,勿重复尝试」。
- **风险 / 回滚**:风险低(纯实验,不改产品代码)。回滚 = 还原 `check-all.ps1`/`.cargo/config.toml`。
  **唯一真实风险**:实验可能写坏 rlib(条目 17/29 的产物损坏),故每轮失败后按
  条目 30 清孤立 `.rmeta` 与成对 `.fingerprint` 目录再继续。
- **依赖 / 需确认**:与 `build.ps1` **不得并发**;矩阵结论直接影响 `AGENTS.md` 验证命令。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §6

### P-11 角色卡列表解析与启动 N+1(**需单独确认,风险最高**)

- **目标**:列表接口与启动路径的 JSON 解析次数不随「卡数量 × 卡体积」线性增长。
- **现状证据**:
  - `character_service.rs:95-113` 的 `list()`:注释称「列表不含 data_raw」,但 SQL **仍取
    `data_raw` 列**,且 `row_to_character`(`:39-54`,以 `with_data_raw=false` 调用)对它做
    完整 `serde_json::from_str`,只为取派生字段:`first_mes`、`alternate_greetings`、
    `regex_scripts`、`creator`、`character_version`、`creator_notes`。
  - `character_service.rs:165-205` 的 `reflatten_v3_cards` 是 N+1:先 `SELECT id FROM characters`,
    再**逐个 id** `SELECT data_raw`;启动期由 `seed_default_character` 触发
    (`:127-131`,`app_state.rs:144` 调用)。
  - `list()` 用 `prepare` 而非 `prepare_cached`。
  - `SCHEMA_VERSION = 1`(`schema.rs:8`);最近一次加列(`finished_at`)的模式是
    `migration/ddl.rs:244` 的 `add_column_if_missing`,且**不动 SCHEMA_VERSION**。
  - **2026-09-16 复核**:`character_service.rs` 无 `derived_json`;
    `reflatten_v3_cards` 仍在 `:165`;`SCHEMA_VERSION` 仍为 1 → 本批次未启动。
- **改动点**:
  1. **先写失败测试**:造 50 张卡(其中含 1MB 级 `data_raw`),断言
     `list()` 的解析次数/耗时**不随卡体积线性增长**;断言 `reflatten_v3_cards`
     的查询次数为 **O(1)**(不是 O(卡数))。
  2. **派生列方案**:为 `characters` 增加派生列(建议单列 `derived_json` 存一个 JSON 对象,
     避免 6 个散列),写入路径(upload/编辑/迁移)同步计算并落盘;
     列表与 `get` 直读列 → **零 JSON 解析**。
  3. **迁移**:用既有 `add_column_if_missing` 模式加列(不动 `SCHEMA_VERSION`);
     新增幂等回填:对已有卡按需计算 `derived_json`(一次性,靠 backfill 标记幂等)。
  4. `reflatten_v3_cards` 改**单次全表** `SELECT id, data_raw` 遍历,消除 N+1。
  5. `list()` 的 `prepare` 改 `prepare_cached`。
  6. **不改列表接口返回字段集合**(派生字段本来就在返回体里,只是改为读列而非现算)。
- **验收断言**:
  - `cargo test --manifest-path server-rs/Cargo.toml -j 2` 全绿;
  - **必须同步 `tests/schema_migration_meta.rs` 与 `tests/fixtures/schema_baseline_v0_3_0_beta.sql`**
    (该测试断言老库升级后结构一致性并冻结基线);
  - 50 卡基准(列表耗时、启动耗时)记入 `docs/perf-baseline.md`;
  - 手工:上传/编辑/删除角色卡后列表派生字段正确(封面、开场白、正则脚本均正常)。
- **风险 / 回滚**(**最高**):
  - schema 变更牵连迁移链、降级策略与冻结基线;`db/mod.rs:66-76` 的「库比代码新则拒绝启动」
    意味着**一旦抬高 `user_version` 后回滚,用户库需从备份恢复**。
  - 故:**本批次执行前需单独确认**;执行前先备份 `data/`;建议先在副本库上验证迁移。
  - 回滚 = revert 代码 + 从备份恢复库(不可仅靠 git revert)。
- **依赖 / 需确认**:**§10 决策点 2**:是否执行本批次(唯一有数据影响的批次)。
  若决定做,**先备份 `data/` 并在副本库上验迁移**。建议排在最后。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §7 与 §10-2

### P-8~P-13 全程纪律(原文移植)

- **一逻辑批次一 commit**:Conventional Commits 前缀 + 中文 scope/描述,正文写清
  「改了什么 / 为何这么改 / 验证证据(实际命令与结果)」。
- **每批次先写失败测试再最小实现**(`AGENTS.md` 核心约定)。
- **新增测试后必须同步计数门禁**:`node tools/count-tests.mjs` → 更新 `MAINTENANCE.md`
  (`--check` 是硬门禁;当前记录:后端 **1154** = 930 单测 + 224 集成(23 文件),前端 **834** / 87 文件)。
- 每批次收尾跑:
  `cargo test --manifest-path server-rs/Cargo.toml -j 2`、
  `npm test -w web`、`npm run typecheck -w web`、`node tools/check-frontend-lint.mjs`、
  `node tools/check-arch.mjs`、`node tools/check-contract.mjs`、
  `node tools/count-tests.mjs --check`;改前端另跑 `npm run build -w web`。
- **交付 exe 前**跑一次完整 `.\build.ps1`(`dist\` 与根 `Kedai.exe` 不会自动更新;
  改前端后必须重建,因为 `web/dist` 经 `include_dir!` 编入 server 二进制)。
- 本机 `cargo` 需先 `call vcvars64.bat`;`cargo test` 必须带 `-j 2`(直到 P-13 改变结论)。
- 文档回填:`docs/perf-baseline.md`(体积/延迟前后对比)、必要时
  `docs/known-limitations.md`(P-10 裁剪上限)与 `MAINTENANCE.md`(P-12 新门禁、P-13 矩阵)。
- **来源**:archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §8

### P-G1 补测 p95 前后对比(承接 P-1~P-7 未做项)

- **目标**:为 P-1(写路径 pragma)、P-5(批量嵌入)、P-6(落库让出 worker)补上**端到端延迟数据**,
  把「收益方向明确但幅度未实测」这一缺口填上。
- **现状证据**:`tools/perf-baseline.mjs` 需要服务在运行(`-Perf` opt-in),上一轮未启动服务,
  故**没有**端到端延迟数据(`后端性能批次P1-P7-变更说明-2026-09-16.md` §4)。
- **改动点**:在真实数据量下启动服务,`powershell -File tools/check-all.ps1 -Perf`
  或其底层 `node tools/perf-baseline.mjs`,记录 **P-1 的写入吞吐**与 **P-6 的并发读 p95**;
  结果写入 `docs/perf-baseline.md`。
- **验收断言**:`docs/perf-baseline.md` 含 P-1/P-5/P-6 的 p50/p95/rps 实测行;
  与基线(`archive/2026-09-16-consolidation/优化实施方案-2026-09.md` 附录 C 的
  characters_list p50=293ms p95=435ms rps=31.7;chat_history p50=18ms p95=30ms rps=513)可对比。
- **风险 / 回滚**:风险低(只跑测量,不改产品代码)。回滚 = 无需回滚。
- **依赖 / 需确认**:需本机启动 Kedai 服务;测量期间不得与 `build.ps1` 并发(内存争用)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/后端性能批次P1-P7-变更说明-2026-09-16.md §4

### P-G2 P-6/P-7 集成用例的判别性补强(承接 P-1~P-7 未做项)

- **目标**:让「必须让出 worker」这件事有**判别性**证明,而非只靠单测。
- **现状证据**:现行 multi-thread 集成用例的价值是**防生产路径回归**(让出链路不破坏功能/
  事件完整性);在只读池不争写锁的场景下,**即使不让出也可能通过**——
  真正证明「必须让出」的是 `utils::blocking` 的单测(已实测判别力)。
  见 `后端性能批次P1-P7-变更说明-2026-09-16.md` §4。
- **改动点**:为 P-6/P-7 设计一个**让出与不让出结果不同**的集成场景
  (例如构造只读池与写锁真实争用的时序),或明确登记「判别性由 `utils::blocking` 单测承担,
  集成用例只作防回归」。
- **验收断言**:新增或登记后,`cargo test -j 2` 全绿;判别性场景手动退化实现能使该用例变红。
- **风险 / 回滚**:风险低(测试基建)。回滚 = revert 测试文件。
- **依赖 / 需确认**:需先确认能否在测试内可靠构造争用时序;若不可行则按上述「明确登记」路径收敛。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/后端性能批次P1-P7-变更说明-2026-09-16.md §4

### P-G3 `TaskService` 余量同步落库出清(承接 P-1~P-7 未做项)

- **目标**:覆盖 `TaskService` 中仍在包装外的少量同步落库调用。
- **现状证据**:`TaskService` 仍有少量同步落库不在 `Self::blocking(|| …)` 包装内
  (如 `list_*` 读方法走只读池,未包装);只读池无写锁竞争,风险低,故按「最小必要」
  只包装写方法(`后端性能批次P1-P7-变更说明-2026-09-16.md` §4)。
- **改动点**:逐个判定剩余未包装调用点:属读方法的保持不包装并补注释说明理由;
  若发现真在写路径上的遗漏则补包装。
- **验收断言**:`cargo test -j 2` 全绿;补包装处有对应用例;注释写明「读方法不包装」的依据。
- **风险 / 回滚**:风险低。回滚 = revert 包装改动。
- **依赖 / 需确认**:无(与其余批次独立)。
- **状态**:**未启动**(源文档定位为「按最小必要暂不处理」的余量)
- **来源**:archive/2026-09-16-consolidation/后端性能批次P1-P7-变更说明-2026-09-16.md §4

---

## 二、工程韧性批次 5~8

> 承自 `archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md`。该文档批次 0/1/2/3/4 已完成,
> **批次 5–8 未启动**。以下逐批移植(含依赖图、逐批验收、明确排除项、冻结点清单)。
>
> **批次依赖图(原文移植)**:
> ```
> 批次 0 基线固化 ──────────────────────────────┐
>                                             │
> 批次 1 错误面收口 ──┬── 批次 7 测试护栏 ────┤
>                    │                       │
> 批次 2 可观测性骨架 ─┬── 批次 6 日志容量 ──┤── 批次 8 文档回填与终验
>                     │                     │
> 批次 3 数据库版本与升级事务化 ────────────┤
> 批次 4 静默吞错与错误分类类型化 ──────────┤
> 批次 5 前端兜底与 SSE 可观测 ─────────────┘
> ```
>
> **全程执行纪律(原文移植)**:
> 1. **每批次独立可交付、独立可回滚**;不跨批次顺手改别的东西(`AGENTS.md` 禁用项)。
> 2. **先写失败测试**(`AGENTS.md` 约定);新测试须经**变异测试**确认能捕获目标缺陷,
>    避免「永远通过」的装饰性断言。
> 3. **ratchet 只降不升**:改动 `tools/check-arch.mjs` 基线必须同步下调并记录前后值。
> 4. **数字同步**:测试数量以 `MAINTENANCE.md` 为唯一真值源。
> 5. **`-j 2` 是必需参数**(默认并行度下 rustc 可能崩)。
> 6. **Git 提交按 `AGENTS.md`「Git 提交纪律」**;需 exe 版时另跑 `.\build.ps1`。
> 7. **不可动的冻结点**:
>    - `models/db/schema.rs` 的 `CREATE_TABLES` 静态文本(须与 `ddl.rs` 在 `normalize_sql` 后一致);
>    - `SseEvent` / `TaskEventKind` 线格式值(`check-contract.mjs` 守卫);
>    - EJS 能力面(规则 H 冻结,只降不升);
>    - `/api/health` 的 `ok` 语义(桌面壳单实例判定依赖它);
>    - SSE 重连算法(只加可观测信号,不改策略)。

### 批次 5 前端兜底与 SSE 可观测

> 批次依赖:5.4 依赖 1.4;前端侧补齐,与后端解耦。**整体状态:部分完成**
> (5.2 的去重已在代码中存在,见 §九 裁定 4;5.1、5.3 已完成;5.4 部分完成)。

#### 5.1 全局错误兜底(当前完全缺失)

- **目标**:组件内未捕获异常不再静默消失;用户能看到可诊断提示而非空白区块。
- **现状证据**:`web/src/main.ts` 仅 `createApp().use(createPinia()).mount('#app')`,无
  `app.config.errorHandler`、无 `onErrorCaptured`、无 `unhandledrejection` / `window.onerror`
  (唯一一处在角色卡沙箱 iframe 内部)。**2026-09-16 复核**:`web/src/main.ts` 全文
  9 行,**仍无任何 errorHandler** → 未启动。
- **改动点**:注册 `app.config.errorHandler` 与 `window.addEventListener('unhandledrejection')`,
  异常写入既有横幅状态(`stores/uiPrefs.ts` 的 `dataLoadError`)或最小 toast。
- **理由(原文)**:A 的 TypeError 正是「组件内未捕获异常无兜底」的实例——异常静默消失,
  用户只看到一个空白区块。
- **验收断言**:未捕获 rejection 的兜底用例;`npm test -w web` / `lint` / `typecheck` 全绿。
- **风险 / 回滚**:风险低;回滚 = revert 注册点。
- **依赖 / 需确认**:无。
- **状态**:**已完成**(2026-09-16):新增 `web/src/globalErrorHandlers.ts` 统一覆盖三条路径
  (`app.config.errorHandler` / `window 'unhandledrejection'` / `window 'error'`),`main.ts` 在
  `mount` 前注册;**未复用 `dataLoadError`** 而新开 `stores/uiPrefs.ts` 的 `globalError`——
  因为 `App.vue` 的数据加载横幅挂着一个硬绑 `loadCharacters()` 的「重试」按钮,未捕获异常
  没有对应的重试动作,混用会引导用户点到无关操作(横幅只给「关闭」)。内置同消息 3s 去重
  (渲染路径异常每帧复抛,不去重会刷屏并自激)、300 字截断、上报通道自身出错不外泄。
  新增测试文件 `globalErrorHandlers.test.ts`(6 例,含变异验证:删去重块 / 删截断各使对应用例 FAIL)。
  `npm test -w web` 862 例全绿、`lint` 0 error、`typecheck` 通过
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 5 · 5.1

#### 5.2 SSE 错误分类去重

- **目标**:消除 `api/chat.ts` 与 `api/tasks.ts` 各自重写的 `apiErrorMessage` 分类判断漂移。
- **现状证据**:`engine/修复计划` 原文称两处各自重写;**2026-09-16 复核**:
  `grep -n "function apiErrorMessage|const apiErrorMessage" web/src/api/*.ts` **仅
  `web/src/api/client.ts:83` 一处定义**;`chat.ts:2`、`tasks.ts:5` 均
  `import { … apiErrorMessage … } from './client'`;`client.test.ts:133` 有
  「分类口径(与 errors.rs 契约一致)」用例 → **本项实际已完成**(见 §九 裁定 4)。
- **改动点**(若将来再出现分叉):统一复用 `client.ts` 的实现,禁止本地重写。
- **验收断言**:`grep` 全仓 `apiErrorMessage` 定义仅 1 处;
  复用后 `api/chat.test.ts` / `api/tasks.test.ts` 仍通过。
- **风险 / 回滚**:风险低。
- **依赖 / 需确认**:无。
- **状态**:**部分完成(本项已完成)**——批次整体仍列未启动,但代码实测已收敛。
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 5 · 5.2

#### 5.3 SSE 重连可观测

- **目标**:让「前端当时发生了什么」在控制台可见。
- **现状证据**:`stores/task.ts` 的重连(指数退避 1s→15s、settle 1.5s、兜底轮询 5s)
  目前**无任何日志**。**2026-09-16 复核**:`stores/task.ts` 仅 3 处 `console.error`
  (列表/累计/详情加载失败,`:203,226,257`),**无重连相关 `[kedai]` 前缀 warn** → 未启动。
- **改动点**:补带 `[kedai]` 前缀的 `console.warn`(沿用仓库既有前缀约定)。
  **不改重连算法本身。**
- **验收断言**:`npm test -w web` / `lint` / `typecheck` 全绿;重连路径触发时控制台有 warn。
- **风险 / 回滚**:风险低(仅加日志);回滚 = revert。
- **依赖 / 需确认**:无。**冻结点**:SSE 重连算法不得改(只加可观测信号)。
- **状态**:**已完成**(2026-09-16):断线输出一条 `console.warn`(含下次退避毫秒数与
  「兜底轮询已接管」),settle 窗口内未再断开输出 `console.info` 报恢复(首次连接不刷,
  靠 `reconnectDelay > RECONNECT_BASE_MS` 区分),兜底轮询启停与订阅关闭各补 `console.debug`。
  退避算式与定时器**一字未改**。新增 2 例锁定(`task.test.ts`,含变异验证:删掉 warn 行
  与删掉 info 分支各使对应用例 FAIL)。`npm test -w web` 856 例全绿、`lint` 0 error、`typecheck` 通过
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 5 · 5.3

#### 5.4 API 形状闸门(承接 1.4)

- **目标**:其余易被直接解构的 API 封装补运行时形状校验。
- **现状证据**:**2026-09-16 复核**:`web/src/api/shape.ts` 已被
  `tasks.ts:6`、`sessions.ts:7`、`memory.ts:10` 引用;`diagnostics.ts:69` 有
  `assertDiagnostics`。即形状闸门已在多个高频模块落地,**但并非全覆盖**(须逐模块清点剩余)。
- **改动点**:把 `diagnostics.ts` 的 `assertDiagnostics` 模式推广到其余「`request<T>()`
  返回值被直接解构」的 API 封装;抽「契约化形状校验」辅助函数复用。
- **理由(原文)**:可选链只能挡 `null`/`undefined`,挡不住「形状不对的真值对象」——
  A 的 TypeError 正是这样产生的。
- **验收断言**:逐模块清点表落盘;每个被改模块有形状异常用例;
  `npm test -w web` / `lint` / `typecheck` 全绿。
- **风险 / 回滚**:风险低;回滚 = 按文件回滚。
- **依赖 / 需确认**:**依赖批次 1.4**(形状闸门推广的首批已落地)。
- **状态**:**部分完成**(shape.ts + diagnostics/tasks/sessions/memory 已落地;余模块未清点)
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 5 · 5.4

### 批次 6 日志容量与运行时调级

> **前置**:依赖批次 2(同改 `utils/logging.rs`,须在 `PinoFormat` 定型后再动)。
> 批次 2 已完成,故前置已满足。**整体状态:部分完成**(6.1 已完成;6.2、6.3 未启动)。

#### 6.1 容量上限

- **目标**:一次失控的 debug 级日志不再能在一天内写满磁盘。
- **现状证据**:现状:仅按天滚动 + **启动时**删除 3 天前文件;**无单文件大小上限、无压缩、
  无运行中清理**。**2026-09-16 复核**:`utils/logging.rs` 的 `DailyFileWriter`
  (`:207-244`)只有 `open`/`write`/`flush`,无大小上限与轮转 → 未启动。
- **改动点**:加单文件大小上限 + 超限轮转;轮转产物纳入既有 3 天清理范围
  (`cleanup_old_logs`,`logging.rs:304`)。
- **验收断言**:构造超限日志 → 文件被轮转/清理;`npm test -w web` 与
  `cargo test -j 2` 全绿(含 logging.rs 既有格式单测不回归)。
- **风险 / 回滚**:风险低(仅日志写入路径);回滚 = revert `logging.rs`。
- **依赖 / 需确认**:**依赖批次 2**(已完成)。改前须确认 `PinoFormat` 已定型,避免同文件冲突。
- **状态**:**已完成**(2026-09-16):`DailyFileWriter` 新增单文件上限 `MAX_FILE_BYTES = 32 MiB`
  与同日轮转份数上限 `MAX_ROTATED_PER_DAY = 8`(两者相乘 = 单日日志硬上限 256 MiB)。
  超限切至 `kedai-YYYY-MM-DD.<n>.log`——**前缀与 `.log` 后缀都保留**,故既有
  `cleanup_old_logs` 的过滤(`starts_with(FILE_PREFIX) && ends_with(".log")`)**无需改动**
  即覆盖轮转产物;重启时按目录实况续写(未满则续写,已满则另起下一序号,不撞名);
  `written` 取文件实际长度而非从 0 累加。新增 8 例单测,含变异验证(停轮转/停淘汰/破坏
  轮转文件后缀各使对应用例 FAIL)。`cargo test -j 2` 全绿、`cargo fmt --check` 与
  `clippy -D warnings` 通过。**注**:`MAX_ROTATED_PER_DAY` 是计划的补充项——只有大小上限
  而无份数上限时,疯狂刷日志仍能靠不断轮转写满磁盘,故必须两者并存
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 6 · 6.1

#### 6.2 运行时调级

- **目标**:改日志级别不再必须重启进程。
- **现状证据**:级别由 `LOG_LEVEL` / `RUST_LOG` 在启动时读一次(`logging.rs:272` 的
  `init(level, log_dir)`),**改级别必须重启**。
- **改动点**:增加运行时调整入口(端点或信号),并纳入安全白名单决策
  (该端点是否需要 token —— 建议需要)。
- **验收断言**:运行时调级生效且不需重启;新增端点有鉴权用例;
  `check-arch` 与 `check-contract` 通过。
- **风险 / 回滚**:风险中(新增端点属 API 面);回滚 = 移除端点与接线。
- **依赖 / 需确认**:**依赖批次 2**(已完成)。**待确认**:该端点是否需要 token(建议需要)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 6 · 6.2

#### 6.3 级别分布再平衡

- **目标**:正常流程可在日志中复原「用户做了什么」。
- **现状证据**:实测 160 处 `tracing::*!` 中 warn 116 / error 15 / info 28 / **debug 1**。
  **2026-09-16 复核实测**:`tracing::warn!` **116**、`tracing::info!` **29**、
  `tracing::debug!` **0**(debug 已归零,比原文更极端)。
- **改动点**:按「关键业务动作打 info、可复现的中间态打 debug」补齐,
  **优先覆盖 A 暴露的排查盲区**(如 API 层关键写操作)。
- **验收断言**:info 级可在日志中复原一次完整会话的关键步骤;`check-arch` 通过。
- **风险 / 回滚**:风险低(仅日志语句);回滚 = revert。
- **依赖 / 需确认**:无(与 6.1/6.2 同批)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 6 · 6.3

### 批次 7 测试体系护栏

> **目标**:消除「测试全绿但功能坏掉」这一系统性盲区。A 的 P0 即因此存活。
> **依赖批次 1**(已完成)。

#### 7.1 后端负路径覆盖约定(收益最大,无需新机制)

- **目标**:把「错误分支是否被真实执行过」变成可检查的事实。
- **现状证据**:当前 105 处裸 `{error}`(`check-arch` 规则 G 基线,`tools/check-arch.mjs:400`
  为 `BASELINE_BARE_ERROR = 105`)中大部分**从未被测试触达**——
  `diagnostics/cache` 的无参分支就是典型(既有测试永远带 `session_id`)。
- **改动点**:**约定**:凡 handler 存在错误分支,**集成测试必须至少执行一次并断言
  真实 HTTP 状态码 + `code`**。
- **验收断言**:统计脚本或人工核对:批次 1 改动的每个错误分支都有对应断言;
  逐个被改动的 handler 错误分支必须有集成测试断言真实状态码 + `code`。
- **风险 / 回滚**:风险低(测试增量)。
- **依赖 / 需确认**:**依赖批次 1**(已完成)。与 7.2 同批。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 7 · 7.1

#### 7.2 mock 契约对齐

- **目标**:前端 mock 的状态码不再能与后端实现不一致而无人发现。
- **现状证据**:问题——前端单测 mock 的状态码可以与后端实现不一致而无人发现
  (`diagnostics.test.ts` 原用 `status: 500` 模拟一个实际返回 200 的路径)。
  **2026-09-16 复核**:`diagnostics.test.ts:73-74` 已加注释「状态码与后端实现对齐:
  失败走 500 + code(见 api/diagnostics.rs 的 err_with_code)」;
  `memory.test.ts`、`RepoIndexModal.test.ts` 也有同类注释 → **部分完成**。
- **改动点**:凡前端 mock 后端错误响应,须在注释标注对应后端源码位置与真实状态码;
  进一步可抽「契约化 mock」辅助函数。
- **验收断言**:清点所有 mock 后端错误响应的测试文件,逐一致对齐注释;
  抽 helper 后 `npm test -w web` 全绿。
- **风险 / 回滚**:风险低(测试基建)。
- **依赖 / 需确认**:**依赖批次 1**(已完成)。
- **状态**:**部分完成**(diagnostics/memory/repoIndex 已按范例改正;余 mock 未清点)
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 7 · 7.2

#### 7.3 既有 flake 根因排查(`task_tool_events_are_not_duplicated`)

- **目标**:定位 `tests/task_events.rs::task_tool_events_are_not_duplicated` 的低概率时序 flake 根因。
- **现状证据**:该用例在全量并发下曾失败 1 次(断言「调用工具」事件 0 条),
  单独重跑 10 次全过、最终全量通过。**判定**:低概率时序 flake,
  **与三维主题无关,已单独报备**。
- **改动点**:**先做根因定位再决定修法(不猜着改)**;若定位成本高,登记为已知 flake
  并在测试注释说明重跑方式。
- **验收断言**:`cargo test --manifest-path server-rs/Cargo.toml -j 2` 连续 2 次 exit 0;
  若登记为已知 flake,测试注释含重跑方式。
- **风险 / 回滚**:风险低(测试域)。回滚 = revert 测试/注释。
- **依赖 / 需确认**:无。可与 7.1/7.2 独立进行(原文档标为「可选」)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 7 · 7.3

### 批次 8 文档回填与终验

- **目标**:收尾——数字、状态、叙事与代码对齐;门禁全绿。
- **现状证据**:前序批次(0–4)已落地,但下列回填动作属本批次范围。**2026-09-16 复核**:
  `docs/工程韧性修复-变更说明.md` 已存在(部分回填已做,如 L18/L21 状态),
  但 `known-limitations.md` 的「有意不做」登记项(L24/L25/L26/L27)与终验清单需按本批次口径复核。
- **改动点**:
  1. `MAINTENANCE.md`:同步测试数字(数字权威);health 字段描述;§8 日志节补
     `requestId` 字段约定与访问日志格式;批次 0 的两条构建异常排查。
  2. `docs/known-limitations.md`:按既有体例(参照 L12/L19/L20/L23)把 **L18、L21 置为「已修复」**
     并附验收证据;**对有意不做的项新增登记**(无 metrics/OTLP、无 down 迁移、
     事件流无持久化回放、413 例外),写明判定与补齐路径,**避免后误判为遗漏**。
  3. `docs/数据库版本与降级行为.md`:§一/§三 按新现实改写(原文把代码改动列为待办),
     §四 保留为已兑现方案的历史索引。
  4. `docs/ARCHITECTURE-3H.md §7.1`:若本轮发现新的叙事/代码偏差,按既有偏差登记体例补行。
  5. `docs/README.md`:登记新增文档与源文件。
  6. 变更说明:每批次一份(`docs/工程xxx-变更说明.md`),沿用「逐轮改动的对外契约」体例。
  7. 终验:`node tools/check-all.ps1` 全绿。
  8. 若需交付 exe:补跑 `.\build.ps1`(debug 通过 ≠ exe 已更新)。
- **验收断言**:`node tools/check-all.ps1` 全绿;`node tools/count-tests.mjs --check` 通过;
  L18/L21 在 `known-limitations.md` 标记「已修复」;有意不做项有登记;
  `docs/README.md` 索引与新文档一致。
- **风险 / 回滚**:风险低(文档);回滚 = revert 文档改动。
- **依赖 / 需确认**:**依赖全部批次**(收尾项)。
- **状态**:**部分完成**(部分回填已做:`工程韧性修复-变更说明.md`、L18/L21 状态、
  ARCHITECTURE-3H §7.2 第三轮核查;`known-limitations.md` 的 L24–L27 有意不做登记已存在;
  终验 `check-all.ps1` 全绿待跑)
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md 批次 8

### 批次 0–4 已完成的待修清单项(供溯源,不重复跟踪)

> 以下 15 项为 `工程修复计划-2026-09-15.md` §0.3 待修清单,**已修/部分完成**,
> 保留于此仅为跨文档引用可追溯,不再作为待办:
> 项 1(内部细节泄露,批次 1)、项 2(提取器拒绝未统一,批次 1;413 登记为例外 L27)、
> 项 3(错误码覆盖,批次 1 部分:112→105 + 规则 K)、项 4(零 requestId/访问日志/span,批次 2)、
> 项 5(health 不探测依赖,批次 2)、项 6(无 schema 版本号 + 升级链无事务,批次 3)、
> 项 7(任务终态落库失败静默,批次 4)、项 8(`Lagged` 仅 debug,批次 4)、
> 项 9(`classify_engine_error` 字符串匹配,批次 4)、项 14(测试盲区,部分:形状闸门已建)、
> 项 15(环境侧踩坑入档,批次 0)。
> **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md §0.3

### 批次排除项与冻结点(原文移植,便于复查)

- **排除项**:引入 metrics / Prometheus / OpenTelemetry,加 `/metrics`;
  down 迁移;一次性改完裸 `{error}`;新增第三方依赖;事件流持久化 / 回放;
  改 `CREATE_TABLES` / `SseEvent` 线格式 / EJS 能力面;
  改 `/api/health` 的 `ok` 语义;改 SSE 重连算法。
  (理由见 [§八 明确不做](#八明确不做知情排除便于复查))。
- **修订说明(2026-09-15)**:原表列有「不提交 Git」一项,已按 `AGENTS.md` 的新纪律撤销——
  本轮起改为「完成并验证后按体例提交,一逻辑批次一提交」。
- **来源**:archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md「不做的事」与「修订说明」

---

## 三、残余任务与后续计划

> 承自 `archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md`。
> 该文档部分 4.x/5.x/6.x 条目已被后续批次完成,**下列条目均已在 2026-09-16 逐条核实实际状态**。
>
> **注意**:已完成项(4.1 / 4.2 / 4.3 / 4.4 / 5.1 / 5.2 / 5.3 / 3.1 / 3.2 / 3.7 / 6.1 / 6.3 / 6.5)
> 不列入本文件,仅在此留一行索引供跨文档引用:
> 4.1 四份截断重试合一 ✅、4.2 `TaskBackend` 拆窄接口 ✅、4.3 `AgentEngine` 依赖结构体分组 ✅、
> 4.4 状态机 transition 非静默 ✅、5.1 `storeBridge` 对齐 ✅、5.2 `MODAL_FLAGS` 单点定义 ✅、
> 5.3 ESLint/Prettier 引入(主体完成,全量格式化未做——见 FT-1)、
> 3.1 message 作用域变量孤儿 ✅、3.2 构建指纹链闭环(评估后维持现状 + 补文档)✅、
> 3.7 用量统计非原子 ✅、6.1 审计升级 ✅、6.3 代码签名/SmartScreen(文档)✅、6.5 EJS 冻结门禁 ✅。
> **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §一/§三/§六

### CHG-3 子任务 END 收尾的机械校验（**落点方案已证伪，需重新设计**）

- **目标**:让「子任务产出被截断」在 `length` 之外多一条可机器判定的判据。
- **现状证据**:模板（`tools/agent_tools_agent.rs:39`）要求「缺 END 即视为截断」,但
  `classify_subtask_result`(`:527-546`)只判 `content.is_empty()` 与
  `finish_reason == "length"`;END 仅作为指令约束下发给模型,执行侧不解析。关联遗留:**CHG-3**。
- **改动点（原方案不可用）**:原候选落点「要求 END 的指令 + 末尾缺 END → Truncated」
  **不能照字面实现**——模板原文是「长度: 目标 ≤ N 字;**若被迫截断,须以单独一行 END 收尾**」,
  即 **END 只在截断时才被要求输出,正常完成的产出本来就不带 END**。照字面实现会把
  **每一次正常完成**判成截断。另有 `services/task_service/prompt.rs:294-298` 的护栏测试
  锁定「END 只属子任务指令(不得进 `EXECUTOR_PROMPT` 污染用户可见正文)」。
- **验收断言**:（待重新设计后补）新判据须在**不误杀正常完成**的前提下捕获至少一类
  现有 `length` 判据抓不到的截断;须有判别性用例(正常完成 + 真截断各一)。
- **风险 / 回滚**:风险中——任何「内容形态」启发式都天然有误杀面(半截产出与完整产出的
  文本边界不是可判定事实,见 `遗留.md` 的 CHG-6「拒绝执行态无法自动识别」同族问题)。
- **依赖 / 需确认**:**须先重新设计判据再动手**;设计稿未产出前本项不应被当作「小改」领取。
- **状态**:**未启动（方案已证伪，待重新设计）**
- **来源**:archive/2026-09-16-consolidation/任务编排契约与plan模式加固-变更说明.md §4

### 4.5 错误类型收敛(渐进,勿一次替换)

- **目标**:在 services 边界引入错误枚举,替代「错误分类只在 HTTP 边缘反推」。
- **现状证据**:`Result<_, String>` 遍布 server-rs(约 105 个文件);`err_json` 帮助函数在
  8 个 handler 里重复。**2026-09-16 复核**:`err_json` 仍分散在
  `api/characters.rs:221`、`api/contract_history.rs:147`、`api/chat.rs:419`、`api/skills.rs:110`、
  `api/kaleido.rs:50`、`api/plugins.rs:227`、`api/sessions.rs:617`、`api/undo.rs:49`
  (共 8 处);全仓**无 `enum ServiceError`** → 未启动。
- **改动点**:先 `api/util.rs` 收口 `err_json`;再在 **services 边界**引入枚举
  (`ServiceError::{NotFound, Conflict, Upstream, Db, Validation}`)+ `From<String>`,
  只改**最高频 10 个服务**;`agents/` 内部保持 String(避免大范围波及)。
- **验收断言**:`check-arch` 规则 G(裸 `{error}` ratchet)不反弹;新增错误映射测试。
- **风险 / 回滚**:风险中(触及错误面契约);按服务逐个提交、按文件回滚。
- **依赖 / 需确认**:建议最后做(与 4.6 同批,风险最高)。`err_json` 收口须确认不改变既有
  状态码与 `code` 取值(现存处已带非 2xx 状态码)。
- **状态**:**第一步已完成(2026-09-16),第二步未启动**。
  - **第一步 `err_json` 收口 ✅**:8 份私有 `err_json` 影子实现全部删除(102 个调用点),
    连同 6 份私有错误助手(`not_found`/`bad_request`/`internal_error`/`permission_error`);
    出口统一到 `api/errors.rs` 的 `err_status` + 语义构造器;105 处内联裸 `{error}` 收敛到
    **1 处**(`api/security.rs:177` 的 403/429 拒绝路径,无匹配 ErrorCode,有意保留);
    规则 G 基线 **105 → 1**;5xx 出口补泄露分档(原文只进日志)。
    证据:提交 `d765047`;`docs/功能-变更史.md` 的
    [错误出口收口与契约门禁](功能-变更史.md#2026-09-16-error-consolidation) 章。
    **状态码与 `code` 取值逐处保真**(`skills.rs` 的恒 404 已显式书写,未按签名的默认值推断)。
  - **第二步 `ServiceError` 枚举 ⏳**:仍未启动(`grep "enum ServiceError"` 零命中),
    按原计划作为独立批次推进。
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §一 · 4.5

### 4.6 Cargo workspace 化(可选,根治双锁)

- **目标**:以根 workspace 收纳三成员,一份 lock,根治双锁漂移。
- **现状证据**:无根 `Cargo.toml`,server-rs 与 src-tauri 各持 lock;`check-lock-sync` 只是护栏。
  **2026-09-16 复核**:`ls Cargo.toml` → **不存在**(仍无根 workspace)→ 未启动。
- **改动点**:根 workspace 收纳 `server-rs`/`src-tauri`/`launcher` 三成员,**一份 lock**;
  `launcher` 零依赖可保留独立(若 workspace 化影响 cxflags 行为需实测)。
- **验收断言**:`npm run check:lock`(基线清空)、双端构建指纹一致、Android 双 ABI 出包。
- **风险 / 回滚**:风险高——Tauri Android 构建依赖 `gen/android` 与 target 布局,
  须完整回归双端构建;建议独立分支试验,失败可弃。
- **依赖 / 需确认**:与 4.5 同批,最后做。**需实测** workspace 化对 cxflags/Android 布局的影响。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §一 · 4.6

### 4.7 工具名 6 处登记收敛

- **目标**:新增工具不再需要改 6 处,漏改不再静默降级。
- **现状证据**:新增工具需改 6 处(注册/风险级/场景白名单/工具指南/渲染意图/前端白名单),
  漏改静默降级。清单见 `archive/2026-09-16-consolidation/威胁模型与残余风险.md` §四。
  **2026-09-16 复核**:`grep -rn "ToolSpec" server-rs/src/tools/*.rs` **零命中** → 未启动。
- **改动点**:合并为单一 `ToolSpec` 表(注册时一次性携带 risk/sets/when/render),
  或写元测试遍历注册表断言 5 处映射齐全。
- **验收断言**:新增一个哑工具只需改 1–2 处;元测试能拦漏改。
- **风险 / 回滚**:风险中(触及工具注册表);按文件回滚。
- **依赖 / 需确认**:与 `威胁模型与残余风险.md` §四 的清单保持同步(6 处位置不得漂移)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §一 · 4.7

### 5.4 E2E 关键路径(Playwright)

- **目标**:把最脆弱路径(单实例端口接管、关闭确认、Android 返回键、导出分享)从「全靠手测」
  变为可重复执行。
- **现状证据**:零 E2E。**2026-09-16 复核**:`playwright.config.ts`、
  `web/playwright.config.ts`、`e2e/` 均**不存在**;`package.json` / `web/package.json`
  **无 `e2e`/`playwright` 脚本与依赖** → 未启动。
- **改动点**:Playwright(桌面 WebView 场景可用 `tauri-driver` 或直接对
  `http://127.0.0.1:3001` 跑浏览器 E2E,先覆盖「聊天发送 → SSE 流 → finish」与
  「设置保存」两条主链路)。
- **验收断言**:E2E 可在本机 `npm run e2e` 跑通;与 `check-all.ps1` 解耦(重活不进 pre-push)。
- **风险 / 回滚**:风险中(引入新依赖与测试基建,须过 `check:lock`/审计门禁);
  回滚 = 移除依赖与配置。
- **依赖 / 需确认**:新依赖需过一次 lint/audit/lock 门禁;确认 `tauri-driver` 路线是否可用。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §二 · 5.4

### 3.3 命令混淆绕过 `command_risk`(T2)

- **目标**:把「base64/转义后再执行」的常见形态纳入启发式,降低漏判面。
- **现状证据**:`tools/command_risk.rs` 自述「黑名单 + 启发式,**不是安全边界**」;
  命令级硬门只对已识别的 destructive/admin 生效。**2026-09-16 复核**:
  `command_risk.rs:5` 头部仍自述为启发式,**未新增 base64/转义形态识别** → 未启动。
  关联遗留:**T2**。
- **改动点**:至少把「base64/转义后再执行」的常见形态加进启发式;
  或在宽松档对未识别命令降级为「需授权」而非放行。
- **验收断言**:新增混淆形态用例(base64 解码执行、转义拼接)能被判为 sensitive/需授权;
  `cargo test -j 2` 全绿;硬门先于自动放行的既有不变量不破。
- **风险 / 回滚**:风险中(误判会拦合法命令);回滚 = revert 启发式。
- **依赖 / 需确认**:缓解面已存在(`exec_enabled` 默认关;`bash` 风险恒 Dangerous),
  本项属「收紧」而非「补洞」,**不得**把它当安全边界依赖。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.3
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md T2)

### 3.4 Android `\u{1f}` 分隔符(T3)

- **目标**:消除「命令原文含 `\u{1f}` 导致字段错位」的解析扰动。
- **现状证据**:`services/exec/android.rs` 用 `\u{1f}` 分隔 command / cwd / timeout;
  命令原文含该字符会字段错位(**不能提权,仅扰动解析**)。**2026-09-16 复核**:
  `services/exec/android.rs:21` 仍为 `const SEP: char = '\u{1f}';`;
  Kotlin 侧 `ShellExecutorBridge.kt:31` 仍为 `const val SEP = '\u001f'` → 未启动。
  关联遗留:**T3**。
- **改动点**:改长度前缀或 base64 编码 command/cwd/timeout;**双侧(Kotlin
  `ShellExecutorBridge`)同改**。
- **验收断言**:命令含 `\u{1f}` 时字段不错位;双侧编解码对拍测试;
  `cargo test -j 2` 与 Android 编译通过;`cargo test --lib` 桌面路径零回归。
- **风险 / 回滚**:风险中(改 JNI 编解码协议,须双侧同步);回滚 = 双侧一起 revert。
- **依赖 / 需确认**:**双侧必须同改**,否则 Android 执行层整体失效;
  真机验证随 6.4 一并做。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.4
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md T3)

### 3.5 `is_public_ip` 黑名单枚举(T4)

- **目标**:以白名单语义收敛私网判定,消除 IPv6 特殊段的理论绕过面。
- **现状证据**:`tools/agent_tools_search.rs` 的 `is_public_ip`(`:128-152`)为黑名单式枚举
  (IPv6 特殊段存在理论绕过面;已有 DNS 解析校验 + 已解析 IP 绑定 + 禁重定向三层防护)。
  **2026-09-16 复核**:仍在 `agent_tools_search.rs:128`(被 `:119` 消费) → 未启动。
  关联遗留:**T4**。
- **改动点(2026-09-16 实测修正,原方案不可行)**:原写「改用 `Ipv4Addr::is_global` /
  `Ipv6Addr::is_global` 语义收敛判定」——**该 API 在本仓工具链上编不过**。实测证据
  (rustc 1.97.1,`~/.cargo/bin/rustc --edition 2021` 直编最小复现程序):
  ```text
  error[E0658]: use of unstable library feature `ip`
    --> ipcheck.rs:3:22
    = note: see issue #27709 <https://github.com/rust-lang/rust/issues/27709> for more information
  ```
  `Ipv6Addr::is_global` 至今仍是 **nightly-only 实验 API**(`ip` #27709,std 文档标注
  「nightly-only experimental API」)。注意 `Ipv4Addr::is_global`(1.84 稳定)与
  `Ipv6Addr::is_global` 是**不同方法**,不可混用;仓库亦**无任何 IP 网络 crate**
  (`grep ipnet|ip_network|ipnetwork` 于 `server-rs/Cargo.toml` 与 `src-tauri/Cargo.toml` 零命中)。
  修订后的候选路径(二选一,**须先定方案再动手**):
  ① **自维护 IANA 特殊用途地址段的显式白名单表**(零新依赖,与 `is_ipv6_unique_local` /
     `is_ipv6_link_local` 既有的手写位掩码判定同风格;需覆盖 `::/128`、`::1/128`、
     `2001:db8::/32`、`fc00::/7`、`fe80::/10`、`64:ff9b::/96`、`::ffff:0:0/96` 等);
  ② 引入 `ipnet` / `ip_network` 一类依赖(须过 `check-lock-sync` 双锁一致 + `cargo audit`)。
  按仓库「已知依赖面从简」的既有取向,**①为首选**。
- **验收断言**:表驱动用例覆盖私网段(10/8、172.16/12、192.168/16、169.254/16、
  127/8、fc00::/7、::1、IPv4-mapped IPv6、以及 `2001:db8::/32` 等文档/特殊段);
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(判定放宽会短暂扩大面);**必须用白名单语义**,
  不得退回黑名单。回滚 = revert。
- **依赖 / 需确认**:**方案需重定**(见上);CGNAT(100.64/10)等段的语义需按所选方案核实。
  可扩展的既有测试:`server-rs/src/tools/agent_tools.rs:190-207`(已覆盖 fe80::1 / fc00::1 /
  IPv4-mapped 私网 / 169.254.169.254 与 8.8.8.8 正例),直接加表驱动用例即可。
- **状态**:**未启动**(方案已修正,原 `is_global` 路线证伪)
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.5
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md T4)

### 3.6 `resource-frame` 外联收紧(T1)

- **目标**:提供设置项「禁用作者页外联」,把兼容取舍变为用户可选。
- **现状证据**:`api/security.rs` 的 resource-frame CSP 放开 connect/img/style/font/frame
  (`security.rs:329`:`connect-src https: http: blob:`);
  `resource_frame_template.html` 提供 `TavernHelper.generate`/`getPreset` 等 RPC。
  **2026-09-16 复核**:resource-frame CSP 仍全放开,**无「禁用作者页外联」设置项**
  → 未启动。关联遗留:**T1**。
- **改动点**:设置项「禁用作者页外联」(**默认保持现状 = 开**);关时收紧 CSP 的
  `connect-src` 并跳过 TavernHelper RPC 的外联部分。
- **验收断言**:关闭后 CSP 的 `connect-src` 不含 `https:`/`http:`;
  作者页 RPC 外联被跳过;默认值下行为逐字节不变;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(改动安全头与 RPC 面);回滚 = revert 设置项与 CSP 分支。
- **依赖 / 需确认**:设置项默认值须保持「开(=现状)」,避免兼容性变化;
  UI 文案须说明代价(部分作者页功能不可用)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.6
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md T1)

### 3.8 `kaleido_changelog` / `undo_snapshots` 增长上限

- **目标**:为两张只增不减的表补每会话/每角色保留上限。
- **现状证据**:现只在会话删除时清理;需补保留上限(参考 `exec_audit` 的 2000 行保留策略与
  `session_compactions` 的 20 条)。**2026-09-16 复核**:`kaleido_state_service.rs` 只有
  读取侧 `limit.clamp(1, 1000)`(`:234`),**无写入侧保留上限/清理策略**;
  `undo_service.rs` 无残留上限逻辑 → 未启动。
- **改动点**:补每会话/每角色保留上限,参考 `exec_audit`(2000 行)与
  `session_compactions`(20 条)的既有范式。
- **验收断言**:超限后旧记录被裁剪;裁剪策略有单测;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险低(纯清理策略);回滚 = revert 策略。
- **依赖 / 需确认**:保留上限取值需按业务可接受度确认(回退快照被裁剪会影响撤销深度)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.8

### 死配置与一致性 5 项(P2)

> 来源:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四。
> 每项均为「字段存在但仅存取无消费」或「双份定义漂移」,**须二选一:接线或标注废弃**,
> 不得继续留「看似可用」的字段。

#### DC-1 技能 `allowed_tools`/`run_as_subagent`/`model` 仅存取无消费

- **目标**:消除「看似可用实则不生效」的技能字段。
- **现状证据**:`services/skill_service.rs`、`skills` 表;**无生产消费点**
  (不会约束工具、不会转子智能体、不会切模型)。**2026-09-16 复核**:`allowed_tools`/
  `run_as_subagent` 仅出现在 `api/skills.rs:35,38,87,88`(写入)与迁移补列
  (`migration/ddl.rs:95,97`)、迁移测试;**无读取消费点**。
- **改动点**:二选一——**接线**(read 技能时按 `allowed_tools` 收窄工具集、
  `run_as_subagent` 走子智能体)或**标注废弃**(含 API/UI 说明)。
- **验收断言**:接线则新增消费点测试;标注废弃则 API/UI 有显式说明;
  全仓无「仅存取不消费」的字段残留。
- **风险 / 回滚**:风险中(接线会改变技能执行语义);按文件回滚。
- **依赖 / 需确认**:选接线还是废弃属产品决策,**须确认后动手**。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四

#### DC-2 MCP `ModeSettings` 覆盖层无效

- **目标**:接线按模式合并,或删除该覆盖层与对应测试。
- **现状证据**:`services/settings_service/params.rs` 的 `ModeSettings` 允许按模式覆盖
  `mcp_enabled/mcp_servers`,但装配读扁平快照 → **覆盖层无效**。
  **2026-09-16 复核**:`params.rs:437` 的 `for_mode` **确实会**合并
  `mcp_enabled`(`:550`)/`mcp_servers`(`:553`);但装配点
  `api/app_state.rs:460-466` 的 `start_mcp` 读的是
  `self.settings_snapshot()` 的**扁平权威值**,并注释明说「读扁平权威设置,
  **不按模式合并**——MCP 是进程级全局能力,不随请求模式切换」。
  即**覆盖层仍无效(但已由注释显式声明为有意)**,属部分完成(决策已定,字段未清)。
- **改动点**:二选一——**接线**(装配按 `for_mode` 合并)或**删除该覆盖层与对应测试**
  (并更新 `模式提示词边界.md` 的隔离规则)。若维持「进程级全局」定位,则应**删除**覆盖字段,
  避免留「看似可配」的字段。
- **验收断言**:选接线则有「按模式装配 MCP」的用例;选删除则字段与测试一并移除,
  `check-contract`/`check-arch` 通过。
- **风险 / 回滚**:风险中(MCP 装配是启动路径);回滚 = 还原字段与装配。
- **依赖 / 需确认**:**须确认** MCP 是否应随模式切换(现状注释主张「进程级全局」)。
- **状态**:**部分完成**(行为已由注释显式声明;覆盖字段尚未清理)
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md §三)

#### DC-3 `PermissionConfig.classifications` 无入口

- **目标**:开 API/UI 入口,或标注「保留字段,暂不可配」。
- **现状证据**:`tools/permissions.rs:43` 的 `classifications: BTreeMap<String, ToolRisk>`;
  `:94` 的 `risk_for` 会读它。**全仓无写入通路**(无 API、无 UI);
  生产中插件与 MCP 工具恒为危险级(等价硬编码)。
  **2026-09-16 复核**:`grep -rn "classifications"` 仅命中
  `permissions.rs:43,94,723`(定义/读取/测试夹具),**无 API/UI 写入点** → 未启动。
  与 [L8](#l8-授权自定义风险级无入口外部工具区域判定受限待办) 同一问题。
- **改动点**:开 API/UI,或标注「保留字段,暂不可配」。
- **验收断言**:接 API/UI 则有写入+生效用例;标注废弃则文档与字段注释显式说明。
- **风险 / 回滚**:风险中(让用户自定义风险级会扩大安全面);回滚 = 移除入口。
- **依赖 / 需确认**:与 L8 同源,若做应一并设计(需独立产品与安全设计)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四

#### DC-4 roleplay 默认词双份(措辞已漂移)

- **目标**:合并为单一常量,消除措辞漂移。
- **现状证据**:`settings_service/params.rs::default_roleplay_agent_prompt`(`:310`)
  vs `agents/engine/messages/build.rs` 内置兜底模板(`:156` 起)。
  **2026-09-16 复核**:两处仍并存——`params.rs:310` 的定义被
  `secret.rs:11,71`、`settings_service/mod.rs:25,993` 消费;
  `messages/build.rs:156` 仍有独立的内置默认模板(注释「用户清空自定义系统提示词时兜底」)。
- **改动点**:合并为单一常量(兜底改调同一函数);注意单测断言的是
  「五段落 + 四占位符」,措辞合并后复核。
- **验收断言**:全仓该默认词仅一处定义;`messages/build.rs` 的兜底单测
  (`:454,472,507` 附近)在合并后仍通过;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(改动默认提示词可能影响生成质量);回滚 = revert。
- **依赖 / 需确认**:合并后须复核「五段落 + 四占位符」断言与措辞一致性。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四

#### DC-5 tiktoken 对第三方模型的计数偏差

- **目标**:允许配置 tokenizer 或按 provider 比例校准,提升 trim/水位估算精度。
- **现状证据**:`services/token_service.rs` 的编码映射为精确匹配 → 前缀匹配 →
  **回退 cl100k_base**(`encoding_for_model`/`resolve_encoding_name`),第三方模型
  (deepseek 等)只能落回退编码,存在系统性偏差。
  **2026-09-16 复核**:`grep -rn "tokenizer" token_service.rs settings_service/*.rs`
  **零命中**(无配置项)→ 未启动。
- **改动点**:允许配置 tokenizer 或按 provider 比例校准。
- **验收断言**:配置后同一文本的计数按配置口径变化;回退路径行为不变;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险低-中(仅估算精度);回滚 = revert 配置项。
- **依赖 / 需确认**:需确认校准比例的取值来源(可配置 vs 内置表)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四

### 性能与容量 4 项(P2)

> 来源:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §五。

#### PC-1 token 计数每轮全量逐条(D3)

- **目标**:消除长会话下的重复计数开销。
- **现状证据**:`agents/engine/messages/context.rs`、`compaction.rs`。
  **2026-09-16 复核**:**已落地**——`services/token_service.rs` 新增
  `count_cache: HashMap<(String, u64, usize), i64>` 与 `COUNT_CACHE_CAP = 8192`
  (`:18-24`),`clear_count_cache`(`:111`)与命中/淘汰逻辑(`:120-131`)齐备,
  注释明确「2026-09-14」与提速依据(单次计数 29.4ms → 缓存后近似 O(N))。
  即 §五 原「先观察再加缓存」的建议**已被实施**(见 §九 裁定 5)。
- **改动点**(余量):清点是否所有长会话路径都走 `TokenService` 计数口径
  (是否仍有绕过缓存的直接调用)。
- **验收断言**:长会话下计数开销不随历史条数线性膨胀;缓存失效面有测试
  (`count_cache_returns_same_values_and_separates_models`、`count_cache_handles_empty_and_long_text`
  已存在);`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险低(缓存有界 8192);回滚 = revert 缓存。
- **依赖 / 需确认**:与 L23(手工参数计数)已修的口径保持一致。
- **状态**:**部分完成**(缓存已落地;余量为路径清点)
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §五
  (与 archive/2026-09-16-consolidation/威胁模型与残余风险.md D3)

#### PC-2 大文件拆分

- **目标**:按职责切分超千行文件,纯代码搬运提交(不改行为)。
- **现状证据**:源文档列 `services/memory_service.rs`(1612)、`task_engine/team.rs`(1488)、
  `ejs/exec.rs`(1312)、`engine/executor.rs`(1297)、`models/types.rs`(1277);
  前端 `sandbox/boot-script.ts`(907)、`api/types.ts`(870)、`TaskBoard.vue`(734)。
  **2026-09-16 复核实测**(`wc -l`):`memory_service.rs` **1661**、
  `task_engine/team.rs` **1580**、`ejs/exec.rs` **1313**、`engine/executor.rs` **1489**、
  `models/types.rs` **1398**;另新增 `world_book.rs` **1531**、`tool/agent_tools.rs` **1299**、
  `api/settings.rs` **1163**、`session_service.rs` **1040**、
  `settings_service/mod.rs` **1037**、`api/chat.rs` **1027**、`task_service/executor.rs` **1022**
  也超千行(前端三项与源文档一致:`boot-script.ts` 907、`api/types.ts` 875、`TaskBoard.vue` 734)。
  → **超千行文件数较源文档记录不降反升**(见 §九 裁定 7)。
- **改动点**:按职责切分 + 纯代码搬运提交(不改行为);`memory_service` 建议拆 4 件
  (见变更说明 §二注释)。
- **验收断言**:每项拆分提交后测试零修改(纯搬运)或同步更新;`cargo test -j 2` /
  `npm test -w web` 全绿;`check-arch` 通过(顶层模块归属不变,规则 I 不触发)。
- **风险 / 回滚**:风险中(大范围改 import);每个文件独立提交、独立回滚。
- **依赖 / 需确认**:`models/types.rs` 的拆分与 [架构分析 批次 3](#架构分析-2026-09-14-批次-3--消除重复实现)
  的 `step_runner`/重复实现收敛有交叉,须排先后(建议先收敛再拆)。
- **状态**:**未启动**(实测文件数较 2026-09 记录增加)
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §五

#### PC-3 三处数据迁移逻辑并存

- **目标**:收敛为 server-rs 单一实现,壳只做调用。
- **现状证据**:`launcher/src/main.rs`、`src-tauri/src/lib.rs`、`server-rs/src/migration/`
  三处并存。**2026-09-16 复核**:`launcher/src/main.rs:251` 的 `migrate_data_if_needed`(被 `:708` 调用)
  与 `src-tauri/src/lib.rs:542` 的 `migrate_data_if_needed`(被 `:254` 调用)仍是**两份独立实现**;
  `server-rs/src/migration/` 为第三处 schema 迁移 → 未启动。
- **改动点**:收敛为 server-rs 单一实现,壳只做调用;注意 launcher 零依赖约束。
- **验收断言**:壳层不再含迁移算法(仅调用);迁移行为一致(同一输入产出相同结果);
  双端构建通过。
- **风险 / 回滚**:风险中(启动路径与数据安全);建议独立分支试验。
- **依赖 / 需确认**:launcher 零依赖约束(不得引入 server-rs 依赖)——
  可能需要把算法下沉为「无依赖的共享实现」或改为调 server-rs 的独立命令。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §五

#### PC-4 `netstat -ano` 文本解析杀进程

- **目标**:换 Windows API 或把解析失败降级为「提示用户手动处理」。
- **现状证据**:`src-tauri/src/lib.rs`(`:63` 起)、`launcher/src/main.rs`(`:180` 起)
  均解析 `netstat -ano` 文本。**2026-09-16 复核**:两处仍是文本解析;
  `src-tauri/src/lib.rs:58` 注释已说明「依赖 netstat/taskkill,仅 Windows 桌面存在」
  → 未启动。
- **改动点**:换 Windows API(IP Helper / `GetExtendedTcpTable`),或至少把本地化解析失败
  降级为「提示用户手动处理」。
- **验收断言**:本地化系统(非英文 netstat 输出)下不再静默失败;
  解析失败时给出用户可操作提示;`cargo check` 通过。
- **风险 / 回滚**:风险中(单实例端口接管路径);回滚 = revert 解析层。
- **依赖 / 需确认**:Windows API 路线需新增 `windows-sys` 特性面,须过 `check-lock-sync`。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §五

### 6.4 Android 真机验证

- **目标**:在真机上验证 Shizuku / ROOT 三档执行器,并顺手验证 `\u{1f}`(见 3.4)修复。
- **现状证据**:`archive/2026-09-16-consolidation/android-port-plan.md` 阶段 5:
  Shizuku / ROOT 三档执行器**未真机验证**(模拟器不可验);`\u{1f}` 修复待验证。
  沙箱档与桌面路径已端到端实测。
- **改动点**:准备 root 真机(或 Shizuku 可用设备),逐档验证
  ROOT → Shizuku(DEBUGGER) → 沙箱 的探测与执行;验证 `\u{1f}` 修复后字段不错位。
- **验收断言**:三档执行器在真机上各自可探测、可执行、可审计落库;
  `exec` 授权面板展示完整命令原文 + 执行器等级 + 风险标签;
  `\u{1f}` 修复后含该字符的命令字段不错位。
- **风险 / 回滚**:风险中(root 通道安全面);回滚 = 关闭 ROOT/Shizuku 通道(设置项)。
- **依赖 / 需确认**:**是否有 root 真机可测?**(`android-port-plan.md` §六 待确认第 2 条
  至今未确认)。3.4 的验证依赖本项。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §六 · 6.4
  (与 archive/2026-09-16-consolidation/android-port-plan.md 阶段 5)

### 6.6 提示词预览补宏展开(可选)

- **目标**:前端可拿到「所见即所发」的预览。
- **现状证据**:`api/settings.rs::prompt_preview`(`:902` 起)现在**不做宏展开**。
  **2026-09-16 复核**:`grep -n "expand" server-rs/src/api/settings.rs` **零命中**
  → 未启动。
- **改动点**:若前端需要「所见即所发」,可加 `?expand=1` 参数。
- **验收断言**:带 `?expand=1` 时预览含展开后的宏值;不带时行为逐字节不变;
  `check-contract.mjs` 通过(不新增字段集合变化时)。
- **风险 / 回滚**:风险低(可选参数);回滚 = 移除参数分支。
- **依赖 / 需确认**:属**可选**项;须确认前端是否需要该能力后再做。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §六 · 6.6

---

## 四、二维自定义工作流

> 承自 `archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md`(分析稿,**未开工**)。
> 范围:把现有「一维自定义 Agent 流程」升级为类 ComfyUI 的二维节点图,并补齐节点级配置、
> 严格/宽松节点、强制/对比选择机制、多套 API 与多套流程的存储与导出。
> 代码锚点以 `file:line` 给出,行号基于 2026-09-15 的 `main`。
>
> **三条前置判断(原文移植)**:
> 1. **需求 2 的「节点级模型」是全局架构瓶颈**。当前引擎只持有一个全局连接器实例,
>    聊天与任务共用;要支持「节点 A 用 A 家模型、节点 B 用 B 家」必须先做需求 5 的多套 API,
>    并把连接器选择从「进程级单例」下推到「请求/节点级」。
> 2. **需求 3 的「严格节点嵌入宽松节点」和需求 4 的「流程类工具化调用」应该合并设计**,
>    合成一套机制后实现量约为分别实现的一半。
> 3. **这不是一个批次能落地的事**——约 7 个批次,其中 2 个架构级重构、1 个安全敏感改动。
>
> **批次依赖**:批次 1 与 4 可并行(无交集);批次 3 必须等批次 1 的数据模型冻结。
> **里程碑**:一(批次 0~2)后端二维化 → 二(批次 3)画布可用 → 三(批次 4~5)多套 API 与节点级模型
> → 四(批次 6~7)严格/宽松嵌入与对比模式。
> **建议的第一个动作**:不要从画布开始,先做**批次 1**(它是唯一能反过来证伪整个方案的一步)。

### 二维批次 0 修 `SettingsHub` 的 `flow` 入口错位

- **目标**:任务模式下也能看到「执行流程」编辑区(custom 任务依赖它)。
- **现状证据**:`SettingsHub.vue:125` 把 `'flow'` 列入 `ROLEPLAY_ONLY`,任务模式看不到
  「执行流程」编辑区——而 custom 任务恰恰依赖它。这是**现存的入口错位缺陷**,做画布前必须先修。
  (关联风险 6:`二维自定义工作流` §4.2 中风险第 6 条。)
- **改动点**:把 `'flow'` 从 `ROLEPLAY_ONLY` 移出(或在任务模式下单独放行该分区)。
- **验收断言**:前端单测——任务模式下流程编辑区**可见**;`npm test -w web` 全绿;
  `npm run typecheck -w web` 通过。
- **风险 / 回滚**:风险低(单点 UI 分支);回滚 = revert `SettingsHub.vue`。
- **依赖 / 需确认**:无(建议作为整个二维计划的第一个动作)。
- **状态**:**已完成**(2026-09-16;`ROLEPLAY_ONLY` 缩为 `['prompt', 'preset']`,`'flow'`
  在两种模式下均可见。`SettingsHub.test.ts` 补 2 例锁定「任务模式下『执行流程』可见」与
  「角色扮演专属域仍整体隐藏」;实测把 `'flow'` 放回集合时前者即 FAIL(判别性已验证)。
  `npm test -w web` 854 例全绿、`npm run typecheck -w web` 通过。修复记录见
  `功能-变更史.md` §设置入口;`遗留.md` 的 IFW-3 已按 R6 移入 §九 已封堵项索引)
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 0

### 二维批次 1 图数据模型(规模 M,1~3 天)

- **目标**:在 `PlanStep` 上加二维字段,custom 执行器改拓扑序串行,行为与现在等价。
- **现状证据**:
  - `PlanStep` 是**双路径共享契约**:任务 custom(`task_engine/custom.rs:236`)与聊天
    `agent_mode=custom`(`api/chat.rs:178-206`、`agents/planner.rs:82-98`)吃同一份结构;
    改它必须保证聊天路径不退化。
  - 流程定义**不在 DB**:落 `data/agent_flows.json`,且被数据合并白名单引用
    (`migration/merge.rs:619,738`)——格式变更需走版本迁移。
  - `PlanStep` 现有节点级字段:`temperature`/`max_tokens`/`tools`/`tool_choice`/
    `parallel_tool_calls`(`models/types.rs:219-236`),已接线并在跑(`custom.rs:113-122`)。
  - 执行器缝 + 上下文:`task_engine/executor.rs:13-18`、`task_engine/context.rs:10-26`。
- **改动点**(按决策 D2/D3/D4/D5 的建议方案):
  1. `PlanStep` 加 `x`/`y`/`inputs: Vec<String>`(上游节点 id)/`is_output: bool`/`kind`;
     `inputs` 缺省为空 → 语义回退为「按数组顺序串联」,**存量流程 JSON 逐字节兼容**。
  2. 多父节点输入合并:user 消息 = 任务目标 + **按父节点在数组中下标升序**逐段拼接
     「父节点名 + 产出」(顺序必须确定,否则同一流程两次运行结果不可复现)。
  3. 最终成果节点:显式 `is_output` 优先,未标注时取无后继汇点(存量线性流程语义不变)。
  4. 环检测:校验期(`validate_flow` 扩展)与运行期双重拦截;自环/不可达节点同样拒绝。
  5. custom 执行器改**拓扑序串行**(Kahn 算法 + 就绪队列),行为与现在等价;
     聊天 custom 路径把图**按拓扑序线性化**即可继续工作(分支被忽略,需在文档声明)。
  6. `agent_flows.json` 走 v2 迁移,旧 JSON 加载迁移幂等。
- **验收断言**:**存量线性流程跑通且结果逐字节不变**;环/孤立点被拒并给中文错误;
  旧 JSON 加载迁移幂等;`cargo test -j 2` 全绿(含聊天 `agent_mode=custom` 回归)。
- **风险 / 回滚**:风险中-高(共享契约,改动可能悄悄破坏聊天 custom 路径——§4.2 高风险第 4 条);
  **每批都必须跑聊天侧回归**。回滚 = revert 单个 commit(数据模型 + 校验 + 执行器)。
- **依赖 / 需确认**:依赖批次 0。**批次 3 必须等本批次数据模型冻结**。
  决策点见 [§四·D1~D8](#四二维自定义工作流决策点d1d8)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 1

### 二维批次 2 图执行器并行(规模 L,3~10 天)

- **目标**:图节点可并行执行,并保证并发下的状态与取消语义正确。
- **现状证据**:
  - 并行编排先例:`task_engine/team.rs`(JoinSet 并行主 agent)——图并行节点可照抄该并发模型。
  - **已识别的真实缺陷风险**:`set_plan` 是「整列 JSON 覆写」。多个节点并发完成时,
    各自持有 `plan` 的旧快照做读-改-写,会**丢失更新**(节点 A 的 Done 被节点 B 的写入覆盖)。
  - `TaskStep` 现在只有 `name/goal/status/result` 四个字段(`models/types.rs:927-942`),
    而六模式文档把它列为「不动(兼容承诺)」。
- **改动点**:
  1. 图执行器**并行**:JoinSet 调度 + `max_parallel_nodes` 上限;
  2. `TaskStep` 加 `node_id: Option<String>`(serde default)做画布映射——**加性改动**,
     旧客户端反序列化不受影响,**但需同步修订文档中的兼容承诺表述**;
  3. plan 写入串行化:**v1 建议 (a)** 在服务层对 plan 写入做串行化(成本最低,plan 体积小),
     **并补一条并发回归测试**;(b) 新建 `task_nodes` 表做行级更新成本高,为将来留路;
  4. **取消传播到并行分支**;
  5. 并发回归测试。
- **验收断言**:并发 N 节点无丢更新(专测);取消时全部分支收口;usage 汇总正确;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险高(并发丢更新是真实缺陷风险);回滚 = revert(执行器 + plan 写入串行化)。
- **依赖 / 需确认**:依赖批次 1。决策 D5(plan 写入方案)、D8(是否要并行)须确认。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 2

### 二维批次 3 前端画布(规模 L,3~10 天)

- **目标**:引入图库,提供节点检查器、连线校验、存量流程自动布局。
- **现状证据**:
  - 动画/画布**不存在**:`web/package.json` 无任何图库。
  - 已验证 npm registry 可达,`@vue-flow/core` 最新 `1.48.2`(MIT,Vue 3 原生),
    直接提供拖拽、连线、端口(handle)、缩放/平移、minimap——自研等价物是数周工作量。
  - 仓库当前依赖极精简且有审计门禁;新依赖需过一次 lint/audit/CI。
  - **画布在 jsdom 下渲染受限**,前端测试应断言**数据层与交互逻辑**,而非画布像素。
- **改动点**:引入 `@vue-flow/core` + `background` + `controls` + `minimap` 四个包;
  节点 Inspector(逐字段编辑);连线合法性校验;存量流程自动纵向布局;
  前端数据层/交互测试。
- **验收断言**:`npm test -w web` 全绿;`check:lint`/`check:lock` 过闸;
  连线成环被前端拦截;**测试不断言像素/布局坐标**。
- **风险 / 回滚**:风险中——新前端依赖过审计门禁(§4.2 中风险第 8 条);
  画布测试受限(第 9 条)。回滚 = 移除依赖与画布组件。
- **依赖 / 需确认**:**必须等批次 1 的数据模型冻结**;新依赖需过 `npm run check:arch` /
  `check:lock` / `check:lint`。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 3

### 二维批次 4 多套 API + 凭据加密泛化(规模 L,3~10 天,**独立**)

- **目标**:支持多套连接配置,并把凭据加密逻辑从「按字段名硬编码」泛化为数组遍历。
- **现状证据**:
  - `RuntimeSettings` 现在只有单份 `openai_base_url / openai_api_key / model`
    (`settings_service/mod.rs:72-75`),且 `docs/模式提示词边界.md` 明确
    「连接信息始终全局共享,不入覆盖层」(§一)。要多套必须改动这条设计承诺。
  - `Connector` 是 `Clone` 的实例枚举(`connectors/mod.rs:27-30`),
    `build_connector` 是纯工厂(`:114-122`),**天然支持多实例**,不需要改连接器层。
  - **凭据加密是最大的技术风险点**:现有加密逻辑是**按字段名硬编码**的——
    `secret.rs:183-186` 只处理 `openai_api_key` / `embedding_api_key`,
    `read_preserved_ciphertexts` 的 `pick("openai_api_key")` 同样是固定键名
    (`secret.rs:203-238`)。
- **改动点**:
  1. 数据形态:`connections: Vec<ConnectionProfile>` 新增;
     `active_connection_id: Option<String>` 新增(默认连接);
     **保留扁平 `openai_base_url/api_key/model` 作为兼容字段**。
     `ConnectionProfile { id, name, connector_type, base_url, api_key, model, enabled }`;
     `connector_type` 复用 `available_connector_types()`(`connectors/mod.rs:133-137`)。
  2. 加密泛化(键名硬编码 → 数组遍历)+ 迁移幂等播种。
  3. **必须继承既有的两条不变量**(已有测试锁死在 `secret.rs:266-280`):
     - **解密失败/空值时必须保留磁盘原密文**,绝不能把密钥擦成空串——
       否则用户重装/换机器唤醒 DPAPI 失败时会**静默丢失所有 API Key**;
     - **落盘不得出现明文**(迁移旧明文配置的路径见 `secret.rs:146-162`)。
  4. **导出默认不含密钥**;需要含密钥的导出走**显式二次确认**的独立选项,
     文件名加 `-含密钥` 标识;无论如何**不得**写入日志或错误消息。
- **验收断言**:
  - 保存后读原始 JSON,断言无任何 profile 的 key 以明文出现;
  - 读入时 key 为空的 profile,其磁盘密文保持不变(不擦除);
  - 旧单份配置 → 首次读入时自动播种为 `connections[0]` 且**幂等**(重复读不重复播种);
  - `embedding_api_key` 行为不变(回归);
  - `cargo test -j 2` 全绿;**批次 4 涉及凭据,必须额外人工检查:落盘 JSON 无明文、日志无 Key**。
- **风险 / 回滚**:**高**——多套 API 的凭据静默丢失(§4.2 高风险第 2 条);
  回滚 = revert 代码(注意已播种的 `connections` 数组需兼容读回扁平字段)。
- **依赖 / 需确认**:独立于批次 0~3(可与批次 1 并行)。决策 D7(导出是否含密钥)须确认:
  **默认不含** + 显式二次确认才含。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 4

### 二维批次 5 节点级模型 + 强制模式(规模 XL,>10 天)

- **目标**:同一任务内不同节点可走不同 provider;任务可绑定指定流程。
- **现状证据**:
  - 节点级模型依赖链(§3.2):① 多套 API 配置(批次 4);② 连接器注册与缓存——
    引擎只有一个 `Arc<RwLock<Connector>>`(`agents/engine/mod.rs:78,131`);
    ③ 请求级路由:`GenerationParams`(`types.rs:374-389`)**未实现 Serialize**,
    可直接加字段而不动线格式;④ 可观测性:调用追踪的 `model` 列现在恒取当前全局模型
    (`custom.rs:163`),须改为逐节点真实模型。
  - **任务不绑定流程**:`POST /api/tasks` 只有 `title/character_id/task_mode`
    (`api/tasks.rs:16-25`),custom 执行时现读全局 `current_flow_id`
    (`task_engine/custom.rs:236`)——所以「不同任务用不同流程」现在做不到。
  - `TaskFlowAccess::current_flow`(`task_core/backend.rs:153-156`)。
  - 流程快照先例:`AgentRunRequest.flow` 就是快照(`engine/types.rs:38`)。
- **改动点**:
  1. 连接器解析器(profile id → `Arc<Connector>`,带缓存),注入 `AgentEngine` 与
     `TaskService`;**不要**在 `TaskService` 里另建一份连接器缓存(否则数据源分裂);
  2. `GenerationParams.connection_id: Option<String>`(未实现 Serialize,不动线格式);
  3. 节点级 `max_context`(请求侧通道
     `AgentRunRequest.max_context_tokens: Option<u32>` 已在,`engine/types.rs:35`)、
     节点级 `max_tool_rounds`(`GenerationParams.max_tool_rounds` 已是参数,`types.rs:383`);
  4. 调用追踪 `model` 列改逐节点真实值;
  5. `CreateTaskBody` 增加可选 `flow_id`(可选 = 旧客户端行为不变,回退全局当前流程);
     `tasks` 表加 `flow_id` 列(走既有 `ensure_*` 幂等迁移范式,
     `migration/ddl.rs` + `models/db/mod.rs:81-108` 注册调用点);
     `TaskFlowAccess::current_flow` 扩为「先按任务绑定,回退全局」;
  6. **流程快照**:在任务创建时把流程定义快照落库(或至少落 `flow_id` + 版本),
     否则用户执行中途改流程会导致任务行为漂移;
  7. 前端:任务创建处加流程选择器。
- **验收断言**:同一任务内两个节点走不同 provider 且**调用面板显示各自真实模型**;
  任务绑定流程后改全局流程**不影响**该任务;`cargo test -j 2` + `check-contract.mjs` 全绿。
- **风险 / 回滚**:风险高(架构级重构 + 触及 `tasks` 表 schema);
  回滚 = revert 代码 + 新增列留在库中无害。
- **依赖 / 需确认**:**依赖批次 4**;决策 D1(需求 2「使用的模式」的含义)须先确认——
  推荐 **(甲)执行档位=严格/宽松**,与需求 3 合并。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 5

### 二维批次 6 严格/宽松 + 静态子图(规模 M,1~3 天)

- **目标**:把现有两个生成分支提升为节点的一等属性,并支持静态子图嵌套。
- **现状证据**:
  - 语义定义(建议固化进文档):**严格节点** = 单次模型调用即完成,**不进入工具自循环**
    (定位是确定性、低成本的原子步骤);**宽松节点** = 允许多轮完成,即现有
    `run_tool_loop` 语义,工具轮次上限取 `max_tool_rounds`(`engine/executor.rs:475-476`);
    **嵌入** = 宽松节点内含严格节点组成的子图。
  - 现有代码映射很直接:严格 = `generate_text` 分支(`custom.rs:289-307`),
    宽松 = `run_tool_loop` 分支(`custom.rs:311-317`)。所以这**不是新执行范式**。
  - **推荐 v1 只做「静态子图」**:不触碰引擎工具分派内核,风险小、可独立验证;
    动态调用留给批次 7,届时两者共用同一套「子执行 + 深度守卫 + 记账」原语。
- **改动点**:`kind: Strict | Loose`(严格=单次调用,宽松=工具自循环);子图执行;
  深度守卫;嵌套记账(口径 D6:(a)编码进 `phase` 字符串,零 schema 改动)。
- **验收断言**:**严格节点不进入工具循环(断言只 1 次模型调用)**;子图产出正确注入;
  超深嵌套被拒;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中-高(需改引擎工具分派内核——§3.3);回滚 = revert 分支改造。
- **依赖 / 需确认**:依赖批次 2;决策 D1(模式含义)、D6(嵌套记账口径)须确认。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 6

### 二维批次 7 对比模式 + 导出整合(规模 L,3~10 天)

- **目标**:模型可在任务中类工具化调用流程名单;导出/导入不产生重复 id。
- **现状证据**:
  - **不要**在运行期往全局工具注册表里注册/注销流程对应的工具:理由——
    `ToolRegistry` 是全局单例,`register_external` 会失效 `definitions_cache`
    (`tools/registry.rs:123-130`);并发任务各自注册会造成缓存抖动与竞态,
    且任务结束时的注销时机会与仍在跑的并发任务冲突。
  - **正确做法**:注册一个稳定的内置工具 `run_flow`(启动时注册一次),参数携带目标流程名/节点名;
    运行期通过 `params.tools` 是否包含它来控制「可见性」——
    `GenerationParams.tools` 是**逐次调用传入的定义列表**(`types.rs:378`,
    消费点 `custom.rs:91-99`),天然支持按任务差异化下发,完全不碰全局注册表。
  - 工具实现取上下文的路径已有先例:`ToolContext { session_id, character_id, agent_depth }`
    (`types.rs:560-568`);`agentgo`/`run_subtask` 已是同类实现
    (`tools/agent_tools_agent.rs`,依赖经 `ToolDeps` 的 `OnceLock<Weak<...>>` 后注入)。
  - `session_id` 格式 `task:{id}:step:{n}`(`custom.rs:123`),可反解任务 id 再查流程名单。
  - 流程导入恒分配新 id(`useAgentFlow.ts:154`),「批量导出 → 导入合并」需明确策略。
  - **已有实测先例**:普通工具循环的重复调用熔断(`engine/executor.rs:478-481` 注释记载
    实测 7 分钟烧 150 万 token 未收敛)。
- **改动点**:
  1. `run_flow` 内置工具(启动时注册一次)+ 按流程生成工具定义并释放名单;
     建议走「每流程一个工具定义」(OpenAI 兼容协议下模型对多个平铺工具的选择准确率
     高于一个带枚举参数的工具);
  2. **必须同时加的三道闸**(否则是烧钱/死循环事故):
     - **深度守卫**:子流程调用 `agent_depth + 1`,沿用现有 `subagent_max_depth` 口径;
     - **流程级环检测**:A 调 B、B 调 A 必须在**调用链**上拦截(图内环检测挡不住跨流程环);
     - **每任务调用预算**:总节点调用次数上限,`run_flow` 必须纳入同一熔断或更严的预算;
  3. **权限边界**:被调流程内部的工具集仍必须经 `task_tool_policy` 编译后再收窄
     (参照 `custom.rs:85-99` 现有口径),**不得**因为「是子流程」而放宽;
  4. 名单来源:任务级(或设置级)的流程 id 列表,建议随任务落库(`flow_ids` JSON 列),
     与强制模式的 `flow_id` 同批迁移;
  5. 前端:任务创建处选择「强制 / 对比」+ 名单多选;
  6. 批量导出/导入合并策略。
- **验收断言**:模型可自主调用名单内流程;**名单外流程被拒**;A↔B 互调被拦;超预算熔断;
  **批量导出→导入不产生重复 id**;
  **记账不变量**:「`/calls` 各行 token 求和 == 详情 `usage_total`」不破——嵌套记账若漏行会
  直接破坏该不变量,**必须补集成测试**;`cargo test -j 2` + `check-contract.mjs` 全绿。
- **风险 / 回滚**:风险高(嵌套调用烧钱/死循环,§4.2 高风险第 3 条);回滚 = 注销 `run_flow`
  并从名单移除(内置工具保留但不可见)。
- **依赖 / 需确认**:依赖批次 5、6;决策 D6(嵌套记账口径)须确认。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五 · 批次 7

### 四·二维自定义工作流决策点(D1~D8)

> 来源:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §4.1。
> 均为**需要拍板**的决策点,附原文建议;未确认前不应进入对应批次。

| # | 决策 | 选项 | 建议(原文) |
|---|---|---|---|
| D1 | 需求 2「使用的模式」的含义 | (甲)执行档位=严格/宽松 /(乙)节点内跑 solo/multi/... 任务模式 /(丙)roleplay/task 顶层模式 | **(甲)**。与需求 3 合并,实现量最小、语义自洽。(乙)会把范围显著放大;(丙)会破坏 `模式提示词边界` 的类型级隔离不变量,**不建议** |
| D2 | 图数据模型 | (A1)扩 `PlanStep` 加 `x/y/inputs` /(A2)新增 `graph{nodes,edges}` 并存 | **(A1)**。`inputs` 缺省回退线性,存量流程零迁移;A2 必然双事实源漂移 |
| D3 | 多父节点输入合并顺序 | 按下标升序 / 按连接创建序 / 仅取第一个 | **按下标升序**(确定、可复现) |
| D4 | 最终成果节点 | 显式 `is_output` / 无后继汇点自动判定 | **两者结合**:显式标注优先,未标注时取汇点(存量线性流程语义不变) |
| D5 | 节点运行时状态存储 | (a)串行化 `tasks.plan` 覆写 /(b)新建 `task_nodes` 表 | **(a)** v1;plan 体积小,成本最低 |
| D6 | 嵌套记账口径 | (a)编码进 `phase` 字符串 /(b)新增 `node_path` 列 | **(a)**;零 schema 改动,与既有 phase 命名习惯一致 |
| D7 | 导出是否含 API 密钥 | 默认不含 / 默认含 | **默认不含** + 显式二次确认才含。凭据外泄不可逆 |
| D8 | 是否要节点并行执行 | 要 / 只要拓扑序串行 | **要**,否则二维图相对一维只多一个画布;但须配并发上限与并行成本提示 |

### 四·二维自定义工作流产品风险(建议写进 UI 文案)

- **二维画布对普通用户是负担**(§4.2 产品风险第 10 条):ComfyUI 式画布的价值在表达能力,
  代价是门槛。建议**保留线性列表为默认视图**,画布作为「高级模式」切换;
  存量流程进入画布时自动布局为纵向链,视觉上仍是一维,不惊吓用户。
- **并行 = 成本倍增**(第 11 条):并发节点数应默认保守(如 2),并在 UI 提示
  「并行节点会成倍消耗 token」。
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §4.2

### 四·二维自定义工作流每批次必须跑的验证(仓库既有口径)

```powershell
cargo test --manifest-path server-rs/Cargo.toml -j 2   # 环境先加载 vcvars64
npm test -w web
npm run build -w web
node tools/check-arch.mjs
```

- `cargo test` **必须带 `-j 2`**(本机默认并行度下 rustc 会崩,见 `../AGENTS.md`);
- 本计划新增文件都落在既有顶层目录(`services/`、`components/` 等),
  **不需要**改 `tools/arch-layers.json`(它登记的是顶层模块);若将来新增顶层目录,
  规则 I 会直接判 FAIL,须同步登记;
- 改完代码后 `dist\` 与 `Kedai.exe` **不会自动更新**,交付前跑 `.\build.ps1`;
- 批次 4 涉及凭据,必须额外人工检查:落盘 JSON 无明文、日志无 Key。
- **来源**:archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §五

---

## 五、Android 移植阶段 5

> 承自 `archive/2026-09-16-consolidation/android-port-plan.md` 阶段 5 待办与阶段 0/2 未完成项。
> 状态基线:阶段 0/1/2/3/4 已完成,**阶段 5(验证、性能与发布)进行中**;
> 已产出 arm64/x86_64 release APK。**沙箱档与桌面路径已端到端实测;ROOT/Shizuku 运行时待真机验证**
> (模拟器不可验)。
>
> **与本文其他章节的交叉**:`\u{1f}` 修复的验证见 [3.4](#34-android-1f-分隔符t3) 与
> [6.4](#64-android-真机验证);T3 的分隔符改动方案与本阶段真机验证互为前置。

### A5-1 Phase 2 release 版 `network_security_config.xml` 只放行 `127.0.0.1`

- **目标**:release 构建(loopback HTTP 明文)不再白屏,同时不用全局开门。
- **现状证据**:release 构建下 `usesCleartextTraffic=false`(`app/build.gradle.kts:20`),
  而 loopback HTTP 需要明文放行;debug 构建默认 `true` 故验证通过;
  **release 版必须补 `network_security_config.xml` 只放行 `127.0.0.1`**,
  否则正式包会白屏(Phase 2 待办)。
  **2026-09-16 复核**:**已完成**——
  `src-tauri/gen/android/app/src/main/res/xml/network_security_config.xml` 已存在,
  内容为「只对 `127.0.0.1`/`localhost`/`::1` 放行明文,`base-config cleartextTrafficPermitted="false"`」;
  `AndroidManifest.xml:27` 已引用 `android:networkSecurityConfig="@xml/network_security_config"`
  (见 §九 裁定 6)。
- **改动点**(已完成,余量为**回归确认**):确认 release 签名包上 loopback 加载正常;
  该文件位于 `src-tauri/gen/android/` 内,**重新 `tauri android init` 会被覆盖,需重打补丁**
  (与 `BuildTask.kt`、gradle 镜像同属「重新 init 即失效」清单)。
- **验收断言**:release 签名包启动后 `http://127.0.0.1:<port>/` 加载成功(非白屏);
  非 loopback 域名仍强制 HTTPS。
- **风险 / 回滚**:风险低(已落地);回滚 = 移除该引用(会退回白屏,不可取)。
- **依赖 / 需确认**:须在**每次** `tauri android init` 后核对本文件仍在(易被覆盖)。
- **状态**:**部分完成**(配置文件与 Manifest 引用已落地;release 包端到端确认待随发布做)
- **来源**:archive/2026-09-16-consolidation/android-port-plan.md §五之二 与 §五之四

### A5-2 阶段 0 Spike 3(Android Keystore JNI)未做

- **目标**:验证 Android Keystore 经 JNI 的读写最小闭环(为阶段 1 第 2 项探路)。
- **现状证据**:`android-port-plan.md` 阶段 0 执行结果明写「**阶段 0 未完成项:**
  Spike 3(Android Keystore JNI)尚未做」。但**阶段 1 的密钥存储实际已落地并通过上机验证**:
  Kotlin `KeystoreBridge.kt`(AES-256-GCM)+ Rust `services/keystore_android.rs`(两个 JNI 约束:
  `JNI_OnLoad` 缓存 `GlobalRef` 类加载器、VM 指针经 `JNI_OnLoad` 传入)已实测
  `PUT /api/settings` → 200、落盘 `enc:v1:…`、重启后 `has_api_key: true`。
  **即 Spike 3 的目的已由阶段 1 的落地实现达成**,但作为「独立 spike 结论报告」这一交付物未单独立项。
- **改动点**:决定是否需要补一份独立的 Spike 3 结论记录(把阶段 1 的实测证据归位),
  或将本项标注为「目标已由阶段 1 实现达成」并关闭。
- **验收断言**:Keystore 读写最小闭环有可复现证据(阶段 1 §五之三 的上机验证表即为该证据);
  release 签名包上 Keystore 路径实测通过(阶段 1 §五之五 已记录 R8 混淆修复后的实测)。
- **风险 / 回滚**:无(纯记录性)。
- **依赖 / 需确认**:需确认「闭环已由阶段 1 达成」是否可作为 Spike 3 的关闭依据。
- **状态**:**部分完成**(实现与实测已达成 Spike 目的;独立 spike 报告未单独立项)
- **来源**:archive/2026-09-16-consolidation/android-port-plan.md 阶段 0(未完成项)与 §五之三

### A5-3 ROOT/Shizuku 三档执行器真机未验证

- **目标**:在真机上验证 ROOT → Shizuku(DEBUGGER) → 内置 busybox → 禁用 的逐级探测与执行。
- **现状证据**:阶段 3 已实现并通过编译验证(Exec 抽象层 `services/exec/`、
  命令分级与硬门 `tools/command_risk.rs` + `permissions`、审计表与 API、
  Kotlin `ShellExecutorBridge` 三档探测 + exec + Shizuku、前端授权面板);
  **ROOT/Shizuku 运行时待真机验证**(模拟器不可验);沙箱档与桌面路径已端到端实测。
  与本文设计的差异:① Shizuku 改用**反射**访问(不引入编译期依赖,未安装时静默降级);
  ② 沙箱档用系统自带 `sh -c`,**未打包 busybox**(留作可选增强)。
- **改动点**:准备 root 真机(或 Shizuku 可用设备),逐档验证探测优先级与执行;
  核对 Shizuku 反射路径在真机可用。
- **验收断言**:ROOT 档可提权执行并逐条确认;Shizuku 档可用(未安装时静默降级不崩);
  沙箱档保底可用;三档均经审计落库;UI 显示当前执行器等级且可一键关闭通道。
- **风险 / 回滚**:风险高(root 能力合规与安全);默认只开沙箱档,
  ROOT/Shizuku 由用户显式开启;回滚 = 关闭对应档位。
- **依赖 / 需确认**:**是否有 root 真机可测?**(§六 待确认第 2 条,至今未确认)。
  与 [6.4](#64-android-真机验证) 为同一验证动作。
- **状态**:**未启动**(模拟器不可验的形态)
- **来源**:archive/2026-09-16-consolidation/android-port-plan.md 阶段 3 执行状态与 §六

### A5-4 `\u{1f}` 修复顺带验证

- **目标**:在真机验证三档执行器时,顺带验证 `\u{1f}`(3.4)的修复。
- **现状证据**:`残余任务与后续计划-2026-09-13.md` §六 6.4 明写「顺手验证 `\u{1f}`(3.4)修复」。
  3.4 本身**未启动**(见 [3.4](#34-android-1f-分隔符t3)),
  故本项为**前置未满足**状态:修复未做,无从验证。
- **改动点**:先完成 3.4(长度前缀/base64 双侧同改),再在本阶段真机上验证含 `\u{1f}`
  的命令字段不错位。
- **验收断言**:含 `\u{1f}` 的命令在真机上执行正确、`cwd`/`timeout` 不错位;
  双侧编解码对拍测试通过。
- **风险 / 回滚**:风险中(双侧 JNI 协议);回滚 = 双侧一起 revert。
- **依赖 / 需确认**:**依赖 3.4 完成**;与 A5-3 同批真机执行。
- **状态**:**未启动**(前置 3.4 未做)
- **来源**:archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §六 · 6.4

### A5-5 MVP 是否含 root/Shizuku/MCP 待确认

- **目标**:确认 MVP 范围是否包含 root/Shizuku/MCP,仅沙箱 shell 是否可接受。
- **现状证据**:`android-port-plan.md` §六「待确认的前置条件」第 3 条:
  「是否接受 MVP(阶段 0–2+4,约 5–7 周)**不含 root/Shizuku/MCP,仅沙箱 shell**,
  执行层作为独立阶段 3 追加?」——**仍待确认**。第 2 条「是否有 root 真机可测?」同样未确认。
  (注:实际执行时阶段 3 已落地实现,故本决策点的现实形态已变为
  「阶段 3 是否纳入正式发布范围」。)
- **改动点**:确认发布范围与 Play 上架策略:含 root/Shizuku 能力建议自签 APK 侧载;
  上架需另做「无 root 能力」Play 变体。
- **验收断言**:范围决策落文档;若含 root/Shizuku,发布说明写清默认关闭与风险;
  若上架 Play,产出「无 root 能力」变体的构建配置。
- **风险 / 回滚**:风险中(产品与合规决策)。
- **依赖 / 需确认**:**此为本节唯一的产品决策阻塞点**,须用户拍板。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/android-port-plan.md §六 待确认第 2/3 条

---

## 六、架构与工程化欠账

> 汇集四类来源:(a) `ARCHITECTURE-3H.md §8` 的知情接受遗留;
> (b) `架构分析-2026-09-14.md §6` 的落地路线图批次 3~5;
> (c) `优化实施方案-2026-09.md` 附录 C 未达项;
> (d) `三结合落实核对表.md` 的「未做项与理由」8 项;
> (e) `前端工具链与审计升级-变更说明.md` §七;
> (f) `任务引擎六模式.md` 的「尚未落地」项;(g) `known-limitations.md` 的待办项。

### ARCHITECTURE-3H §8-1 步进循环三份

- **目标**:抽 `step_runner`,统一 legacy/plan/custom 三模式的步进与取消口径。
- **现状证据**:legacy/plan/custom 各写一遍「置 running→写库→失败继续」;
  取消兜底三种口径(plan 扫 Pending、team 扫 Running、custom 直接 return)。
  **2026-09-16 复核**:`services/task_engine/step_runner.rs` **不存在** → 未启动。
- **改动点**:抽 `services/task_engine/step_runner.rs`,三模式改调;取消口径统一。
- **验收断言**:三模式行为等价(逐字节不变为硬要求);取消回归测试;
  `cargo test -j 2` 全绿;`check-arch` 规则 C 不反弹。
- **风险 / 回滚**:风险中——会同时改动三个**已稳定运行**的模式,收益(一致性)低于回归风险;
  **须在 `tools/` 收口后执行**。回滚 = revert 单 commit。
- **依赖 / 需确认**:与 `B.4b` 同一事项(见下);
  `架构分析-2026-09-14.md §6 批次 3` 第 4 条与 `§6 批次 5` 均指向本项。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §8

### ARCHITECTURE-3H §8-2 `models/types.rs` 混装传输类型

- **目标**:把传输类型从数据契约文件中拆出,并保持线格式不变。
- **现状证据**:`LlmMessage`/`LlmStreamChunk`/`SseEvent`/`GenerationParams`/`ToolContext`
  与数据契约同处一文件;实测 **1398 行**(见 [PC-2](#pc-2-大文件拆分))。
- **改动点**:拆分不改线格式,但会大范围改动 import;已用 `check-arch` 规则 D 的豁免项显式标注。
- **验收断言**:拆分后 `check-arch` 规则 D 通过(无新增越代边);`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(大范围改 import,收益仅为文件整洁);回滚 = revert。
- **依赖 / 需确认**:与 [PC-2](#pc-2-大文件拆分) 同一事项;须确认是否值得(源文档判定为「纯整洁收益」)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §8

### ARCHITECTURE-3H §8-3 契约不变量仅 mutex(即 L2)

- **目标**:接线 range/require_if 两个不变量变体。
- **现状证据**:`contracts/op.rs:209-225`:P1 落地 mutex(互斥)类型,
  `check_invariants()`(`:214` 起)只判定 **mutex**(同组路径同时有值 ≥2 即违反);
  **range / require_if 两个变体恒判为满足**(匹配分支为空操作,注释
  `:225`「range/require_if 留待谓词引擎(§17.8),P1 不判定」已留痕)。
  **2026-09-16 复核**:`op.rs:209,225` 注释原样仍在,两个空分支未接线 → 未启动。
- **改动点**:实现谓词白名单解析与求值(比较/区间/存在性),在 `op.rs` 的两个空分支接线;
  **注意谓词引擎本身是另一项独立设计,不要在此顺手发明语法**。
- **验收断言**:声明了 range/require_if 的契约卡会被真实校验;每个变体有违反/通过双向用例;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(属独立设计);回滚 = 移除接线。
- **依赖 / 需确认**:**依赖未实现的谓词引擎(§17.8 CES)**,属独立设计;
  待办期间**前端不应向用户暴露「不变量已生效」的措辞**。
- **状态**:**未启动**(关联遗留:**L2**)
- **来源**:archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §8
  (与 archive/2026-09-16-consolidation/known-limitations.md L2)

### L2 契约不变量仅落地 mutex(待办)

- **目标**:与 `ARCHITECTURE-3H §8-3` 同一目标——补齐 range/require_if 的判定。
- **现状证据**:同 `§8-3`。契约(Contract)可声明三类跨字段不变量,`check_invariants()`
  (`server-rs/src/contracts/op.rs:214` 起)只判定 mutex;**range / require_if 恒判为满足**
  (匹配分支为空操作,注释已留痕)。当前恒通过意味着声明了 range/require_if 的契约卡
  **不会得到校验,也不会报错**。
- **改动点**:实现谓词白名单解析与求值(比较/区间/存在性),在 op.rs 的两个空分支接线;
  注意谓词引擎本身是另一项独立设计,**不要在此顺手发明语法**。
- **验收断言**:range/require_if 的违反能被报出;存在性/区间/比较三类谓词各有用例;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中;回滚 = 移除接线。
- **依赖 / 需确认**:依赖「白名单谓词引擎(§17.8 CES)」这一后续里程碑。
- **状态**:**未启动**(与 `§8-3` 为同一事项的两种登记,本文件保留 L2 编号以维持跨文档引用)
- **来源**:archive/2026-09-16-consolidation/known-limitations.md L2
  (转计划来源:archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §8)

### L3 MCP 孙进程不保证回收(v1 保守语义,待评估)

- **目标**:保证 MCP 服务器禁用/退出后整棵进程树被回收,不再残留占端口。
- **现状证据**:MCP 服务器子进程以 `kill_on_drop(true)` 托管、显式 kill 后 wait 回收僵尸
  (`server-rs/src/mcp/process.rs:1-10`);但 kill 只作用于**直接子进程**——若服务器命令是
  `cmd /c xxx` / `sh -c xxx` 这类包装,真正干活的孙进程不保证被杀掉。
  **2026-09-16 复核**:`grep -rn "JobObject" server-rs/src/mcp/process.rs` **零命中** → 未启动。
  文件头注释已声明「如需进程组级清理另起批次」。
- **改动点**:Windows 上用 Job Object 把子进程树编入作业对象
  (`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`),或 spawn 时包一层 `taskkill /T`;
  跨平台方案需分别处理 Unix 进程组。
- **验收断言**:包装形态(`cmd /c`)下的孙进程在禁用服务器后被回收;新增进程树清理用例;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(进程生命周期);回滚 = revert 回收逻辑。
- **依赖 / 需确认**:与 [L16](#l16-mcp-服务器死亡后工具不注销且无热重连待办) 同属 MCP 韧性面,
  建议同批设计;多数 MCP 服务器(npx/uvx 直起)不是包装形态,影响面有限。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/known-limitations.md L3

### L8 授权:自定义风险级无入口、外部工具区域判定受限(待办)

- **目标**:让用户可自定义风险级,并让外部工具的路径区域判定更精确。
- **现状证据**(`server-rs/src/tools/permissions.rs`、`tools/action_class.rs`):
  1. `PermissionConfig.classifications`(自定义风险级)字段存在,但**全仓无写入通路**
     (无 API、无 UI),插件与 MCP 工具恒按危险级处理(`default_risk` 的未知分支)。
     **2026-09-16 复核**:`classifications` 仅出现在 `permissions.rs:43`(定义)、
     `:94`(读取)、`:723`(测试夹具),无 API/UI 写入点(同 [DC-3](#dc-3-permissionconfigclassifications-无入口))。
  2. 插件 / MCP 工具的路径区域靠扫描参数中的路径样式(盘符 / UNC / 绝对路径 / `..`)判定:
     无路径参数的调用按「非系统路径」处理;带路径参数则一律按系统路径写/删拦截
     (无法区分只读与写入,故偏严)。
- **改动点**:新增 `PUT /api/agent/tool-permissions/classifications` 与设置页入口;
  区域判定若需更精确,可要求插件/MCP 在工具定义中声明参数语义。
- **验收断言**:自定义风险级持久化并可生效;区域判定有可区分只读/写入的用例;
  **三档授权模式的硬底线(系统路径写/删恒需授权)不因风险级自定义而改变**;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中-高——让用户自定义风险级会扩大安全面(把危险工具降级即放开自动执行),
  **需要独立的产品与安全设计**,不在授权改造批次内。回滚 = 移除入口。
- **依赖 / 需确认**:需独立产品与安全设计;与 [DC-3](#dc-3-permissionconfigclassifications-无入口) 同源。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/known-limitations.md L8

### L16 MCP 服务器死亡后工具不注销,且无热重连(待办)

- **目标**:服务器进程退出时注销其工具并标记禁用;补 `read_line` 行长上限;支持热重连。
- **现状证据**:MCP 客户端仅在**启动时**装配(`server-rs/src/mcp/mod.rs:1-5`,
  改设置需重启生效、无热重连);服务器进程中途退出时,其工具**不从 `ToolRegistry` 注销**,
  注册表内这些工具会持续报错直到进程重启。另 `client.rs:252` 的 `read_line` 对服务器
  单行输出无长度上限。**2026-09-16 复核**:
  - **行长上限已落地**:`mcp/client.rs:248` 的
    `MAX_LINE_BYTES: usize = 8 * 1024 * 1024`,`:262-266` 超限判协议异常并结束读循环
    (即 `三结合落实核对表` E.3 的「MCP 行长上限」已完成);
  - **EOF 注销工具未做**:`grep -n "unregister|注销" mcp/client.rs` 无工具注销逻辑;
    `start_mcp` 注释仍写「v1 仅启动时装配,PUT 改 mcp_* 后重启生效」。
- **改动点**:EOF 时从注册表注销该服务器全部工具并标记禁用;`read_line` 加最大行长(已做);
  孙进程回收见 [L3](#l3-mcp-孙进程不保证回收v1-保守语义待评估)(Windows Job Object)。
- **验收断言**:服务器进程退出后其工具从注册表消失(不再报错);禁用服务器后
  工具不可见;热重连后可重新看到工具;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(改 `ToolRegistry` 生命周期——注册表目前只增不减,属结构性改动);
  回滚 = revert 注销逻辑。
- **依赖 / 需确认**:需改 `ToolRegistry` 生命周期(结构性);与 `三结合落实核对表` 的
  `E.3`(部分完成)为同一事项。
- **状态**:**部分完成**(行长上限 8MB 已落地;EOF 注销工具与热重连未做)
- **来源**:archive/2026-09-16-consolidation/known-limitations.md L16
  (与 archive/2026-09-16-consolidation/三结合落实核对表.md 批次 E · E.3)

### 架构分析-2026-09-14 · 批次 3 消除重复实现

- **目标**:消除 6 组重复实现,尤其**唯一一处已发生语义漂移**的世界书注入三处合一。
- **现状证据**:`架构分析-2026-09-14.md §4.6` 列重复实现 6 组(带证据);
  `§6 批次 3` 按风险排序给出 5 项。**2026-09-16 复核**:
  - **第 1 项(世界书注入三处合一)已落地**:`services/prompt_kit.rs` 为单一实现,
    `api/chat.rs:466-495` 已改调 `prompt_kit::world_entry_roll` / `triggered_entry_hits` /
    `world_entry_text`;`agents/engine/worldbook.rs:90-100` 同样收敛到
    `prompt_kit::world_entry_text` / `world_entry_roll`;
  - **第 2 项(`ModalShell.vue`)未做**:`web/src/components/ModalShell.vue` **不存在**;
  - **第 3 项(后端 SSE helper)已落地**:`api/util.rs:47` 的
    `sse_response<S>(stream: S)` 已存在(注释记「2026-09-14」收口 `/api/chat/send` 与
    `/api/tasks/events` 两处样板);
  - **第 4 项(步进循环收敛 `step_runner`)未做**:见 [ARCHITECTURE-3H §8-1](#architecture-3h-8-1-步进循环三份);
  - **第 5 项(localStorage 抽统一读写模块)未做**:`web/src` 多文件仍各自读写
    (`lastPosition.ts`、`resourceStore.ts`、`renderHtmlPreference.ts`、`AgentPanel.vue` 等),
    无统一键名集中声明模块。
- **改动点**:
  1. 最高优先:世界书注入三处合一(以 `prompt_kit.rs` 为单一实现)——
     **此项已落地,余量为确认无第四份**;
  2. 抽 `web/src/components/ModalShell.vue`,18 个弹窗模板收敛为一处
     (`modals.ts` 已是注册表 SSOT,加 Shell 即闭环);
  3. 抽后端 SSE 响应 helper(`api/chat.rs` + `api/tasks.rs` 两处样板)——**已落地**;
  4. 步进循环收敛为 `task_engine/step_runner.rs`(建议在 `tools` 收口后再做);
  5. `localStorage` 抽统一读写模块(键名集中声明),顺带解决 10+ 处硬编码。
- **验收断言**:每项独立提交、独立验收;重复实现数下降;
  `cargo test -j 2` / `npm test -w web` / `check-arch` 全绿。
- **风险 / 回滚**:风险低-中(纯结构重构,行为不变);按项回滚。
- **依赖 / 需确认**:第 4 项须等 `tools` 收口(避免同时改动三个稳定模式)。
- **状态**:**部分完成**(第 1、3 项已落地;第 2、4、5 项未做)
- **来源**:archive/2026-09-16-consolidation/架构分析-2026-09-14.md §6 批次 3

### 架构分析-2026-09-14 · 批次 4 清晰化:注释与文档

- **目标**:**不损失原有术语的前提下,把「为什么」补进代码**。
- **现状证据**:`§4.9 注释覆盖两极分化` 给出按行数/注释比例的倒挂清单。**2026-09-16 复核**:
  `ejs/exec.rs` 1313 行、`tools/agent_tools.rs` 1299 行(与源文档的数字已增长)仍为低注释大文件。
- **改动点**(按 `§4.9` 的倒挂清单):
  1. `parsing/assistant/ejs/exec.rs` 补「冻结纪律 + 三重守卫(迭代/墙钟/解析深度)+
     能力面不得增长」的模块头注释,并说明新能力应走 `scripts/runtime.rs`;
  2. `tools/agent_tools.rs` 补每个工具的风险等级、授权要求、副作用(写哪些表);
  3. `world_book.rs` 的**三份注入实现**都要加交叉引用注释,指明「单一实现在 `prompt_kit.rs`」,
     防止第四份出现(注:注入已合一,余量为交叉引用注释);
  4. `task_core/backend.rs` 补 `§4.8` 所述的「窄接口存在理由」注释;
  5. 前端 5 个低注释大组件(`WorldBooksModal` 2% / `ScriptsModal` 3% / `PromptManager` 5% /
     `TaskBoard` 10%)补关键算法注释,尤其是树转换与随机 id 生成;
  6. 每个模块头统一 `// <file> — <职责>` 一行(前端已有此惯例,后端不统一),
     并在其中标注**代际**,让「读代码时就知道该按哪代纪律改」。
- **验收断言**:每项为纯注释改动,测试零修改;`check-arch` 通过;
  `cargo fmt --check` / `npm run lint -w web` 不新增 error。
- **风险 / 回滚**:风险极低(纯注释);回滚 = revert。
- **依赖 / 需确认**:无。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/架构分析-2026-09-14.md §6 批次 4

### 架构分析-2026-09-14 · 批次 5 工程化收口

- **目标**:CI 如实标注、测试空洞补齐、规则 B 白名单清理、晋升台账建立。
- **现状证据**. **2026-09-16 复核**:
  - **第 5 项(晋升台账)已落地**:`ARCHITECTURE-3H.md §6.1` 已有「晋升台账」表
    (含 2026-09-14 的多条登记);
  - **规则 B 白名单**:`架构分析 §4.10` 记「41 处命中、41 处全部白名单,待修 0」——
    需复核当前值(以 `check-arch --verbose` 为准);
  - **CI**:`.github/workflows/ci.yml` 存在但仓库无远端,从未执行;
  - **前端测试空洞**:`stores/modelConn.ts`、`stores/resources.ts`、14 个 api 模块等
    仍待确认覆盖。
- **改动点**:
  1. **CI 要么真跑、要么如实标注**:在 `docs/README.md` 与 `MAINTENANCE.md` 明确
     「CI 为纸面,实际闸门是 `build.ps1` + `pre-push`」;
  2. `pre-push` 可被 `--no-verify` 绕过;建议在 `MAINTENANCE.md` 写明「发布前必须跑一次完整
     `build.ps1`」,并把 `count-tests --check` 从 WARN 提升为硬 FAIL(它是数字 SSOT 的守护者);
  3. 补前端测试空洞(`stores/modelConn.ts`、`stores/resources.ts`、14 个 api 模块、
     `SettingsModal` 等高风险组件);
  4. **清理规则 B 的白名单**:当前待修 0,护栏只剩「拦未来新增」的作用;
     逐字段判定哪些该补 action,把白名单压下来;
  5. 建立**晋升台账**(**已落地**,余量为持续维护)。
- **验收断言**:`docs/README.md` / `MAINTENANCE.md` 有 CI 纸面说明;
  `count-tests --check` 为硬 FAIL;新增组件测试用例;
  规则 B 白名单数量下降并有记录;`npm test -w web` 全绿。
- **风险 / 回滚**:风险低;按项回滚。
- **依赖 / 需确认**:第 4 项改动 `check-arch.mjs` 白名单须遵守 ratchet 只降不升纪律。
- **状态**:**部分完成**(第 5 项已落地;第 1–4 项未做)
- **来源**:archive/2026-09-16-consolidation/架构分析-2026-09-14.md §6 批次 5

### C-1 `modal-settings` chunk 138.5K(目标 ≤60K 未达)

- **目标**:`modal-settings` 拆到 ≤60K。
- **现状证据**:源文档附录 C 记「modal-settings **138.5K**(≤60K 目标未达,留后续拆
  SettingsModal 内部)」。**2026-09-16 复核实测**:`web/dist/assets/modal-settings-DgSe9Odc.js`
  = **188,565 B**(约 184K)——**不仅未达 60K,且较 138.5K 记录 REBOUND 上升**
  (见 §九 裁定 3)。该目标与 [P-8](#p-8-首屏拆-modal-settings-反向耦合) 强相关:
  P-8 先解决「首屏预载」,本项解决「chunk 内部继续拆」。
- **改动点**:继续拆 `SettingsModal` 内部(8 个 section 已各自懒加载,
  余量在 `SettingsModal` 本体与其共享依赖);或在 P-8 完成后重新量基线再定目标。
- **验收断言**:`modal-settings` gzip 体积 ≤60K(或按 P-8 后实测值重定预算);
  `npm run build -w web` + `npm test -w web` 全绿。
- **风险 / 回滚**:风险中(拆弹窗可能触发反向耦合);回滚 = revert。
- **依赖 / 需确认**:**应先做 P-8**;60K 目标需按 P-8 后的新基线复核是否仍适用。
- **状态**:**未启动**(实测值较记录上升,见 §九 裁定 3)
- **来源**:archive/2026-09-16-consolidation/优化实施方案-2026-09.md 附录 C

### C-2 超千行文件 10→6(部分仍 >1000 行)

- **目标**:把超千行文件数压到 ≤6。
- **现状证据**:源文档附录 C 记 M3 后「超千行文件数 **6**」;`M3 完成记录` 记
  「10→7,目标 ≤6 差 1」后达 6。**2026-09-16 复核实测**:`find … | wc -l` 超千行的
  **源文件(不含测试)为 12 个**:`memory_service.rs` 1661、`task_engine/team.rs` 1580、
  `parsing/world_book.rs` 1531、`agents/engine/executor.rs` 1489、`models/types.rs` 1398、
  `ejs/exec.rs` 1313、`tools/agent_tools.rs` 1299、`api/settings.rs` 1163、
  `services/session_service.rs` 1040、`settings_service/mod.rs` 1037、`api/chat.rs` 1027、
  `services/task_service/executor.rs` 1022(另有 `characterScriptSandbox.test.ts` 1061 属测试)
  → **指标从 6 回退到 12,远未达 ≤6**(见 §九 裁定 7)。
- **改动点**:按 [PC-2](#pc-2-大文件拆分) 逐文件按职责切分(纯代码搬运);
  优先新入榜且无既有拆分计划的(`world_book.rs`、`agent_tools.rs`、`api/settings.rs`、
  `session_service.rs`、`settings_service/mod.rs`、`api/chat.rs`)。
- **验收断言**:超千行文件数 ≤6 并纳入门禁(或由 `check-arch` 记基线只降不升);
  每项拆分测试零修改(纯搬运)。
- **风险 / 回滚**:风险中(大范围改 import);按文件回滚。
- **依赖 / 需确认**:与 [PC-2](#pc-2-大文件拆分) 同一事项;
  建议把「超千行文件数」做成 ratchet 门禁,防再次回退。
- **状态**:**未启动**(实测 12 > 目标 6)
- **来源**:archive/2026-09-16-consolidation/优化实施方案-2026-09.md 附录 C 与 M3 完成记录

### C-3 `build.ps1` 双端指纹验证待重跑

- **目标**:完成 build.ps1 双端构建指纹一致性验证。
- **现状证据**:源文档记「受阻:运行中实例锁 `target\release`(os error 5/112),
  已用 `CARGO_TARGET_DIR=D:\kedai-target\release-check` 降级验证 release 编译通过;
  **build.ps1 双端指纹验证待 Kedai 关闭后重跑**」。
  **2026-09-16 复核**:该项属「需关闭运行中实例后跑」的验证动作,无代码阻塞;
  `../AGENTS.md` 已把「交付 exe 前必须跑完整 `.\build.ps1`」列为纪律。
- **改动点**:关闭运行中的 Kedai 实例(释放 `target\release` 与 `dist\`),
  跑一次完整 `.\build.ps1`,核对双端指纹(sidecar + 二进制内嵌)一致。
- **验收断言**:`.\build.ps1` 成功;双端指纹比对一致;`dist\` 与根 `Kedai.exe` 同步更新。
- **风险 / 回滚**:风险低(构建流程);回滚 = 无需回滚。
- **依赖 / 需确认**:**必须先关闭运行中的 Kedai**(否则锁文件);
  构建期间不得与 `cargo test`/Android 构建并发(内存争用)。
- **状态**:**未启动**(待跑)
- **来源**:archive/2026-09-16-consolidation/优化实施方案-2026-09.md 附录 C 与 M1 完成记录

### C-4 sccache / 增量构建未评估

- **目标**:评估引入 sccache 或保留增量构建的收益。
- **现状证据**:源文档附录 B 记 `server-rs/target`、`src-tauri/target` 为 GB 级编译中间产物,
  「build.ps1 已删;**D-8 评估 sccache 后可保留增量**」;附录 C 未列入实测值。
  **2026-09-16 复核**:`grep -rn "sccache" build.ps1 ../MAINTENANCE.md` **零命中**
  → 未评估。
- **改动点**:评估 sccache(本机多 target/多分支场景)与保留增量构建的取舍;
  结合 [P-13](#p-13--j-2-并发限制解除实验允许失败产出入档) 的并发结论一并决定。
- **验收断言**:评估结论落文档(引入或明确不引入及理由);
  若引入,记录冷/热构建耗时对比。
- **风险 / 回滚**:风险低(仅构建链路);回滚 = 移除 sccache 配置。
- **依赖 / 需确认**:与 P-13 的并发/内存结论互为输入;
  `../AGENTS.md` 记「不动『构建后清 target』策略」(用户已说明为省盘有意为之),
  故本项**不得**擅自改变清理策略。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/优化实施方案-2026-09.md 附录 B 与附录 C

### B.4b 步进循环抽 `step_runner`

- **目标**:统一 legacy/plan/custom 的步进循环与取消口径。
- **现状证据**:见 [ARCHITECTURE-3H §8-1](#architecture-3h-8-1-步进循环三份)。
  `三结合落实核对表` 明确本项状态为 **已放弃(知情接受)**:
  「会同时改动 legacy/plan/custom 三个**已稳定运行**的模式,收益(取消口径一致)低于回归风险;
  三者取消兜底口径差异已如实登记」。
- **改动点**:抽 `step_runner::run_steps`;三模式改调。
- **验收断言**:三模式行为等价;取消口径统一有测试;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中;回滚 = revert。
- **依赖 / 需确认**:**待实跑暴露不一致时再做**;`ARCHITECTURE-3H.md §8` 有登记。
- **状态**:**未启动**(三结合核对表标为「已放弃/知情接受」;本文件按源文档收为待办,
  与 §8-1 同事项)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md「未做项与理由」

### B.6 `models/transport.rs` 拆分

- **目标**:把传输类型从 `models/types.rs` 拆出为 `models/transport.rs`。
- **现状证据**:`三结合落实核对表` B.6 实现「**`models/transport.rs` 拆分改为知情接受**」,
  理由「仅文件整洁收益,却要大范围改 import;已由 check-arch 规则 D 的豁免项显式标注」。
  **2026-09-16 复核**:`models/transport.rs` **不存在**(`check-arch` 规则 D 豁免项仍指
  `models/transport.rs` 这一未来路径);`models/types.rs` 1398 行。
- **改动点**:机械搬迁传输类型(`LlmMessage`/`LlmStreamChunk`/`SseEvent`/`GenerationParams`/
  `ToolContext`)到 `models/transport.rs`,保持线格式不变;同步去掉规则 D 的豁免标注。
- **验收断言**:拆分后规则 D 通过;**线格式冻结测试**(`check-contract.mjs`)全绿;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(大范围改 import);回滚 = revert。
- **依赖 / 需确认**:**需要时可机械搬迁,无行为风险**;
  与 [ARCHITECTURE-3H §8-2](#architecture-3h-8-2-modelstypesrs-混装传输类型)、
  [PC-2](#pc-2-大文件拆分) 同事项。
- **状态**:**未启动**(知情接受)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 B · B.6

### D.2b memory/session「吞错返空」

- **目标**:区分「失败」与「空结果」,避免把降级伪装成正常空。
- **现状证据**:`三结合落实核对表` D.2b 记「本轮仅覆盖取连接失败路径;
  **memory/session 吞错返空未改**(需逐个定语义)」。**2026-09-16 复核**:
  `session_service.rs` 仍有大量 `.ok()?` / `filter_map(|r| r.ok())`
  (`:113,134,158,165,236,321,328,452,460,482,499,506,515,522,541` 等),
  失败被静默转为空/`None` → 未启动。
- **改动点**:逐个函数判定「失败」与「空结果」的语义边界;需业务知识决定哪些调用方真能区分;
  **盲改会把「降级」变成「报错」**,故须逐个函数评审。
- **验收断言**:关键路径的失败与空结果可区分(日志 + 返回值);
  每个被改函数有失败路径用例;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中-高(语义边界需业务知识,盲改会产生用户可见报错);按函数回滚。
- **依赖 / 需确认**:**如需推进应逐个函数评审**;与 [D.3](#d3-存量裸-error-逐个补-code) 同批口径。
- **状态**:**部分完成**(取连接失败路径已覆盖;memory/session 吞错返空未改)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 D · D.2b

### D.3 存量裸 `{error}` 逐个补 `code`

- **目标**:逐个补齐存量裸 `{error}`,使错误体带稳定 `code`。
- **现状证据**:`三结合落实核对表` D.3 记「**加 ratchet 规则 G 防新增**(基线 117,当前 116);
  存量逐个补 code 需逐处判定 HTTP 状态码,盲改会改 API 行为 → 登记跟踪」。
  **2026-09-16 复核实测**:`tools/check-arch.mjs:400` 为
  `const BASELINE_BARE_ERROR = 105; // 2026-09-15 实测(批次 1 错误面收口后由 117 下调);只降不升`
  → 基线与当前值均为 **105**(批次 1 已净降 12,见 §九 裁定 9)。
- **改动点**:按域分批推进(settings/memory/world_books/agent 集中);每处判定真实 HTTP
  状态码后再补 `code`;**不得盲改状态码**。
- **验收断言**:规则 G 基线同步下调并记录前后值;每批被改 handler 有断言
  「真实状态码 + code」的集成测试(承 [7.1](#71-后端负路径覆盖约定收益最大无需新机制));
  `node tools/check-arch.mjs` exit 0。
- **风险 / 回滚**:风险中——每处需判定 HTTP 状态码,盲改**会改变 API 行为**
  (如把 400 变 500);按域回滚。
- **依赖 / 需确认**:建议与 [4.5](#45-错误类型收敛渐进勿一次替换) 的错误类型收敛合并规划。
- **状态**:**部分完成**(规则 G 已建 + 基线 117→105;存量仍 105 处待逐域推进)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 D · D.3
  (与 archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md §0.3 项 3)

### E.1 脚本授权打通

- **目标**:把后端角色卡脚本执行路径纳入授权台账。
- **现状证据**:`三结合落实核对表` E.1 状态为「**待办(需定方案)**」;
  理由「复核发现**前端已有**授权台账,缺的是后端路径与台账的对接——涉及跨端信任模型设计,
  非机械改动」。**2026-09-16 复核**:**实际已完成**——
  `server-rs/src/services/script_authorization_service.rs` 存在(含
  `compute_hash`、`current_hash`、`script_authorizations` 表 CRUD);
  `models/db/schema.rs:404` 建 `script_authorizations` 表(注释「默认无行 = 未授权 =
  后端不执行该卡脚本(fail-closed);global 脚本属用户自有,不受此表约束」);
  `api/user_scripts.rs:107-240` 提供 GET/PUT/DELETE 授权接口;
  `agents/engine/run_scripts.rs:193-197` 的 `run_character_scripts` 已改 **fail-closed**
  (台账无「与当前脚本内容一致的哈希」即不执行并告警);
  `agents/engine/mod.rs:123-151` 注入 `script_authorizations` 门。
  与 `known-limitations.md` **L12 的「已修复(2026-09-14)」一致**(见 §九 裁定 1)。
- **改动点**(已完成):后端新增 `script_authorizations` 表 + `ScriptAuthorizationService`;
  `run_character_scripts` 把 **global(用户自有,可信,不走门禁)与 character(受门禁)拆分**,
  character 部分 fail-closed;哈希唯一来源在后端(`GET /api/script-authorizations`
  返回实时 `current_hash`,前端原样回传)。
- **验收断言**:未授权不执行、授权后执行、撤销后不执行、陈旧哈希 409
  (`tests/scripts_e2e.rs` 4 条已存在);`cargo test -j 2` 全绿。
- **风险 / 回滚**:**这是有意的兼容性收紧**(旧行为自动执行),已按兼容性变更登记;
  ST 生态大量卡依赖脚本自动写变量,**默认开启授权会让这类卡静默失效**,
  须在 UI 给出清晰的重新授权引导。回滚 = 关闭门禁(需评估安全面)。
- **依赖 / 需确认**:已完成;余量为文档/UI 引导复核。
- **状态**:**已完成**(2026-09-14;`三结合落实核对表` 的「待办」记录未回填,见 §九 裁定 1)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 E · E.1
  (与 archive/2026-09-16-consolidation/known-limitations.md L12)

### E.3 MCP EOF 注销工具

- **目标**:EOF 时注销该服务器的全部工具。
- **现状证据**:`三结合落实核对表` E.3 状态「**部分完成**」:
  `mcp/client.rs:8MB 行长上限` 已做;**EOF 注销工具未做**(需改注册表生命周期,登记 L16)。
  **2026-09-16 复核**:行长上限已落地(`MAX_LINE_BYTES`),注销逻辑不存在
  → 与 [L16](#l16-mcp-服务器死亡后工具不注销且无热重连待办) 为同一事项。
- **改动点**:EOF 时从注册表注销该服务器全部工具并标记禁用。
- **验收断言**:服务器退出后其工具从注册表消失;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(`ToolRegistry` 目前只增不减,属结构性改动);回滚 = revert。
- **依赖 / 需确认**:需改 `ToolRegistry` 生命周期。
- **状态**:**部分完成**(行长上限已做;注销未做)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 E · E.3
  (与 archive/2026-09-16-consolidation/known-limitations.md L16)

### G.3 `scriptBlocksCache` 上限

- **目标**:为 `scriptBlocksCache` 加上限,避免长生成期间内存峰值。
- **现状证据**:`三结合落实核对表` G.3 状态「**已放弃**」,理由「需先确认长生成期间的
  实际内存峰值是否构成问题(未测得压力证据),避免为未证实的风险引入 LRU 复杂度」。
  **2026-09-16 复核**:`ChatWindow.vue:67` 的
  `let scriptBlocksCache = new Map<string, ReturnType<typeof renderScopedScripts>>()`
  **仍无容量上限**(`:78` 清空、`:84-89` 读写)→ 未启动。
- **改动点**:先加埋点测量长生成期间的实际内存峰值;若构成问题再加 LRU 上限
  (可复用 [P-9](#p-9-虚拟滚动与渲染缓存生命周期对齐) 的容量上限模式)。
- **验收断言**:压力证据落文档;若加上限,则超限淘汰有测试;
  `npm test -w web` 全绿。
- **风险 / 回滚**:风险低;回滚 = 移除上限。
- **依赖 / 需确认**:**需实测内存峰值**后再定是否值得引入 LRU 复杂度;
  与 [P-9](#p-9-虚拟滚动与渲染缓存生命周期对齐) 的 `renderCache` 设计可统一。
- **状态**:**未启动**(知情接受,未测得压力证据)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 G · G.3

### G.5 弹窗开关单一注册表

- **目标**:弹窗开关单点注册。
- **现状证据**:`三结合落实核对表` G.5 状态「**已放弃**」,理由「纯整洁性重构,
  改动面涉及 App.vue 模板与返回键逻辑,收益低」。**2026-09-16 复核**:**实际已收敛**——
  `web/src/modals.ts` 存在(3895 B),`App.vue:9` `import { MODALS, MODAL_FLAGS } from './modals'`,
  `:99-100` 的返回键逻辑由 `MODAL_FLAGS` 派生;`modals.test.ts` 有元测试锁三方一致。
  与 `known-limitations.md **L15「已收敛(2026-09-13)」一致**(见 §九 裁定 8)。
- **改动点**(已完成):单点源 `web/src/modals.ts`(flag/label/组件,声明序=渲染序);
  App.vue 渲染与返回键、check-arch 白名单全由它派生(解析失败显式 FAIL),
  SettingsHub 类型取 `ModalFlag`;新增弹窗降到 2 处;`modals.test.ts` 9 例锁三方一致。
- **验收断言**:`npm test -w web` 的 `modals.test.ts` 元测试通过(flag 无重复 / uiPrefs 双向 /
  App.vue 派生);`node tools/check-arch.mjs` 通过(白名单由 `modals.ts` 派生)。
- **风险 / 回滚**:风险低;已完成。
- **依赖 / 需确认**:已完成;余量为文档回填。
- **状态**:**已完成**(核对表「已放弃」记录未回填,见 §九 裁定 8)
- **来源**:archive/2026-09-16-consolidation/三结合落实核对表.md 批次 G · G.5
  (与 archive/2026-09-16-consolidation/known-limitations.md L15)

### FT-1 Prettier 全量格式化(179 文件)未做

- **目标**:统一前端代码格式,使 `prettier --check` 可进门禁。
- **现状证据**:`前端工具链与审计升级-变更说明.md §七` 记「**Prettier 全量格式化**(179 文件)未做,
  `format:check` 未进门禁——需独立专项」。实测受影响 179/223 = **80%**,
  因现有代码是手写紧凑风格(多个名字挤一行、超长行手写折行),Prettier 分组断行模型
  无法复现,任何 `printWidth` 组合最小差异面仍 ≥74%。故 `prettier --check` **未接入门禁**
  (接入会直接拦死)。**2026-09-16 复核**:`web/.prettierrc.json` / `.prettierignore` 已存在,
  `web/package.json:16-17` 有 `format` / `format:check` 脚本,
  但 `tools/check-all.ps1:227` 注释明写「prettier --check 未接入」→ 未做。
- **改动点**:全量格式化留作**独立专项**(单独提交、避开其它批次改动窗口);
  格式化与现有批次改动**不得混在同一提交**(否则回滚粒度失效)。
- **验收断言**:`npm run format:check -w web` exit 0;`npm test -w web` / `typecheck` 全绿;
  格式化提交与业务改动提交分离。
- **风险 / 回滚**:风险中——纯格式大 diff(80% 文件),会与其它在制品冲突;
  **必须避开其它批次改动窗口**。回滚 = revert 格式化提交。
- **依赖 / 需确认**:需先与其余前端批次(P-8/P-9/P-10、二维批次 3)排期,
  避免同文件冲突。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/前端工具链与审计升级-变更说明.md §七
  (与 archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §二 · 5.3)

### FT-2 ESLint 46 条 warn 未清零

- **目标**:把存量 ESLint warn 清零,使 `lint` 成为零告警门禁。
- **现状证据**:`前端工具链与审计升级-变更说明.md §七` 记「ESLint 46 条 warn 未清零
  (存量,已在配置中可见)」。**2026-09-16 复核实测**(`npm run lint -w web`):
  **46 problems (0 errors, 46 warnings)**——与记录一致,其中 14 warnings potentially
  fixable with `--fix`。
- **改动点**:按规则逐条清偿(`--fix` 能解决 14 条);确属误报的规则按需调整配置
  (**不得在源文件加 `eslint-disable`**——变更说明明确「未在源文件加任何 `eslint-disable`」,
  该纪律应延续)。
- **验收断言**:`npm run lint -w web` 输出 `0 problems`;`check-all.ps1` 的 `web: eslint`
  阶段仍为 0 error 硬门禁;**未新增任何源文件 `eslint-disable`**。
- **风险 / 回滚**:风险低;按文件回滚。
- **依赖 / 需确认**:与 [FT-1](#ft-1-prettier-全量格式化179-文件未做) 排期需协调
  (格式化会改变行号,与 lint 清偿同文件冲突)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/前端工具链与审计升级-变更说明.md §三 与 §七

### TE-1 `ModeSettings.task_modes`(每子模式独立覆盖字段)

- **目标**:为每个子模式(solo/multi/plan/team/custom)提供独立的设置覆盖字段。
- **现状证据**:`任务引擎六模式.md §四` 末段明写「`ModeSettings` 的每子模式独立覆盖字段
  (`task_modes`)**为设计预留,尚未落地**:当前 `services/settings_service/params.rs` 的
  `ModeSettings` 无该字段,子模式共享同一份任务模式设置」。
  **2026-09-16 复核**:`grep -n "task_modes" services/settings_service/params.rs` **零命中**
  → 未启动。
- **改动点**:新增 `ModeSettings.task_modes`;数值字段默认继承,
  进 system 的文本字段按 `archive/2026-09-16-consolidation/模式提示词边界.md`
  走**类型级隔离 + untrusted 包裹**。
- **验收断言**:每子模式可独立覆盖且默认继承;模式隔离不变量不破
  (进 system 的文本字段仍由类型承载);`check-contract.mjs` 通过;
  `cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(触及双模式提示词隔离不变量);回滚 = 移除字段。
- **依赖 / 需确认**:**改设置字段前必须先读
  `archive/2026-09-16-consolidation/模式提示词边界.md`**;
  `settings.json` 线格式须保持不变。
- **状态**:**未启动**(设计预留)
- **来源**:archive/2026-09-16-consolidation/任务引擎六模式.md §四 末段

### TE-2 team 手动拓扑(UI 指定主 agent 数量/人设/分工)

- **目标**:允许用户在 UI 指定 team 主 agent 的数量、人设与分工(手动优先于自动)。
- **现状证据**:`任务引擎六模式.md §五` 明写「首版(批次 4.3b)只做自动拓扑(规划器决定);
  手动拓扑(用户在 UI 指定主 agent 数量/人设/分工,手动优先)**预留,待前端入口批次落地**」。
  **2026-09-16 复核**:`task_engine/team.rs:11` 注释仍写
  「手动拓扑(用户指定主 agent 数量/人设/分工)预留,待前端入口批次」;
  `parse_team_topology`(`:105`)只解析规划器输出 → 未启动。
- **改动点**:加手动拓扑数据结构与前端入口;手动拓扑优先于 `parse_team_topology`;
  保留自动路径的归并与全局封顶(≤15 子目标、N∈[2,4]、4×4=16 由解析侧截断)。
- **验收断言**:UI 指定拓扑后被真实使用(调用面板显示各主 agent 分工);
  未指定时回退自动拓扑、行为不变;`cargo test -j 2` + `npm test -w web` 全绿。
- **风险 / 回滚**:风险中(team 模式编排语义);回滚 = 移除手动路径。
- **依赖 / 需确认**:需前端入口批次;须确认手动与自动的优先级与冲突处理。
- **状态**:**未启动**(设计预留)
- **来源**:archive/2026-09-16-consolidation/任务引擎六模式.md §五

---

## 七、安全与数据残余（T1/T3/T4）

> 承自 `archive/2026-09-16-consolidation/威胁模型与残余风险.md` 的**待办项**。
> 该文档为**活文档**,登记「已知且被接受 / 待处理」的安全与正确性残余风险。
>
> **编号说明**:威胁模型用 T1~T5 编号,残余任务文档用 3.x 编号,二者为同一批事项的两种登记:
> T1 = [3.6](#36-resource-frame-外联收紧t1)、T2 = [3.3](#33-命令混淆绕过-command_riskt2)、
> T3 = [3.4](#34-android-1f-分隔符t3)、T4 = [3.5](#35-is_public_ip-黑名单枚举t4)。
> 本文件**两套编号都保留**(不得重编号),并在每条内互相引用。
> **T5** 为「主站 token 经明文 HTTP loopback 交付」——判定为**本地信任模型的固有边界**
> (bootstrap 仅 loopback + 令牌桶限频),非待办,故不在此列(见 [§八](#八明确不做知情排除便于复查))。

### T1 `resource-frame` 外联收紧开关

- **目标**:提供设置项「禁用作者页外联」,把兼容取舍变为用户可选(**默认保持现状=开**)。
- **现状证据**:`api/security.rs` 的 resource-frame CSP 放开 connect/img/style/font/frame
  (`security.rs:329`:`connect-src https: http: blob:`);
  `resource_frame_template.html` 提供 `TavernHelper.generate`/`getPreset` 等 RPC。
  现状判定为**有意的兼容取舍**(等价于「在浏览器新窗口打开作者页」);
  opaque origin 隔离宿主凭据,拿不到 token/DOM。**待办**:如需收紧,可在设置提供
  「禁用作者页外联」开关。**2026-09-16 复核**:CSP 仍全放开,无该设置项 → 未启动。
- **改动点**:设置项「禁用作者页外联」(默认保持现状=开);关时收紧 CSP 的 `connect-src`
  并跳过 TavernHelper RPC 的外联部分。
- **验收断言**:关闭后 `connect-src` 不含 `https:`/`http:`;作者页 RPC 外联被跳过;
  **默认值下行为逐字节不变**;`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中(改安全头 + RPC 面);回滚 = 移除设置项与 CSP 分支。
- **依赖 / 需确认**:默认值须保持「开(=现状)」以免兼容性变化;
  与 [3.6](#36-resource-frame-外联收紧t1) 为同一事项(3.6 为其在残余任务文档中的登记)。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/威胁模型与残余风险.md T1
  (与 archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.6)

### T3 Android 分隔符改长度前缀/base64

- **目标**:消除 `\u{1f}` 分隔符导致的字段错位。
- **现状证据**:Android 执行入参用 `\u{1f}` 分隔 command / cwd / timeout
  (`services/exec/android.rs`);命令原文含该字符会字段错位(**不能提权,仅扰动解析**)。
  **2026-09-16 复核**:`services/exec/android.rs:21` 仍为 `const SEP: char = '\u{1f}';`;
  注释 `:8` 仍写「入参编码约定(沿用 `\u{1f}` 单元分隔符,避免额外 JNI 签名)」;
  Kotlin `ShellExecutorBridge.kt:31` 仍为 `const val SEP = '\u001f'` → 未启动。
- **改动点**:改为长度前缀或 base64 编码 command/cwd/timeout;**双侧
  (Kotlin `ShellExecutorBridge`)同改**。
- **验收断言**:命令含 `\u{1f}` 时字段不错位;双侧编解码对拍测试;
  `cargo test -j 2` + Android 编译通过;桌面路径零回归。
- **风险 / 回滚**:风险中(改 JNI 编解码协议,双侧必须同步);回滚 = 双侧一起 revert。
- **依赖 / 需确认**:真机验证随 [6.4](#64-android-真机验证) 一并做;
  与 [3.4](#34-android-1f-分隔符t3) 为同一事项。
- **状态**:**未启动**
- **来源**:archive/2026-09-16-consolidation/威胁模型与残余风险.md T3
  (与 archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.4)

### T4 `is_public_ip` 改白名单语义（原 `is_global` 路线已证伪）

- **目标**:以白名单语义收敛私网判定,消除 IPv6 特殊段的理论绕过面。
- **现状证据**:`is_public_ip`(`tools/agent_tools_search.rs:128-152`)为黑名单式枚举
  (IPv6 特殊段存在理论绕过面);已有 DNS 解析校验 + 已解析 IP 绑定 + 禁重定向三层防护。
  **2026-09-16 复核**:仍为自研 `is_public_ip`(被 `:119` 消费) → 未启动。
- **改动点(方案修正)**:**原写的「改用 `Ipv4Addr::is_global` / `Ipv6Addr::is_global`」
  在本仓工具链上编不过**——实测 `rustc 1.97.1` 直编最小复现即
  `error[E0658]: use of unstable library feature 'ip'`(见
  [3.5](#35-is_public_ip-黑名单枚举t4) 的完整实测记录)。`Ipv6Addr::is_global` 至今
  仍是 nightly-only 实验 API(`ip` #27709),仓库亦无任何 IP 网络 crate。
  修订后二选一:**① 自维护 IANA 特殊段显式白名单表(零新依赖,首选)**;
  ② 引入 `ipnet`/`ip_network` 类依赖(须过 `check-lock-sync` + `cargo audit`)。
  **保留现有 DNS 绑定 + 禁重定向**。
- **验收断言**:表驱动用例覆盖私网与特殊段(10/8、172.16/12、192.168/16、169.254/16、
  fc00::/7、::1、IPv4-mapped IPv6、`2001:db8::/32` 等);`cargo test -j 2` 全绿。
- **风险 / 回滚**:风险中;**必须用白名单语义**,不得退回黑名单;回滚 = revert。
- **依赖 / 需确认**:**方案需先定**(①/②);CGNAT(100.64/10)等段的语义按所选方案核实;
  与 [3.5](#35-is_public_ip-黑名单枚举t4) 为同一事项。
- **状态**:**未启动**(原方案证伪,已改)
- **来源**:archive/2026-09-16-consolidation/威胁模型与残余风险.md T4
  (与 archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §三 · 3.5)

---

## 八、明确不做（知情排除，便于复查）

> 把各源文档的「明确不做 / 有意排除」集中列出并注明理由,**防止后来者重复提议**。
> 以下条目**不应**被当作待办;**若将来要改做,须先推翻对应理由**(理由均为已实测或已决策)。

| 排除项 | 理由 | 来源 |
|---|---|---|
| 改 API 契约(新增分页参数;改列表返回字段集合) | 沿用已确认的边界;P-11 的派生列属服务端内部存储优化 | archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md §9 |
| 削减 debuginfo 换并发 | 用户已定;P-13 只走参数与栈两条路 | 同上 §9 |
| 动「构建后清 target」策略 | 用户已说明为省盘有意为之 | 同上 §9(与 ../AGENTS.md 构建提醒一致) |
| 加 `release` 的 `panic="abort"` | 仓库依赖 unwind:锁中毒 ROLLBACK、`pending_runs` RAII drop | 同上 §9 |
| 做 SSR/预渲染 | loopback 场景收益极低;`useVirtualMessages` 已是零依赖自研 | 同上 §9 |
| 引入 gzip/brotli 服务端压缩 | 同上(loopback 场景收益极低) | 同上 §9 |
| 引入图表 / 虚拟滚动第三方库 | `useVirtualMessages` 已是零依赖自研 | 同上 §9 |
| 引入 metrics / Prometheus / OpenTelemetry,加 `/metrics` | 本地单用户应用,无集中采集端与跨实例聚合需求;收益需独立评估(详见 L24) | archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md「不做的事」与 known-limitations.md L24 |
| down 迁移(降级脚本) | 回滚仍依赖用户备份;本轮只把「库比代码新」从**静默**变**显式**(详见 L25) | 同上与 known-limitations.md L25 |
| 一次性改完存量裸 `{error}` | 属独立批次;按域分批并用 ratchet 防新增 | archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md「不做的事」 |
| 新增第三方依赖(为统一提取器) | 提取器统一用自研薄包装,避免动 `Cargo.lock` 与 `check-lock-sync` 门禁 | 同上 |
| 事件流持久化 / 回放 | 属独立决策:前端已有 settle 全量刷新 + 兜底轮询的补偿设计(详见 L26) | 同上与 known-limitations.md L26 |
| 改 `CREATE_TABLES` / `SseEvent` 线格式 / EJS 能力面 | **冻结点**(见 §二 第 7 条);`check-contract.mjs` 与规则 H 守卫 | archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md「不做的事」 |
| 改 `/api/health` 的 `ok` 语义 | 桌面壳单实例判定依赖它(`src-tauri/src/lib.rs` 的 `health_ok()`) | 同上 |
| 改 SSE 重连算法 | 批次 5 只加可观测信号(见 [5.3](#53-sse-重连可观测)) | 同上 |
| 413(请求体过大)统一为 JSON 错误体 | 有意保留例外:根因在 `DefaultBodyLimit` 的 tower 层,**提取器被调用前**即拒绝;收益低(前端上传前已校验体积)成本高(动全站 body 流)(详见 L27) | 同上与 known-limitations.md L27 |
| 加「达到 max_tokens 自动续写」 | 属新功能,记录未要求 | archive/2026-09-16-consolidation/截断与Agent工具面修复计划-2026-09-16.md §5 |
| 改 generate-raw 的默认预算与 32768 自愈逻辑 | 同上(有意不做) | 同上 §5 与 §1.3 |
| 实现 `todo` 的汇总位数 | 记录第(10)条,本轮未选中(有意不做) | 同上 §5 |
| 细分 `error` 状态语义(除 `ended` 之外的) | 有意不做——只收窄了 `ended` 的覆盖面 | 同上 §5 |
| 前端展示 `finished_at` | 同上(数据已落库,但前端展示有意不做) | 同上 §5 与 §1.3 |
| 动 `docs/archive/` 下历史文档 | 历史文档只读不改(`../AGENTS.md` 纪律) | 同上 §5 |
| 技能 `allowed_tools` 等字段的**未定方案** | 须先定「接线 vs 标注废弃」,不得在其它批次顺手实现(见 [DC-1](#dc-1-技能-allowed_toolsrun_as_subagentmodel-仅存取无消费)) | archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md §四 |
| T5 主站 token 经明文 HTTP loopback 交付的「修复」 | **本地信任模型的固有边界**:bootstrap 仅 loopback + 令牌桶限频;属威胁模型声明而非待办 | archive/2026-09-16-consolidation/威胁模型与残余风险.md T5 |
| 引入 `glib` unsound 的 `-D warnings` 硬拦 | 会让 warning 变硬错误、把门禁变成阻塞点;保持 allowed warning | archive/2026-09-16-consolidation/前端工具链与审计升级-变更说明.md §四 与 §七 |
| 在源文件加 `eslint-disable` | 既有纪律:存量问题降级为 warn,**未在源文件加任何 eslint-disable** | archive/2026-09-16-consolidation/前端工具链与审计升级-变更说明.md §三 |
| 打包 busybox(Android 沙箱档) | 避免第三方二进制与许可问题;沙箱档用系统自带 `sh -c`,busybox 留作可选增强 | archive/2026-09-16-consolidation/android-port-plan.md 阶段 3 执行状态 |
| Android 第一版支持 MCP 的 `npx`/`uvx` 服务器 | 需额外打包 Node 运行时;`exec` 工具本身已覆盖「跑命令」需求 | archive/2026-09-16-consolidation/android-port-plan.md 阶段 3 第 6 条 |
| 运行时往全局工具注册表注册/注销流程工具 | `ToolRegistry` 是全局单例,`register_external` 会失效 `definitions_cache`,并发任务会造成缓存抖动与竞态;改用稳定内置 `run_flow` + `params.tools` 控可见性 | archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §3.3 |
| 图数据模型方案 A2(`graph{nodes,edges}` 与 `steps` 并存) | 两份事实源必然漂移,且所有既有消费者都要写双分支(决策 D2 已否决) | archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §3.1 与 §4.1 |
| 顶层模式(roleplay/task)下推到节点 | 会破坏 `模式提示词边界` 的类型级隔离不变量(决策 D1 选项丙,**不建议**) | archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md §3.2 与 §4.1 |
| 为统一 413 而自定义 tower 层拦截 body Frame | 见上「413」条(有意保留例外) | archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md「不做的事」 |
| 不改 `docs/archive/` `contract-drift-2026-09-09.md` 等归档件 | 归档只读,漂移表已归档 | archive/2026-09-16-consolidation/三结合落实核对表.md 批次 0 · T0.6 |

---

## 九、状态漂移裁定登记

> 对**同一条目在多份文档状态不一致**的情况,逐条给出「文档 A 说 X / 文档 B 说 Y /
> 代码实测结论 / 裁定依据(跑了什么命令、看了哪个文件)」。
> **裁定原则:以代码实测为准。** 无法确认处标注 `【待核实】`。

### 裁定 1 · E.1 脚本授权是否已打通

- **文档 A 说**:`archive/2026-09-16-consolidation/三结合落实核对表.md` 批次 E · E.1
  状态为「**待办(需定方案)**」,理由「复核发现前端已有授权台账,缺的是后端路径与台账的对接」。
- **文档 B 说**:`archive/2026-09-16-consolidation/known-limitations.md` **L12 标题即为
  「已修复(2026-09-14)」**,并给出 `script_authorizations` 表 + `GET /api/script-authorizations`
  端点 + 4 条 e2e(`tests/scripts_e2e.rs`);
  `archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §7.1` 第 13 行亦记「角色卡脚本后端授权路径打通(L12)」。
- **代码实测结论**:**已修复**。后端授权门已完整落地。
- **裁定依据**:
  - `grep -rn "script_authorizations" server-rs/src --include=*.rs` →
    `services/script_authorization_service.rs:118,144,157,169`(表 CRUD)、
    `models/db/schema.rs:404`(`CREATE TABLE script_authorizations`)、
    `api/app_state.rs:70-71,145,331,379`(注入服务)、
    `api/user_scripts.rs:153,204,229,240`(四个端点)。
  - `grep -n "run_character_scripts|current_hash|ScriptAuthorizationService" server-rs/src` →
    `agents/engine/run_scripts.rs:193-197` 注释与实现:「前端沙箱要授权、后端自动跑,
    授权不对称。现改为 **fail-closed**」;`agents/engine/mod.rs:123-151` 注入
    `script_authorizations: Arc<ScriptAuthorizationService>` 门;
    `api/user_scripts.rs:107-198` 提供 GET(含实时 `current_hash`)/PUT/DELETE,含 409 陈旧哈希分支。
  - `ls server-rs/src/services/` → `script_authorization_service.rs` 存在。
  - `grep -rn "script_authorizations" server-rs/tests` 未单独核对文件清单,但
    `known-limitations.md` L12 记 `tests/scripts_e2e.rs` 4 条——【待核实】该测试文件当前是否仍全绿。
- **处置**:`三结合落实核对表` 的 E.1 状态为**过期记录**,已在本文 [E.1](#e1-脚本授权打通)
  标注为「已完成」;建议后续修订核对表时回填。
- **`【待核实】`**:`tests/scripts_e2e.rs` 当前用例数与其是否随 L12 一并收录在本轮测试计数内。

### 裁定 2 · 6.2 DB schema 版本号「代码部分待办」

- **文档 A 说**:`archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md` §六 6.2
  「**代码部分待办**:引入 `PRAGMA user_version`」;
  `archive/2026-09-16-consolidation/威胁模型与残余风险.md` D 区同类表述。
  `archive/2026-09-16-consolidation/优化实施方案-2026-09.md` 附录 C 亦未含该指标。
- **文档 B 说**:`archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md` 批次 3
  与 `archive/2026-09-16-consolidation/known-limitations.md` **L18/L21「已修复(2026-09-15)」**;
  `archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §7.2` 亦记「新增 `SCHEMA_VERSION` +
  `user_version` 读写与『库比代码新则拒绝启动』」。
- **代码实测结论**:**已修复**。
- **裁定依据**:
  - `grep -n "SCHEMA_VERSION" server-rs/src/models/db/schema.rs` → `:8` 定义
    `pub const SCHEMA_VERSION: i32 = 1;`。
  - `archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md` §0 基线表亦记
    `SCHEMA_VERSION | 1 | server-rs/src/models/db/schema.rs:8`。
  - `known-limitations.md` L21 给出验收证据
    `tests/schema_migration_meta.rs:273` 的
    `failed_ensure_rolls_back_whole_upgrade_chain_and_keeps_version`。
- **处置**:残余任务文档 6.2 的「代码部分待办」为**过期记录**;6.2 整体视为已完成,
  故本文 [§三](#三残余任务与后续计划) 的完成项索引已含 6.2(文档 + 代码),
  **不再列作待办**。
- **`【待核实】`**:无。

### 裁定 3 · `modal-settings` chunk 体积(138.5K vs 188,565 B)

- **文档 A 说**:`archive/2026-09-16-consolidation/优化实施方案-2026-09.md` 附录 C 记
  「modal-settings chunk **138.5K**(本批前 188.8K,-26.6%…)**≤60K 目标未达**」,
  且 M3 完成记录重复该值。
- **文档 B 说**:`archive/2026-09-16-consolidation/性能批次P8-P13执行计划-2026-09-16.md`
  §0 记 2026-09-16 实测 **188,565 B / 61,850 gz**,且被首屏预载。
- **代码/产物实测结论**:**B 正确,A 已过期**。当前构建产物为 188,565 B(约 184K),
  较 A 记录的 138.5K **回升**,且仍在首屏 `modulepreload` 中。
- **裁定依据**:
  - `ls -la web/dist/assets/ | grep modal-settings` →
    `modal-settings-DgSe9Odc.js` **188565** 字节(2026-09-16 09:46);
  - `grep -o "modal-settings[^\"]*" web/dist/index.html` → 命中,证明**仍被预载**;
  - `grep -n "manualChunks" -A 12 web/vite.config.ts` → `:29` 仍把
    `/components/SettingsHub` 与 `/components/SettingsModal` 归入 `modal-settings`。
- **处置**:[C-1](#c-1-modal-settings-chunk-1385k目标-60k-未达) 的 60K 目标**须在 P-8 完成后
  按新基线复核**;当前不视为「已改善」。
- **`【待核实】`**:A 记录的 138.5K 对应的是哪次构建(功能回填导致回升属合理推测,
  但未逐版本比对 history,标注为**待核实**)。

### 裁定 4 · 批次 5.2 SSE 错误分类去重是否待做

- **文档 A 说**:`archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md`
  批次 5 · 5.2 列 `api/chat.ts` 与 `api/tasks.ts` **各自重写**了 `apiErrorMessage` 的分类判断,
  状态待做(批量 5–8 未启动)。
- **文档 B 说**:无直接反证;但 `archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md`
  §一 4.x/§二 5.x 的后续批次记录中包含前端收敛类改动。
- **代码实测结论**:**已实现(去重完成)**。
- **裁定依据**:
  - `grep -n "function apiErrorMessage|const apiErrorMessage" web/src/api/*.ts` →
    **仅 `web/src/api/client.ts:83` 一处**定义;
  - `web/src/api/chat.ts:2` 与 `web/src/api/tasks.ts:5` 均为
    `import { BASE, …, apiErrorMessage, … } from './client'`;
  - `web/src/api/client.test.ts:133` 有「`apiErrorMessage` 分类口径(与 `errors.rs` 契约一致)」用例。
- **处置**:[5.2](#52-sse-错误分类去重) 标注为「部分完成(本项已完成)」;
  批次 5 整体仍为未启动/部分完成。
- **`【待核实】`**:无。

### 裁定 5 · D3 token 计数是否已有缓存

- **文档 A 说**:`archive/2026-09-16-consolidation/残余任务与后续计划-2026-09-13.md` §五
  与 `archive/2026-09-16-consolidation/威胁模型与残余风险.md` D3 均为
  「**先加计时日志观察长会话实际耗时;确认值得再引入按消息 id 缓存**」——即列为待办。
- **文档 B 说**:`archive/2026-09-16-consolidation/known-limitations.md` L23 记
  「已修(2026-09-14):① 计数计入 `tool_calls` 的 name/arguments(新增
  `count_single_message_tokens` 统一口径)」,但**未提缓存**。
- **代码实测结论**:**缓存已落地(2026-09-14)**,D3 的「待引入」前提已不成立。
- **裁定依据**:
  - `grep -n "count_cache" server-rs/src/services/token_service.rs` →
    `:20` `count_cache: HashMap<(String, u64, usize), i64>`、`:76` 初始化、
    `:111-112` `clear_count_cache`、`:120` 命中、`:127-131` 超限清空后写入;
  - `:12-17` 注释:`COUNT_CACHE_CAP = 8192`,「为什么需要:工具循环的预算闸门每摘要一轮就
    重算**全部**消息的 token…实测 30 条 × 2k token 历史单次计数 29.4 ms…
    有缓存后只有被改动的消息需要重编码」,并标注「2026-09-14」;
  - `:258,273` 有 `count_cache_returns_same_values_and_separates_models` /
    `count_cache_handles_empty_and_long_text` 两个用例。
- **处置**:[PC-1](#pc-1-token-计数每轮全量逐条d3) 标注为「部分完成(缓存已落地;
  余量为路径清点)」。
- **`【待核实】`**:是否所有长会话路径都已走 `TokenService` 的缓存口径(是否仍有绕过缓存的直接调用),
  **未逐调用点核对**,标注为**待核实**。

### 裁定 6 · Android Phase 2 release `network_security_config.xml`

- **文档 A 说**:`archive/2026-09-16-consolidation/android-port-plan.md` §五之二
  「release 构建下 `usesCleartextTraffic=false`…**release 版必须补
  `network_security_config.xml` 只放行 `127.0.0.1`**,否则正式包会白屏(**Phase 2 待办**)」。
- **文档 B 说**:同文档「已完成的原生配置补丁」清单第 1、2 项**列有**该文件及其在
  `AndroidManifest.xml` 的引用——**同文档内自相矛盾**。
- **代码实测结论**:**已完成**。
- **裁定依据**:
  - `ls src-tauri/gen/android/app/src/main/res/xml/` → `network_security_config.xml` 存在;
  - 文件内容:只对 `127.0.0.1` / `localhost` / `::1` 放行明文,
    `<base-config cleartextTrafficPermitted="false" />`(未全局开门);
  - `grep -n "networkSecurityConfig" src-tauri/gen/android/app/src/main/AndroidManifest.xml`
    → `:27` `android:networkSecurityConfig="@xml/network_security_config"`。
- **处置**:[A5-1](#a5-1-phase-2-release-版-network_security_configxml-只放行-127001)
  标注为「部分完成(配置与引用已落地;release 包端到端确认待随发布做)」;
  文档 A 的「Phase 2 待办」为过期记录。
- **`【待核实】`**:release 签名包上的 loopback 加载端到端确认(本次未重跑发布验证)。

### 裁定 7 · 超千行文件数(6 vs 12)

- **文档 A 说**:`archive/2026-09-16-consolidation/优化实施方案-2026-09.md` 附录 C 记
  M3 后「超千行文件数 10 → **6**(≤6 达成…)」,剩余 6 个文件列名;
  `残余任务与后续计划-2026-09-13.md` §五 亦列 5 后端 + 3 前端为待拆。
- **文档 B 说**:无文档记「回升」。
- **代码实测结论**:**已回退到 12 个源文件 >1000 行**,目标 6 远未达成。
- **裁定依据**:
  `find server-rs/src web/src -name "*.rs" -o -name "*.ts" -o -name "*.vue" | xargs wc -l`
  排序后(>1000,剔除测试文件):`memory_service.rs` **1661**、`task_engine/team.rs` **1580**、
  `parsing/world_book.rs` **1531**、`agents/engine/executor.rs` **1489**、
  `models/types.rs` **1398**、`ejs/exec.rs` **1313**、`tools/agent_tools.rs` **1299**、
  `api/settings.rs` **1163**、`services/session_service.rs` **1040**、
  `settings_service/mod.rs` **1037**、`api/chat.rs` **1027**、
  `services/task_service/executor.rs` **1022**;另有 `web/src/characterScriptSandbox.test.ts` 1061(测试)。
  其中源文档明确列过并已「收尾拆分」的 `build.rs` 已不在列(1484→575 属实)。
- **处置**:[C-2](#c-2-超千行文件-106部分仍-1000-行) 标注为「未启动(实测 12 > 目标 6)」;
  建议把该指标做成 ratchet 门禁防再次回退。
- **`【待核实】`**:无。

### 裁定 8 · G.5 弹窗开关单一注册表是否已放弃

- **文档 A 说**:`archive/2026-09-16-consolidation/三结合落实核对表.md` 批次 G · G.5
  状态「**已放弃**」,理由「纯整洁性重构,改动面涉及 App.vue 模板与返回键逻辑,收益低」。
  同表「未做项与理由」再次列 G.5 已放弃。
- **文档 B 说**:`archive/2026-09-16-consolidation/known-limitations.md` **L15
  「已收敛(2026-09-13)」**,并记「弹窗开关已由 `web/src/modals.ts` 单点注册表派生
  (`App.vue` 与 check-arch 白名单同源)」;
  `残余任务与后续计划-2026-09-13.md` §二 5.2 亦记「✅ 已完成(2026-09-13)」。
- **代码实测结论**:**已完成**。
- **裁定依据**:
  - `ls web/src/modals.ts` → 存在(3895 B);
  - `grep -n "MODAL_FLAGS" web/src/App.vue web/src/stores/uiPrefs.ts` →
    `web/src/App.vue:9` `import { MODALS, MODAL_FLAGS } from './modals'`,
    `:99-100` 返回键逻辑由 `MODAL_FLAGS` 逆序遍历派生;
  - `web/src/modals.test.ts` 存在(元测试)。
- **处置**:[G.5](#g5-弹窗开关单一注册表) 标注为「已完成」;
  核对表的「已放弃」为**过期记录**(与 L15 矛盾)。
- **`【待核实】`**:无。

### 裁定 9 · 裸 `{error}` 基线(117 / 116 / 112 / 105)

- **文档 A 说**:`archive/2026-09-16-consolidation/三结合落实核对表.md` D.3 记
  「基线 117,当前 **116**」;`archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md`
  §0.1 与 §0.3 记「规则 G 实测裸 `{error}` **112** 处」、项 3「降至 **105** 并加规则 K」。
- **文档 B 说**:`archive/2026-09-16-consolidation/ARCHITECTURE-3H.md §7.2` 记
  「`[G]` 裸 `{error}` **117 → 105** 并新增**规则 K** ratchet(未接入 `JsonBody` 的提取器基线 32)」。
- **代码实测结论**:**基线 = 105**。
- **裁定依据**:`grep -n "BASELINE_BARE_ERROR" tools/check-arch.mjs` →
  `:400` `const BASELINE_BARE_ERROR = 105; // 2026-09-15 实测(批次 1 错误面收口后由 117 下调);只降不升`。
- **处置**:[D.3](#d3-存量裸-error-逐个补-code) 与 [7.1](#71-后端负路径覆盖约定收益最大无需新机制)
  均按 **105** 记;核对表的 116 与修复计划 §0.1 的 112 均为中间过程值(已过期)。
- **`【待核实】`**:无。

### 裁定 10 · 日志级别分布(warn 116 / info 28 / debug 1)

- **文档 A 说**:`archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md` 批次 6 · 6.3
  记「实测 160 处 `tracing::*!` 中 warn 116 / error 15 / info **28** / **debug 1**」。
- **文档 B 说**:无更新记录。
- **代码实测结论**:`warn = 116`、`info = 29`、`debug = 0`
  —— warn 与 info 基本一致(各 ±1),但 **debug 已归零**(比原文更极端)。
- **裁定依据**:
  - `grep -rn "tracing::info!" server-rs/src --include=*.rs | wc -l` → **29**;
  - `grep -rn "tracing::debug!" server-rs/src --include=*.rs | wc -l` → **0**;
  - `grep -rn "tracing::warn!" server-rs/src --include=*.rs | wc -l` → **116**。
- **处置**:[6.3](#63-级别分布再平衡) 的现状证据按实测值记录(debug 归零);
  级别失衡问题仍在,待办状态不变。
- **`【待核实】`**:无。

### 裁定 11 · 批次 5.4 / 7.2 的「未做」与实际覆盖

- **文档 A 说**:`archive/2026-09-16-consolidation/工程修复计划-2026-09-15.md`
  批次 5.4「其余易被直接解构的 API 封装补运行时形状校验」与批次 7.2「mock 契约对齐」
  均列在未启动的批次 5/7 内。
- **文档 B 说**:`工程修复计划` §0.3 项 14 记「**部分**(形状闸门已建;mock 契约对齐属批次 7)」;
  A 中已按该模式改正 `diagnostics.test.ts` 作为范例。
- **代码实测结论**:**两项均已部分落地**。
- **裁定依据**:
  - 形状闸门:`web/src/api/shape.ts` 被 `tasks.ts:6`、`sessions.ts:7`、`memory.ts:10` 引用;
    `diagnostics.ts:69` 有 `assertDiagnostics`;`tasks.ts:13,29,94`、
    `sessions.ts:19,55,80,149`、`memory.ts:52,80` 均带「形状闸门」注释。
  - mock 契约:`diagnostics.test.ts:73-74` 注释「状态码与后端实现对齐:失败走 500 + code
    (见 api/diagnostics.rs 的 err_with_code)」;`memory.test.ts`、`RepoIndexModal.test.ts`
    经 `grep -rln "状态码与后端|后端实现一致|与后端.*对齐"` 命中。
- **处置**:[5.4](#54-api-形状闸门承接-14) 与 [7.2](#72-mock-契约对齐) 均标注「部分完成」。
- **`【待核实】`**:形状闸门与 mock 对齐的**完整模块清单**未逐文件清点,
  标注为**待核实**(须补一份清点表)。

### 裁定 12 · 阶段 0 Spike 3(Android Keystore JNI)

- **文档 A 说**:`archive/2026-09-16-consolidation/android-port-plan.md` 阶段 0 执行结果
  「**阶段 0 未完成项:** Spike 3(Android Keystore JNI)尚未做」。
- **文档 B 说**:同文档 §五之三「阶段 1(Android Keystore 密钥存储)执行结果(2026-09-11 完成)」
  ——Kotlin `KeystoreBridge.kt` + Rust `services/keystore_android.rs` 已落地并上机验证
  (`PUT /api/settings` → 200、落盘 `enc:v1:…`、重启后 `has_api_key: true`)。
- **代码实测结论**:Keystore JNI 闭环**已实际达成**;但「作为独立 spike 的结论报告」这一交付物
  **未单独立项**。
- **裁定依据**:
  - `ls src-tauri/gen/android/app/src/main/java/com/kedai/app/` →
    含 `KeystoreBridge.kt`(另 `grep -n "Keep" KeystoreBridge.kt` 可见 R8 keep 规则修复);
  - `ls server-rs/src/services/keystore_android.rs` → 存在;
  - 上机验证证据见 `android-port-plan.md` §五之三 的验证表与 §五之五 的 R8 修复后实测。
- **处置**:[A5-2](#a5-2-阶段-0-spike-3android-keystore-jni未做) 标注为
  「部分完成(实现与实测已达成 Spike 目的;独立 spike 报告未单独立项)」。
- **`【待核实】`**:是否需要补一份形式上的 Spike 3 结论记录(纯流程性,待用户/维护者定)。

### 裁定 13 · `TaskStep` 四字段的「兼容承诺」与二维批次 2 的加字段

- **文档 A 说**:`archive/2026-09-16-consolidation/任务引擎六模式.md §二` 把
  「TaskStep 四字段」列为「**不动(兼容承诺)**」。
- **文档 B 说**:`archive/2026-09-16-consolidation/二维自定义工作流-可行性与实施计划.md`
  §3.1 与 §4.2 高风险第 5 条:**`TaskStep` 加 `node_id: Option<String>`(serde default)
  是加性改动**,旧客户端反序列化不受影响,**但确实触及该结构,需同步修订文档中的兼容承诺表述**。
- **代码实测结论**:`TaskStep` 当前仍为四字段(`models/types.rs:927-942`),
  **尚未加 `node_id`**;故承诺表述**目前仍成立**,冲突只在二维批次 2 实施后才会发生。
- **裁定依据**:`Read server-rs/src/services/task_engine/...` 与
  `models/types.rs:927-942` 的 `TaskStep` 定义(原文引用);
  `grep -n "node_id" server-rs/src/models/types.rs` 未命中【待核实】具体行号需在实施前复核。
- **处置**:[二维批次 2](#二维批次-2-图执行器并行规模-l310-天) 的改动点已含
  「同步修订文档中的兼容承诺表述」;本裁定仅登记**预置冲突**,避免后续评审按旧承诺判违规。
- **`【待核实】`**:`models/types.rs` 当前 `TaskStep` 定义的确切行号(实施前须复核)。

---

## 十、来源与溯源

### 10.1 本文的源文档(全部位于 `docs/archive/2026-09-16-consolidation/`)

| 序号 | 源文件名 | 采纳内容 | 对应本文章节 |
|---|---|---|---|
| 1 | `性能批次P8-P13执行计划-2026-09-16.md` | §0 基线 / §1 总览与顺序 / §2~§7(P-8~P-11) / §8 全程纪律 / §9 明确不做 / §10 决策点 | [§一](#一活跃性能批次p-8p-13)、[§八](#八明确不做知情排除便于复查) |
| 2 | `后端性能批次P1-P7-变更说明-2026-09-16.md` | §4 未做与已知缺口、§5 后续建议顺序 | [§一](#一活跃性能批次p-8p-13)(P-G1/P-G2/P-G3) |
| 3 | `工程修复计划-2026-09-15.md` | 批次 5~8(逐批)、依赖图、执行纪律、排除项与冻结点、§0.3 完成项索引 | [§二](#二工程韧性批次-58)、[§八](#八明确不做知情排除便于复查) |
| 4 | `残余任务与后续计划-2026-09-13.md` | 4.5 / 4.6 / 4.7 / 5.4 / 3.3~3.6 / 3.8 / §四死配置 5 项 / §五性能容量 4 项 / 6.4 / 6.6 | [§三](#三残余任务与后续计划)、[§八](#八明确不做知情排除便于复查) |
| 5 | `二维自定义工作流-可行性与实施计划.md` | §五批次 0~7、§4.1 决策点 D1~D8、§4.2 风险、§五验证口径 | [§四](#四二维自定义工作流) |
| 6 | `android-port-plan.md` | 阶段 5 待办、阶段 0 未完成项、阶段 2 原生配置、阶段 3 执行状态、§六 待确认 | [§五](#五android-移植阶段-5) |
| 7 | `known-limitations.md` | L2 / L3 / L8 / L16(判定为「待办」的项) | [§六](#六架构与工程化欠账)、[§八](#八明确不做知情排除便于复查) |
| 8 | `威胁模型与残余风险.md` | T1 / T3 / T4(待办项)、T2 交叉引用、T5 与 §三 死配置(排除项) | [§七](#七安全与数据残余t1t3t4)、[§八](#八明确不做知情排除便于复查) |
| 9 | `ARCHITECTURE-3H.md` | §8 偏离纪律的结构性遗留(3 项)、§7.1/§7.2 核查结论(裁定依据) | [§六](#六架构与工程化欠账) |
| 10 | `架构分析-2026-09-14.md` | §6 落地路线图批次 3~5 | [§六](#六架构与工程化欠账) |
| 11 | `优化实施方案-2026-09.md` | 附录 C 未达项、附录 A CI「暂不创建」、附录 B 退役数据 | [§六](#六架构与工程化欠账)、[§八](#八明确不做知情排除便于复查) |
| 12 | `三结合落实核对表.md` | 「未做项与理由(知情接受)」8 项:B.4b / B.6 / D.2b / D.3 / E.1 / E.3 / G.3 / G.5 | [§六](#六架构与工程化欠账)、[§九](#九状态漂移裁定登记) |
| 13 | `前端工具链与审计升级-变更说明.md` | §七 未做/留待(Prettier 全量、ESLint warn、glib、残余计划其余项)、§三 未全量格式化 | [§六](#六架构与工程化欠账)(FT-1/FT-2)、[§八](#八明确不做知情排除便于复查) |
| 14 | `截断与Agent工具面修复计划-2026-09-16.md` | §1.3 遗留、§5 明确不做 | [§八](#八明确不做知情排除便于复查) |
| 15 | `任务引擎六模式.md` | §四 末段 `ModeSettings.task_modes`(未落地)、§五 team 手动拓扑(预留) | [§六](#六架构与工程化欠账)(TE-1/TE-2) |

> **注**:上表 15 份源文档将随原文档一并移入
> `docs/archive/2026-09-16-consolidation/`(该目录在本文写作时尚未创建,
> 引用按约定路径书写;若归档目录名最终不同,须整体同步)。

### 10.2 本文引用的其他仓库文件(非源文档)

- `../AGENTS.md` —— 验证命令(`cargo test -j 2`)、构建提醒、Git 提交纪律;
- `../MAINTENANCE.md` —— 数字权威(测试计数)、§10 踩坑条目 17/29/30/31、§11 门禁清单、§12 Roadmap;
- `docs/known-limitations.md` —— 与本文同级的「遗留」类登记(本文以 `L*` 编号交叉引用);
- `docs/perf-baseline.md` —— 性能基线与前后对比的落盘处;
- `docs/模式提示词边界.md` —— 双模式提示词隔离规则的权威;
- `docs/数据库版本与降级行为.md` —— `user_version` 方案的历史索引;
- `tools/check-arch.mjs`(规则 C/D/G/H/I/J/K)、`tools/check-contract.mjs`、
  `tools/check-frontend-lint.mjs`、`tools/check-lock-sync.mjs`、`tools/count-tests.mjs`、
  `tools/check-all.ps1` —— 门禁脚本(本文多处以其基线值为裁定依据);
- `web/vite.config.ts`、`web/src/modals.ts`、`web/src/sseReducer.ts`、
  `web/src/composables/useVirtualMessages.ts`、`web/src/api/client.ts` /
  `shape.ts` / `diagnostics.ts`、`web/src/components/ChatWindow.vue` —— 前端实测锚点;
- `server-rs/src/services/script_authorization_service.rs`、`agents/engine/run_scripts.rs`、
  `api/user_scripts.rs`、`models/db/schema.rs`、`services/token_service.rs`、
  `utils/logging.rs`、`mcp/client.rs`、`contracts/op.rs`、`services/exec/android.rs`、
  `tools/agent_tools_search.rs`、`tools/command_risk.rs`、`api/security.rs` —— 后端实测锚点;
- `src-tauri/gen/android/app/src/main/res/xml/network_security_config.xml`、
  `.../AndroidManifest.xml`、`.../java/com/kedai/app/KeystoreBridge.kt` —— Android 实测锚点;
- `.cargo/config.toml`、`tools/check-all.ps1` —— P-12/P-13 的接线目标。

### 10.3 状态汇总(便于快速巡检)

| 状态 | 条目 |
|---|---|
| **已完成**(保留编号以维持引用) | [E.1](#e1-脚本授权打通)、[G.5](#g5-弹窗开关单一注册表)、[二维批次 0](#二维批次-0-修-settingshub-的-flow-入口错位) |
| **部分完成** | [5.2](#52-sse-错误分类去重)、[5.4](#54-api-形状闸门承接-14)、[7.2](#72-mock-契约对齐)、[批次 8](#批次-8-文档回填与终验)、[DC-2](#dc-2-mcp-modesettings-覆盖层无效)、[PC-1](#pc-1-token-计数每轮全量逐条d3)、[A5-1](#a5-1-phase-2-release-版-network_security_configxml-只放行-127001)、[A5-2](#a5-2-阶段-0-spike-3android-keystore-jni未做)、[架构分析批次 3](#架构分析-2026-09-14-批次-3-消除重复实现)、[架构分析批次 5](#架构分析-2026-09-14-批次-5-工程化收口)、[D.2b](#d2b-memorysession吞错返空)、[D.3](#d3-存量裸-error-逐个补-code)、[E.3](#e3-mcp-eof-注销工具)、[L16](#l16-mcp-服务器死亡后工具不注销且无热重连待办) |
| **未启动** | 其余全部条目(P-8~P-13、P-G1~P-G3、5.1、5.3、6.1~6.3、7.1、7.3、4.5~4.7、5.4(E2E)、3.3~3.6、3.8、DC-1/DC-3/DC-4/DC-5、PC-2~PC-4、6.4、6.6、二维批次 1~7、A5-3~A5-5、ARCH-3H §8-1/§8-2、L2/L3/L8、J4、C-1~C-4、B.4b/B.6、G.3、FT-1/FT-2、TE-1/TE-2) |

### 10.4 本文的维护约定

- 新增「计划」类条目**必须先有改动点 + 验收断言**;否则应进 `展望.md`。
- 若条目实际是**缺陷**而非改动方案,应迁 `遗留.md`,本文留一行 `转遗留:<ID>` 注记。
- 状态变化时更新对应条目的「状态」行与 [10.3](#103-状态汇总便于快速巡检);
  若发生新的跨文档不一致,补入 [§九](#九状态漂移裁定登记)。
- **不得**改动源文档的编号;`【待核实】` 项解决后应移入裁定依据并去掉标记。
- 归档后若需引用源文档原文,路径为
  `archive/2026-09-16-consolidation/<原文件名>`(相对 `docs/`)。

---

> **本文状态**:整合自 15 份源文档(见 [10.1](#101-本文的源文档全部位于-docsarchive2026-09-16-consolidation))。
> **未解决标记**:全文共 `【待核实】` 项 7 处(逐一列于各裁定条目内),
> 分别为:裁定 1(`tests/scripts_e2e.rs` 当前用例数与计数)、裁定 3(138.5K 对应构建版本)、
> 裁定 5(是否有路径绕过 `TokenService` 缓存)、裁定 6(release 签名包端到端确认)、
> 裁定 11(形状闸门/mock 对齐的完整模块清单)、裁定 12(是否补 Spike 3 结论记录)、
> 裁定 13(`TaskStep` 定义的确切行号)。这些均**不影响条目是否待办的判定**,
> 属细节复核项。
